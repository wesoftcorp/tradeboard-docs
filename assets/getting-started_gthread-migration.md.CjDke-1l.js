import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Migrating to gthread (Experimental)","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/gthread-migration.md","filePath":"getting-started/gthread-migration.md"}'),n={name:"getting-started/gthread-migration.md"};function r(o,e,l,h,d,p){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="migrating-to-gthread-experimental" tabindex="-1">Migrating to gthread (Experimental) <a class="header-anchor" href="#migrating-to-gthread-experimental" aria-label="Permalink to &quot;Migrating to gthread (Experimental)&quot;">​</a></h1><p>Tradeboard is moving off the <strong>eventlet</strong> worker and onto Gunicorn&#39;s threaded <strong>gthread</strong> worker. This page explains why, what changes, how to opt in, how to verify it, and how to go back.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><strong>This is experimental and opt-in. It is not the default.</strong></p><p>If you upgrade and do nothing, Tradeboard continues to run on eventlet exactly as before. Only set the variable described below if you are willing to test and report results.</p></div><p><strong>Branch:</strong> <a href="https://github.com/wesoftcorp/tradeboard/tree/gthread" target="_blank" rel="noreferrer">github.com/marketcalls/tradeboard/tree/gthread</a><strong>Discussion and reports:</strong> <a href="https://github.com/wesoftcorp/tradeboard/issues/1722" target="_blank" rel="noreferrer">issue #1722</a></p><hr><h3 id="why-this-migration-is-necessary" tabindex="-1">Why this migration is necessary <a class="header-anchor" href="#why-this-migration-is-necessary" aria-label="Permalink to &quot;Why this migration is necessary&quot;">​</a></h3><p>Tradeboard has always run as <code>gunicorn --worker-class eventlet -w 1</code>.</p><p><strong>Eventlet is retired software, and Gunicorn 26 removed the eventlet worker entirely.</strong> That pins Tradeboard to <code>gunicorn&gt;=25.0,&lt;26</code> permanently, a version that will stop receiving fixes, with no upgrade path.</p><p>This is not a performance project. It is about not being stranded on a dead dependency.</p><p>Two options were evaluated and rejected before settling on gthread:</p><table tabindex="0"><thead><tr><th>Option</th><th>Verdict</th></tr></thead><tbody><tr><td><strong>Granian</strong></td><td>Rejected. Its WSGI mode cannot provide the socket that <code>simple_websocket</code> needs, so Socket.IO WebSocket transport breaks.</td></tr><tr><td><strong>uvicorn / ASGI</strong></td><td>Deferred. Would require converting Flask to an ASGI application, a far larger change than the problem justifies today.</td></tr><tr><td><strong>gthread</strong></td><td>Chosen. Supported by Flask-SocketIO, keeps Flask and WSGI, and changes one launch flag rather than the framework.</td></tr></tbody></table><hr><h3 id="what-actually-changes" tabindex="-1">What actually changes <a class="header-anchor" href="#what-actually-changes" aria-label="Permalink to &quot;What actually changes&quot;">​</a></h3><p>This is the single sentence that governs the whole migration:</p><blockquote><p>Under eventlet, code that does not yield is atomic relative to other greenlets. Under gthread it is not.</p></blockquote><p><strong>Eventlet</strong> uses cooperative green threads. Your code runs uninterrupted until it chooses to yield (on I/O, or an explicit sleep). Two requests never interleave in the middle of a calculation.</p><p><strong>gthread</strong> uses real operating system threads. The OS can suspend a thread <strong>anywhere</strong>, between two lines, or between reading a dictionary and writing it back.</p><p>The practical consequence is that code which was <em>accidentally</em> safe under eventlet is genuinely racy under real threads. This matters most on a server that runs all day, places real orders, and is never restarted.</p><h4 id="what-this-found" tabindex="-1">What this found <a class="header-anchor" href="#what-this-found" aria-label="Permalink to &quot;What this found&quot;">​</a></h4><p>These were reproduced, not theorised:</p><ul><li>Order cancellation releasing blocked margin <strong>twice</strong></li><li>Expired-contract settlement releasing margin twice, triggerable by leaving two browser tabs open</li><li>The symbol lookup going <strong>blank during its daily refresh</strong>, so a valid symbol briefly looks like it does not exist</li><li>MCP quota admitting 7 and 8 concurrent requests against a configured limit of 5</li><li>Sandbox catch-up and square-off sweeps each running twice</li><li>A Python strategy launch path that could <strong>deadlock before the strategy started</strong></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p><strong>If you run Tradeboard on Windows or macOS with <code>uv run app.py</code>, you have never been on eventlet.</strong> The development server uses standard threading. Every one of the issues above has been reachable on your setup all along.</p><p>The migration did not introduce them. It found them.</p></div><hr><h3 id="what-we-gain-from-this" tabindex="-1">What we gain from this <a class="header-anchor" href="#what-we-gain-from-this" aria-label="Permalink to &quot;What we gain from this&quot;">​</a></h3><h4 id="strategic-the-actual-reason" tabindex="-1">Strategic: the actual reason <a class="header-anchor" href="#strategic-the-actual-reason" aria-label="Permalink to &quot;Strategic: the actual reason&quot;">​</a></h4><ul><li><strong>Unblocks Gunicorn 26 and beyond.</strong> Eventlet&#39;s removal currently pins Tradeboard to <code>gunicorn&lt;26</code> permanently.</li><li><strong>Removes a retired dependency</strong> that no longer has active maintenance.</li><li><strong>Keeps Flask and WSGI.</strong> No framework rewrite, unlike the ASGI route.</li><li><strong>One launch flag changes</strong>, not the architecture.</li></ul><h4 id="correctness-the-unexpected-payoff" tabindex="-1">Correctness: the unexpected payoff <a class="header-anchor" href="#correctness-the-unexpected-payoff" aria-label="Permalink to &quot;Correctness: the unexpected payoff&quot;">​</a></h4><ul><li>Forced a full concurrency audit that surfaced <strong>real money-path defects</strong>: margin released twice on cancellation, the symbol cache blanking mid-refresh, duplicate sandbox sweeps.</li><li>Those defects were <strong>already live for every Windows and macOS user</strong>, whose development server has always used real threads.</li><li>Locking and lifecycle rules are now <strong>documented and enforced by tests</strong> rather than holding by accident.</li></ul><h4 id="operational" tabindex="-1">Operational <a class="header-anchor" href="#operational" aria-label="Permalink to &quot;Operational&quot;">​</a></h4><ul><li>An <strong>explicit, tunable thread budget</strong> instead of an unbounded pool of green threads.</li><li>Real OS threads are <strong>visible to standard tooling</strong>, <code>top</code>, <code>py-spy</code> and thread dumps all work normally, where green threads are invisible to them.</li><li>Diagnostics report the <strong>live worker class, thread count and open stream counts</strong>.</li><li><strong>No dependency change to adopt.</strong> Gunicorn 25.3 already ships both workers.</li></ul><h4 id="risk-profile" tabindex="-1">Risk profile <a class="header-anchor" href="#risk-profile" aria-label="Permalink to &quot;Risk profile&quot;">​</a></h4><ul><li><strong>Opt-in behind a single <code>.env</code> line</strong>; the default is unchanged.</li><li><strong>Rollback is deleting that line and restarting</strong>, no rebuild, no dependency change.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p><strong>This is not a performance improvement.</strong> Expect broadly similar throughput. The value is being able to move to a supported Gunicorn, plus the correctness work the migration forced. Treat any claim that gthread makes Tradeboard faster with suspicion.</p></div><hr><h3 id="who-is-affected-by-the-switch-itself" tabindex="-1">Who is affected by the switch itself <a class="header-anchor" href="#who-is-affected-by-the-switch-itself" aria-label="Permalink to &quot;Who is affected by the switch itself&quot;">​</a></h3><table tabindex="0"><thead><tr><th>How you run Tradeboard</th><th>Uses eventlet today?</th><th>Affected by this switch</th></tr></thead><tbody><tr><td>Docker</td><td>Yes</td><td><strong>Yes</strong></td></tr><tr><td>Ubuntu server via <code>install.sh</code> (systemd + nginx)</td><td>Yes</td><td><strong>Yes</strong></td></tr><tr><td>Ubuntu multi-instance via <code>install-multi.sh</code></td><td>Yes</td><td><strong>Yes</strong></td></tr><tr><td>Windows / macOS desktop (<code>uv run app.py</code>)</td><td>No, already real threads</td><td>No change</td></tr></tbody></table><p>Windows and macOS desktop users do not run Gunicorn at all, so there is nothing to opt into. The concurrency fixes on the branch still benefit you.</p><hr><h3 id="current-status" tabindex="-1">Current status <a class="header-anchor" href="#current-status" aria-label="Permalink to &quot;Current status&quot;">​</a></h3><p>Being direct about readiness:</p><ul><li><strong>122 files changed, 41 commits</strong></li><li><strong>Roughly 64% of tracked migration items complete</strong> (70 of 109 actionable)</li><li>Several known issues remain open and are recorded on the branch</li><li>Three independent audits have already found cases where an earlier fix was itself wrong</li></ul><p>Progress notes live in <code>docs/progress/gthread/</code> on the branch, and every item is tracked in <code>docs/plans/2026-08-01-gthread-migration-tracker.csv</code>.</p><p><strong>Do not run this on a production trading account you cannot afford to babysit.</strong></p><hr><h3 id="step-1-get-the-branch" tabindex="-1">Step 1: Get the branch <a class="header-anchor" href="#step-1-get-the-branch" aria-label="Permalink to &quot;Step 1: Get the branch&quot;">​</a></h3><p>The opt-in variable does nothing on code built from <code>main</code>. The worker-resolution logic only exists on the <code>gthread</code> branch, so you must switch the checkout first.</p><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>Take a backup before switching branches. On Ubuntu, <code>install/update.sh</code> backs up your databases automatically; for Docker, copy your <code>.env</code> and back up the <code>db/</code> volume yourself.</p></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # or wherever you cloned Tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fetch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gthread</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gthread</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gthread</span></span></code></pre></div><p>Your <code>.env</code> is not tracked by git and is preserved.</p><hr><h3 id="step-2-opt-in" tabindex="-1">Step 2: Opt in <a class="header-anchor" href="#step-2-opt-in" aria-label="Permalink to &quot;Step 2: Opt in&quot;">​</a></h3><p>Add <strong>one line</strong> to your <code>.env</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TRADEBOARD_WORKER_CLASS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;gthread&#39;</span></span></code></pre></div><p>That is sufficient. A safe thread count is chosen for you.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><strong>Do not set the thread count on its own.</strong> <code>TRADEBOARD_GUNICORN_THREADS</code> does nothing without the worker class, and Gunicorn&#39;s own default of one thread would let a single live strategy log or MCP stream block the entire server.</p></div><p>There is no dependency change. Gunicorn 25.3, the version already pinned in Tradeboard, ships <strong>both</strong> the eventlet and gthread workers.</p><hr><h3 id="step-3-apply-it" tabindex="-1">Step 3: Apply it <a class="header-anchor" href="#step-3-apply-it" aria-label="Permalink to &quot;Step 3: Apply it&quot;">​</a></h3><h4 id="docker" tabindex="-1">Docker <a class="header-anchor" href="#docker" aria-label="Permalink to &quot;Docker&quot;">​</a></h4><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span></span></code></pre></div><p><code>.env</code> is bind-mounted into the container, so the setting <strong>survives <code>docker pull</code></strong> and does not require regenerating <code>docker-compose.yaml</code>.</p><h4 id="ubuntu-server-systemd" tabindex="-1">Ubuntu server (systemd) <a class="header-anchor" href="#ubuntu-server-systemd" aria-label="Permalink to &quot;Ubuntu server (systemd)&quot;">​</a></h4><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install/update.sh</span></span></code></pre></div><p>The updater rewrites <code>ExecStart</code> in your systemd unit, <strong>backs up the previous unit first</strong>, verifies the new one before touching dependencies, and <strong>restores the backup automatically if the service fails to start</strong>.</p><h4 id="ubuntu-multi-instance" tabindex="-1">Ubuntu multi-instance <a class="header-anchor" href="#ubuntu-multi-instance" aria-label="Permalink to &quot;Ubuntu multi-instance&quot;">​</a></h4><p>Each instance has its own <code>.env</code>. Set the variable per instance and re-run the updater for that instance.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Threads are per instance, so the host cost is <code>threads x instances</code>. With the default of 64 threads and 4 instances that is 256 request threads on one box. <code>install-multi.sh</code> prints the total; divide a per-host budget with <code>TRADEBOARD_GUNICORN_THREADS</code> if that is too high for your VPS.</p></div><hr><h3 id="step-4-verify-it-is-actually-running" tabindex="-1">Step 4: Verify it is actually running <a class="header-anchor" href="#step-4-verify-it-is-actually-running" aria-label="Permalink to &quot;Step 4: Verify it is actually running&quot;">​</a></h3><p>Do not trust the <code>.env</code> file alone. Confirm the running process.</p><p><strong>Docker:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tradeboard-web</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Starting application&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [Tradeboard] Starting application on port 5000 with gthread (64 threads)...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> top</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tradeboard-web</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> worker-class</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ... gunicorn --worker-class gthread --threads 64 --workers 1 ...</span></span></code></pre></div><p><strong>Ubuntu:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tradeboard</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> worker-class</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -eo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[g]unicorn&quot;</span></span></code></pre></div><p><strong>In the web UI:</strong> the admin runtime panel reports the live worker class, configured thread count, active thread count and open stream counts. Use it to see the real numbers under load rather than guessing.</p><hr><h3 id="tuning-the-thread-count" tabindex="-1">Tuning the thread count <a class="header-anchor" href="#tuning-the-thread-count" aria-label="Permalink to &quot;Tuning the thread count&quot;">​</a></h3><p>The default is <strong>64</strong>, chosen because Tradeboard holds a request thread for the entire life of certain connections:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>required threads &gt;=</span></span>
<span class="line"><span>    active Socket.IO clients x 2       (polling holds a GET and a POST)</span></span>
<span class="line"><span>  + live Python Strategy log streams   (each holds a thread until closed)</span></span>
<span class="line"><span>  + live MCP streams                   (same)</span></span>
<span class="line"><span>  + internal loopback reserve          (MCP, Telegram and WhatsApp re-enter the API)</span></span>
<span class="line"><span>  + requests parked in broker rate limiters</span></span>
<span class="line"><span>  + peak ordinary HTTP concurrency</span></span>
<span class="line"><span>  + failure and reconnect headroom</span></span></code></pre></div><p>To override:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TRADEBOARD_GUNICORN_THREADS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;96&#39;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Behaviour</th><th>Value</th></tr></thead><tbody><tr><td>Default when gthread is selected</td><td>64</td></tr><tr><td>Values below 16</td><td>raised to 16, with a warning</td></tr><tr><td>Values above 512</td><td>permitted, with a warning about memory</td></tr><tr><td>Non-numeric</td><td>falls back to 64, with a warning</td></tr></tbody></table><div class="info custom-block"><p class="custom-block-title">INFO</p><p><strong>Market data does not consume Gunicorn threads.</strong> The options tools, charting and scalping terminals stream over a direct WebSocket to the proxy on port 8765, and all tabs share a single connection. Opening the entire tools suite adds zero request threads.</p></div><hr><h3 id="rolling-back" tabindex="-1">Rolling back <a class="header-anchor" href="#rolling-back" aria-label="Permalink to &quot;Rolling back&quot;">​</a></h3><p>Removing the line and restarting is the entire rollback. No rebuild is needed.</p><p><strong>Docker:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/TRADEBOARD_WORKER_CLASS/d;/TRADEBOARD_GUNICORN_THREADS/d&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .env</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>Ubuntu:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/TRADEBOARD_WORKER_CLASS/d;/TRADEBOARD_GUNICORN_THREADS/d&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard/.env</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install/update.sh</span></span></code></pre></div><p>To leave the branch entirely, <code>git checkout main</code> and rebuild or re-run the updater.</p><hr><h3 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h3><p><strong>The log still says eventlet.</strong> The image or unit was not rebuilt, or you are still on <code>main</code>. Confirm with <code>git rev-parse --abbrev-ref HEAD</code>, then rebuild.</p><p><strong>A warning names an unknown worker class.</strong> The value is misspelled. Only <code>eventlet</code> and <code>gthread</code> are accepted; anything else falls back to eventlet <strong>and warns</strong>, so a typo cannot silently leave you thinking you are testing gthread.</p><p><strong>The service will not start after <code>update.sh</code>.</strong> The updater restores the previous unit automatically. Check <code>install/logs/</code> for the run log, and <code>journalctl -u tradeboard -n 50</code>.</p><p><strong>The server becomes unresponsive when a strategy is running.</strong> Report it on issue #1722 with your thread count. This is exactly the failure mode the thread budget exists to prevent.</p><p><strong>&quot;database is locked&quot; errors.</strong> Real threads make SQLite writers genuinely collide where green threads did not. A 15-second busy timeout and a retry for stale-snapshot conflicts are already in place, please report the full entry from <code>log/errors.jsonl</code>.</p><hr><h3 id="what-to-test-and-report" tabindex="-1">What to test and report <a class="header-anchor" href="#what-to-test-and-report" aria-label="Permalink to &quot;What to test and report&quot;">​</a></h3><p>Starting up is not evidence. What is genuinely useful:</p><ul><li><strong>Your broker, through a full trading day</strong>, login, order placement, positions, and the roughly 3:00 AM IST token rollover</li><li><strong>Live WebSocket streaming</strong>, <code>/websocket/test</code> and the option chain tools under real market data</li><li><strong>Python strategies</strong>, especially multi-file strategies and scheduled start/stop</li><li><strong>Sandbox mode</strong>, order fills, square-off, expiry settlement</li><li><strong>Telegram alerts, scalping and charting terminals</strong></li><li><strong>Thread and stream counts from the admin runtime panel under real load</strong>, these numbers are what will justify the final thread budget</li></ul><p>Report on <a href="https://github.com/wesoftcorp/tradeboard/issues/1722" target="_blank" rel="noreferrer">issue #1722</a> with your <strong>broker, operating system, deployment method and thread count</strong>. Negative results are as valuable as positive ones.</p><hr><h3 id="when-does-this-become-the-default" tabindex="-1">When does this become the default <a class="header-anchor" href="#when-does-this-become-the-default" aria-label="Permalink to &quot;When does this become the default&quot;">​</a></h3><p>When it has been through real trading days, on real brokers, on both Docker and Ubuntu, without surprises. <strong>There is no target date.</strong></p><p>Tradeboard is self-hosted, so there is no central rollback: once a change is on <code>main</code>, it reaches your machine whenever you choose to update, and it cannot be recalled. That is precisely why this ships opt-in first and why the default will not change until the evidence supports it.</p><hr><h3 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h3><p><strong>Will this speed up Tradeboard?</strong> That is not the goal. Expect broadly similar throughput. The point is being able to move to a supported Gunicorn.</p><p><strong>Do I need to change my strategies?</strong> No. Strategies run as isolated subprocesses, not inside the web worker.</p><p><strong>Does this affect the WebSocket proxy or ZeroMQ?</strong> No. The proxy runs as its own process on port 8765, and the ZeroMQ bus is unchanged.</p><p><strong>Do I need Node.js or a frontend rebuild?</strong> No. The branch carries a built frontend, as <code>main</code> does.</p><p><strong>Can I run one instance on gthread and another on eventlet?</strong> Yes. The setting is per instance, and comparing the two on one host is a genuinely useful test.</p><p><strong>Is my data at risk?</strong> The switch does not alter the database schema. Normal upgrade care still applies, take backups before switching branches.</p>`,115)])])}const u=t(n,[["render",r]]);export{g as __pageData,u as default};
