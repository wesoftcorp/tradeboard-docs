import{_ as s,o as t,c as o,a2 as a}from"./chunks/framework.BXzK3EA4.js";const h=JSON.parse('{"title":"Version 2.0.1.4 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.4-released.md","filePath":"change-log/release/version-2.0.1.4-released.md"}'),r={name:"change-log/release/version-2.0.1.4-released.md"};function i(n,e,d,c,l,p){return t(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="version-2-0-1-4-released" tabindex="-1">Version 2.0.1.4 Released <a class="header-anchor" href="#version-2-0-1-4-released" aria-label="Permalink to &quot;Version 2.0.1.4 Released&quot;">​</a></h1><p><strong>Date: 21st June 2026</strong></p><p><strong>Feature Release: Scalping Terminal (keyboard-driven order entry with a server-side SL/TP/TSL engine across all F&amp;O exchanges, live candlestick/volume charts), the TradeSmart (Noren v2) broker integration, two new options tools (Gamma Density and OI Range), a /chart/test live multi-chart streaming page, technical-indicator research tools for the MCP server, F&amp;O freeze-quantity correctness across every exchange, and a startup/runtime performance sweep (WAL SQLite, warm broker modules, deferred traffic-log purge)</strong></p><p>This release spans 90+ commits since v2.0.1.3. The headline feature is the <strong>Scalping Terminal</strong> (<code>/scalping</code>) — a keyboard-first order-entry surface with a server-side stop-loss / target / trailing-stop engine, full F&amp;O exchange + segment coverage (NSE/BSE equity &amp; futures, NFO/BFO/CDS/MCX options), live WebSocket-driven tickers and candlestick/volume charts, and freeze-quantity-safe exits. Alongside it ships the <strong>TradeSmart (Noren v2)</strong> broker integration, two new analytics tools — <strong>Gamma Density</strong> (Γ×OI density and convexity zones with forward-based Black-76 IV) and <strong>OI Range</strong> (open interest by strike for a custom range) — and a <strong>/chart/test</strong> page for live multi-chart streaming. The MCP server gains <strong>technical-indicator research tools</strong>, F&amp;O <strong>freeze-quantity handling</strong> is corrected for CDS/MCX and BSE indices, and a <strong>performance sweep</strong> enables WAL on every SQLite connection, warms broker modules at startup, and purges traffic logs past their retention window off the hot path.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Scalping Terminal (<code>/scalping</code>)</strong> — A new keyboard-driven scalping surface, built up across phases: <ul><li><strong>Order entry</strong>: keyboard shortcuts for buy/sell/exit, NIFTY + current-week expiry + ATM defaults on load, <code>NRML</code> default product to avoid post-square-off MIS rejections.</li><li><strong>Server-side risk engine</strong>: stop-loss, target and browser-driven auto-trailing move to a server-side SL/TP/TSL engine with analyze/live segregation, predefined auto SL &amp; target, and book scoping so <code>F6</code> closes only the scalping strategy&#39;s positions.</li><li><strong>All F&amp;O exchanges &amp; segments</strong>: option chain + expiry for every F&amp;O exchange (NFO/BFO/CDS/MCX), futures by underlying + expiry, and a unified exchange/segment frontend including NSE/BSE equity &amp; futures single-instrument views.</li><li><strong>Live data &amp; charts</strong>: WebSocket-live LTP with a MultiQuotes after-hours fallback, OHLC range visualization on the tickers, and live candlestick + volume charts on a shared timeframe.</li><li><strong>Freeze-safe</strong>: exits never exceed exchange freeze limits, with whole-lot splitting on close and an unknown-freeze fallback.</li></ul></li><li><strong>TradeSmart (Noren v2) broker integration (#1548)</strong> — New broker plugin on the Noren v2 stack, with a common broker-integration testing guide and an automated runner added alongside it.</li><li><strong>Gamma Density tool (<code>/gammadensity</code>, #1553)</strong> — Γ×OI density and convexity zones (Intraday and To-Expiry panels, ±1σ/±2σ expected-move bands, ATM IV) for all exchanges and underlyings. Index <strong>weekly</strong> options price off the per-expiry <strong>synthetic future</strong> as the Black-76 forward (monthly index / stocks use spot), fixing IV/Greeks alignment for weekly index options.</li><li><strong>OI Range tool (<code>/oirange</code>)</strong> — Open interest by strike for a custom strike range with ATM-relative quick selectors and optional 1-minute auto-refresh.</li><li><strong>Chart Test page (<code>/chart/test</code>)</strong> — Live multi-chart streaming page for validating real-time candlestick/volume rendering across instruments.</li><li><strong>MCP technical-indicator research tools</strong> — The MCP server gains indicator-calculation/research tools for use from Claude Desktop / Cursor / Windsurf.</li><li><strong>F&amp;O freeze-quantity correctness</strong> — Freeze quantities now honored for <strong>all</strong> F&amp;O exchanges including <strong>CDS and MCX</strong>, and <strong>BFO</strong> freeze quantities supported for SENSEX / BANKEX / SENSEX50.</li><li><strong>Performance &amp; startup sweep</strong> — WAL + <code>synchronous=NORMAL</code> on every SQLite connection, broker modules warmed at startup with keep-warm pings, order hot-path logs demoted to debug across all 33 brokers, latency percentiles bounded to a 30-day window and cached, and traffic logs purged past their retention window at startup.</li><li><strong>Platform version bump</strong> — <code>2.0.1.3</code> → <code>2.0.1.4</code>. SDK pin (<code>tradeboard</code>) <code>2.0.1</code> → <code>2.0.2</code>.</li></ul><hr><p><strong>Scalping Terminal — deep dive</strong></p><p>A keyboard-first scalping workflow delivered in phases (<code>c1c2923d</code> plan → <code>cba25cb7</code> hardening):</p><ul><li><strong>Plumbing &amp; entry</strong> — <code>a38d37df</code> (Phase 0: symbol/expiry/strike plumbing + live tickers), <code>358b0eef</code> (Phase 1: keyboard order entry, sandbox-ready), <code>97d9426f</code> (NIFTY + current-week expiry + ATM defaults), <code>11853ca4</code> (default product <code>NRML</code> to avoid MIS post-square-off 400s).</li><li><strong>Risk engine</strong> — <code>f3e390c0</code> (Phase 2: stop-loss + browser-driven auto-trailing), <code>4a7e56ba</code> (server-side SL/TP/TSL engine, analyze/live segregation, all-underlyings), <code>2594d16e</code> (predefined auto SL &amp; target, WebSocket-driven, book scoping), <code>626e1d2f</code> (rate-limited trailing-SL writes + cached mode lookup), <code>cba25cb7</code> (drop stale mode cache; guard SL exit against mode toggle).</li><li><strong>Exchanges &amp; segments</strong> — <code>f066564c</code> (Phase A: option chain + expiry for all F&amp;O exchanges), <code>2b95004a</code> (Phase B: unified exchange/segment frontend), <code>b9e9aa4d</code> (futures by underlying + expiry), <code>20ab79d9</code> (MCX/CDS exchanges + 1cliq control-row), <code>3baabc2f</code> (NSE/BSE equity &amp; futures single-instrument view).</li><li><strong>Live data &amp; charts</strong> — <code>7be6bf00</code> (WebSocket-live LTP with MultiQuotes after-hours fallback), <code>1bcd3fb2</code> (OHLC range on tickers), <code>0e56b9de</code> (live candlestick + volume charts on a shared timeframe), <code>db516b84</code> (structure-only option chain + event-driven order updates for lower latency).</li><li><strong>Freeze-safety &amp; correctness</strong> — <code>787ba14e</code> (whole-lot freeze split on close), <code>d31efa1f</code> (exits never exceed freeze limit, unknown-freeze fallback), <code>71a61ca9</code> (<code>F6</code> closes only the scalping strategy&#39;s positions), <code>af6751d2</code> (trust positionbook for net qty so closed legs aren&#39;t phantom-open), <code>c4c5523c</code> (coerce string position/trade numerics so live mode doesn&#39;t crash), <code>a8363edc</code> (CDS 4-decimal precision and live charts when the broker has no history).</li><li><strong>Hardening</strong> — <code>45ebd799</code> and <code>30ec6617</code> (security + correctness review fixes: SL exit safety, server lot cap), <code>6f5a9e2d</code> (stop Socket.IO reconnect churn + live-mode order apikey), <code>6b8fbf4e</code>/<code>6b2c4c00</code> (de-duplicate order notifications + trader-friendly errors), <code>7186446a</code> (share one Socket.IO connection per tab — fixes multi-tab hang).</li></ul><p>A new <code>database/scalping_db.py</code> table persists scalping strategy/SL state; it is created automatically.</p><hr><p><strong>TradeSmart (Noren v2) — deep dive</strong></p><p><code>feat: add TradeSmart (Noren v2) broker integration</code> — <code>71359747</code>, merged via PR #1548.</p><ul><li>New broker plugin following the standard <code>api/</code>, <code>mapping/</code>, <code>database/</code>, <code>streaming/</code>, <code>plugin.json</code> layout on the Noren v2 stack.</li><li>Data-API throttling: <code>7c65b839</code> caps the data API at 120 requests/min with retry on rate-limit responses.</li><li><code>test: add common broker-integration testing guide + automated runner</code> — <code>3c67f7dc</code> adds a reusable broker test harness used to validate the integration.</li></ul><hr><p><strong>Options tools</strong></p><ul><li><strong>Gamma Density</strong> (<code>44a7fe50</code>, PR #1553) — <code>services/gamma_density_service.py</code> reuses the option chain for OI + LTP and computes per-strike gamma via <code>opengreeks</code> Black-76 for two horizons (intraday 1-day and full to-expiry), returning Γ×OI density plus daily/to-expiry ±1σ/±2σ expected-move bands. Index weekly options use the per-expiry synthetic future as the Black-76 forward; the expected-move band and Spot marker stay on cash spot.</li><li><strong>OI Range</strong> (<code>dc05c7ac</code>) — open interest by strike for a custom range, with ATM-relative quick selectors (5/10/15/20 strikes each side) and optional 1-minute auto-refresh.</li><li><strong>Chart Test</strong> (<code>e745e319</code>) — <code>/chart/test</code> live multi-chart streaming page; <code>59d41f35</code> adds the explicit <code>/scalping</code> route to <code>react_app.py</code>.</li></ul><hr><p><strong>Broker fixes</strong></p><ul><li><code>71359747</code> — <strong>TradeSmart</strong>: new Noren v2 broker integration (#1548).</li><li><code>64d58909</code> — <strong>Arrow</strong>: multiquotes made resilient and diagnosable.</li><li><code>26fb5e3f</code> — <strong>5paisa</strong>: market orders via MPP, smart-order auth fix, historical candles for index symbols, WebSocket FD/thread leak fixes in the reconnect path, hardened reconnect (ping timeout, interruptible backoff, race), batched WS subscriptions (#1524); <code>b707dc99</code> — real intraday history timestamps + review fixes.</li><li><code>1c93aeaf</code> — <strong>Dhan</strong>: stop dropping 90-day intraday chunks on weekend boundaries (#1517).</li><li><code>119f8012</code> — <strong>Definedge</strong>: anchor intraday history end-time to IST so data doesn&#39;t lag on non-IST hosts.</li><li><code>4c816c83</code> — <strong>Firstock</strong>: map <code>BSE_INDEX</code> to <code>BSE</code> in quote/depth/history paths.</li></ul><hr><p><strong>Freeze quantities</strong></p><ul><li><code>e087866c</code> — honor freeze quantities for all F&amp;O exchanges, including <strong>CDS</strong> and <strong>MCX</strong> (#1541 family).</li><li><code>55184509</code> — support <strong>BFO</strong> freeze quantities for SENSEX / BANKEX / SENSEX50.</li></ul><hr><p><strong>Performance &amp; reliability</strong></p><ul><li><code>7b7505af</code> — enable WAL + <code>synchronous=NORMAL</code> for all SQLite connections.</li><li><code>3ec2084e</code> — warm broker modules at startup with fast-start keep-warm pings.</li><li><code>9f94a857</code> — demote order hot-path logs to debug across all 33 brokers.</li><li><code>4e0154cc</code> — bound latency stats percentiles to a 30-day window and cache the result.</li><li><code>d221c9cd</code> — purge traffic logs past the retention window at startup (<code>TRAFFIC_LOG_RETENTION_DAYS</code>).</li><li><code>db516b84</code> — structure-only option chain + event-driven order updates (scalping).</li></ul><hr><p><strong>WhatsApp / messaging</strong></p><ul><li><code>2259ade6</code> — marshal <code>wars</code> callbacks off the eventlet hub to prevent freezes (#1515).</li><li><code>eedbed45</code> — bump <code>wars</code> to 0.1.4 (PN→LID delivery) and fix the <code>on_disconnect</code> crash (#1512).</li></ul><hr><p><strong>Other fixes</strong></p><ul><li><code>61985786</code> — <strong>Playground</strong>: redirect legacy <code>/playground/</code> instead of returning 500 (#1526).</li></ul><hr><p><strong>Security</strong></p><ul><li><code>c8517646</code> — patch Dependabot security advisories (<code>fix/dependabot-security-bumps</code>, #1535).</li></ul><hr><p><strong>Documentation</strong></p><ul><li><code>c1b0a168</code> … <code>5324469d</code> — Flask→FastAPI migration plan, risk assessment, and a 63-item migration tracker CSV (assessment only; no runtime change).</li><li><code>c1c2923d</code>, <code>664cb548</code>, <code>4d4e5311</code> — scalping terminal implementation plan + sandbox order-lifecycle validation.</li><li><code>2677dd8c</code> — group indicator prompt docs into <code>docs/prompt/indicators/</code> (#1529).</li><li><code>ae6f0224</code> — add the current Tradeboard PRD.</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>tradeboard</code> SDK pin: <code>2.0.1</code> → <strong><code>2.0.2</code></strong> (PyPI: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a>).</li><li><code>opengreeks</code> <code>&gt;=0.2.0</code> (unchanged).</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>utils/version.py</code>: <code>VERSION = &quot;2.0.1.4&quot;</code></p><p><code>pyproject.toml</code>: <code>version = &quot;2.0.1.4&quot;</code>; <code>tradeboard==2.0.2</code> (<code>uv.lock</code> regenerated).</p><p><code>.sample.env</code>:</p><ul><li><code>VALID_BROKERS</code> gains <code>tradesmart</code>.</li><li>New <strong>optional</strong> variable <code>TRAFFIC_LOG_RETENTION_DAYS=&#39;30&#39;</code> — days to retain request traffic logs in <code>logs.db</code> (purged at startup; default applies if absent, no action required).</li></ul><p><strong>Database</strong>: a new <code>scalping_db</code> table backs the Scalping Terminal&#39;s strategy/SL state. It is created automatically on startup — no manual migration required.</p><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend: a plain pull already ships the CI-built dist. Only rebuild if</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you are editing React code:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management; Scalping Terminal (keyboard order entry, server-side SL/TP/TSL engine, all-exchange option chain/futures, live charts, freeze-safe exits, hardening); Gamma Density and OI Range tools; Chart Test live-streaming page; MCP technical-indicator research tools; F&amp;O freeze-quantity fixes (CDS/MCX/BSE indices); WAL/startup/latency performance sweep; WhatsApp eventlet-hub and <code>wars</code> fixes; SDK pin bump.</li><li><strong>@Kalaiviswa</strong> — TradeSmart (Noren v2) broker integration (#1548); Arrow multiquotes resilience; 5paisa market orders/smart-order/WS-reconnect overhaul (#1524) and intraday timestamp fixes; Firstock <code>BSE_INDEX</code>→<code>BSE</code> mapping; Definedge intraday IST anchoring.</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul>`,64)])])}const u=s(r,[["render",i]]);export{h as __pageData,u as default};
