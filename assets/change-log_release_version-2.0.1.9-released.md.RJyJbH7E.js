import{_ as o,o as t,c as a,a2 as n}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Version 2.0.1.9 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.9-released.md","filePath":"change-log/release/version-2.0.1.9-released.md"}'),d={name:"change-log/release/version-2.0.1.9-released.md"};function r(s,e,i,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="version-2-0-1-9-released" tabindex="-1">Version 2.0.1.9 Released <a class="header-anchor" href="#version-2-0-1-9-released" aria-label="Permalink to &quot;Version 2.0.1.9 Released&quot;">​</a></h1><p><strong>Date: 7th August 2026</strong></p><p><strong>Sandbox and Reliability Release: brings GTT orders to analyzer mode with full live parity; migrates Nubra to OMS V3 and adds IndMoney TOTP login with stop orders; closes a forced-logout CSRF vector and a logout that left users stranded mid-login; and fixes three unbounded-growth defects that degraded a long-running worker over hours</strong></p><p>This release spans 72 commits since 2.0.1.8. The headline work is <strong>GTT orders in analyzer mode</strong> (issue #1740), taken from first implementation through a hardening pass that made every transition atomic, correctly funded and recoverable — the sandbox now mirrors live GTT behaviour rather than approximating it. On the broker side, <strong>Nubra</strong> was migrated to OMS V3 with phone-OTP login, <strong>IndMoney</strong> gained TOTP login and stop orders, <strong>5paisa</strong> completed a live-verified audit across orders, market data, master contract and order updates, and <strong>Shoonya</strong> had daily historical data restored via <code>EODChartData</code>. A single class of bug — market data keyed on token alone, which collides across exchanges — was fixed across <strong>six Noren-family brokers</strong> in one sweep.</p><p>Three <strong>security</strong> items landed: a forced-logout CSRF vector on <code>/auth/logout</code>, a logout that silently did nothing when broker OAuth was never completed, and <code>h2</code> upgraded past a request-smuggling advisory. Separately, three <strong>unbounded-growth defects</strong> were fixed — the kind that only surface on a single Gunicorn worker that never restarts, where nothing reclaims what leaks.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Security: forced-logout CSRF on <code>/auth/logout</code> (<code>4fd69d718</code>)</strong> — the endpoint was CSRF-exempt under the comment &quot;safe - only destroys session&quot;, which was not true of it: logout revokes the broker token, publishes <code>CACHE_INVALIDATE_ALL</code> to tear down the shared WebSocket feed, clears every device&#39;s session and flushes the symbol cache. Flask-WTF never validates GET, and <code>SESSION_COOKIE_SAMESITE=&quot;Lax&quot;</code> still attaches the session cookie to top-level cross-site navigations, so a link on any page the user visited could revoke their broker session mid-trading-day. The exemption is gone, and GET is now covered by a fetch-metadata check that honours only <code>same-origin</code> and <code>none</code>. The endpoint had been POST-only after <code>43e8a1b5b</code> and regressed in <code>74c25ef26</code>; the regression test that should have caught it was passing vacuously against a server that was not running, and has been rewritten offline.</li><li><strong>Security: logout left users stranded in a half-logged-in session (<code>63da7e8d5</code>, #1762, #1756)</strong> — <code>session.clear()</code> sat inside an <code>if session.get(&quot;logged_in&quot;)</code> guard, so a session with the password step done but broker OAuth not completed survived logout entirely and <code>/auth/login</code> bounced straight back to <code>/broker</code>, with no in-app escape. That state is also the normal path every morning after the ~03:00 IST broker token expiry, not just an abandoned login. The session is now cleared unconditionally and before teardown, so a failure in revocation cannot strand the user; the heavy side effects stay gated on a fully active session, since running them for a half-logged-in one would kill the live feed on the user&#39;s other devices (#1591). It also closed an incomplete-logout weakness: the residual <code>session[&quot;user&quot;]</code> was enough to complete broker OAuth via the CSRF-exempt <code>brlogin.broker_callback</code>. Reported and fixed by Sebastian Legarraga.</li><li><strong>Security: <code>h2</code> 4.4.1 (<code>9c664d8f6</code>, <code>087a20e4c</code>, GHSA-6hr6-w5qg-qmwg)</strong> — a duplicate <code>Host</code> header could facilitate request smuggling. <code>h2</code> is on the hot path rather than incidental: <code>utils/httpx_client</code> enables HTTP/2 for the shared client, so every broker API call negotiates through it. 4.4.1 requires <code>hpack&gt;=4.2</code>, so that pin moved too, and the advisory was filed against four manifests — <code>pyproject.toml</code>, <code>uv.lock</code>, <code>requirements.txt</code> and <code>requirements-nginx.txt</code> — all of which now agree.</li><li><strong>Sandbox: GTT orders in analyzer mode (<code>3c001183a</code>, <code>5df370d7c</code>, #1740)</strong> — GTT support in the sandbox with websocket monitoring, catch-up processing, expiry and reconciliation, reaching parity with the live path.</li><li><strong>Sandbox: GTT correctness hardening (<code>551805904</code>, <code>13f896da7</code>, <code>67e2b521a</code>, <code>8c4ec5abe</code>, <code>54baa011c</code>, <code>bfc43cb5a</code>, <code>f001d7cc8</code>)</strong> — every GTT transition now moves funds and state in a single commit, claims conditionally, and is recoverable; no state change is published unless the funds actually moved; order correlation is durable with no double-block and an honest cancel; the cancel/fire race is closed and immutability honoured. A release-blocker pass corrected direction, OCO, cancel, margin, ticks and payload shape.</li><li><strong>Sandbox: fund-accounting integrity (<code>f91567238</code>, <code>38ab7b62c</code>, <code>bbff5fbc7</code>)</strong> — staged margin is computed in SQL rather than Python, closing a lost-update window; margin release can no longer exceed what was reserved; and every committing fund path is guarded, with three tests that were passing without asserting anything made real.</li><li><strong>Sandbox: execution cycles no longer scale with resting orders (<code>b6cf20595</code>, #1768)</strong> — <code>check_and_execute_pending_orders</code> slept 1s per 10 pending orders, so a cycle cost <code>ceil(N/10)-1</code> seconds on queue depth alone — 14s behind 150 resting orders — and paid it even when nothing was fillable. The sleep protected nothing: quotes for the whole cycle are fetched up front in one batch, leaving only dict lookups and local writes with no broker call to throttle. It compounded, since a slower cycle stranded more orders. Measured, one order&#39;s time-to-fill went from 4.6s with no backlog to 34.8s behind 150; now flat.</li><li><strong>EventBus and Flow lock registry no longer grow without bound (<code>42776df82</code>, #1739)</strong> — <code>EventBus</code> dispatched through a <code>ThreadPoolExecutor</code> whose work queue is an unbounded <code>SimpleQueue</code>, so a publisher outrunning its subscribers grew it until the process was OOM-killed. Publishing now reserves one of 1000 in-flight slots and sheds load when saturated, which is the right trade for best-effort side effects and avoids pushing the stall back into the order path. The Flow workflow-lock registry moved to a <code>WeakValueDictionary</code> so entries track workflows executing now rather than every workflow ever run — deliberately not a size-capped dict, since evicting a lock a thread is holding would let the same workflow execute twice and place every order in it twice.</li><li><strong>Nubra: OMS V3 migration (<code>97a196a0c</code>, <code>ad144314a</code>, <code>67980aef0</code>, <code>79644572d</code>, <code>e512abe08</code>, <code>94bdc3e54</code>, <code>8cac1c4b0</code>)</strong> — migrated to OMS V3 with phone-OTP login, corrected position fields, F&amp;O exchange mapping and history window; session expiry now propagates through depth, multiquotes and cancel-all instead of surfacing as a generic failure; index history returns candles again (<code>tick_volume</code> is invalid for INDEX); and exchange mapping moved into <code>transform_data</code> per the standard broker layout.</li><li><strong>IndMoney: TOTP login and stop orders (<code>7834da0d9</code>, <code>1be1d9238</code>, <code>2f9f3b32e</code>, <code>2a991a8d9</code>, <code>43e3ad2a6</code>)</strong> — TOTP login, stop orders and live-verified feed fixes, with REST calls paced against one shared per-category clock, the underlying root stored in <code>SymToken.name</code>, the order-id cache scoped per user, and token rejection hardened.</li><li><strong>5paisa: live-verified audit (<code>2d230d1d4</code>, #1772)</strong> — orders, market data, master contract and order updates verified against a live session.</li><li><strong>Shoonya: daily historical data restored (<code>b5cdf635e</code>, <code>376f3c313</code>, <code>3ac3ced43</code>)</strong> — daily candles now come from <code>EODChartData</code>, the date-only fallback is pinned to 00:00 UTC, and an incomplete master-contract row is skipped rather than aborting the whole exchange.</li><li><strong>Six brokers: market data keyed on exchange and token (<code>e6b7e0028</code>, <code>e5f11b690</code>, <code>b1eac93a3</code>, <code>baa9cbb53</code>, <code>7349d24ae</code>, <code>c1476766e</code>)</strong> — Shoonya, Flattrade, Zebu, Tradesmart, Definedge and Firstock all keyed their market data cache or routing on token alone, which collides across exchanges. Fixed uniformly.</li><li><strong>Tools: MCX and NCO option chains (<code>dc233afa9</code>, <code>fe3a01826</code>, <code>21bd191a0</code>, <code>ae6d85756</code>, #1747, #1748)</strong> — MCX option chains are priced against the near-month future rather than a spot that does not exist; NCO and BCD chains and expiries are supported; MCX appears in the options tools with NCO accepted at the API boundary; and expiry lookups no longer return a mix of options and futures.</li><li><strong>Flow: explainability (<code>1139da6c4</code>, <code>59617068e</code>, <code>efb1f82c4</code>)</strong> — a diagnostic that explains why a workflow placed no orders, a persisted node trace so a run that did nothing can still be examined, and the market-hours window taken from the calendar with a per-flow override.</li><li><strong>Market calendar: NFO/BFO close moved to 15:40 (<code>e0f7536a6</code>)</strong> — for SEBI&#39;s Closing Auction Session.</li><li><strong>Startup and scheduling (<code>f708aaa1f</code>, <code>4e6d370fb</code>, <code>eb54e9d1c</code>, <code>413534d8c</code>)</strong> — the SQLite startup lock is fixed by serializing init and cleaning sessions, and the APScheduler job store tables are created both at db init and in the migrations, so existing installations get them too.</li><li><strong>Reliability</strong> — Historify releases its scoped sessions when a scheduled download finishes (<code>730cd7dc7</code>); the order-update adapter no longer connects or retries against a dead broker token (<code>7b6db0bc7</code>); GTT latency is retained rather than purged after a week (<code>7433e5f7c</code>); Tradesmart quotes are paced at the new 100/sec cap separately from history (<code>f5705c4f0</code>); and the chart terminal no longer offers CNC on MCX, NCO and CDS, where only NRML applies (<code>694795e01</code>).</li><li><strong>Testing and CI</strong> — the CI-safe backend suite grew from 5 tests to 35. Two files in the allowlist had been passing without asserting anything, because they drive a live server that CI does not run; both are rewritten offline. New regression coverage guards the logout paths, the sandbox execution backlog, and the EventBus and lock-registry bounds.</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>h2</code>: upgraded to <strong>4.4.1</strong> (GHSA-6hr6-w5qg-qmwg)</li><li><code>hpack</code>: upgraded to <strong>4.2.0</strong> (required by <code>h2</code> 4.4.1)</li></ul><hr><p><strong>Upgrading</strong></p><p><code>git pull</code> on <code>main</code> is the canonical path — <code>frontend/dist</code> is tracked there and rebuilt by CI, so no Node.js is needed on the server. Run <code>cd upgrade &amp;&amp; uv run migrate_all.py</code> to pick up the APScheduler job store tables on an existing installation.</p>',14)])])}const p=o(d,[["render",r]]);export{u as __pageData,p as default};
