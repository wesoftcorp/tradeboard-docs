import{_ as t,o as a,c as s,a2 as o}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Strategy RMS API","description":"","frontmatter":{},"headers":[],"relativePath":"api-documentation/v1/strategy-rms-api/index.md","filePath":"api-documentation/v1/strategy-rms-api/index.md"}'),i={name:"api-documentation/v1/strategy-rms-api/index.md"};function d(n,e,r,l,c,h){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="strategy-rms-api" tabindex="-1">Strategy RMS API <a class="header-anchor" href="#strategy-rms-api" aria-label="Permalink to &quot;Strategy RMS API&quot;">​</a></h1><p>The Strategy RMS API is the lifecycle and audit surface for strategies that already exist in the browser at <code>/strategy</code>. It is registered under <code>/api/v1/strategy</code> and uses an Tradeboard API key in every JSON request body.</p><p>It can list strategies, inspect configuration and current runs, start a batch run, request exits, and query durable history. It deliberately cannot create or edit strategies, enable live mode, rotate a webhook token, or delete a strategy. Those privileged configuration actions stay in the browser&#39;s session-authenticated surface.</p><h2 id="base-url-and-authentication" tabindex="-1">Base URL And Authentication <a class="header-anchor" href="#base-url-and-authentication" aria-label="Permalink to &quot;Base URL And Authentication&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http://127.0.0.1:5000/api/v1/strategy</span></span></code></pre></div><p>Every endpoint below is a <code>POST</code> and includes <code>apikey</code>. <code>strategy_id</code> is a positive integer in the JSON body, not a URL path parameter.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;apikey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;your_app_apikey&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;strategy_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>An invalid API key returns 403. A strategy that is missing or belongs to another user returns the same 404 response, <code>Strategy not found</code>; ownership cannot be probed through this API.</p><h2 id="endpoints" tabindex="-1">Endpoints <a class="header-anchor" href="#endpoints" aria-label="Permalink to &quot;Endpoints&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td>POST</td><td><code>/list</code></td><td><a href="./list.html">List strategies</a></td></tr><tr><td>POST</td><td><code>/status</code></td><td><a href="./status.html">Strategy configuration and current run</a></td></tr><tr><td>POST</td><td><code>/start</code></td><td><a href="./start.html">Start a batch run</a></td></tr><tr><td>POST</td><td><code>/stop</code></td><td><a href="./stop.html">Request a durable stop for the current run</a></td></tr><tr><td>POST</td><td><code>/close_all</code></td><td><a href="./close-all.html">Record and request an operator close-all</a></td></tr><tr><td>POST</td><td><code>/close_leg</code></td><td><a href="./close-leg.html">Close one current-run leg</a></td></tr><tr><td>POST</td><td><code>/runs</code></td><td><a href="./runs.html">Read run history</a></td></tr><tr><td>POST</td><td><code>/orders</code></td><td><a href="./orders.html">Read strategy order history</a></td></tr><tr><td>POST</td><td><code>/events</code></td><td><a href="./events.html">Read the risk-event audit trail</a></td></tr></tbody></table><h2 id="lifecycle-rule-accepted-does-not-mean-flat" tabindex="-1">Lifecycle Rule: Accepted Does Not Mean Flat <a class="header-anchor" href="#lifecycle-rule-accepted-does-not-mean-flat" aria-label="Permalink to &quot;Lifecycle Rule: Accepted Does Not Mean Flat&quot;">​</a></h2><p><code>/stop</code> and <code>/close_all</code> can return HTTP 200 with <code>stop_pending: true</code>. That means the stop intent is durable and an exit was accepted, but the run remains open, subscribed, and managed until its exact owners are proven flat by fill evidence. A 409 can also describe a refused exit while a run remains open. Always inspect <code>stop_pending</code> and each exit&#39;s <code>ok</code> field; do not infer flatness from the status code or success envelope.</p><p><code>/close_leg</code> reports <code>run_stopped: true</code> only when this call could already prove the last owned position is flat. A normally asynchronous broker acknowledgement is not that proof.</p><h2 id="request-validation-limits-and-times" tabindex="-1">Request Validation, Limits, And Times <a class="header-anchor" href="#request-validation-limits-and-times" aria-label="Permalink to &quot;Request Validation, Limits, And Times&quot;">​</a></h2><p>Request schemas reject fields that the endpoint does not declare. An unknown field, missing required field, invalid enum, or out-of-range limit returns 400.</p><p>All routes use the deployment&#39;s <code>API_RATE_LIMIT</code> configuration. This module&#39;s in-code fallback is <code>10 per second</code>; deployments may set a different value.</p><p>Times are ISO 8601 UTC strings with an explicit <code>+00:00</code> offset. Strategy <code>entry_time</code> and <code>exit_time</code> configuration values are wall-clock IST <code>HH:MM</code> strings. Money values are JSON numbers.</p><h2 id="public-alert-webhook" tabindex="-1">Public Alert Webhook <a class="header-anchor" href="#public-alert-webhook" aria-label="Permalink to &quot;Public Alert Webhook&quot;">​</a></h2><p>Alert senders should use <a href="./webhook.html">Public Strategy Webhook</a>, not this API. It is <code>POST /strategy/webhook/&lt;token&gt;</code>, outside <code>/api/v1</code>, and authenticates with its high-entropy URL token rather than <code>apikey</code>.</p><hr><p><strong>Back to</strong>: <a href="./../README.html">V1 API documentation</a></p>`,21)])])}const g=t(i,[["render",d]]);export{u as __pageData,g as default};
