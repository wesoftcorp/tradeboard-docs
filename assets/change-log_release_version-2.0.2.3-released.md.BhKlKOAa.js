import{_ as t,o,c as a,a2 as r}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.2.3 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.2.3-released.md","filePath":"change-log/release/version-2.0.2.3-released.md"}'),s={name:"change-log/release/version-2.0.2.3-released.md"};function n(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="version-2-0-2-3-released" tabindex="-1">Version 2.0.2.3 Released <a class="header-anchor" href="#version-2-0-2-3-released" aria-label="Permalink to &quot;Version 2.0.2.3 Released&quot;">​</a></h1><p><strong>Date: 6th September 2026</strong></p><p><strong>Feature Release: adds the Strategy Module and RMS at <code>/strategy</code>, a multi-leg options engine with end-to-end risk management built on a broker-agnostic risk core; ships the Tradeboard Agent at <code>/agent</code>, an LLM assistant that reads live account and market data and can place orders at the operator&#39;s direction, usable on a ChatGPT subscription instead of an API key; rebuilds the charting terminal on tradeboard-charts 2.0.2 with a bottom dock, armed one-click trading and a chart-side agent panel; and implements Kotak Neo historical data, which the plugin previously did not serve at all</strong></p><p>This release spans 217 commits since 2.0.2.2 and is dominated by two new modules rather than by fixes to existing ones.</p><p>The larger is the <strong>Strategy Module and RMS</strong>. The legacy <code>/strategy</code> module was retired and replaced by a multi-leg options engine in which risk is the point rather than an afterthought. Two kinds of strategy share one engine: a <code>batch</code> strategy enters and exits every leg together, a <code>signal</code> strategy moves one leg per alert, which is what makes per-alert TradingView trading work without a second code path. Underneath both sits <code>services/risk/</code>, a core that performs no I/O of any kind - no database, no broker, no market data, no clock, no logging. Every input arrives as an argument and every decision leaves as a return value, which is what makes it testable without a running platform, identical across the scalping terminal, Flow and the REST surface, and safe to call from a green thread and a real one alike.</p><p>That design was not chosen for elegance. The module this was ported from had its own evaluator, and four defects lived in it undetected: a leg with no recorded side evaluated as a short, so its stop fired on a favourable move; an exit derived from configuration doubled a short instead of covering it; an aggregate summed from a stale per-leg field; and a peak and trough persisted as zero on most stop paths. Each is now pinned by a test marked <code>PORTED DEFECT</code>, and <code>test/risk/vectors.json</code> is the contract between the Python core and its TypeScript counterpart so the two cannot drift.</p><p>A large share of this release&#39;s 44 strategy fixes are order-path safety work, and they converge on three rules, each learned from a defect that reversed a real position. <strong>Claim under the same lock that checks</strong>: the duplicate check and the marker write happen in one hold, and the marker is written before dispatch rather than after the order returns, because testing the order id let two rules firing on one leg send a covering order each. <strong>Match a fill to the order it belongs to, not to the leg</strong>: a signal flip squares one side and opens the other immediately, so one leg id names two positions until the closing order fills, and applying an exit fill by leg alone closed the position that had just been opened. <strong>A caller that has already decided the pipe says so</strong>: <code>place_order_with_auth</code> reads the platform-wide analyzer toggle first, so an operator switching to analyze mode mid-run sent every exit to the sandbox, which reports success.</p><p>The second module is the <strong>Agent</strong>. It is an LLM assistant with access to Tradeboard&#39;s own service layer rather than a chat window bolted on: it can read the order book, positions, holdings, funds, quotes and history, compute Greeks and payoff diagrams, and place orders when the operator turns trading on. It runs on any LiteLLM-supported provider, and notably on a ChatGPT Plus or Pro subscription through OAuth device flow, which reaches Codex rather than the ChatGPT web app. It appears both as a full page at <code>/agent</code> and as a right-rail panel inside the charting terminal.</p><p>Outside those, the charting terminal moved from tradeboard-charts 1.8.2 to 2.0.2 and gained a bottom dock, an explicit armed mode for one-click trading, watchlist and option chain side panels, and a user guide chapter. Kotak Neo gained historical data. Several broker plugins were repaired.</p><hr><p><strong>Highlights</strong></p><ul><li><p><strong>Strategy Module and RMS at <code>/strategy</code> (<code>db57bf9ca</code> through <code>e62d6cc7f</code>, 71 commits)</strong> - multi-leg options strategies with stop loss, target and trailing stop evaluated per leg and in aggregate, driven by the live tick feed with a REST fallback. Legs are measured in points or percent and counted in lots or units. The kill switch flattens rather than merely stopping. Crash recovery and periodic checkpointing mean a restart mid-run does not lose the position, and the books are served from the broker rather than from stored rows, so what the screen shows is what the broker holds. The module is exposed over the API-key surface at <code>/api/v1/strategy</code> and over a webhook, so a TradingView alert can move a single leg. Scheduling runs on IST times with cron jobs synced on every write.</p><p>The safety work is the bulk of it. A stop whose exit orders were refused leaves the run <strong>open and managed</strong>, because the position is still there - reporting success and clearing the state is how a position ends up with nothing watching it. A refused dispatch releases its claim, or that leg is skipped by its stop loss, its target and every square-off for the rest of the session while the broker still holds it. Stale position marks are rejected, nullable broker positions are preserved rather than read as flat, and terminal partial fills are reconciled instead of assumed complete.</p><p>Full reference: <a href="https://github.com/wesoftcorp/tradeboard/blob/main/docs/prompt/strategy_rms_documentation.md" target="_blank" rel="noreferrer"><code>docs/prompt/strategy_rms_documentation.md</code></a>.</p></li><li><p><strong>The broker-agnostic risk core (<code>55984920d</code>)</strong> - <code>services/risk/</code> decides whether a position has hit its stop, taken its target, earned a tighter trailing stop, or whether a set of positions has run past its combined limits. It does no I/O, so it is exercised directly by golden vectors rather than through a running platform. Consumers translate, they do not decide: <code>services/strategy_module/risk_adapter.py</code> maps a leg onto <code>PositionRisk</code>, calls the core and writes the decision back. Adding a rule means changing the core, not adding a second evaluator beside it.</p></li><li><p><strong>The Tradeboard Agent at <code>/agent</code> (<code>72a5edea6</code> through <code>a62931aea</code>, #1997)</strong> - an LLM agent wired to Tradeboard&#39;s service layer. It answers from live data: order book, positions, holdings, funds, quotes, history, 127 indicators, streaming instrument cards, option Greeks, payoff diagrams and combined premium charts, with three renderers for charts in chat. Conversation threads persist, questions can be edited in place, and answers can be copied or retried. Trading is off until the operator switches it on, and reasoning effort is selectable per turn.</p><p><strong>It runs on a ChatGPT subscription (<code>835ca8bd5</code>)</strong>, authenticating by OAuth device flow instead of an API key, which reaches Codex rather than the ChatGPT web app. The provider catalogue is read live from LiteLLM so a package bump brings new models with it; the <code>chatgpt/*</code> entries are the one exception and are maintained by hand in <code>services/agent/chatgpt_models.py</code>, because a model absent from LiteLLM&#39;s registry has no <code>mode</code> and gets routed through the chat-completions bridge, landing on a Cloudflare interstitial that returns <code>403 Enable JavaScript and cookies to continue</code> rather than reaching the API. Web search falls back to Tavily rather than DuckDuckGo (<code>14833f2f4</code>).</p></li><li><p><strong>Charting terminal: tradeboard-charts 1.8.2 to 2.0.2 (<code>962310609</code>, <code>19c5d7089</code>)</strong> - drawings saved by the old version are upgraded on load with their text, styling and Fibonacci ratios intact.</p><p><strong>A bottom dock (<code>7d61076ea</code>)</strong> shows orders, positions, trades and GTT across every symbol, not just the charted one, updating live from the account order stream and reconciling against the broker book. Cancel, modify and per-position close act on one row; cancel all and close all sit behind a confirmation; every write refuses while a pane is replaying.</p><p><strong>One-click trading is now an explicit armed mode, off by default (<code>71d293e91</code>)</strong>. A click on the chart used to send a live market order with no confirmation and no way to switch that off. Disarmed, the same click opens an order ticket prefilled with what the chart would have sent. Arming gates new risk only: closing a position, cancelling an order and dragging one to a new price work either way.</p><p>Also: watchlist and option chain side panels (<code>f132e41f2</code>), a watchlist that chooses its own columns and symbol display (<code>7de2819d7</code>), replay start-bar selection that live ticks cannot corrupt (<code>c6afaf6f0</code>), copy as well as save from the camera (<code>dba8b659c</code>), a drawing tool that can stay armed, Delete removing the selected drawing, and a double-click that maximizes the pane instead of resetting the view and quietly fetching another page of history (<code>be23baa89</code>). The user guide gains a chapter: <a href="https://github.com/wesoftcorp/tradeboard/blob/main/docs/userguide/32-charting-terminal/README.md" target="_blank" rel="noreferrer">32 - Charting Terminal</a>.</p></li><li><p><strong>Kotak Neo historical data (<code>44c488c25</code>, #2008, #2010)</strong> - the plugin previously served no historical data at all: <code>get_history</code> was a placeholder returning an empty frame and the interval lists were empty. It now serves 1m, 3m, 5m, 10m, 15m, 30m, 1h, D and W across NSE, BSE, NFO, BFO, NSE_INDEX and BSE_INDEX, as a direct REST integration on the shared pooled HTTP/2 client rather than a wrapper around the vendor SDK.</p><p>Three behaviours were measured against the live API rather than taken from the specification. Kotak publishes no historical rate limit, but the endpoint returns HTTP 429 after roughly five requests in a second, so requests are paced at four per second by a module-level limiter - module-level because <code>history_service</code> builds a fresh <code>BrokerData</code> per request and Historify runs concurrent workers that would otherwise burst - with a 429 retrying the same request rather than falling through to another candidate. An empty range arrives as a <strong>400 fault</strong>, not an empty success, under two different messages, so a pull whose last chunk landed on a weekend or on today before the open used to abort entirely. And index names are matched case-sensitively here and disagree with the quotes endpoint: INDIAVIX answers to <code>INDIA VIX</code>, not <code>India VIX</code>.</p><p><strong>MCX and CDS have no historical data on Kotak.</strong> The API refuses every contract in those segments with <code>Historical data is not supported for this exchange</code>, confirmed across four commodities and three segment spellings, so Tradeboard rejects the request with a clear error before sending it rather than returning an empty series that would read as a market holiday. Both segments continue to work for quotes, depth, orders and streaming. Open interest is not populated in Kotak&#39;s current phase, so the <code>oi</code> column reads 0.</p><p>Verified against a live session: a two year 1-minute pull returns 184,595 candles across 497 trading sessions with no duplicates and no gaps at the chunk seams.</p></li><li><p><strong>Kotak Neo account data (<code>41fc1e52e</code>, <code>45fb2d7bc</code>, <code>05a137423</code>, <code>1bedf5ab5</code>, <code>4152451bf</code>, #1971, #1973, #1955, #1730, #1961)</strong> - positions backfill their LTP through batched multiquotes, realized P\\P&amp;LL is reported for fully-closed positions, <code>availablecash</code> reports cash only rather than including collateral, the scripmaster fallback accessibility check uses a ranged GET instead of HEAD, and multiquotes are batched at 25 with failed sub-batches isolated so one bad symbol cannot empty the response.</p></li><li><p><strong>Broker fixes</strong> - Fyers HSM WebSocket authentication timeout (<code>41f9e4f12</code>, #1950); Shoonya abandoned WebSocket clients leaking sockets and threads (<code>4ec984cec</code>, #1988) and <code>get_history</code> raising on session errors instead of returning an empty success (<code>5806b8a16</code>, #1952); mstock dropped subscribes recovered and dead tokens stood down on (<code>fb58d0615</code>, #1983), plus order handling, batched WebSocket subscribes and the one-off quote socket closed on every exit path (<code>9c129feb6</code>, #1974); Upstox sending price and trigger price only where the order type allows (<code>5390dcd89</code>, #1966); Flattrade&#39;s order socket no longer evicting the market-data feed (<code>4152451bf</code>, #1961); and the underlying picker no longer showing company names on cash exchanges (<code>a0e372acc</code>, #1989).</p></li><li><p><strong>Docker base images move off EOL bullseye to trixie (<code>a7fc508a9</code>, #2004)</strong>.</p></li></ul><hr><p><strong>Dependencies</strong></p><ul><li><strong>New</strong>: <code>litellm==1.99.0</code>, <code>agno==3.0.5</code> and <code>ddgs&gt;=9.16.0</code>, all for the Agent module</li><li><code>tradeboard-charts</code>: <strong>1.8.2</strong> to <strong>2.0.2</strong></li><li>npm and pip advisories flagged by Dependabot patched (#1968)</li><li>Routine patch bumps across the pinned Python set (<code>cryptography</code>, <code>SQLAlchemy</code>, <code>APScheduler</code>, <code>pyzmq</code>, <code>python-socketio</code> and others)</li><li>The pinned <code>tradeboard</code> SDK stays at <strong>2.0.3</strong></li></ul><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajeev Upadhyay)</strong> - release management; the Strategy Module and RMS in full, including the risk core, signal mode, webhook, scheduler, crash recovery and the order-path safety work (#1976); the Tradeboard Agent, including ChatGPT subscription support (#1997); the charting terminal upgrade to tradeboard-charts 2.0.2 with the bottom dock, armed one-click trading, side panels and the user guide chapter; Kotak Neo historical data (#2008, #2010); retirement of the legacy <code>/strategy</code> module.</li><li><strong>@Kalaiviswa</strong> - Fyers HSM WebSocket authentication timeout (#1950); Shoonya socket and thread leak (#1988) and <code>get_history</code> session errors (#1952); mstock dropped subscribes and dead tokens (#1983), order handling and quote socket cleanup (#1974); Upstox order-type price fields (#1966); Flattrade order socket evicting the market-data feed and Kotak multiquote batching (#1961); Flow strike offsets and QA fixture cleanup (#1953); the underlying picker on cash exchanges (#1989); Kotak follow-ups (#1985); Dependabot advisory patches (#1968).</li><li><strong>@arsalanansari17</strong> - Kotak LTP backfill for positions via batched multiquotes (#1973); realized P\\P&amp;LL for fully-closed positions (#1971); <code>availablecash</code> reporting cash only (#1955); ranged GET for the scripmaster fallback check (#1730).</li><li><strong>@aravindgandavadi (Aravind Gandavadi)</strong> - Docker base images moved from EOL bullseye to trixie (#2004).</li></ul><p>Thank you to everyone who filed an issue, reproduced a defect or reviewed a pull request this cycle. The Kotak historical data work in particular came directly from #2008 and #2010, and the market-feed deprecation raised in #2010 is next.</p><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard/issues</a></li></ul>',22)])])}const u=t(s,[["render",n]]);export{g as __pageData,u as default};
