import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"ChatGPT Subscription","description":"","frontmatter":{},"headers":[],"relativePath":"new-features/agent/chatgpt-subscription.md","filePath":"new-features/agent/chatgpt-subscription.md"}'),s={name:"new-features/agent/chatgpt-subscription.md"};function i(r,e,d,h,l,c){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="chatgpt-subscription" tabindex="-1">ChatGPT Subscription <a class="header-anchor" href="#chatgpt-subscription" aria-label="Permalink to &quot;ChatGPT Subscription&quot;">​</a></h1><p>A ChatGPT Plus or Pro plan can drive the Tradeboard agent instead of an OpenAI API key. This is a second billing path, not a second key. The same model name exists on both paths and bills differently:</p><table tabindex="0"><thead><tr><th>Model id you register</th><th>How it authenticates</th><th>What it spends</th></tr></thead><tbody><tr><td><code>openai/gpt-5.4</code></td><td>an API key you paste</td><td>your OpenAI API credits</td></tr><tr><td><code>chatgpt/gpt-5.4</code></td><td>an OAuth sign-in</td><td>your ChatGPT Plus or Pro plan</td></tr></tbody></table><p>Of the ten <code>chatgpt/</code> models LiteLLM lists, eight share a bare name with an OpenAI API model. Only <code>gpt-5.3-instant</code> and <code>gpt-5.3-codex-spark</code> exist on the plan path alone. So two rows in your model registry can both read GPT-5.4 and send the bill to two different places, and the only thing separating them is the prefix on the stored model name. Everything described below exists to make that difference visible before you send a question rather than after.</p><h2 id="when-to-use-which" tabindex="-1">When to Use Which <a class="header-anchor" href="#when-to-use-which" aria-label="Permalink to &quot;When to Use Which&quot;">​</a></h2><p>Use the plan path for high-volume, routine work: repeated chart reads, option chain questions, indicator tables, anything you run many times a day. Those turns come out of quota you have already paid for.</p><p>Use an API key when you need a model the plan does not serve. New frontier models reach the API first, and the plan catalogue lags it. The plan is a way to move volume off metered billing, not a replacement for a key.</p><p>A plan turn is never free. It consumes plan quota, which is why Tradeboard reports it as tokens with no cost rather than as <code>$0.00</code>.</p><h2 id="where-the-panel-is" tabindex="-1">Where the Panel Is <a class="header-anchor" href="#where-the-panel-is" aria-label="Permalink to &quot;Where the Panel Is&quot;">​</a></h2><p>The ChatGPT subscription panel lives on <code>/agent/config</code>, the agent configuration page. Reach it from the admin section, where it is the <strong>Agent Config</strong> tile, or from the settings control in the <code>/agent</code> chat header.</p><p>On that page the panel is the second section, directly under the registered models table and above the trading switch and the provider catalogue. It is titled <strong>ChatGPT subscription</strong>.</p><h2 id="connecting-a-plan-step-by-step" tabindex="-1">Connecting a Plan, Step by Step <a class="header-anchor" href="#connecting-a-plan-step-by-step" aria-label="Permalink to &quot;Connecting a Plan, Step by Step&quot;">​</a></h2><ol><li>Open <code>/agent/config</code> and find the <strong>ChatGPT subscription</strong> card. When no plan is connected it says so, and offers one button: <strong>Connect ChatGPT plan</strong>.</li><li>Press it. Tradeboard makes a single request to OpenAI&#39;s device endpoint and comes straight back with a short code. It does not hold the request open.</li><li>The panel now shows <strong>Your code</strong> in large type, a <strong>Copy code</strong> button, an <strong>Open the sign-in page</strong> button, and a live countdown reading <code>Expires in m:ss</code>.</li><li>Press <strong>Open the sign-in page</strong>. It opens a new tab on OpenAI&#39;s own site, at the Codex device authorisation page. This is the Codex device flow, not the ChatGPT web app sign-in.</li><li>Sign in there with the account your ChatGPT plan belongs to, and enter the code when it asks for one. <strong>Your ChatGPT password and any second factor go to OpenAI in that tab and never pass through Tradeboard.</strong> Tradeboard only ever sends the device identifier and the code back to OpenAI while it polls, and receives the resulting token.</li><li>Come back to the Tradeboard tab. The panel polls every couple of seconds and notices on its own, including while the tab is in the background, so you do not have to refresh anything.</li></ol><p>A code is accepted for about fifteen minutes and then stops working. If the countdown runs out, the panel says the code was not entered in time and offers <strong>Try connecting again</strong>, which issues a fresh one. <strong>Cancel sign-in</strong> stops a sign-in you no longer want.</p><p>One security note the panel makes itself: nobody but you should ever be asked for that code. If anything other than OpenAI&#39;s own device page asks you to enter it, cancel instead.</p><h2 id="if-the-sign-in-page-asks-you-to-log-in-first" tabindex="-1">If the Sign-In Page Asks You to Log In First <a class="header-anchor" href="#if-the-sign-in-page-asks-you-to-log-in-first" aria-label="Permalink to &quot;If the Sign-In Page Asks You to Log In First&quot;">​</a></h2><p>If the browser you open the device page in has no ChatGPT session, OpenAI asks you to sign in, or to create an account, before it will show the code prompt. That is the page doing its job, not a broken feature, and it is why step 5 above is a step of its own.</p><p>Sign in there with the account the plan belongs to, then use the code that is still on the Tradeboard panel. It stays valid for the full fifteen minutes, and the panel is still polling for it.</p><p>Do not reach for Cancel and Connect as the first move. One sign-in runs at a time, and the device endpoint applies a five-minute cooldown after issuing a code, so pressing Connect again inside that window hands you back the code you already have rather than a new one. Start over only once the countdown has actually expired.</p><h2 id="what-connected-shows" tabindex="-1">What Connected Shows <a class="header-anchor" href="#what-connected-shows" aria-label="Permalink to &quot;What Connected Shows&quot;">​</a></h2><p>Once the code is approved, the card title gains a <strong>Connected</strong> badge and the panel lists four things:</p><ul><li><strong>Credential</strong>, a fingerprint, in the same form an API key row shows: the last four characters and a truncated SHA-256. The token itself is never rendered, not masked and not partial. The fingerprint is taken over the durable half of the credential, so it stays the same identifier after a routine token refresh.</li><li><strong>Account</strong>, the OpenAI account id the plan belongs to, when the record names one.</li><li><strong>Access token expires</strong>, a local date and time, followed by &quot;It is refreshed on its own.&quot; You do not have to do anything when it passes.</li><li><strong>Stored</strong>, where the credential lives. Normally &quot;Encrypted in this instance database, and cached for LiteLLM in&quot; followed by the cache directory under <code>db/chatgpt_oauth</code>. The database copy is the record of truth and the file is a cache rebuilt from it, so a restored database is already authorised.</li></ul><p>Two buttons sit below: <strong>Sign in again</strong>, which is how you move this instance to a different ChatGPT account, and <strong>Disconnect</strong>.</p><h2 id="adding-a-plan-model" tabindex="-1">Adding a Plan Model <a class="header-anchor" href="#adding-a-plan-model" aria-label="Permalink to &quot;Adding a Plan Model&quot;">​</a></h2><p>Connecting the plan does not register a model. Do that from the same page:</p><ol><li>Scroll to <strong>Providers</strong>. The ChatGPT provider is not one of the pinned cards, so use the provider search box or <strong>Show more providers</strong> to find the card named <strong>ChatGPT</strong>. It carries a <code>runs on your plan</code> badge instead of the key badges the other providers show.</li><li>Open it. A notice at the top reads &quot;These bill to your plan, not to API credits&quot;, and repeats that most of these models also exist under OpenAI with the same name and a per-token price.</li><li>Pick a model and add it. The dialog offers no API key field for a <code>chatgpt/</code> model, because there is no key to give: its credential is the sign-in you just made. The model is stored with its <code>chatgpt/</code> prefix intact and its provider kind is LiteLLM.</li><li>The suggested display name is <strong>ChatGPT Plan: GPT-5.4</strong> rather than GPT-5.4. The billing path leads because that is the half you scan for when two rows share a model name, and because the model column is the one most likely to truncate. The name stays editable.</li><li>Press <strong>Test</strong> on the new row. The test is a real one-token streamed completion against the provider, so it answers the only question that matters: whether your account can actually run that model. A refusal is stored verbatim and shown in the row&#39;s detail.</li></ol><h2 id="telling-the-two-paths-apart" tabindex="-1">Telling the Two Paths Apart <a class="header-anchor" href="#telling-the-two-paths-apart" aria-label="Permalink to &quot;Telling the Two Paths Apart&quot;">​</a></h2><p>Once a plan model is registered, the billing path is labelled everywhere the model is named:</p><ul><li>In the models table, a <code>ChatGPT plan</code> badge sits next to the model id, and the Key column reads the plan fingerprint with &quot;your ChatGPT plan sign-in&quot; under it. Before a plan is connected it reads &quot;No plan connected yet&quot; instead.</li><li>In the composer&#39;s model button, the word <code>on plan</code> appears next to the model name. That button is the last thing you see before sending a question.</li><li>In the model menu, each plan row carries the same <code>ChatGPT plan</code> badge.</li><li>Under the answer, the usage line reports tokens as usual and then, in place of a price, the words <code>included in your ChatGPT plan</code>. Not <code>$0.00</code>, which would claim the turn was free, and not the dash used for a model whose price is unknown. A conversation held entirely on the plan says so instead of showing a running total.</li></ul><p>If a plan model is the registry default, it is also what the chart agent panel on <a href="./../trading-terminal.html"><code>/trading</code></a> runs, because that panel always uses the default model and offers no picker of its own.</p><h2 id="which-models-the-plan-serves" tabindex="-1">Which Models the Plan Serves <a class="header-anchor" href="#which-models-the-plan-serves" aria-label="Permalink to &quot;Which Models the Plan Serves&quot;">​</a></h2><p>LiteLLM lists ten models under the <code>chatgpt</code> provider. Tradeboard registers four more that LiteLLM omits, measured against a real subscription, because a model missing from LiteLLM&#39;s registry is routed to the wrong endpoint and comes back as a Cloudflare interstitial rather than an answer. That is fourteen models offered on the ChatGPT provider card.</p><table tabindex="0"><thead><tr><th>Measured against a live plan</th><th>Models</th></tr></thead><tbody><tr><td>Listed by LiteLLM</td><td><code>gpt-5.4</code>, <code>gpt-5.4-pro</code>, <code>gpt-5.3-chat-latest</code>, <code>gpt-5.3-instant</code>, <code>gpt-5.3-codex</code>, <code>gpt-5.3-codex-spark</code>, <code>gpt-5.2</code>, <code>gpt-5.2-codex</code>, <code>gpt-5.1-codex-max</code>, <code>gpt-5.1-codex-mini</code></td></tr><tr><td>Added by Tradeboard</td><td><code>gpt-5.5</code>, <code>gpt-5.6-sol</code>, <code>gpt-5.6-luna</code>, <code>gpt-5.6-terra</code></td></tr><tr><td>Refused by the backend on a plan</td><td><code>gpt-5.6</code>, <code>gpt-5.6-cyber</code>, <code>gpt-5.5-pro</code>, <code>gpt-5.5-codex</code>, <code>gpt-5.6-codex</code></td></tr></tbody></table><p><code>gpt-5.6</code> being refused while three of its variants work is how the backend behaves, so the list is enumerated rather than derived from a pattern.</p><p>Treat the catalogue as advisory. These are the models the provider serves; which of them a particular plan may use is between you and OpenAI, and a Plus plan need not match a Pro one. The <strong>Test</strong> button on the model row is what answers for your account.</p><h2 id="signing-out-and-a-lapsed-plan" tabindex="-1">Signing Out, and a Lapsed Plan <a class="header-anchor" href="#signing-out-and-a-lapsed-plan" aria-label="Permalink to &quot;Signing Out, and a Lapsed Plan&quot;">​</a></h2><p><strong>Disconnect</strong> asks for confirmation first, and the dialog says what it does: the stored credential is deleted and cannot be read back, so reconnecting means signing in again. Any model you registered under <code>chatgpt/</code> stays in the registry and stops working until you do, which is why those rows are not removed for you. That is deliberate: the rows are your configuration, and you probably want them back the moment you sign in again.</p><p>The same applies when a subscription lapses rather than being disconnected. The model row stays, and the plan behind it does not. A <code>chatgpt/</code> model with no usable credential is refused before the first token of the answer, with a message telling you to sign in to ChatGPT from agent settings, rather than being allowed to start and fail halfway. Press <strong>Test</strong> on the row to see the provider&#39;s own reason.</p><h2 id="operational-boundaries" tabindex="-1">Operational Boundaries <a class="header-anchor" href="#operational-boundaries" aria-label="Permalink to &quot;Operational Boundaries&quot;">​</a></h2><ul><li>A plan turn reports tokens and no cost. It is neither free nor unpriced: it consumed quota you pay for monthly, and there is no per-token price to show.</li><li>A plan is not an entitlement. Model availability is decided by OpenAI for your account, and the newest frontier models reach the API before the plan.</li><li>Nothing here is read from <code>.env</code>. The credential is encrypted in this instance&#39;s own database and cached in <code>db/chatgpt_oauth</code>, which Docker mounts as a volume.</li><li>The panel needs a LiteLLM new enough to carry its ChatGPT provider. On an older one the panel says this server cannot answer for a ChatGPT plan yet, and offers <strong>Try again</strong>. Every other model on the page is unaffected.</li><li>One sign-in runs at a time. Pressing Connect while a code is outstanding returns the code already on screen rather than issuing a second one.</li><li>The billing path changes nothing about order safety. Every order tool still pauses for your approval whichever model proposed it. See <a href="./README.html">Agent</a> for that path, and <a href="./../api-analyzer.html">Analyzer mode</a> for where an approved order lands.</li></ul><h2 id="see-also" tabindex="-1">See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See Also&quot;">​</a></h2><ul><li><a href="./README.html">Agent</a> for the overview and the three surfaces.</li><li><a href="./voice.html">Voice Agent</a> for Vega, the microphone on <code>/agent</code>.</li><li><a href="./configuration.html">Agent Configuration</a> for the rest of <code>/agent/config</code>: registering a model, storing an API key, the trading switch and web search.</li><li><a href="./capabilities.html">What the Agent Can Do</a> for the toolkits a plan model is given, which are the same ones any other model gets.</li><li><a href="./examples.html">Agent by Example</a> for what a plan turn looks like in a conversation.</li></ul>',42)])])}const u=t(s,[["render",i]]);export{g as __pageData,u as default};
