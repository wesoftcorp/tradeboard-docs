import{_ as a,o as t,c as s,a2 as n}from"./chunks/framework.BXzK3EA4.js";const h=JSON.parse('{"title":"34 - Application Startup","description":"","frontmatter":{},"headers":[],"relativePath":"developers/design-documentation/34-app-startup.md","filePath":"developers/design-documentation/34-app-startup.md"}'),o={name:"developers/design-documentation/34-app-startup.md"};function r(i,e,d,p,c,l){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="_34-application-startup" tabindex="-1">34 - Application Startup <a class="header-anchor" href="#_34-application-startup" aria-label="Permalink to &quot;34 - Application Startup&quot;">​</a></h1><h2 id="startup-sequence" tabindex="-1">Startup Sequence <a class="header-anchor" href="#startup-sequence" aria-label="Permalink to &quot;Startup Sequence&quot;">​</a></h2><p><code>app.py</code> validates the environment before importing the rest of the application. This ordering is intentional because auth/database modules require installation secrets at import time.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. load and validate .env</span></span>
<span class="line"><span>2. import Flask extensions, databases, services, RESTX, proxy integration</span></span>
<span class="line"><span>3. create_app()</span></span>
<span class="line"><span>   - Flask + Socket.IO</span></span>
<span class="line"><span>   - EventBus subscribers</span></span>
<span class="line"><span>   - CSRF + limiter + CORS + CSP</span></span>
<span class="line"><span>   - security, traffic, latency, health hooks</span></span>
<span class="line"><span>   - React, RESTX, and feature blueprints</span></span>
<span class="line"><span>   - optional Remote MCP</span></span>
<span class="line"><span>   - request guards, broker keepalive, order-update adapters</span></span>
<span class="line"><span>4. setup_environment()</span></span>
<span class="line"><span>   - plugin discovery and broker capabilities</span></span>
<span class="line"><span>   - database/table initialization</span></span>
<span class="line"><span>   - strategy book database and subscriber</span></span>
<span class="line"><span>   - schedulers and restoration</span></span>
<span class="line"><span>   - sandbox workers and scalping risk monitor</span></span>
<span class="line"><span>   - configured Telegram and WhatsApp bots</span></span>
<span class="line"><span>5. module-scope teardown handler for scoped sessions</span></span>
<span class="line"><span>6. start WebSocket proxy when embedded mode applies</span></span>
<span class="line"><span>7. run Socket.IO server for direct startup, or expose app to gunicorn</span></span></code></pre></div><h2 id="required-configuration" tabindex="-1">Required Configuration <a class="header-anchor" href="#required-configuration" aria-label="Permalink to &quot;Required Configuration&quot;">​</a></h2><p><code>APP_KEY</code> must exist and contain at least 32 characters, checked in <code>create_app()</code>. <code>API_KEY_PEPPER</code> carries the same 32-character requirement, enforced at import time in <code>database/auth_db.py</code>. <code>utils/env_check.py</code> detects the placeholder <code>APP_KEY</code>, <code>API_KEY_PEPPER</code>, and <code>FERNET_SALT</code> values shipped in <code>.sample.env</code> and rotates them to fresh random secrets on first run. Official install paths generate unique values; placeholders are not accepted as production configuration.</p><p>Cookie security is derived from <code>HOST_SERVER</code>: HTTPS deployments use secure cookies while local HTTP development remains usable. External debug binding is refused unless explicitly allowed.</p><h2 id="route-registration-order" tabindex="-1">Route Registration Order <a class="header-anchor" href="#route-registration-order" aria-label="Permalink to &quot;Route Registration Order&quot;">​</a></h2><p>When <code>frontend/dist</code> exists, the React blueprint is registered before REST/UI blueprints. Public REST is then registered and CSRF-exempted. Feature blueprints follow, with explicit CSRF exemptions for the broker OAuth callback, the Chartink/strategy/Flow webhooks, the broker postback endpoint, and the two health endpoints. <code>auth.logout</code> is deliberately not exempt: it revokes broker tokens and tears down the shared feed, so it keeps CSRF protection.</p><p>Flask-RESTX documentation is deliberately disabled (<code>doc=False</code>). Startup does not register Swagger UI.</p><h2 id="remote-mcp-gate" tabindex="-1">Remote MCP Gate <a class="header-anchor" href="#remote-mcp-gate" aria-label="Permalink to &quot;Remote MCP Gate&quot;">​</a></h2><p>Remote MCP is off unless <code>MCP_HTTP_ENABLED=True</code>. Enabling it requires:</p><ul><li>Flask debug mode off.</li><li><code>MCP_PUBLIC_URL</code> set as the canonical public origin.</li><li>OAuth/MCP blueprints imported only after the boot guard environment marker is set.</li></ul><p>Startup logs warn when write scope is enabled or client approval is disabled.</p><h2 id="database-readiness" tabindex="-1">Database Readiness <a class="header-anchor" href="#database-readiness" aria-label="Permalink to &quot;Database Readiness&quot;">​</a></h2><p>Initialization runs before normal service use, and requests wait up to 30 seconds on <code>app.db_ready</code>. Table creation runs on a background thread through a <code>ThreadPoolExecutor</code> with <code>max_workers=1</code>, because most of those functions target the same SQLite file and SQLite allows one writer per file. Every initialization function must be idempotent and safe after partial prior setup. Sandbox initialization explicitly repairs missing tables.</p><p>The strategy book database and its EventBus subscriber are registered before <code>db_ready</code> is set, with up to three attempts, so no order can be accepted before its strategy tag can be recorded.</p><p>After each request, <code>app.py</code>&#39;s <code>teardown_appcontext</code> handler calls <code>remove_all_scoped_sessions()</code> from <code>utils/db_sessions.py</code>, which holds the registry of every scoped session in the project. Background threads without an app context must call the same function themselves. This is part of the file-descriptor reliability contract.</p><h2 id="background-services" tabindex="-1">Background Services <a class="header-anchor" href="#background-services" aria-label="Permalink to &quot;Background Services&quot;">​</a></h2><p><code>setup_environment()</code> initializes/restores background work for Flow (scheduler, order-update watches, price alerts, job reconciliation), Python strategies, Historify, sandbox execution/square-off plus catch-up settlement, scalping stops, and configured messaging services. The broker keepalive service and the real-time order-update adapters start earlier, inside <code>create_app()</code>. Failures should be logged with enough context and should only abort startup when the component is required for a coherent runtime.</p><h2 id="websocket-startup" tabindex="-1">WebSocket Startup <a class="header-anchor" href="#websocket-startup" aria-label="Permalink to &quot;WebSocket Startup&quot;">​</a></h2><p>The market-data proxy is skipped when Docker/standalone configuration owns it. Under eventlet/gunicorn it runs in a child process; direct development startup uses an OS thread. See <a href="./authentication-platforms.html">06 WebSockets</a>.</p><h2 id="direct-run" tabindex="-1">Direct Run <a class="header-anchor" href="#direct-run" aria-label="Permalink to &quot;Direct Run&quot;">​</a></h2><p>The <code>__main__</code> block reads <code>FLASK_HOST_IP</code>, <code>FLASK_PORT</code>, and <code>FLASK_DEBUG</code>, performs bind/debug safety checks, and starts the Socket.IO server. Debug mode on a non-loopback host exits with an explanation unless <code>FLASK_DEBUG_ALLOW_EXTERNAL</code> is set to a truthy value. Production deployment is orchestrated by <code>start.sh</code>, which runs Gunicorn with <code>--worker-class eventlet --workers 1</code>.</p><h2 id="adding-startup-work" tabindex="-1">Adding Startup Work <a class="header-anchor" href="#adding-startup-work" aria-label="Permalink to &quot;Adding Startup Work&quot;">​</a></h2><ol><li>Decide whether it belongs in factory registration or environment setup.</li><li>Make initialization idempotent and bounded.</li><li>Add teardown/stop ownership for threads, processes, sockets, or scoped sessions.</li><li>Avoid importing secret-dependent modules before environment validation.</li><li>Add startup diagnostics and a focused failure/restart test.</li></ol>`,26)])])}const b=a(o,[["render",r]]);export{h as __pageData,b as default};
