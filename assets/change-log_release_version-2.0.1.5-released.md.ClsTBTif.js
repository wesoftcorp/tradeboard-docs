import{_ as o,o as t,c as s,a2 as a}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.1.5 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.5-released.md","filePath":"change-log/release/version-2.0.1.5-released.md"}'),i={name:"change-log/release/version-2.0.1.5-released.md"};function r(n,e,d,l,c,p){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="version-2-0-1-5-released" tabindex="-1">Version 2.0.1.5 Released <a class="header-anchor" href="#version-2-0-1-5-released" aria-label="Permalink to &quot;Version 2.0.1.5 Released&quot;">​</a></h1><p><strong>Date: 10th July 2026</strong></p><p><strong>Reliability Release: a ZeroMQ bus fan-in fix that restores live ticks under gunicorn+eventlet, a three-part multi-session login audit that stops one device&#39;s login from tearing down another device&#39;s broker feed, a real-time futures calendar-spread scanner (Arbitrage), an IndMoney broker hardening pass (FD leaks, batching, IndStocks API alignment), Nubra market/stop-market order emulation with MCX support, and fixes across Angel, Groww, AliceBlue, Fyers, Arrow, sandbox startup and Action Center</strong></p><p>This release spans 49 commits since v2.0.1.4. The headline fix is the <strong>ZeroMQ bus fan-in</strong> correction (<code>c9591ae6</code>) — under Gunicorn+eventlet the WebSocket proxy and the cache-invalidation publisher both tried to <em>bind</em> the same ZMQ port from separate processes, silently losing the race and delivering <strong>zero market-data ticks</strong> even though subscribe calls succeeded. The bus is now strictly fan-in: the proxy&#39;s SUB is the sole binder and every publisher connects to it. Close behind is a <strong>three-commit multi-session login audit</strong> (#1591) that fixes a family of bugs where a second device/browser logging in — or a stale idle cookie at the daily 3 AM token rollover — could tear down or revoke the <em>first</em> device&#39;s already-working broker WebSocket feed or auth token. On the feature side, a new <strong>Arbitrage</strong> tool (<code>/arbitrage</code>) scans all NFO and MCX futures for executable calendar-spread edges in real time, and <strong>IndMoney</strong> gets a five-commit hardening pass (WebSocket FD-leak fixes, batched subscriptions, IndStocks API realignment, option-chain quote resilience). <strong>Nubra</strong> gains market/stop-market order emulation and MCX support, and broker fixes land for Angel (rate-limit pacing, gapless history), Groww (master-contract download resilience), AliceBlue (missing NFO/CDS futures), Fyers (shared rate-limit pacing, option-chain fast path) and Arrow (heartbeat fix for dead data streams). Sandbox gets a self-healing DB-init fix for a fresh-install race, and Action Center now executes all pending order types (multi-leg options, GTT), not just the original subset.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>ZeroMQ bus fan-in fix (<code>c9591ae6</code>)</strong> — The proxy SUB socket is now the sole binder of <code>ZMQ_PORT</code>; every publisher (broker adapters, cache-invalidation) connects instead of binding. Removes the port-scan/5556 fallback and the runtime <code>ZMQ_PORT</code> mutation that let the two binders silently disagree on which port ticks actually flowed through. Broker-agnostic; fixes silent tick loss under Gunicorn+eventlet (bare-metal <code>install.sh</code> subprocess and Docker <code>start.sh</code>) that worked fine on the single-process dev server. Invariant documented in <code>CLAUDE.md</code>.</li><li><strong>Multi-session login audit (#1591, 3 commits)</strong> — Devices/browsers sharing one broker session no longer step on each other: <ul><li><code>3aadeb33</code> — a second device&#39;s login no longer tears down the first device&#39;s live broker WebSocket feed; <code>upsert_auth</code> now gates the ZMQ <code>CACHE_INVALIDATE_ALL</code> + pool cleanup on an <em>actual</em> token change, not just a re-persist of the same token.</li><li><code>350f0733</code> — the daily ~3 AM rollover no longer revokes a token another device already refreshed that morning; the revoke path checks for a fresher active session before tearing down.</li><li><code>fcf1f4ee</code> — <code>active_sessions.last_seen</code> now updates on a throttled heartbeat (was wired but never called), so the active-session list reflects real device liveness instead of freezing at login time.</li></ul></li><li><strong>Arbitrage tool (<code>/arbitrage</code>)</strong> — Real-time futures calendar-spread scanner across all NFO and MCX underlyings: near-vs-next and near-vs-third month pairs ranked by executable bid/ask spread percentage, live Depth-mode pricing, and one-click two-leg basket order placement.</li><li><strong>IndMoney broker hardening (5 commits)</strong> — API realignment to the current IndStocks docs (positions, order status, trade book, WS URI), two rounds of WebSocket FD/thread-leak fixes in the adapter and client, Zerodha-style two-level subscription batching, and option-chain quote resilience (bisect + cache poison scrip codes instead of failing the whole batch).</li><li><strong>Nubra: market/stop-market emulation + MCX (<code>ca8985dd</code>)</strong> — Nubra is limit-only and rejected <code>MARKET</code>/<code>SL-M</code> orders outright; both are now emulated (Market-Protection-Price limit, stop-limit beyond trigger). Adds MCX instrument support and hardens TOTP auth + order-status mapping.</li><li><strong>Broker fixes</strong> — Angel (real rate-limit pacing, gapless multi-year history, faster streaming lookups), Groww (master-contract download no longer aborts on a blank-symbol row), AliceBlue (NFO/CDS futures were silently dropped from the master contract), Fyers (shared rate-limit clock across requests, native option-chain fast path), Arrow (app-level PONG heartbeat so the data stream doesn&#39;t go silently dead).</li><li><strong>Sandbox self-heal (<code>fe65bc13</code>, #1580)</strong> — A fresh-install DB-init race could leave <code>sandbox.db</code> permanently partial (&quot;no such table: sandbox_funds&quot;); <code>create_all</code> now self-heals from an orphaned-index state and retries once.</li><li><strong>Action Center: execute all order types (<code>1db1294d</code>)</strong> — Approved pending orders of type <code>optionsmultiorder</code> and <code>placegttorder</code> now execute (previously only single-leg/basket/split/options orders were wired); DB isolation added for Action Center tests.</li><li><strong>Telegram alert gating fixes</strong> — Menu P&amp;L now matches <code>/pnl</code> (both source from funds, not positionbook) (#1576); Flow&#39;s &quot;send Telegram alert&quot; action is now gated on bot-active state like order alerts, so stopping the bot silences Flow alerts too (#1577, follow-up <code>2904b5ef</code>).</li><li><strong>Latency logger crash fix (<code>3880ba72</code>)</strong> — A dict-typed validation error (e.g. <code>/history</code> called without <code>apikey</code>) crashed <code>OrderLatency.log_latency</code> on the String-typed error column; now coerced to string at the storage boundary.</li><li><strong>Dependency bumps</strong> — pydantic 2.13.4, mcp 1.28.0, SQLAlchemy 2.0.51, uvicorn 0.49.0, kaleido/choreographer 1.3.0 (fixes broken Plotly→PNG rendering used by Telegram chart commands, #1578), plus a broad low-risk patch/minor sweep (certifi, click, plotly, pytest, python-telegram-bot, tzdata, etc.).</li></ul><hr><p><strong>ZeroMQ bus fan-in — deep dive</strong></p><p><code>c9591ae6</code> — Under Gunicorn+eventlet the WebSocket proxy runs <em>out-of-process</em> (a subprocess on bare-metal <code>install.sh</code>, or a separate <code>python -m websocket_proxy.server</code> on Docker <code>start.sh</code>) while the cache-invalidation publisher runs inside the gunicorn process. Both previously created a <code>SharedZmqPublisher</code> that <strong>bound</strong> <code>ZMQ_PORT</code> (5555). As a per-process singleton, the two binders raced: the gunicorn process usually won port 5555, so the WebSocket process&#39;s market-data publisher silently fell back to 5556 via a bind port-scan — while the proxy&#39;s SUB socket stayed fixed on 5555. Result: auth and subscribe calls succeeded, but no <code>market_data</code> ticks were ever delivered. It worked on the single-process dev server (only one binder existed) and broke only under eventlet servers, which made it hard to spot in testing.</p><p>The fix flips the topology to strict fan-in: the proxy SUB is the sole binder, and every publisher — broker adapters and the cache-invalidation publisher alike — <strong>connects</strong> to it (many PUB-connects to one SUB-bind is valid ZMQ). The port-scan/5556 fallback and the runtime <code>os.environ[&quot;ZMQ_PORT&quot;]</code> mutation are removed, so <code>ZMQ_PORT</code> is fixed by config and never drifts (5555 for a single instance, <code>5555 + i-1</code> per instance under <code>install-multi.sh</code>). Applies to all deployment modes — dev thread, <code>install.sh</code> subprocess, Docker <code>start.sh</code>, <code>install-multi.sh</code> — and is broker-agnostic since the bus itself is shared plumbing. The invariant is now documented in <code>CLAUDE.md</code> so it isn&#39;t reintroduced.</p><hr><p><strong>Multi-session login audit — deep dive (#1591)</strong></p><p>Tradeboard is single-user/single-broker per instance, but the same user can be logged in from up to <code>MAX_SESSIONS_PER_USER</code> (5) devices at once, all sharing <strong>one</strong> server-side broker WebSocket feed. Three bugs in that sharing surfaced together:</p><ul><li><code>3aadeb33</code> — A second device&#39;s login resumes the existing broker session and re-persists the <em>same</em> token via <code>upsert_auth()</code>, which unconditionally published a ZMQ <code>CACHE_INVALIDATE_ALL</code> (disconnecting the shared adapter + pool) and ran <code>cleanup_pools_for_user()</code> — tearing down the feed the first device was actively streaming from. Symptoms: Shoonya ticks stopped until a page refresh; Flattrade&#39;s single-active-session Noren backend dropped the token entirely (&quot;broker session expired&quot;). The fix gates that teardown on an <em>actual</em> token/feed-token/broker/revoke-flag change (comparing decrypted plaintext, since Fernet ciphertext is non-deterministic) — an unchanged session now just clears cheap in-process caches and returns early.</li><li><code>350f0733</code> — The daily ~3 AM IST rollover path revoked the shared broker token based only on the <em>current</em> cookie&#39;s <code>login_time</code>. A device left idle overnight would, on its first request after rollover, blindly revoke a token another device had already re-established that morning — kicking the fresh device and collaterally killing API-key/webhook access. The guard now checks <code>active_sessions</code> for a session authenticated at/after today&#39;s rollover boundary before revoking; if one exists, only the stale device&#39;s own session row is dropped.</li><li><code>fcf1f4ee</code> — <code>update_session_last_seen</code> existed but was never called, so <code>active_sessions.last_seen</code> stayed frozen at login time — the active-session list couldn&#39;t distinguish a live device from one whose browser was closed. A throttled heartbeat (once per device per 30s) is now wired into the SPA&#39;s <code>/session-status</code> poll.</li></ul><p>Together these mean multiple devices genuinely stream and stay authenticated concurrently, which is the deployment model Tradeboard already assumed but didn&#39;t fully enforce.</p><hr><p><strong>Options tools</strong></p><ul><li><strong>Arbitrage</strong> (<code>2f5acd24</code>) — <code>services/arbitrage_service.py</code> builds the calendar-spread universe from the master-contract cache (near/next/third futures per underlying); <code>blueprints/arbitrage.py</code> exposes <code>GET /arbitrage/api/universe</code>. Frontend subscribes to the shared <code>MarketDataManager</code> in Depth mode for live bid/ask, with a ranked/filterable table and a two-leg trade dialog via the existing basket-order service (NRML default, BUY leg before SELL).</li></ul><hr><p><strong>Broker fixes</strong></p><ul><li><code>ca8985dd</code> — <strong>Nubra</strong>: emulate MARKET (Market-Protection-Price limit) and SL-M (stop-limit beyond trigger) since Nubra is limit-only and has no native stop-market type; add MCX instrument support; map the real <code>OrderStatus</code> enum (add SENT/TRIGGERED, drop non-existent PARTIALLY_FILLED); harden TOTP for leading-zero codes.</li><li><code>24477b48</code>, <code>9f2e295e</code>, <code>e8f02ea3</code>, <code>85a91e0d</code>, <code>96a64da0</code> — <strong>IndMoney</strong>: realign positions/orders/order-status/trade-book/WS-URI to the current IndStocks API; bisect-and-cache poison scrip codes so one unquotable symbol no longer 400s the whole option-chain batch; two rounds of WebSocket FD/thread-leak fixes (single-owner reconnect loop, interruptible backoff, teardown-before-reinit); Zerodha-style two-level subscription batching (500ms flush, 1000-instrument chunking, dedup); reject unsupported exchanges before order placement instead of silently defaulting to NSE.</li><li><code>48807197</code> — <strong>Angel</strong>: shared per-category rate limiter sized to real caps (was ~7x too slow on quotes), retry on rate-limit 403/429 instead of misreporting as auth failure, retry (not skip) failed 30-day history chunks so multi-year downloads are gapless, O(1) tick-to-subscription lookup (was O(N) per tick).</li><li><code>84146cbd</code> — <strong>Groww</strong>: master-contract download no longer aborts entirely on a single blank-<code>trading_symbol</code> row (a stray BSE index entry); malformed rows are skipped and logged instead of rolling back the whole insert. Download also now validates against Groww&#39;s real CSV header by name instead of relabelling columns by position.</li><li><code>e55e1de0</code> — <strong>AliceBlue</strong>: NFO/CDS futures were silently dropped from the master contract because the V2 CSV identifies futures by Instrument Type with a NaN Option Type (not <code>&#39;XX&#39;</code>); futures Instrument Types are now mapped to <code>&#39;XX&#39;</code> before symbol construction, matching the existing BFO/MCX/BCD processors.</li><li><code>16a01dcd</code> — <strong>Fyers</strong>: rate-limit pacing moved from a per-request instance clock (never persisted, so concurrent requests collectively blew past the 10 req/sec cap) to a process-wide shared clock with 429 retry/backoff; native <code>/data/options-chain-v3</code> fast path replaces per-symbol bulk-quote + depth calls for option-chain OI/bid-ask.</li><li><code>44532dd4</code> — <strong>Arrow</strong>: send the app-level text PONG heartbeat every 3s (matching the official SDK) instead of relying on protocol-level WebSocket pings, which the server ignores — ticks were silently not delivered even though auth/subscribe succeeded (#1583).</li></ul><hr><p><strong>Reliability &amp; fixes</strong></p><ul><li><code>fe65bc13</code> — <strong>Sandbox</strong>: self-heal a partial <code>sandbox.db</code> from a fresh-install DB-init race (#1580); <code>db/</code> directory now created up front before any DB module imports; sandbox index names de-duplicated against <code>action_center_db</code>/<code>symtoken</code>.</li><li><code>1db1294d</code> — <strong>Action Center</strong>: execute <code>optionsmultiorder</code> and <code>placegttorder</code> pending order types (previously unwired); order-ID summarization for the DB column when a split/batch order returns multiple broker order IDs.</li><li><code>c32c41a3</code> — <strong>Action Center</strong>: isolate the test database so Action Center tests don&#39;t share state with other suites.</li><li><code>967d0d04</code> — <strong>Telegram</strong>: <code>/menu</code> P&amp;L now sources from funds (matching <code>/pnl</code>) instead of positionbook day-P&amp;L, which disagreed and looked stale (#1576); order alerts now gated on bot-active state so stopping the bot actually silences them (#1577).</li><li><code>2904b5ef</code> — <strong>Telegram</strong>: Flow&#39;s &quot;send Telegram alert&quot; action gated on bot-active state too, closing the same gap for background Flow strategies.</li><li><code>3880ba72</code> — <strong>Latency</strong>: a dict-typed validation error crashed <code>OrderLatency.log_latency</code> (String-typed column); coerced to string at the storage boundary, bounded to 500 chars.</li><li><code>f6136f74</code> — <strong>Scalping</strong>: charts toggle (default off) so per-instrument chart components — each opening their own market-data subscription and history poll — don&#39;t run unless explicitly enabled; toggle and timeframe persist in <code>localStorage</code>.</li></ul><hr><p><strong>Security</strong></p><ul><li><code>0df92a57</code> — rotate hardcoded API keys in examples to <code>os.getenv</code>; refresh sample NIFTY expiry/lot size.</li></ul><hr><p><strong>Documentation</strong></p><ul><li><code>2643db5f</code>, <code>4526a8fc</code> — add an Open Knowledge Format (OKF) bundle (63 concepts across REST API, Python SDK, indicator library), then refactor it to thin pointer concepts linking back to <code>docs/</code> so the source of truth stays in one place; add <code>docs/INDEX.md</code> as a flat documentation router.</li><li><code>e27546fd</code> — remove stale <code>PRD.md</code> pointer stub and a resolved <code>REVIEW_QUEUE.md</code>.</li><li><code>b67d674a</code>, <code>65126531</code>, <code>55b541e3</code>, <code>1acb0099</code> — full documentation reconciliation sweep against current codebase: broker count 33→34 (TradeSmart), 7 new routes added to the inventory (<code>/arbitrage</code>, <code>/gammadensity/api/gamma-data</code>, <code>/scalping/api/history</code>, <code>/chart/test</code>), stale API/design docs rewritten, internal services reference refreshed.</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>pydantic</code> 2.12.5 → 2.13.4 (+ <code>pydantic-core</code> 2.46.4, <code>pydantic-settings</code> 2.14.2)</li><li><code>mcp</code> 1.27.0 → 1.28.0, <code>sse-starlette</code> 2.4.1 → 3.4.5</li><li><code>python-socketio</code> 5.16.3, <code>python-engineio</code> 4.13.3</li><li><code>SQLAlchemy</code> 2.0.49 → 2.0.51, <code>uvicorn</code> 0.44.0 → 0.49.0, <code>requests</code> 2.33.1 → 2.34.2</li><li><code>kaleido</code> + <code>choreographer</code> → 1.3.0 (matched pair; fixes Plotly→PNG rendering broken since kaleido 1.2.0, used by Telegram chart commands, #1578)</li><li>Broad low-risk patch/minor sweep: <code>certifi</code>, <code>click</code>, <code>plotly</code> 6.8.0, <code>pytest</code> 9.1.1, <code>python-telegram-bot</code> 22.8, <code>tzdata</code>, <code>duckdb</code>, <code>orjson</code>, and others</li><li><code>tradeboard</code> SDK pin unchanged at <code>2.0.2</code> (PyPI: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a>)</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>utils/version.py</code>: <code>VERSION = &quot;2.0.1.5&quot;</code></p><p><code>pyproject.toml</code>: <code>version = &quot;2.0.1.5&quot;</code> (<code>uv.lock</code> regenerated).</p><p><code>.sample.env</code>: no new variables; <code>ZMQ_HOST</code>/<code>ZMQ_PORT</code> comment updated to reflect the fan-in topology (proxy SUB binds, publishers connect — was previously documented as same-process only).</p><p><strong>Database</strong>: no new tables. <code>sandbox_db</code> index names changed (<code>idx_user_status</code> → <code>idx_sandbox_user_status</code>, <code>idx_symbol_exchange</code> → <code>idx_sandbox_symbol_exchange</code>) as part of the sandbox self-heal fix; applied automatically via the existing self-healing <code>create_all</code> path, no manual migration required.</p><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend: a plain pull already ships the CI-built dist. Only rebuild if</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you are editing React code:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management; ZeroMQ bus fan-in fix; multi-session login audit (#1591, 3 commits); Arbitrage tool; Nubra market/stop-market emulation + MCX; Angel/Groww broker fixes; sandbox self-heal; Action Center order-type execution + test isolation; Telegram P&amp;L/alert-gating fixes; latency logger fix; scalping charts toggle; documentation reconciliation sweep; dependency bumps.</li><li><strong>@Kalaiviswa</strong> — AliceBlue NFO/CDS futures fix; IndMoney broker hardening (API realignment, WebSocket FD-leak fixes, subscription batching, option-chain resilience, #1604/#1610/#1613).</li><li><strong>@Raj27i</strong> — Arrow app-level PONG heartbeat fix (#1583, #1614).</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard/issues</a></li></ul>`,54)])])}const u=o(i,[["render",r]]);export{g as __pageData,u as default};
