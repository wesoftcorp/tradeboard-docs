import{_ as o,o as t,c as r,a2 as a}from"./chunks/framework.BXzK3EA4.js";const h=JSON.parse('{"title":"Version 2.0.1.6 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.6-released.md","filePath":"change-log/release/version-2.0.1.6-released.md"}'),i={name:"change-log/release/version-2.0.1.6-released.md"};function s(n,e,d,c,l,p){return t(),r("div",null,[...e[0]||(e[0]=[a(`<h1 id="version-2-0-1-6-released" tabindex="-1">Version 2.0.1.6 Released <a class="header-anchor" href="#version-2-0-1-6-released" aria-label="Permalink to &quot;Version 2.0.1.6 Released&quot;">​</a></h1><p><strong>Date: 24th July 2026</strong></p><p><strong>Broker &amp; Reliability Release: a new HDFC Sky broker integration, a ground-up TypeScript rewrite of the /trading chart-trading page with a multi-chart grid, real-time order-update streaming brought to IIFL Capital and Upstox, sandbox order lifecycle now includes a first-class &quot;trigger pending&quot; status for SL/SL-M orders, a Definedge connector overhaul, and a broad sweep of Dhan/Flattrade/Fyers/Zerodha broker fixes</strong></p><p>This release spans 119 commits since v2.0.1.5. The headline addition is <strong>HDFC Sky</strong>, a new broker integration (orders, WebSocket market depth, funds, OI tracker, and SL-M order protection under HDFC Sky&#39;s live margin regime) <br><br> Tradeboard now supports 35 brokers. The <strong><code>/trading</code> page was rewritten from scratch</strong> as a TypeScript React route with a multi-chart grid, TradingView-style timeframe/chart-type menus, an inline Buy/Sell panel, and a symbol search modal with per-pane state. <br><br><strong>Real-time order-update streaming</strong> — started in the previous cycle for a handful of brokers — was extended end-to-end, with IIFL Capital and Upstox gaining live order/trade push feeds and a new dedicated <strong><code>/websocket/order</code></strong> page for watching the raw event stream. <br><br> The <strong>sandbox engine</strong> gained a proper <strong>&quot;trigger pending&quot;</strong> status for SL/SL-M orders (matching how a live exchange&#39;s Stop-Loss book actually works, instead of showing &quot;open&quot; the whole time) plus F&amp;O expiry settlement timing, event-driven MTM, and several T+1/holdings settlement correctness fixes. <br><br> On the broker side: <strong>Definedge&#39;s connector was overhauled</strong> for the current API (orders, OI, rate limits, WebSocket), <strong>Dhan and IIFL Capital both got SL-M emulation</strong> (a bare stop-loss-market order is blocked under SEBI&#39;s market-protection regime on several brokers - it&#39;s now transparently converted to a protective stop-limit order), and Flattrade, Fyers, Zerodha, and Upstox each picked up targeted reliability fixes. <br><br> A large parallel documentation effort (&quot;superpowers&quot;) laid out the architecture, ADRs, and roadmap for future global-market (non-Indian-exchange) broker support.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>HDFC Sky broker integration (<code>cb4ec7d56</code> + 9 follow-up fixes)</strong> — new broker: order placement/modification, WebSocket market depth (served from a live snapshot), funds, OI tracker (batched LTP fetch + OI sourced over WebSocket), and SL-M/MARKET algo orders converted to protected LIMIT/SL under HDFC Sky&#39;s live margin rules. Tradeboard now supports <strong>35 brokers</strong>.</li><li><strong><code>/trading</code> page rewrite (<code>a55a95fe3</code> + 10 follow-ups)</strong> — ported from the legacy page to a TypeScript React route: multi-chart grid layout, TradingView-style timeframe/chart-type menus and product segments, an inline Buy/Sell button panel with live top-of-book bid × ask, a TradingView-style symbol search modal with per-pane state, 2-decimal P&amp;L with platform-stable tick precision (Windows vs macOS), and square-off routed through <code>placeorder</code> (never <code>placesmartorder</code>) to avoid a position-sizing mismatch.</li><li><strong>Real-time order-update streaming extended (<code>2960edace</code> and related, this + prior cycle)</strong> — IIFL Capital and Upstox gained live order/trade push feeds; every order-update source now normalizes to Tradeboard&#39;s standard symbol format; a new dedicated <strong><code>/websocket/order</code></strong> page (<code>074768845</code>) lets you watch the raw live order-update stream with auto-reconnect; the ZMQ publisher now warms up at boot so the very first order event isn&#39;t dropped under gunicorn (<code>e9fc4d761</code>).</li><li><strong>Sandbox: &quot;trigger pending&quot; order status (<code>3e7132a52</code>)</strong> — SL/SL-M orders in sandbox now correctly rest in a &quot;trigger pending&quot; state (mirroring an exchange&#39;s Stop-Loss book) until the trigger price actually fires, instead of showing &quot;open&quot; the entire time. Ships with a schema migration (<code>upgrade/migrate_sandbox_trigger_pending.py</code>) wired into <code>migrate_all.py</code>.</li><li><strong>IIFL Capital: rate limiting + live order updates + SL-M fix (<code>5e562a79c</code>, <code>18c003106</code>)</strong> — previously-unthrottled option-chain/OI requests were silently dropping data past IIFL&#39;s rate caps; a new dedicated MQTT order/trade-update adapter now streams live order status; bare SL-M orders (blocked by IIFL) are now converted to a protective stop-limit order, mirroring the Dhan fix below.</li><li><strong>Dhan: SL-M protective-limit hardening (<code>55b3e2027</code>, <code>828f8d23e</code>, <code>943e39912</code>)</strong> — SL-M orders rest as a protective <code>STOP_LOSS</code> limit order instead of being rejected under Dhan&#39;s live market-protection-percent regime; live order-update payload parsing corrected for Dhan&#39;s camelCase field names.</li><li><strong>Definedge: connector overhaul (<code>0332220a8</code>)</strong> — orders, open interest, rate limits, and WebSocket streaming brought up to date against Definedge&#39;s current API; SL-M orders now show correct Cancel/Modify actions while in &quot;trigger pending&quot; (<code>72e398a3b</code>).</li><li><strong>Sandbox reliability sweep</strong> — F&amp;O expiry settlement timing and option LTP settlement (<code>2342af5de</code>), event-driven MTM via position-feed subscriptions instead of polling (<code>5a245e9d2</code>), corrected weighted-average in T+1 holdings settlement (<code>265fd3419</code>), auto-cancel of open orders on expired F&amp;O contracts (<code>fa1de5794</code>), non-trading days skipped in daily P&amp;L snapshots (<code>ccb8e629b</code>, #876), deferred MARKET fills on stale quotes (<code>6665d6e7f</code>, #1638), and CNC sells against existing holdings settling correctly instead of opening a phantom short (<code>bb27f2ff2</code>, #1640).</li><li><strong>WebSocket protocol fix: per-mode subscription tracking (<code>be65283ef</code>, #1664)</strong> — a client holding both LTP and Depth subscriptions on the same symbol no longer has one silently overwrite the other; the trading terminal now subscribes Depth-only for tradeable instruments (Depth&#39;s payload already carries <code>ltp</code> as a first-class field).</li><li><strong>Broker fixes</strong> — Flattrade (bounded auth-failure retry instead of hammering a dead WebSocket session, burst-friendly dual-window rate limiter for #1663), Fyers (redundant inter-chunk sleep dropped from history fetch, paise conversion fixed for CDS/BCD in WebSocket mapping, empty funds returned on error instead of a fabricated zero), Zerodha (order-update symbol resolution now prefers the broker&#39;s instrument token over name matching), Upstox (portfolio-stream authorize endpoint corrected with a direct-WSS fallback, full RMS rejection reason surfaced in order updates).</li><li><strong>Security &amp; infra</strong> — a pepper mismatch on the encryption key now surfaces clearly with DB recovery tooling instead of failing silently (<code>e8660191d</code>, #1660); <code>api_keys</code> table name corrected in the <code>init_db</code> diagnostic; icons and emojis removed repo-wide and forbidden going forward in all generated output (code, docs, commits, alerts) per updated CLAUDE.md guidance.</li><li><strong>Order dialog / Holdings UI</strong> — cash equity orders now show the real share count instead of a lot-size multiplier, with an NSE/BSE exchange switch; Holdings gained Add/Exit actions and a fix for a zero LTP defeating the REST fallback.</li><li><strong>Global-market architecture planning (&quot;superpowers&quot; docs, 10 commits)</strong> — design, audit, and roadmap documents plus three ADRs (crypto via direct native adapters not CCXT; direct API/WebSocket integration, no vendor SDKs; in-tree broker model) laying groundwork for future non-Indian-exchange broker support. Documentation only in this release — no code shipped yet.</li><li><strong>Dependency bumps</strong> — TypeScript migrated to 7.0 (<code>8b6e5f08d</code>), <code>tradeboard</code> SDK pin bumped to 2.0.3 (adds <code>subscribe_orders</code>), <code>tradeboard-charts</code> 1.0.7, <code>axios</code> 1.18.0, <code>pillow</code> 12.3.0, <code>mcp</code> 1.28.1, <code>svgo</code> 4.0.2, <code>setuptools</code> 83.0.0, <code>actions/setup-node</code> 6 → 7.</li></ul><hr><p><strong>HDFC Sky broker integration — deep dive</strong></p><p><code>cb4ec7d56</code> (Add HDFC Sky broker integration) plus 9 follow-up fixes over the following day: WebSocket auth corrected and index normalization relocated (<code>8ffae60a3</code>), <code>exchange.py</code> folded into <code>transform_data.py</code> for consistency with the standard broker plugin shape (<code>5a3566954</code>), OI tracker fixed by capping the LTP-fetch batch size and sourcing OI over WebSocket instead of REST (<code>cfb05fb8b</code>), MARKET/SL-M algo orders converted to protected LIMIT/SL orders to satisfy HDFC Sky&#39;s live margin checks (<code>2f933dad9</code>), market depth now served from a live WebSocket snapshot rather than polled (<code>f1f63a769</code>), queued WebSocket subscriptions no longer dropped — now batched by type (<code>7a608ac02</code>), margin leg <code>underlying</code> sent as an integer per the API&#39;s actual expectation (<code>d30fd933b</code>), and a final docs pass resolving the margin-underlying TODO and genericizing the funds sample (<code>935817c49</code>).</p><hr><p><strong><code>/trading</code> page rewrite — deep dive</strong></p><p>The chart-trading page moved from its previous implementation to a TypeScript React route (<code>a55a95fe3</code>) with a multi-chart grid layout. Follow-up commits over the next several days: square-off now routes through <code>placeorder</code> instead of <code>placesmartorder</code> to avoid a position-sizing mismatch (<code>c0adca1f3</code>); a guard against a coarse tick feed snapping orders to whole rupees (<code>e85b7800e</code>); 2-decimal P&amp;L display with robust tick precision and a stable default chart zoom (<code>40f2abc9a</code>); platform-stable price decimals reconciling Windows vs macOS floating-point display differences (<code>04021aa1f</code>); live top-of-book bid × ask shown directly on the Buy/Sell panel (<code>02bcd2788</code>); TradingView-style timeframe and chart-type menus plus product-segment selection (<code>934e2a7ed</code>); the &quot;Fit&quot; text button replaced with a fit-to-screen icon (<code>3b4a829ca</code>); cross-platform MIME type and state-persistence fixes plus a BHEL fallback symbol (<code>26723bfaf</code>); an inline Buy/Sell button panel added directly on the chart (<code>f19b2cd9b</code>); and finally a TradingView-style symbol search modal with independent per-pane state (<code>249a4a43f</code>) for the multi-chart grid.</p><hr><p><strong>Order-update streaming — deep dive</strong></p><p>Building on the end-to-end real-time order-update pipeline shipped this cycle (<code>2960edace</code>), follow-up work: Upstox&#39;s portfolio-stream authorize endpoint corrected with a direct-WSS fallback (<code>516b9073b</code>), then its Tradeboard symbol mapping and full RMS rejection reason wired into order updates (<code>705b80fee</code>); every order-update source normalized to emit Tradeboard&#39;s standard symbol format with unit test coverage (<code>3dc0d797c</code>); the <code>tradeboard</code> Python SDK pin bumped to 2.0.3 to pick up <code>subscribe_orders</code>, with order-update environment variables documented (<code>62b60a90f</code>); the shared ZMQ publisher now warms up at application boot so the very first order event of a session isn&#39;t silently dropped under gunicorn (<code>e9fc4d761</code>); &quot;trigger pending&quot; promoted to a first-class order-update status broker-wide (<code>c3870d3a6</code>) — the same status this release&#39;s sandbox work (<code>3e7132a52</code>) then implemented for the simulated engine; sandbox order placement now streams an &quot;open&quot; alert immediately, matching live-broker behavior (<code>15f375278</code>); and a new dedicated <strong><code>/websocket/order</code></strong> page for watching the raw live stream (<code>074768845</code>), with a follow-up fix for a stale-closure bug in its auto-reconnect logic (<code>ac0827779</code>).</p><hr><p><strong>Sandbox: trigger pending — deep dive</strong></p><p><code>3e7132a52</code> — SL and SL-M orders previously sat as &quot;open&quot; from placement until the trigger fired, indistinguishable from a normal resting order. Real exchanges hold SL/SL-M orders in a separate Stop-Loss order book until the trigger price is touched; sandbox now mirrors this with a genuine &quot;trigger pending&quot; state. SL-M fires straight to <code>complete</code> once triggered (no resting phase, matching live brokers); SL transitions to <code>open</code> once triggered unless the limit price is also immediately satisfiable, in which case it fills directly. Modify/cancel and the end-of-day/expired-contract auto-cancel sweeps all now accept the new state. Ships with <code>upgrade/migrate_sandbox_trigger_pending.py</code> (SQLite doesn&#39;t support altering a CHECK constraint in place, so this rebuilds <code>sandbox_orders</code> preserving every row) wired into the standard <code>migrate_all.py</code> upgrade path.</p><hr><p><strong>Broker fixes</strong></p><ul><li><code>55b3e2027</code>, <code>828f8d23e</code>, <code>943e39912</code> — <strong>Dhan</strong>: SL-M orders rest as a protective <code>STOP_LOSS</code> limit order under Dhan&#39;s live market-protection-percent regime instead of being rejected outright; live order-update payload parsing corrected for Dhan&#39;s camelCase field names; the protective-limit calculation itself hardened following code review.</li><li><code>0332220a8</code>, <code>c24d2282a</code>, <code>72e398a3b</code> — <strong>Definedge</strong>: full connector overhaul for the current API (orders, open interest, rate limits, WebSocket streaming); noisy logs quieted and a redundant multiquote batch delay dropped; SL-M orders now show correct Cancel/Modify actions while in &quot;trigger pending&quot;.</li><li><code>41bcaa4f8</code>, <code>96ad15ab9</code> — <strong>Flattrade</strong>: burst-friendly dual-window rate limiter replacing an overly conservative fixed-gap pacer (#1663); WebSocket reconnect logic no longer retries indefinitely on a confirmed auth failure — backs off through a tightly bounded retry path instead of hammering a dead session.</li><li><code>527249885</code>, <code>1815de268</code>, <code>7a3fbc0f6</code> — <strong>Fyers</strong>: redundant inter-chunk sleep dropped from history fetch; paise-to-rupee conversion corrected for CDS/BCD in WebSocket mapping; funds endpoint returns empty on error instead of a fabricated zero balance.</li><li><code>78d048d12</code> — <strong>Zerodha</strong>: order-update symbol resolution now prefers the broker&#39;s instrument token over brsymbol name-matching, more reliable for F&amp;O contracts.</li><li><code>5e562a79c</code>, <code>18c003106</code> — <strong>IIFL Capital</strong>: rate limiting added across the REST API (fixing silent option-chain/OI data loss from an unthrottled concurrent burst); a new dedicated MQTT adapter streams live order/trade updates; bare SL-M orders converted to a protective stop-limit order.</li></ul><hr><p><strong>Reliability &amp; fixes</strong></p><ul><li><code>e8660191d</code> — a mismatched encryption pepper now surfaces as a clear error with accompanying DB recovery tooling, instead of failing silently (#1660).</li><li><code>d4786ca11</code> — corrected the <code>api_keys</code> table name referenced in the <code>init_db</code> diagnostic.</li><li><code>be65283ef</code> — WebSocket per-mode subscription tracking (#1664): a client holding LTP and Depth on the same symbol no longer has one mode silently overwrite the other; the trading terminal now subscribes Depth-only for tradeable instruments since Depth&#39;s payload already carries <code>ltp</code>.</li><li><code>4f928011d</code> — Holdings page gained Add/Exit position actions; fixed a zero LTP value defeating the REST fallback path.</li><li><code>5be274364</code>, <code>932c03bdb</code> — order dialog: dropped the lots UI for cash equity (added an NSE/BSE exchange switch instead), and cash equity orders now show the real share count rather than a lot-size multiplier.</li><li><code>b3e142e9c</code>, <code>891f8b7ae</code> — icons and emojis removed repo-wide (source, comments, logs, docs) and forbidden going forward in all generated output per updated CLAUDE.md guidance.</li></ul><hr><p><strong>Global-market architecture planning (&quot;superpowers&quot; docs)</strong></p><p>Ten documentation-only commits (<code>7cfa323bc</code> through <code>786f5932b</code>) laying out design, audit, and roadmap material for eventual non-Indian-exchange broker support: three ADRs (crypto via direct native adapters rather than CCXT; direct API/WebSocket integration with no vendor SDKs; an in-tree broker model rather than out-of-tree plugins), a <code>problem/</code> directory documenting current challenges, GTT marked as an experimental/optional capability, and a <code>blockers/</code> living register for open questions. No code shipped in this release from this effort — planning only.</p><hr><p><strong>Dependencies</strong></p><ul><li>TypeScript migrated to <strong>7.0</strong> (<code>8b6e5f08d</code>, with a package-lock sync fix <code>a7f2be646</code>)</li><li><code>tradeboard</code> SDK pin: <code>2.0.2</code> → <strong>2.0.3</strong> (adds <code>subscribe_orders</code>)</li><li><code>tradeboard-charts</code> → <strong>1.0.7</strong>, <code>axios</code> → <strong>1.18.0</strong></li><li><code>pillow</code> → <strong>12.3.0</strong>, <code>mcp</code> → <strong>1.28.1</strong></li><li><code>svgo</code> → <strong>4.0.2</strong>, <code>setuptools</code> → <strong>83.0.0</strong></li><li><code>actions/setup-node</code> → <strong>7</strong> (CI only)</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>utils/version.py</code>: <code>VERSION = &quot;2.0.1.6&quot;</code></p><p><code>pyproject.toml</code>: <code>version = &quot;2.0.1.6&quot;</code> (<code>uv.lock</code> regenerated).</p><p><strong>Database</strong>: <code>sandbox_orders.order_status</code> CHECK constraint widened to include <code>&#39;trigger pending&#39;</code>. Existing installs need <code>upgrade/migrate_sandbox_trigger_pending.py</code>, which is wired into <code>upgrade/migrate_all.py</code> and runs automatically via the standard upgrade path (rebuilds the table via SQLite&#39;s create/copy/drop/rename procedure, preserving every existing row). Fresh installs get the widened constraint from schema creation directly — no migration needed.</p><p><strong>Upgrading is recommended for all users, and mandatory for sandbox mode</strong> — without the migration, placing an SL or SL-M order in sandbox will fail with a <code>CHECK constraint failed</code> error.</p><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade/migrate_all.py</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend: a plain pull already ships the CI-built dist. Only rebuild if</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you are editing React code:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><p>Never run <code>cp .sample.env .env</code> on an existing installation — it destroys broker credentials and the <code>API_KEY_PEPPER</code>, permanently invalidating password hashes and encrypted tokens. Compare <code>.env.sample</code> against your <code>.env</code> for new variables instead.</p><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul>`,49)])])}const u=o(i,[["render",s]]);export{h as __pageData,u as default};
