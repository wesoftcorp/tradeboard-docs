import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Telegram","description":"","frontmatter":{},"headers":[],"relativePath":"trading-platform/telegram.md","filePath":"trading-platform/telegram.md"}'),r={name:"trading-platform/telegram.md"};function n(s,e,l,d,c,h){return t(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="telegram" tabindex="-1">Telegram <a class="header-anchor" href="#telegram" aria-label="Permalink to &quot;Telegram&quot;">​</a></h1><h3 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h3><p>The Tradeboard Telegram Bot provides a convenient interface to your trading account through Telegram. It gives you read access to orders, trades, positions, holdings, funds, P&amp;L, quotes and charts, and it also exposes a small set of remote actions: closing all positions, stopping running Python strategies, and switching the instance between Live and Analyze mode. Order alerts are pushed to you automatically when orders flow through the Tradeboard API.</p><h3 id="features" tabindex="-1">Features <a class="header-anchor" href="#features" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Account Linking</strong>: Securely link your Tradeboard account using an API key and host URL</li><li><strong>Real-time Data Access</strong>: View orderbook, tradebook, positions, holdings, and funds</li><li><strong>P&amp;L Tracking</strong>: Monitor realized and unrealized profit/loss</li><li><strong>Quote Information</strong>: Get real-time quotes for any symbol</li><li><strong>Chart Generation</strong>: Generate intraday and daily candlestick charts with volume</li><li><strong>Remote Actions</strong>: Close all positions, stop running Python strategies, toggle Live/Analyze mode</li><li><strong>Automatic Order Alerts</strong>: Every order, basket, split, options and GTT event pushed to your chat</li><li><strong>Interactive Menu</strong>: Inline button interface for quick access</li><li><strong>Programmatic Sends</strong>: <code>POST /api/v1/telegram/notify</code> and <code>client.telegram(...)</code> in the Python SDK</li></ul><h3 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h3><h4 id="_1-create-your-telegram-bot" tabindex="-1">1. Create Your Telegram Bot <a class="header-anchor" href="#_1-create-your-telegram-bot" aria-label="Permalink to &quot;1. Create Your Telegram Bot&quot;">​</a></h4><ol><li>Open Telegram and search for <a href="https://t.me/botfather" target="_blank" rel="noreferrer">@BotFather</a></li><li>Send the <code>/newbot</code> command</li><li>Follow the instructions to name your bot and choose its username</li><li>Copy the bot token provided by BotFather</li><li>Use this token in the Tradeboard configuration below</li></ol><h4 id="_2-enable-the-telegram-bot-in-tradeboard" tabindex="-1">2. Enable the Telegram Bot in Tradeboard <a class="header-anchor" href="#_2-enable-the-telegram-bot-in-tradeboard" aria-label="Permalink to &quot;2. Enable the Telegram Bot in Tradeboard&quot;">​</a></h4><ol><li>Log in to Tradeboard. From the profile dropdown (top right) click <strong>Telegram Bot</strong>, or navigate to <code>/telegram</code></li><li>Click <strong>Configuration</strong> to open <code>/telegram/config</code></li><li>Paste the bot token into the <strong>Bot Token</strong> field</li><li>Optionally adjust <strong>Broadcast Messages</strong> and <strong>Rate Limit (messages per minute)</strong>, then click <strong>Save Configuration</strong></li><li>Return to <code>/telegram</code> and click <strong>Start Bot</strong></li></ol><p>The status badge on <code>/telegram</code> flips to <strong>Online</strong> once polling starts. If the bot was running when the server was last shut down, it auto-starts on the next boot, so you normally only press <strong>Start Bot</strong> once.</p><h4 id="_3-link-your-account" tabindex="-1">3. Link Your Account <a class="header-anchor" href="#_3-link-your-account" aria-label="Permalink to &quot;3. Link Your Account&quot;">​</a></h4><ol><li><p>Start a conversation with your bot on Telegram and send <code>/start</code></p></li><li><p>Send the <code>/link</code> command with your API key and host URL:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/link your_api_key_here http://127.0.0.1:5000</span></span></code></pre></div></li><li><p>The bot validates the credentials by calling <code>funds</code> against that host. On success it stores the encrypted key, records your Tradeboard username and broker, and confirms that the account is linked</p></li></ol><p>Both arguments are required. <code>/link</code> with anything other than exactly two arguments returns a usage message.</p><h3 id="available-commands" tabindex="-1">Available Commands <a class="header-anchor" href="#available-commands" aria-label="Permalink to &quot;Available Commands&quot;">​</a></h3><h4 id="account-management" tabindex="-1">Account Management <a class="header-anchor" href="#account-management" aria-label="Permalink to &quot;Account Management&quot;">​</a></h4><ul><li><code>/start</code> - Initialize the bot and see the welcome message</li><li><code>/link &lt;api_key&gt; &lt;host_url&gt;</code> - Link your Tradeboard account</li><li><code>/unlink</code> - Unlink your account and delete the stored credentials</li><li><code>/status</code> - Check connection status, host URL and link date</li></ul><h4 id="trading-data" tabindex="-1">Trading Data <a class="header-anchor" href="#trading-data" aria-label="Permalink to &quot;Trading Data&quot;">​</a></h4><ul><li><code>/orderbook</code> - View orders (first 10 shown)</li><li><code>/tradebook</code> - View executed trades (first 10 shown)</li><li><code>/positions</code> - View open positions (first 10 shown)</li><li><code>/holdings</code> - View holdings (first 10 shown)</li><li><code>/funds</code> - View available cash, collateral, utilized margin and total</li><li><code>/pnl</code> - View realized, unrealized and total P&amp;L</li></ul><p>Amounts are printed with the currency symbol that matches the broker recorded when you linked: <code>$</code> for crypto brokers (currently Delta Exchange), otherwise the rupee symbol.</p><h4 id="market-data" tabindex="-1">Market Data <a class="header-anchor" href="#market-data" aria-label="Permalink to &quot;Market Data&quot;">​</a></h4><ul><li><code>/quote &lt;symbol&gt; [exchange]</code> - Get quote for a symbol. Exchange defaults to <code>NSE</code><ul><li>Example: <code>/quote RELIANCE</code></li><li>Example: <code>/quote NIFTY NSE_INDEX</code></li><li>Returns LTP, change, change percent, open, high, low, previous close and volume</li></ul></li></ul><h4 id="charts" tabindex="-1">Charts <a class="header-anchor" href="#charts" aria-label="Permalink to &quot;Charts&quot;">​</a></h4><ul><li><code>/chart &lt;symbol&gt; [exchange] [type] [interval] [days]</code> - Generate price charts <ul><li>Exchange: defaults to <code>NSE</code></li><li>Type: <code>intraday</code> (default), <code>daily</code>, or <code>both</code></li><li>Interval: any interval the history API accepts. The bot&#39;s own help lists <code>1m</code>, <code>5m</code>, <code>15m</code>, <code>30m</code>, <code>1h</code> and <code>D</code></li><li>Days: lookback period. Defaults to 5 for intraday and 252 for daily</li><li>Examples: <ul><li><code>/chart RELIANCE</code> - 5-minute intraday chart over 5 days</li><li><code>/chart RELIANCE NSE intraday 15m 10</code> - 15-minute chart for 10 days</li><li><code>/chart RELIANCE NSE daily D 100</code> - Daily chart for 100 days</li><li><code>/chart RELIANCE NSE both</code> - Intraday and daily charts sent together as a media group</li></ul></li></ul></li></ul><h4 id="remote-actions" tabindex="-1">Remote Actions <a class="header-anchor" href="#remote-actions" aria-label="Permalink to &quot;Remote Actions&quot;">​</a></h4><p>These commands change state on the Tradeboard instance. None of them act on the command alone: each replies with inline buttons, and nothing happens until you press one.</p><ul><li><code>/closeall</code> - Close all open positions. Offers <strong>Yes, close all</strong>, <strong>Close all + Stop strategies</strong>, and <strong>Cancel</strong>. The second option closes every position and then stops every running Python strategy</li><li><code>/stoppython</code> - List running Python strategies as buttons. Pick one to stop it, or <strong>Stop All</strong> to stop every running strategy. Each choice is confirmed before it runs. If nothing is running the bot says so and stops there</li><li><code>/mode</code> - Show whether the instance is in Live Mode or Analyze Mode, with a single button to switch to the other. The change is applied server-side and pushed to the web UI over SocketIO</li></ul><h4 id="interactive-interface" tabindex="-1">Interactive Interface <a class="header-anchor" href="#interactive-interface" aria-label="Permalink to &quot;Interactive Interface&quot;">​</a></h4><ul><li><code>/menu</code> - Display the inline button menu. Buttons: Orderbook, Tradebook, Positions, Holdings, Funds, P&amp;L, and Refresh. The Refresh button edits the same message in place and stamps it with the update time</li><li><code>/help</code> - Show the help message with all commands</li></ul><p>The P&amp;L button and the <code>/pnl</code> command read the same fields from the funds response, so the two always agree.</p><h3 id="order-alerts-automatic-notifications" tabindex="-1">Order Alerts (Automatic Notifications) <a class="header-anchor" href="#order-alerts-automatic-notifications" aria-label="Permalink to &quot;Order Alerts (Automatic Notifications)&quot;">​</a></h3><h4 id="overview-1" tabindex="-1">Overview <a class="header-anchor" href="#overview-1" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>The bot automatically sends real-time notifications for order-related API activity. No additional commands are needed: alerts are sent when orders are placed through the Tradeboard API. Delivery goes over a direct HTTPS call to the Telegram Bot API rather than through the polling bot, so alerts do not depend on the polling loop being mid-cycle. They do depend on the bot being started: if the bot is stopped from <code>/telegram</code>, automatic alerts stop with it.</p><h4 id="supported-order-types" tabindex="-1">Supported Order Types <a class="header-anchor" href="#supported-order-types" aria-label="Permalink to &quot;Supported Order Types&quot;">​</a></h4><p>Alerts are driven by the internal event bus. The events that produce a Telegram message are:</p><table tabindex="0"><thead><tr><th>Event topic</th><th>Trigger</th></tr></thead><tbody><tr><td><code>order.placed</code></td><td><code>placeorder</code> or <code>placesmartorder</code> succeeded</td></tr><tr><td><code>order.no_action</code></td><td>Smart order evaluated and found nothing to do</td></tr><tr><td><code>order.modified</code></td><td><code>modifyorder</code> succeeded</td></tr><tr><td><code>order.cancelled</code></td><td><code>cancelorder</code> succeeded</td></tr><tr><td><code>orders.all_cancelled</code></td><td><code>cancelallorder</code> succeeded</td></tr><tr><td><code>position.closed</code></td><td><code>closeposition</code> succeeded</td></tr><tr><td><code>basket.completed</code></td><td>All legs of a <code>basketorder</code> completed</td></tr><tr><td><code>split.completed</code></td><td>All sub-orders of a <code>splitorder</code> completed</td></tr><tr><td><code>options.completed</code></td><td>All legs of an <code>optionsorder</code> completed</td></tr><tr><td><code>multiorder.completed</code></td><td>All legs of an <code>optionsmultiorder</code> completed</td></tr><tr><td><code>gtt.placed</code></td><td><code>placegttorder</code> succeeded</td></tr><tr><td><code>gtt.modified</code></td><td><code>modifygttorder</code> succeeded</td></tr><tr><td><code>gtt.cancelled</code></td><td><code>cancelgttorder</code> succeeded</td></tr><tr><td><code>gtt.triggered</code></td><td>A resting GTT fired and placed its order</td></tr><tr><td><code>gtt.expired</code></td><td>A GTT lapsed past its <code>expires_at</code> without firing</td></tr></tbody></table><p>Failure events (<code>order.failed</code>, <code>order.modify_failed</code>, <code>order.cancel_failed</code>, <code>gtt.failed</code>, <code>gtt.modify_failed</code>, <code>gtt.cancel_failed</code>, <code>analyzer.error</code>) deliberately do not send a Telegram message, so a run of validation rejections cannot flood your chat.</p><p><code>gtt.placed</code>, <code>gtt.modified</code> and <code>gtt.cancelled</code> fire in both Live and Analyze mode. <code>gtt.triggered</code> and <code>gtt.expired</code> are published by the sandbox GTT manager, so today they only reach you in Analyze mode: a live GTT fires at the broker, where Tradeboard does not observe the moment it triggers.</p><p>The alert formatter has a dedicated layout for each order API type. GTT events do not have one yet, so they arrive under the generic <code>Order Update</code> heading with the mode line, strategy name and timestamp but no per-trigger detail.</p><h4 id="alert-format" tabindex="-1">Alert Format <a class="header-anchor" href="#alert-format" aria-label="Permalink to &quot;Alert Format&quot;">​</a></h4><p>Each alert includes:</p><ul><li><strong>Strategy Name</strong>: First line, if <code>strategy</code> was supplied in the API call</li><li><strong>Mode Indicator</strong>: <code>LIVE MODE - Real Order</code> for real broker orders, <code>ANALYZE MODE - No Real Order</code> for sandbox orders, followed by a horizontal rule</li><li><strong>Order Details</strong>: Symbol, action, quantity, price type, exchange, product type</li><li><strong>Status</strong>: Order ID on success, or an error line with the failure message</li><li><strong>Timestamp</strong>: <code>Time: HH:MM:SS</code> on the last line</li></ul><p>The bot sends its messages with Telegram Markdown, so mode indicators, strategy names and symbols arrive bold or monospaced rather than decorated with icons. Status markers inside multi-leg alerts are written as plain tags such as <code>[OK]</code> and <code>[FAILED]</code>.</p><h4 id="example-notifications" tabindex="-1">Example Notifications <a class="header-anchor" href="#example-notifications" aria-label="Permalink to &quot;Example Notifications&quot;">​</a></h4><p><strong>Live order placed:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Order Placed</span></span>
<span class="line"><span>Strategy: MyStrategy</span></span>
<span class="line"><span>LIVE MODE - Real Order</span></span>
<span class="line"><span>─────────────────────</span></span>
<span class="line"><span>Symbol: RELIANCE</span></span>
<span class="line"><span>Action: BUY</span></span>
<span class="line"><span>Quantity: 10</span></span>
<span class="line"><span>Price Type: MARKET</span></span>
<span class="line"><span>Exchange: NSE</span></span>
<span class="line"><span>Product: MIS</span></span>
<span class="line"><span>Order ID: 250408000989443</span></span>
<span class="line"><span>Time: 14:23:45</span></span></code></pre></div><p><strong>Analyze mode order:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Order Placed</span></span>
<span class="line"><span>Strategy: TestStrategy</span></span>
<span class="line"><span>ANALYZE MODE - No Real Order</span></span>
<span class="line"><span>─────────────────────</span></span>
<span class="line"><span>Symbol: RELIANCE</span></span>
<span class="line"><span>Action: BUY</span></span>
<span class="line"><span>Quantity: 10</span></span>
<span class="line"><span>Price Type: MARKET</span></span>
<span class="line"><span>Exchange: NSE</span></span>
<span class="line"><span>Product: MIS</span></span>
<span class="line"><span>Order ID: ANALYZE123456</span></span>
<span class="line"><span>Time: 14:23:45</span></span></code></pre></div><h4 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h4><ul><li>Alerts are enabled by default for every linked user. The gate is the <code>notifications_enabled</code> flag on the linked-user row, which is shown on <code>/telegram/users</code> and defaults to on</li><li>A send that fails (bad token, Telegram API error, timeout) is recorded in the <code>notification_queue</code> table at priority 8. There is no background worker that drains this queue, so treat it as a delivery audit trail rather than an automatic retry</li><li>If Telegram rejects the message because of a Markdown parsing error, the alert is immediately re-sent as plain text</li><li>Zero impact on order execution speed: formatting and sending happen on a five-worker thread pool, never on the order-placement path</li></ul><h4 id="requirements-for-receiving-alerts" tabindex="-1">Requirements for Receiving Alerts <a class="header-anchor" href="#requirements-for-receiving-alerts" aria-label="Permalink to &quot;Requirements for Receiving Alerts&quot;">​</a></h4><ol><li>The Telegram bot must be started (<strong>Start Bot</strong> on <code>/telegram</code>, or <code>POST /telegram/bot/start</code>). While the bot is stopped, automatic alerts are suppressed and <code>POST /api/v1/telegram/notify</code> returns HTTP 409</li><li>Your account must be linked with the <code>/link</code> command</li><li><code>notifications_enabled</code> must be on for your linked user (the default)</li><li>Orders must be placed through the Tradeboard API. Orders placed directly on a broker website do not raise events</li></ol><h3 id="chart-features" tabindex="-1">Chart Features <a class="header-anchor" href="#chart-features" aria-label="Permalink to &quot;Chart Features&quot;">​</a></h3><h4 id="intraday-charts" tabindex="-1">Intraday Charts <a class="header-anchor" href="#intraday-charts" aria-label="Permalink to &quot;Intraday Charts&quot;">​</a></h4><ul><li>Default interval: 5 minutes</li><li>Default period: 5 days</li><li>Candlestick price panel plus a volume panel, with volume bars colored green or red by candle direction</li><li>Category-type x-axis, so there are no blank gaps for non-trading hours</li></ul><h4 id="daily-charts" tabindex="-1">Daily Charts <a class="header-anchor" href="#daily-charts" aria-label="Permalink to &quot;Daily Charts&quot;">​</a></h4><ul><li>Default interval: Daily (<code>D</code>)</li><li>Default period: 252 trading days. The history request reaches back 1.5 times that many calendar days to make sure enough sessions are returned, then the series is trimmed to the requested count</li><li>Candlestick price panel plus a volume panel, using the same layout as the intraday chart. No moving averages or other overlays are drawn</li></ul><h4 id="chart-customization" tabindex="-1">Chart Customization <a class="header-anchor" href="#chart-customization" aria-label="Permalink to &quot;Chart Customization&quot;">​</a></h4><ul><li><strong>Intervals</strong>: passed straight through to the history API. <code>1m</code>, <code>5m</code>, <code>15m</code>, <code>30m</code>, <code>1h</code> for intraday, <code>D</code> for daily</li><li><strong>Days</strong>: customize the lookback period</li><li><strong>Exchange</strong>: any exchange the history API supports, for example NSE, BSE, NFO, CDS, MCX, NSE_INDEX, BSE_INDEX</li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h3><h4 id="api-key-encryption" tabindex="-1">API Key Encryption <a class="header-anchor" href="#api-key-encryption" aria-label="Permalink to &quot;API Key Encryption&quot;">​</a></h4><ul><li>API keys are encrypted with Fernet before storage in the <code>telegram_users</code> table</li><li>The Fernet key is derived with PBKDF2-SHA256 (100,000 iterations) from <code>API_KEY_PEPPER</code> and the <code>TELEGRAM_KEY_SALT</code> salt</li><li>Keys are never stored in plain text and each user&#39;s key is isolated</li></ul><h4 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h4><ul><li><code>/link</code> requires a valid API key and is validated against the Tradeboard server before anything is stored</li><li>The bot management pages and routes under <code>/telegram</code> are behind the Flask login session</li><li>The public <code>/api/v1/telegram/*</code> endpoints are authenticated with an Tradeboard API key, sent either as the <code>apikey</code> body field or the <code>X-API-KEY</code> header</li><li><code>POST /api/v1/telegram/webhook</code> is verified against the <code>X-Telegram-Bot-Api-Secret-Token</code> header. The expected value is <code>TELEGRAM_WEBHOOK_SECRET</code> if set, otherwise the first 32 characters of the SHA-256 digest of the bot token</li></ul><h4 id="privacy" tabindex="-1">Privacy <a class="header-anchor" href="#privacy" aria-label="Permalink to &quot;Privacy&quot;">​</a></h4><ul><li>Each Telegram user can only access their own linked account</li><li>No cross-user data access</li><li>Command usage is logged for security audit</li></ul><h3 id="database-schema" tabindex="-1">Database Schema <a class="header-anchor" href="#database-schema" aria-label="Permalink to &quot;Database Schema&quot;">​</a></h3><p>The bot uses SQLAlchemy ORM with the following tables:</p><h4 id="telegramuser" tabindex="-1">TelegramUser <a class="header-anchor" href="#telegramuser" aria-label="Permalink to &quot;TelegramUser&quot;">​</a></h4><ul><li>Table <code>telegram_users</code>. Stores user-bot linkage: <code>telegram_id</code>, <code>tradeboard_username</code>, <code>encrypted_api_key</code>, <code>host_url</code>, <code>telegram_username</code>, <code>first_name</code>, <code>last_name</code>, <code>broker</code>, <code>is_active</code>, <code>notifications_enabled</code>, <code>last_command_at</code></li></ul><h4 id="botconfig" tabindex="-1">BotConfig <a class="header-anchor" href="#botconfig" aria-label="Permalink to &quot;BotConfig&quot;">​</a></h4><ul><li>Table <code>bot_config</code>, a single row with <code>id=1</code>. Holds the encrypted <code>token</code>, <code>bot_username</code>, <code>is_active</code>, <code>max_message_length</code> (default 4096), <code>rate_limit_per_minute</code> (default 30) and <code>broadcast_enabled</code></li><li>There is no webhook URL or polling-mode column. The bot always runs in polling mode; the webhook route exists but does not yet dispatch updates</li></ul><h4 id="commandlog" tabindex="-1">CommandLog <a class="header-anchor" href="#commandlog" aria-label="Permalink to &quot;CommandLog&quot;">​</a></h4><ul><li>Table <code>command_logs</code>. Audit trail of every command and inline-button callback: <code>telegram_id</code>, <code>command</code>, <code>chat_id</code>, <code>parameters</code>, <code>executed_at</code>. This table backs the <code>/telegram/analytics</code> page and <code>GET /api/v1/telegram/stats</code></li></ul><h4 id="notificationqueue" tabindex="-1">NotificationQueue <a class="header-anchor" href="#notificationqueue" aria-label="Permalink to &quot;NotificationQueue&quot;">​</a></h4><ul><li>Table <code>notification_queue</code>. Rows written when a send fails, carrying <code>message</code>, <code>priority</code>, <code>status</code>, <code>error_message</code></li></ul><h4 id="userpreference" tabindex="-1">UserPreference <a class="header-anchor" href="#userpreference" aria-label="Permalink to &quot;UserPreference&quot;">​</a></h4><ul><li>Table <code>user_preferences</code>. Per-user flags: <code>order_notifications</code>, <code>trade_notifications</code>, <code>pnl_notifications</code>, <code>daily_summary</code>, <code>summary_time</code>, <code>language</code>, <code>timezone</code>. These are readable and writable through <code>GET</code>/<code>POST /api/v1/telegram/preferences</code>; the order-alert path currently gates only on <code>notifications_enabled</code></li></ul><h3 id="technical-architecture" tabindex="-1">Technical Architecture <a class="header-anchor" href="#technical-architecture" aria-label="Permalink to &quot;Technical Architecture&quot;">​</a></h3><h4 id="components" tabindex="-1">Components <a class="header-anchor" href="#components" aria-label="Permalink to &quot;Components&quot;">​</a></h4><ol><li><strong>TelegramBotService</strong> (<code>services/telegram_bot_service.py</code>) <ul><li>Core bot logic and command handlers, built on python-telegram-bot</li><li>Tradeboard Python SDK integration for every data call</li><li>Chart generation using Plotly</li></ul></li><li><strong>TelegramAlertService</strong> (<code>services/telegram_alert_service.py</code>) <ul><li>Formats and delivers order alerts using a synchronous <code>httpx.Client</code> straight to <code>https://api.telegram.org</code>, so no asyncio or greenlet boundary is crossed under Gunicorn plus eventlet</li><li>Owns the five-worker <code>alert_executor</code> thread pool</li></ul></li><li><strong>Event bus subscriber</strong> (<code>subscribers/telegram_subscriber.py</code>) <ul><li>Registered in <code>subscribers/__init__.register_all()</code> on every order, position, batch and GTT topic</li></ul></li><li><strong>Database Layer</strong> (<code>database/telegram_db.py</code>) <ul><li>SQLAlchemy models and queries</li><li>Fernet encryption and decryption helpers</li><li>Configuration management, with TTL caches on user and preference lookups</li></ul></li><li><strong>Blueprint</strong> (<code>blueprints/telegram.py</code>) <ul><li>Session-authenticated routes for bot management, consumed by the React pages</li></ul></li><li><strong>REST namespace</strong> (<code>restx_api/telegram_bot.py</code>) <ul><li>The public <code>/api/v1/telegram/*</code> surface</li></ul></li><li><strong>React pages</strong> (<code>frontend/src/pages/telegram/</code>) <ul><li><code>/telegram</code>, <code>/telegram/config</code>, <code>/telegram/users</code> and <code>/telegram/analytics</code></li></ul></li><li><strong>Auto-start Feature</strong><ul><li>Bot automatically starts on application launch if it was active when the server stopped</li><li>State persistence across restarts, configured in <code>app.py</code></li></ul></li></ol><h4 id="threading-model" tabindex="-1">Threading Model <a class="header-anchor" href="#threading-model" aria-label="Permalink to &quot;Threading Model&quot;">​</a></h4><ul><li>Bot runs in a separate thread with its own event loop</li><li>Non-blocking operation with the main Flask application</li><li>Graceful shutdown handling, with automatic reconnect and backoff on Telegram network errors</li></ul><h4 id="chart-generation" tabindex="-1">Chart Generation <a class="header-anchor" href="#chart-generation" aria-label="Permalink to &quot;Chart Generation&quot;">​</a></h4><ul><li>Uses Plotly for chart creation</li><li>Kaleido engine for PNG export</li><li>Pandas for data manipulation</li><li>Category-type x-axis to handle gaps</li><li>Tradeboard pins <code>kaleido==1.3.0</code>. Kaleido 1.x no longer bundles a browser: it drives a real headless Chromium over the Chrome DevTools Protocol, so Chromium must be installed on the host or in the container. Each render spawns a browser process for roughly one to three seconds. A missing Chromium is the usual reason <code>/chart</code> replies &quot;Failed to generate charts&quot;</li><li>The render runs on a genuine, un-monkey-patched OS thread. Kaleido calls <code>asyncio.run()</code> internally, which cannot run inside the bot&#39;s live event loop, and eventlet&#39;s patched <code>threading.Thread</code> does not escape it</li></ul><h3 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h3><h4 id="bot-not-responding" tabindex="-1">Bot Not Responding <a class="header-anchor" href="#bot-not-responding" aria-label="Permalink to &quot;Bot Not Responding&quot;">​</a></h4><ol><li>Check that the badge on <code>/telegram</code> reads <strong>Online</strong></li><li>Verify the bot token is correct on <code>/telegram/config</code></li><li>Check network connectivity to <code>api.telegram.org</code></li><li>Review the server logs for errors</li></ol><h4 id="chart-generation-issues" tabindex="-1">Chart Generation Issues <a class="header-anchor" href="#chart-generation-issues" aria-label="Permalink to &quot;Chart Generation Issues&quot;">​</a></h4><ol><li>Confirm Chromium is installed and reachable. On Docker: <code>docker exec tradeboard-web /usr/bin/chromium --version</code>. On bare metal: <code>which chromium || which chromium-browser</code></li><li>Ensure market data is available for the symbol</li><li>Check that the exchange is correct</li><li>Verify the interval is supported and the date range is valid</li></ol><h4 id="linking-issues" tabindex="-1">Linking Issues <a class="header-anchor" href="#linking-issues" aria-label="Permalink to &quot;Linking Issues&quot;">​</a></h4><ol><li>Verify the API key is correct and active</li><li>Ensure the host URL is reachable from the machine running the bot, and that you passed both arguments to <code>/link</code></li><li>Check that the API key resolves to an Tradeboard user</li><li>Verify the Tradeboard server is running</li></ol><h4 id="alerts-stopped-arriving" tabindex="-1">Alerts Stopped Arriving <a class="header-anchor" href="#alerts-stopped-arriving" aria-label="Permalink to &quot;Alerts Stopped Arriving&quot;">​</a></h4><ol><li>Check whether the bot was stopped. Stopping the bot also stops automatic order alerts</li><li>Confirm <code>notifications_enabled</code> is on for your row on <code>/telegram/users</code></li><li>Confirm the order actually went through the Tradeboard API</li><li>Look for <code>notification_queue</code> rows, which record sends that failed</li></ol><h3 id="environment-variables" tabindex="-1">Environment Variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment Variables&quot;">​</a></h3><p>The bot respects the following environment variables:</p><ul><li><code>DATABASE_URL</code> - Database connection string. The Telegram tables live in the main Tradeboard database</li><li><code>API_KEY_PEPPER</code> - Pepper fed into the PBKDF2 key derivation for the stored API keys and bot token</li><li><code>TELEGRAM_KEY_SALT</code> - Salt for that derivation. Defaults to <code>telegram-tradeboard-salt</code></li><li><code>HOST_SERVER</code> - Tradeboard server URL</li><li><code>TELEGRAM_RATE_LIMIT</code> - Rate limit for the <code>/api/v1/telegram/*</code> endpoints. Defaults to <code>30 per minute</code></li><li><code>TELEGRAM_MESSAGE_RATE_LIMIT</code> - Rate limit for <code>POST /telegram/send-message</code>. Defaults to <code>10 per minute</code></li><li><code>TELEGRAM_WEBHOOK_SECRET</code> - Shared secret for webhook verification. Derived from the bot token when unset</li></ul><h3 id="api-endpoints" tabindex="-1">API Endpoints <a class="header-anchor" href="#api-endpoints" aria-label="Permalink to &quot;API Endpoints&quot;">​</a></h3><h4 id="web-interface" tabindex="-1">Web Interface <a class="header-anchor" href="#web-interface" aria-label="Permalink to &quot;Web Interface&quot;">​</a></h4><p>All routes below are under <code>/telegram</code> and require a logged-in Flask session. The pages themselves (<code>/telegram</code>, <code>/telegram/config</code>, <code>/telegram/users</code>, <code>/telegram/analytics</code>) are React routes served by the single-page app.</p><ul><li><code>POST /telegram/config</code> - Update bot configuration (<code>token</code>, <code>broadcast_enabled</code>, <code>rate_limit_per_minute</code>)</li><li><code>POST /telegram/bot/start</code> - Start the bot</li><li><code>POST /telegram/bot/stop</code> - Stop the bot</li><li><code>GET /telegram/bot/status</code> - Bot lifecycle state</li><li><code>POST /telegram/broadcast</code> - Send a message to all users with notifications enabled</li><li><code>POST /telegram/test-message</code> - Send a test message to the logged-in user&#39;s linked chat</li><li><code>POST /telegram/send-message</code> - Send a message to one <code>telegram_id</code></li><li><code>POST /telegram/user/&lt;telegram_id&gt;/unlink</code> - Unlink a user from the admin UI</li><li><code>GET /telegram/api/index</code> - Dashboard payload for the React page</li><li><code>GET /telegram/api/config</code> - Config payload (never returns the token itself)</li><li><code>GET /telegram/api/users</code> - Linked users plus 30-day command stats</li><li><code>GET /telegram/api/analytics</code> - 7-day and 30-day command stats</li></ul><p>There is no restart route. To restart the bot, stop it and start it again.</p><h4 id="public-rest-api" tabindex="-1">Public REST API <a class="header-anchor" href="#public-rest-api" aria-label="Permalink to &quot;Public REST API&quot;">​</a></h4><p>Mounted at <code>/api/v1/telegram</code>. Authenticate with an Tradeboard API key in the <code>apikey</code> body field or the <code>X-API-KEY</code> header.</p><ul><li><code>GET /api/v1/telegram/config</code> - Read bot configuration. The token is truncated in the response</li><li><code>POST /api/v1/telegram/config</code> - Update configuration. <code>rate_limit_per_minute</code> must be between 1 and 120</li><li><code>POST /api/v1/telegram/start</code> - Initialize and start the bot</li><li><code>POST /api/v1/telegram/stop</code> - Stop the bot</li><li><code>GET /api/v1/telegram/users</code> - List linked users. Optional <code>broker</code> and <code>notifications_enabled</code> query filters</li><li><code>POST /api/v1/telegram/notify</code> - Send a message to one linked user</li><li><code>POST /api/v1/telegram/broadcast</code> - Broadcast endpoint. Limited to 5 requests per minute and refuses with HTTP 403 when broadcast is disabled. The fan-out itself is not wired up yet, so it returns <code>success_count</code> and <code>fail_count</code> of 0. Use the <strong>Send Broadcast</strong> button on <code>/telegram</code>, which posts to <code>POST /telegram/broadcast</code> and does send</li><li><code>GET /api/v1/telegram/stats</code> - Command usage statistics. Optional <code>days</code> (1 to 365, default 7)</li><li><code>GET /api/v1/telegram/preferences</code> - Read per-user preferences. Requires <code>telegram_id</code></li><li><code>POST /api/v1/telegram/preferences</code> - Update per-user preferences</li><li><code>POST /api/v1/telegram/webhook</code> - Inbound webhook receiver, verified by secret token</li></ul><p><code>POST /api/v1/telegram/notify</code> takes:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>apikey</code></td><td>string</td><td>Required. Tradeboard API key</td></tr><tr><td><code>username</code></td><td>string</td><td>Required. Tradeboard login username, not the Telegram username</td></tr><tr><td><code>message</code></td><td>string</td><td>Required. Message body, up to 4096 characters</td></tr><tr><td><code>priority</code></td><td>integer</td><td>Optional, 1 to 10, default 5. Out-of-range values fall back to 5</td></tr><tr><td><code>wait_for_delivery</code></td><td>bool</td><td>Optional, default <code>false</code>. When <code>true</code>, the call blocks until Telegram replies</td></tr></tbody></table><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://127.0.0.1:5000/api/v1/telegram/notify</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;apikey&quot;: &quot;your_api_key&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;username&quot;: &quot;your_tradeboard_username&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;message&quot;: &quot;NIFTY crossed 24000&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;priority&quot;: 8</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  }&#39;</span></span></code></pre></div><p>The endpoint returns HTTP 409 when the bot is stopped, HTTP 404 when the username has no linked Telegram account, and HTTP 401 for a bad API key.</p><h4 id="python-sdk" tabindex="-1">Python SDK <a class="header-anchor" href="#python-sdk" aria-label="Permalink to &quot;Python SDK&quot;">​</a></h4><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tradeboard </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">api_key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;your_api_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">host</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://127.0.0.1:5000&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.telegram(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    username</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;your_tradeboard_username&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    message</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Stop loss hit on BANKNIFTY&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    priority</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>username</code>, <code>message</code> and <code>priority</code> are keyword-only. <code>username</code> is the Tradeboard login username, not the Telegram handle.</p><h3 id="error-handling" tabindex="-1">Error Handling <a class="header-anchor" href="#error-handling" aria-label="Permalink to &quot;Error Handling&quot;">​</a></h3><ul><li>All errors are logged with context</li><li>User-friendly error messages in Telegram</li><li>A Markdown parse failure is retried once as plain text</li><li>Network errors during polling trigger a reconnect with backoff, up to five attempts</li><li>Graceful degradation for missing data</li></ul><h3 id="performance-considerations" tabindex="-1">Performance Considerations <a class="header-anchor" href="#performance-considerations" aria-label="Permalink to &quot;Performance Considerations&quot;">​</a></h3><ul><li>Alerts are dispatched on a five-worker thread pool, off the request path</li><li>User, credential and preference lookups are served from 30-minute TTL caches</li><li>Database queries are optimized with indexes</li><li>Chart rendering is the one blocking step: it pauses the bot&#39;s event loop for the duration of the Chromium render, which is acceptable for personal, low-volume use</li></ul><h3 id="future-enhancements" tabindex="-1">Future Enhancements <a class="header-anchor" href="#future-enhancements" aria-label="Permalink to &quot;Future Enhancements&quot;">​</a></h3><ul><li>[ ] Real-time price alerts</li><li>[ ] Portfolio analytics</li><li>[ ] Custom indicators on charts</li><li>[ ] Webhook dispatch (the route exists and is authenticated, but updates are not yet processed)</li><li>[ ] Inline queries for quick quotes</li></ul><h3 id="support" tabindex="-1">Support <a class="header-anchor" href="#support" aria-label="Permalink to &quot;Support&quot;">​</a></h3><p>For issues or questions:</p><ol><li>Check the logs in the Tradeboard dashboard</li><li>Review this documentation</li><li>Contact Tradeboard support</li></ol>`,121)])])}const g=a(r,[["render",n]]);export{u as __pageData,g as default};
