import{_ as o,o as t,c as a,a2 as r}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Limitations and Gotchas","description":"","frontmatter":{},"headers":[],"relativePath":"flow-editor/limitations.md","filePath":"flow-editor/limitations.md"}'),n={name:"flow-editor/limitations.md"};function d(i,e,s,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="limitations-and-gotchas" tabindex="-1">Limitations and Gotchas <a class="header-anchor" href="#limitations-and-gotchas" aria-label="Permalink to &quot;Limitations and Gotchas&quot;">​</a></h1><p>Read this before running a Flow strategy with real money. Most items have a workaround; a few are hard boundaries.</p><h2 id="read-this-first-closepositions-ignores-its-filter-in-live-trading" tabindex="-1">Read this first: closePositions ignores its filter in live trading <a class="header-anchor" href="#read-this-first-closepositions-ignores-its-filter-in-live-trading" aria-label="Permalink to &quot;Read this first: closePositions ignores its filter in live trading&quot;">​</a></h2><p><code>closePositions</code> with a symbol set does <strong>not</strong> close only that symbol on a live broker account. It squares off the whole book: every symbol, every exchange, every product, including overnight NRML positions and CNC holdings.</p><p>The node reads <code>symbol</code>, <code>exchange</code> and <code>product</code> and passes them to the close-position service. The service uses them only in Analyzer mode. In live mode it calls the broker&#39;s own square-off-everything endpoint, which accepts no filter at all (<code>services/close_position_service.py</code>). The panel, the node&#39;s canvas badge and the run log all report a scoped close, because they describe what was asked for, not what the broker did.</p><p>That gives the bug the worst possible shape: <strong>it passes sandbox testing and misbehaves only with real money.</strong> In Analyzer mode the sandbox honours the filter exactly as advertised, so a workflow verified end to end in Analyzer looks correct and then flattens the account on its first live run.</p><p>Until this is fixed, treat <code>closePositions</code> as account-wide, always.</p><ul><li>Use it only where the intent genuinely is &quot;flatten everything&quot;, and only on an account holding nothing you did not put there.</li><li>To exit one position, place the opposite side yourself. <code>smartOrder</code> with <code>positionSize: 0</code> targets a flat position in that symbol, or read the quantity with <code>openPosition</code> and send a matching <code>placeOrder</code>.</li><li>Never wire <code>closePositions</code> behind a per-symbol stop-loss or target condition. That is exactly the case where the filter looks essential and does nothing.</li></ul><hr><h2 id="hard-limitations" tabindex="-1">Hard limitations <a class="header-anchor" href="#hard-limitations" aria-label="Permalink to &quot;Hard limitations&quot;">​</a></h2><h3 id="no-state-between-runs" tabindex="-1">No state between runs <a class="header-anchor" href="#no-state-between-runs" aria-label="Permalink to &quot;No state between runs&quot;">​</a></h3><p><code>variable</code> values live for one run only. A counter incremented on Monday is back to zero on Tuesday, and back to zero on the very next run, minutes later.</p><p><strong>Do not</strong> use a variable to answer &quot;have I already entered today?&quot;.</p><p><strong>Instead</strong>, ask the broker, which is the real source of truth:</p><ul><li><code>positionCheck</code> with <code>condition: &quot;not_exists&quot;</code>: no open position</li><li><code>orderBook</code> and inspect <code>&amp;#123;&amp;#123;orders.data.orders[0]...&amp;#125;&amp;#125;</code></li><li><code>openPosition</code> for a specific symbol&#39;s quantity</li></ul><p>This is why <a href="./tutorials#5-previous-day-breakout-with-a-gap-filter">Tutorial 5</a> derives &quot;price retested the level today&quot; from the session low in the quote rather than from a stored flag.</p><h3 id="no-loops-or-iteration" tabindex="-1">No loops or iteration <a class="header-anchor" href="#no-loops-or-iteration" aria-label="Permalink to &quot;No loops or iteration&quot;">​</a></h3><p>You cannot iterate over a list of symbols. One workflow handles one symbol.</p><p><strong>Workaround.</strong> Export the workflow, edit the symbol, import it under a new name. Or drive it from outside: have an external scanner POST to a <code>webhookTrigger</code> with the symbol in the payload, and reference <code>&amp;#123;&amp;#123;webhook.symbol&amp;#125;&amp;#125;</code> in the order node.</p><h3 id="no-backtesting" tabindex="-1">No backtesting <a class="header-anchor" href="#no-backtesting" aria-label="Permalink to &quot;No backtesting&quot;">​</a></h3><p>Flow executes forward in real time only. There is no historical replay.</p><p><strong>Workaround.</strong> Prototype the logic in the Python Strategy Host (<code>/python</code>) or with the <code>tradeboard</code> SDK, which gives you pandas DataFrames and full backtesting libraries. Port the validated logic into Flow for execution.</p><h3 id="no-pandas-objects" tabindex="-1">No pandas objects <a class="header-anchor" href="#no-pandas-objects" aria-label="Permalink to &quot;No pandas objects&quot;">​</a></h3><p>Flow variables are JSON. <code>history</code> returns an array of record objects and <code>indicator</code> returns single values plus a fixed-length array, never a <code>pandas.Series</code>.</p><p>You can read individual values (<code>&amp;#123;&amp;#123;h.data[0].close&amp;#125;&amp;#125;</code>) and feed an array into a nested indicator (<code>sourceSeries</code>), but you cannot do vectorised series maths inside Flow.</p><p><strong>Workaround.</strong> The Python Strategy Host gives you real pandas: <code>client.history(...)</code> returns a DataFrame and <code>df[&#39;close&#39;]</code> is a Series.</p><h3 id="triggers-cannot-read-upstream-data" tabindex="-1">Triggers cannot read upstream data <a class="header-anchor" href="#triggers-cannot-read-upstream-data" aria-label="Permalink to &quot;Triggers cannot read upstream data&quot;">​</a></h3><p>A trigger is the entry point of the graph, so nothing precedes it. Its configuration fields cannot contain <code>&amp;#123;&amp;#123;variables&amp;#125;&amp;#125;</code>.</p><p>The practical case: you cannot make an <code>orderUpdateTrigger</code> watch <code>&amp;#123;&amp;#123;ce.orderid&amp;#125;&amp;#125;</code> from a previous workflow. The validator refuses a <code>&amp;#123;&amp;#123;...&amp;#125;&amp;#125;</code> reference in <code>orderId</code> outright, with a message saying so. Filter by symbol instead.</p><h3 id="two-series-indicators-are-unavailable" tabindex="-1">Two-series indicators are unavailable <a class="header-anchor" href="#two-series-indicators-are-unavailable" aria-label="Permalink to &quot;Two-series indicators are unavailable&quot;">​</a></h3><p><code>crossover</code>, <code>crossunder</code>, <code>cross</code>, <code>correlation</code>, <code>beta</code>, <code>exrem</code>, <code>flip</code>, and <code>valuewhen</code> need two independent series. The <code>indicator</code> node reads one symbol.</p><p><strong>Workaround.</strong> Build crossovers from two indicator nodes plus an <code>andGate</code>; see <a href="./tutorials#3-crossovers">Tutorial 3</a>. For correlation between two symbols, use the Python Strategy Host.</p><h3 id="only-single-series-indicators-can-be-nested" tabindex="-1">Only single-series indicators can be nested <a class="header-anchor" href="#only-single-series-indicators-can-be-nested" aria-label="Permalink to &quot;Only single-series indicators can be nested&quot;">​</a></h3><p><code>sourceSeries</code> collapses an upstream indicator to one numeric series, so anything requiring independent high/low/close (<code>atr</code>, <code>supertrend</code>, <code>adx</code>, <code>stochastic</code>, …) cannot be nested on top of it. You get a clear error, not a wrong number.</p><p><code>sma</code>, <code>ema</code>, <code>rsi</code>, <code>wma</code>, <code>stdev</code>, <code>highest</code>, <code>lowest</code>, and friends nest fine.</p><hr><h2 id="gotchas-that-cost-money" tabindex="-1">Gotchas that cost money <a class="header-anchor" href="#gotchas-that-cost-money" aria-label="Permalink to &quot;Gotchas that cost money&quot;">​</a></h2><h3 id="a-data-key-nothing-reads-is-silently-ignored" tabindex="-1">A <code>data</code> key nothing reads is silently ignored <a class="header-anchor" href="#a-data-key-nothing-reads-is-silently-ignored" aria-label="Permalink to &quot;A `data` key nothing reads is silently ignored&quot;">​</a></h3><p>Flow does not reject unknown keys in a node&#39;s <code>data</code> object. It ignores them. Write <code>strikeOffset</code> where the field is <code>offset</code>, or <code>sourceField</code> where the field is <code>source</code>, and the workflow imports cleanly, runs successfully, and uses the node&#39;s default. Nothing in the run log says a field was dropped, so the only symptom is an order that is subtly not what you configured.</p><p>The editor cannot warn you either, because it only ever writes the keys its own panel knows about. This bites hand-written and AI-generated JSON, which is most imported workflows.</p><p><strong>Check the file before importing it.</strong> In the Tradeboard code repo:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uv run python .claude/skills/flow-builder/validate.py my_workflow.json</span></span></code></pre></div><p>It runs the importer&#39;s own validator first, so it can never disagree with what the server accepts, then reports every <code>data</code> key no executor reads, along with the list of keys that node does read. It also flags a <code>&amp;#123;&amp;#123;reference&amp;#125;&amp;#125;</code> whose root nothing in the graph produces.</p><h3 id="which-bar-123-123-ind-latest-125-125-holds-is-not-fixed" tabindex="-1">Which bar <code>&amp;#123;&amp;#123;ind.latest.*&amp;#125;&amp;#125;</code> holds is not fixed <a class="header-anchor" href="#which-bar-123-123-ind-latest-125-125-holds-is-not-fixed" aria-label="Permalink to &quot;Which bar `&amp;#123;&amp;#123;ind.latest.*&amp;#125;&amp;#125;` holds is not fixed&quot;">​</a></h3><p><code>indicator</code> computes over exactly what the history fetch returned, and does not drop a still-forming bar. So:</p><ul><li>If the feed has already opened the current bar, <code>&amp;#123;&amp;#123;ind.latest.*&amp;#125;&amp;#125;</code> is that forming bar (it repaints) and <code>&amp;#123;&amp;#123;ind.previous.*&amp;#125;&amp;#125;</code> is the last closed one.</li><li>If the feed has not opened it yet, <code>&amp;#123;&amp;#123;ind.latest.*&amp;#125;&amp;#125;</code> <strong>is</strong> the last closed bar and <code>&amp;#123;&amp;#123;ind.previous.*&amp;#125;&amp;#125;</code> is the bar before that.</li></ul><p>The indicator output carries no timestamp, so nothing in the run tells you which of the two you got. A candle-close strategy written against <code>previous</code> therefore reads a bar too far back whenever the feed is a little behind, and one written against <code>latest</code> reads a repainting value whenever the feed is ahead. Both fail intermittently and neither leaves a trace.</p><p><code>offsetBars</code> on the <code>indicator</code> node inherits the same ambiguity: it counts back from <code>latest</code>, so <code>offsetBars: 0</code> and <code>latest</code> are the same bar.</p><p><strong>What to do.</strong></p><ul><li>Prefer a clock-aligned interval schedule. Flow fires interval jobs <code>FLOW_INTERVAL_ALIGN_OFFSET</code> seconds past the boundary (default 2), and at <code>:02</code> past the minute the feed&#39;s last bar is measurably the just-closed one, which puts the closed candle at <code>latest</code>.</li><li>When you need certainty rather than a good default, read the bar with <code>barOffset</code> instead. It explicitly discards a forming bar, so its <code>offsetBars: 0</code> is always the last closed bar, and it returns a <code>timestamp</code> you can log.</li></ul><h3 id="the-history-cache-default-is-half-a-1-minute-candle" tabindex="-1">The history cache default is half a 1-minute candle <a class="header-anchor" href="#the-history-cache-default-is-half-a-1-minute-candle" aria-label="Permalink to &quot;The history cache default is half a 1-minute candle&quot;">​</a></h3><p>All history-reading nodes share one short-TTL cache. <code>FLOW_HISTORY_CACHE_TTL</code> defaults to <strong>30 seconds</strong>, which suits 5-minute bars and above.</p><p>On a 1-minute strategy it is half a candle: a run at 09:31:02 can be served the fetch made at 09:30:34, so the &quot;latest&quot; bar is one candle stale and the workflow acts on the wrong candle without any error. Set it to 2 or 3 in <code>.env</code> for 1-minute work:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FLOW_HISTORY_CACHE_TTL=3</span></span></code></pre></div><h3 id="supertrend-s-direction-output-is-inverted" tabindex="-1">Supertrend&#39;s direction output is inverted <a class="header-anchor" href="#supertrend-s-direction-output-is-inverted" aria-label="Permalink to &quot;Supertrend&#39;s direction output is inverted&quot;">​</a></h3><p><code>supertrend</code> returns a tuple, so its outputs are <code>out0</code> (the line) and <code>out1</code> (the direction), not <code>value</code>. The direction encoding is the reverse of the usual convention:</p><table tabindex="0"><thead><tr><th><code>&amp;#123;&amp;#123;st.latest.out1&amp;#125;&amp;#125;</code></th><th>Means</th></tr></thead><tbody><tr><td><code>-1</code></td><td><strong>uptrend</strong>, price above the line</td></tr><tr><td><code>+1</code></td><td><strong>downtrend</strong>, price below the line</td></tr></tbody></table><p>A strategy written on the usual assumption (<code>+1</code> is bullish) trades backwards: it buys every downtrend and sells every uptrend, and looks like a working strategy with an unlucky edge. Verified against 497 real 1-minute bars and documented the same way in the <code>tradeboard</code> SDK reference.</p><p>Check any tuple indicator&#39;s encoding with <strong>Run Now</strong> in Analyzer mode and read the values before trading it.</p><h3 id="symbol-is-upper-cased-on-order-nodes-not-on-data-nodes" tabindex="-1">Symbol is upper-cased on order nodes, not on data nodes <a class="header-anchor" href="#symbol-is-upper-cased-on-order-nodes-not-on-data-nodes" aria-label="Permalink to &quot;Symbol is upper-cased on order nodes, not on data nodes&quot;">​</a></h3><p>Order nodes (<code>placeOrder</code>, <code>smartOrder</code>, <code>splitOrder</code>, <code>basketOrder</code>, the options nodes) upper-case <code>symbol</code> before the lookup, because an incoming alert does not control its own casing and every Tradeboard symbol is upper case.</p><p><strong>Data nodes do not.</strong> <code>getQuote</code>, <code>getDepth</code>, <code>history</code>, <code>barOffset</code>, <code>priorPeriodOhlc</code>, <code>indicator</code> and <code>openPosition</code> pass the symbol through as written. So a TradingView alert carrying <code>reliance</code> places the order fine and fails the quote or history fetch in the same workflow, with a &quot;symbol not found&quot; style error from the broker that reads like a bad instrument rather than a casing problem.</p><p><strong>Workaround.</strong> Upper-case the symbol at the source (in the alert message), or hard-code the symbol on the data nodes and reference <code>&amp;#123;&amp;#123;webhook.symbol&amp;#125;&amp;#125;</code> only on the order nodes.</p><h3 id="quantity-units-differ-by-node" tabindex="-1">Quantity units differ by node <a class="header-anchor" href="#quantity-units-differ-by-node" aria-label="Permalink to &quot;Quantity units differ by node&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Node</th><th>Unit</th></tr></thead><tbody><tr><td><code>optionsOrder</code>, <code>optionsMultiOrder</code></td><td><strong>lots</strong></td></tr><tr><td><code>placeOrder</code>, <code>smartOrder</code>, <code>splitOrder</code>, <code>basketOrder</code></td><td><strong>shares</strong></td></tr></tbody></table><p><code>quantity: 1</code> on an options node is one lot. The same value on <code>placeOrder</code> is one share. This is the most common sizing error. The lot size itself is resolved from the contract master at run time, so do not hard-code a number you remember: check the current <code>lotsize</code> for the contract you are trading.</p><h3 id="gates-read-values-not-branches" tabindex="-1">Gates read values, not branches <a class="header-anchor" href="#gates-read-values-not-branches" aria-label="Permalink to &quot;Gates read values, not branches&quot;">​</a></h3><p>A wire into a logic gate carries the source condition&#39;s boolean, not control flow. The gate reads each input&#39;s stored result and ignores which handle the wire left from, so a gate fed through <code>sourceHandle: &quot;true&quot;</code> edges still receives a <code>False</code> input and can still drive its <code>false</code> branch. Pass-through wiring (<code>targetHandle</code> only) does the same thing and reads more clearly.</p><p>Two rules do still bite:</p><ul><li><strong>A gate fires once per run</strong>, after every wired input has been evaluated. Until then the log says <code>waiting for N more input(s) before evaluating</code>.</li><li><strong><code>inputCount</code> must match the wiring.</strong> A gate configured for three inputs with only two wired errors out rather than evaluating on part of the condition. Deleting an edge without lowering the count is the usual cause.</li></ul><p>An <strong>errored</strong> condition is the exception: it stores no boolean at all, so every gate downstream stays pending and nothing below it runs. That is deliberate. Treating it as <code>False</code> used to drive the FALSE branch into a real order.</p><p>See <a href="./concepts#wiring-gates-correctly">Concepts</a> for the wiring shapes.</p><h3 id="unresolved-variables-behave-differently-by-node" tabindex="-1">Unresolved variables behave differently by node <a class="header-anchor" href="#unresolved-variables-behave-differently-by-node" aria-label="Permalink to &quot;Unresolved variables behave differently by node&quot;">​</a></h3><p>If <code>outputVariable</code> is not set on a producer, or a path is misspelled, interpolation passes the literal <code>&amp;#123;&amp;#123;name.field&amp;#125;&amp;#125;</code> text through. What happens next depends on where it landed:</p><ul><li><strong>Order nodes refuse to run.</strong> An unresolved reference in any order-defining field (<code>symbol</code>, <code>exchange</code>, <code>action</code>, <code>quantity</code>, <code>product</code>, <code>priceType</code>, <code>price</code>, <code>triggerPrice</code>, <code>orderId</code>, <code>legs</code>, …) fails the node before the broker call. It does not fall back to a default.</li><li><strong>Conditions take neither branch.</strong> <code>varCondition</code> and <code>priceCondition</code> refuse a non-numeric operand and error, so a typo cannot route a trade. <code>positionCheck</code> and <code>fundCheck</code> do the same when the broker read fails.</li><li><strong>Everything else passes the text through</strong> and keeps running. A <code>log</code>, <code>telegramAlert</code> or <code>httpRequest</code> node will happily send the literal <code>&amp;#123;&amp;#123;name.field&amp;#125;&amp;#125;</code>.</li></ul><p>Read the execution log after the first run and look for <code>&amp;#123;&amp;#123;</code> in the output.</p><h3 id="index-symbols-need-index-exchanges" tabindex="-1">Index symbols need index exchanges <a class="header-anchor" href="#index-symbols-need-index-exchanges" aria-label="Permalink to &quot;Index symbols need index exchanges&quot;">​</a></h3><p><code>NIFTY</code> on <code>NSE</code> returns nothing. Use <code>NSE_INDEX</code> (and <code>BSE_INDEX</code> for SENSEX/BANKEX). Their options trade on <code>NFO</code>/<code>BFO</code>.</p><h3 id="offsets-count-bars-not-days" tabindex="-1">Offsets count bars, not days <a class="header-anchor" href="#offsets-count-bars-not-days" aria-label="Permalink to &quot;Offsets count bars, not days&quot;">​</a></h3><p>On <code>barOffset</code>, <code>offsetBars: 5</code> on a daily chart is five <em>trading</em> bars, seven calendar days if a weekend intervenes, more across a holiday. <code>offsetBars: 0</code> is the last <em>closed</em> bar; a still-forming candle is excluded.</p><p><code>offsetBars</code> on the <code>indicator</code> node counts the same way but does <strong>not</strong> exclude a forming bar. See &quot;Which bar <code>&amp;#123;&amp;#123;ind.latest.*&amp;#125;&amp;#125;</code> holds is not fixed&quot; above.</p><h3 id="not-every-broker-supports-every-interval" tabindex="-1">Not every broker supports every interval <a class="header-anchor" href="#not-every-broker-supports-every-interval" aria-label="Permalink to &quot;Not every broker supports every interval&quot;">​</a></h3><p><code>interval</code> is free text on <code>indicator</code> and <code>barOffset</code> because support varies. Use the <code>intervals</code> node to discover what your broker offers. If an interval is unsupported, both nodes report the broker&#39;s own message and point at the Historify alternative. <code>priorPeriodOhlc</code> has no <code>interval</code> field at all: it picks <code>1h</code> or <code>D</code> from the <code>period</code> you chose. The <code>history</code> node is different again: it has a fixed five-value dropdown (<code>1m</code>, <code>5m</code>, <code>15m</code>, <code>1h</code>, <code>1d</code>) and still surfaces the raw broker payload.</p><p>For unsupported timeframes, download 1-minute data into Historify and set <code>source: &quot;db&quot;</code> on an <code>indicator</code>, <code>barOffset</code>, or <code>priorPeriodOhlc</code> node. It resamples any minute/hour interval from 1m, and W/M/Q/Y from D, independent of broker capability. The <code>history</code> node has no <code>source</code> field and always calls the broker.</p><p><code>source</code> takes only <code>api</code> or <code>db</code>. It is not the price field; that is <code>sourceField</code>, and it exists only on <code>indicator</code> in nested (<code>sourceSeries</code>) mode. Putting <code>close</code> in <code>source</code> returns &quot;Source must be either &#39;api&#39; or &#39;db&#39;&quot; wrapped in a history-fetch error, which reads like a data problem.</p><h3 id="history-is-capped-at-200-bars" tabindex="-1">History is capped at 200 bars <a class="header-anchor" href="#history-is-capped-at-200-bars" aria-label="Permalink to &quot;History is capped at 200 bars&quot;">​</a></h3><p>A 10-year 1-minute request is refused by design (~900,000 rows). The cap applies to the request window, not just the response, so the download never happens. Raise <code>FLOW_MAX_HISTORY_BARS</code> if you genuinely need more, and note that a second ceiling, <code>FLOW_MAX_HISTORY_CALENDAR_DAYS</code> (4000, about 11 years), independently truncates W/M/Q/Y windows. Neither key ships in <code>.sample.env</code>; add them to <code>.env</code> yourself.</p><h3 id="broker-rate-limits-are-tight" tabindex="-1">Broker rate limits are tight <a class="header-anchor" href="#broker-rate-limits-are-tight" aria-label="Permalink to &quot;Broker rate limits are tight&quot;">​</a></h3><p>Dhan allows 5 requests/second on history and 1 request/second on quotes (error 805 on breach); Flattrade allows 10/second; some Zerodha paths allow 1/second. Tradeboard also serializes broker history calls behind a process-wide 350 ms gate, about 3 per second.</p><p>Identical history requests within the cache TTL collapse to one broker call, but <em>distinct</em> requests (different symbol, or the same symbol at a different interval) each cost a call and each waits on that gate. A 1-minute schedule with many indicators across many intervals will feel slow and can trip broker limits.</p><p>Prefer one indicator node per distinct symbol/interval and reuse its <code>outputVariable</code>.</p><h3 id="one-run-at-a-time" tabindex="-1">One run at a time <a class="header-anchor" href="#one-run-at-a-time" aria-label="Permalink to &quot;One run at a time&quot;">​</a></h3><p>A second trigger arriving while a workflow is still running returns <code>already_running</code> and is dropped, it is not queued. Keep the schedule interval comfortably longer than the run takes.</p><h3 id="analyzer-mode-is-global" tabindex="-1">Analyzer mode is global <a class="header-anchor" href="#analyzer-mode-is-global" aria-label="Permalink to &quot;Analyzer mode is global&quot;">​</a></h3><p>There is no per-workflow paper-trading switch. Flow inherits the global Analyzer toggle. Verify which mode you are in before activating a workflow. The badge is in the header, and order results carry <code>mode: &quot;analyze&quot;</code> or <code>mode: &quot;live&quot;</code>.</p><p>Analyzer is not a perfect rehearsal of live. <code>closePositions</code> is the known divergence, and the dangerous one: see the top of this page.</p><h3 id="delay-is-capped-at-300-seconds-waituntil-at-30-minutes" tabindex="-1"><code>delay</code> is capped at 300 seconds, <code>waitUntil</code> at 30 minutes <a class="header-anchor" href="#delay-is-capped-at-300-seconds-waituntil-at-30-minutes" aria-label="Permalink to &quot;`delay` is capped at 300 seconds, `waitUntil` at 30 minutes&quot;">​</a></h3><p>Both sleep inside the per-workflow lock and inside the HTTP request that fired the run, so both are bounded. They fail differently:</p><ul><li><strong><code>delay</code> is silently clamped.</strong> Configured for longer than 300 seconds it logs a warning and waits 300 seconds instead. It does not fail. A workflow written as &quot;wait 30 minutes, then exit&quot; therefore exits after five minutes, which is the kind of mistake that only shows up in a live position.</li><li><strong><code>waitUntil</code> errors.</strong> A target time more than 1800 seconds away fails the node with a message naming the limit and pointing at a schedule trigger. It does not wait a partial 30 minutes and it does not proceed.</li></ul><p>The reason for the caps: an unbounded wait pinned a worker for hours and answered <code>already_running</code> to every trigger in between. For a long wait, use a second workflow on its own schedule rather than a wait inside one run.</p><h3 id="graph-and-traversal-limits" tabindex="-1">Graph and traversal limits <a class="header-anchor" href="#graph-and-traversal-limits" aria-label="Permalink to &quot;Graph and traversal limits&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Limit</th><th>Value</th><th>What happens on breach</th></tr></thead><tbody><tr><td>Nodes per workflow</td><td>500</td><td>Rejected at save/import</td></tr><tr><td>Edges per workflow</td><td>1000</td><td>Rejected at save/import</td></tr><tr><td>Node depth per run</td><td>100</td><td>Run aborts with an error</td></tr><tr><td>Node visits per run</td><td>500</td><td>Run aborts with an error</td></tr><tr><td><code>httpRequest</code> timeout</td><td>1000 to 60000 ms</td><td>Rejected at save/import</td></tr></tbody></table><p>The <code>httpRequest</code> timeout field is in <strong>milliseconds</strong>, not seconds. Entering <code>30</code> means 30 milliseconds, and is now refused at save with a message saying so rather than quietly becoming a request that can only fail. The executor also clamps at 60000 ms as a backstop for anything stored before that check existed.</p><h3 id="indicator-names-are-not-validated-until-the-run" tabindex="-1">Indicator names are not validated until the run <a class="header-anchor" href="#indicator-names-are-not-validated-until-the-run" aria-label="Permalink to &quot;Indicator names are not validated until the run&quot;">​</a></h3><p>The validator only checks that <code>indicatorName</code> is present. It never compares it against the supported list, so a typo, or one of the 11 excluded names (8 two-series indicators plus <code>median_bands</code>, <code>ulcerindex</code> and <code>vi</code>, which the installed <code>tradeboard</code> build cannot compute), saves and imports cleanly and fails mid-run. Test every new indicator node with <strong>Run Now</strong> in Analyzer mode before activating.</p><hr><h2 id="strategy-p-l-what-the-book-does-and-does-not-see" tabindex="-1">Strategy P&amp;L: what the book does and does not see <a class="header-anchor" href="#strategy-p-l-what-the-book-does-and-does-not-see" aria-label="Permalink to &quot;Strategy P&amp;L: what the book does and does not see&quot;">​</a></h2><p><code>strategyPnl</code> reads a per-strategy book that Tradeboard maintains from its own order events. That gives it a few boundaries worth knowing before you wire an exit trigger to it.</p><ul><li><strong>Only tagged orders count.</strong> The book is fed by orders placed through Tradeboard carrying a <code>strategy</code> tag, Flow nodes and <code>/api/v1/</code> calls. A position opened by hand in the broker terminal is invisible to it.</li><li><strong>Unrealized needs a live price.</strong> Open legs are marked against the position book. A leg with no matching price is excluded and counted in <code>unpriced_legs</code>; treat a non-zero value as &quot;this total is understated&quot;.</li><li><strong>A broken position book is an error, not a zero.</strong> If the position book cannot be read the node returns an error rather than reporting a calm <code>total: 0</code> that an exit trigger would act on.</li><li><strong><code>realized</code> accumulates across sessions</strong>; <code>today_realized</code> resets at the 03:00 IST session rollover, matching the broker token cycle.</li><li><strong>Do not pair it with <code>closePositions</code> for a scoped exit.</strong> On a live account that node closes the entire book regardless of the symbol you set, so a per-strategy P&amp;L stop wired to it flattens every other strategy too. See the top of this page.</li></ul><h2 id="pre-flight-checklist" tabindex="-1">Pre-flight checklist <a class="header-anchor" href="#pre-flight-checklist" aria-label="Permalink to &quot;Pre-flight checklist&quot;">​</a></h2><p>Before activating a workflow against live money:</p><ul><li>[ ] Ran the JSON through <code>validate.py</code> if it was written or edited by hand</li><li>[ ] Ran it at least once in <strong>Analyzer mode</strong> and read every log line</li><li>[ ] No <code>&amp;#123;&amp;#123;...&amp;#125;&amp;#125;</code> placeholders appear unresolved in the log</li><li>[ ] Order quantity units are right (lots vs shares)</li><li>[ ] Every gate evaluated exactly once, with the expected inputs, and <code>inputCount</code> matches the wires</li><li>[ ] Any tuple indicator&#39;s output encoding was read, not assumed (<code>supertrend</code> direction is inverted)</li><li>[ ] <code>FLOW_HISTORY_CACHE_TTL</code> is below one candle for the interval traded</li><li>[ ] An entry guard exists (<code>positionCheck</code> / <code>fundCheck</code>) so a repeating schedule cannot stack positions</li><li>[ ] The schedule interval is longer than the observed run duration</li><li>[ ] Index symbols use <code>NSE_INDEX</code> / <code>BSE_INDEX</code>, and every data node&#39;s symbol is upper case</li><li>[ ] No <code>closePositions</code> node sits behind a per-symbol condition</li><li>[ ] Square-off or exit logic exists, or you are managing exits manually</li></ul>',112)])])}const m=o(n,[["render",d]]);export{u as __pageData,m as default};
