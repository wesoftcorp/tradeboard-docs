import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Agent","description":"","frontmatter":{},"headers":[],"relativePath":"new-features/agent/README.md","filePath":"new-features/agent/README.md"}'),r={name:"new-features/agent/README.md"};function s(i,e,h,d,l,c){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="agent" tabindex="-1">Agent <a class="header-anchor" href="#agent" aria-label="Permalink to &quot;Agent&quot;">​</a></h1><p>The agent is a large language model wired directly into Tradeboard&#39;s own service layer. It answers questions about instruments you can actually trade, draws charts from your broker&#39;s data, computes indicators over real candles, reads and marks up the chart you have open, and, with your explicit approval on every single call, places orders through the same path the rest of the platform uses.</p><p>It runs on your server. The models it talks to are the ones you register, the keys are stored encrypted in this instance&#39;s own database, and a local model is supported for operators who want nothing to leave the machine.</p><h2 id="three-surfaces" tabindex="-1">Three Surfaces <a class="header-anchor" href="#three-surfaces" aria-label="Permalink to &quot;Three Surfaces&quot;">​</a></h2><p><code>/agent</code> is the full page. A conversation list sits on the left, the thread in the middle, and the composer at the bottom carries the model picker, the reasoning control, attachments and the web search switch. Conversations persist, so a thread can be closed and reopened later.</p><p><code>/trading</code> carries the same agent as a docked panel headed <strong>Assistant</strong>. Open it from the right rail of the charting terminal, where it is the third button under Watchlist and Option chain. The same button closes it again, and so does Escape, as long as the cursor is not in a text box and nothing else is open for Escape to dismiss first. The panel is dragged wider or narrower from its inner edge, and both that width and which panel was last open are remembered for your next visit.</p><p>The panel reads the symbol, interval and bars of the focused pane at the moment you press send, so a question about &quot;this chart&quot; is a question about what is on screen rather than about whatever was loaded when the panel opened. In a multi-chart layout it follows the focused pane. It can draw levels, trendlines and zones back onto that chart, and it can be handed a screenshot of the chart itself. Four starter chips fill the composer rather than sending: Analyse this chart, Draw demand and supply, Candlestick patterns, and Read my drawings.</p><p>The panel is deliberately narrower in what it offers. It runs whichever model you have registered as the default and shows no model picker, and it is given no order tools at all. Its own empty state says as much: it reads the symbol, interval and bars you are looking at, it can mark up the chart, and it places no orders there.</p><p><code>/agent</code> also listens. The microphone in the composer opens a spoken session with <strong>Vega</strong>, which hears the question and reads the answer out while the answer itself still arrives on screen as an ordinary message, with its tool timeline and any chart it drew. It is the same agent on the same tools; only the way you reach it changes. Voice ships off and needs an OpenAI key of its own. See <a href="./voice">Voice Agent</a>.</p><p><code>/agent/config</code> is the configuration screen. It is reachable from the Admin page as <strong>Agent Config</strong>, and from the settings control in the chat header.</p><h2 id="first-run" tabindex="-1">First Run <a class="header-anchor" href="#first-run" aria-label="Permalink to &quot;First Run&quot;">​</a></h2><p>A fresh install answers nothing until four things are true, and the chat page and the chart panel both show a setup screen rather than a composer until they are.</p><ol><li><strong>An Tradeboard API key exists.</strong> Generate one at <code>/apikey</code>. This is the platform&#39;s own key, not a model provider&#39;s: it is what the agent&#39;s tools resolve your broker session through, and without it every chat request is refused with &quot;This deployment has no Tradeboard API key.&quot;</li><li><strong>A model is registered</strong> on <code>/agent/config</code>, with its provider credential pasted in.</li><li><strong>Its credential test has passed.</strong> Press Test on the row; a mistyped key is much better found there than at your first question.</li><li><strong>It holds the default.</strong> The first passing test promotes a model when nothing else holds it, so in practice steps 3 and 4 are one press.</li></ol><p>&quot;Configured&quot; has exactly that meaning on the server too: one model that is enabled, holds the default, and last tested successfully. Until then every chat route answers <code>409</code> rather than opening a stream, so the setup screen is describing a real refusal. <a href="./configuration">Agent Configuration</a> walks through each step.</p><h2 id="order-safety" tabindex="-1">Order Safety <a class="header-anchor" href="#order-safety" aria-label="Permalink to &quot;Order Safety&quot;">​</a></h2><p>Read this section before enabling anything.</p><p><strong>Trading is off by default.</strong> On a fresh install the agent is offered no order tools at all, so it declines to trade rather than asking you to approve anything. Placing, modifying and cancelling are opt-in. The switch lives on <code>/agent/config</code> under <strong>Trading</strong>, labelled &quot;Allow the agent to place, modify and cancel orders&quot;.</p><p>An existing install that never touched the setting also picks the new value up on upgrade, so an agent that could trade stops being able to until someone switches it on. An operator who set the switch either way keeps what they set.</p><p><strong>Every order tool pauses for your approval.</strong> There are seven: place order, place smart order, modify order, cancel order, cancel all orders, close position and close all positions. All seven are declared as requiring confirmation, so the run stops before the tool body executes. The conversation then shows an amber block reading &quot;This turn is waiting for your approval.&quot;, the exact arguments the model proposed rendered as JSON, and Approve and Reject buttons. Nothing reaches a broker until you press Approve.</p><p><strong>Approval is not the last gate.</strong> A risk guard runs inside the tool body after you approve, and it can still refuse. It reads no part of the conversation, so no wording in a prompt, a symbol name or an earlier tool result can talk it round. It checks quantity, order value, price deviation from the last traded price, a duplicate-order window, allowed exchanges and products, symbol allow and block lists, funds utilisation, a bulk-destructive switch and a kill switch. <a href="./capabilities">What the Agent Can Do</a> lists the checks in the order they run, with the shipped limits.</p><p><strong>Where an approved order lands is decided by the platform, not by the agent.</strong> In live mode it goes to your broker with real money, and the Trading panel states that in a red warning. Switch the platform to <a href="./../api-analyzer">Analyzer mode</a> if you want approved orders routed to the sandbox instead.</p><p><strong>The chart panel never asks.</strong> It requests no trading capability, so the backend never builds an order tool into its schema. There is no approval prompt on that panel because there is nothing there to approve.</p><p>Every mutating call writes an audit row before it runs and another when it finishes, so a call that hung or a worker that died mid-order is visible as an attempt with no result.</p><h2 id="what-it-can-do" tabindex="-1">What It Can Do <a class="header-anchor" href="#what-it-can-do" aria-label="Permalink to &quot;What It Can Do&quot;">​</a></h2><p>The agent is entirely tool-driven. Tools are grouped into toolkits, and which toolkits a run is offered depends on the surface and on the switches above. In summary:</p><ul><li>symbol search, contract lookup and expiry dates;</li><li>quotes, market depth, historical candles and supported intervals;</li><li>an instrument card with the day&#39;s move, an intraday chart, the 52 week range, the order book and your own position in the name;</li><li>funds, positions, holdings, order book, trade book and order status, all read-only;</li><li>option chain, strike resolution, Greeks and the synthetic future;</li><li>payoff diagrams and combined option premium charts;</li><li>price charts drawn with the same <code>tradeboard-charts</code> library the <code>/trading</code> terminal uses;</li><li>indicators computed by the Rust-backed <code>tradeboard.ta</code> library, several in one call, or a list of instruments screened for one condition;</li><li>reading the open chart on <code>/trading</code>, analysing trend, structure, momentum and patterns, and drawing levels, trendlines and zones onto it, plus adding and removing chart indicators;</li><li>live streaming quote cards the browser subscribes to, and derived values such as an ATM straddle recomputed on every tick;</li><li>general data cards: bar, line, area and pie charts, tables and metric callouts;</li><li>web search for links, and cited web research;</li><li>generated Python written into <code>strategies/scripts/</code> for the <a href="./../python-strategy-hosting">Python Strategy Host</a>, never started automatically;</li><li>validated <a href="./../flow-visual-strategy-builder">Flow</a> workflow JSON, imported as an inactive workflow;</li><li>the seven order tools, on the chat page only, each behind the approval pause above.</li></ul><p>Prices in a drawn chart come from the tool that fetched them, not from the model. See <a href="./capabilities">What the Agent Can Do</a> for the detail, and <a href="./examples">Agent by Example</a> for prompts that produce each of these.</p><h2 id="models-and-providers" tabindex="-1">Models and Providers <a class="header-anchor" href="#models-and-providers" aria-label="Permalink to &quot;Models and Providers&quot;">​</a></h2><p>Nothing about providers or models is stored in the database as a catalogue. The list is read from the installed LiteLLM package every time the process starts, so upgrading LiteLLM brings new providers and models with it and there is no table to regenerate.</p><p>On the pinned build the provider catalogue on <code>/agent/config</code> lists <strong>93 providers</strong>, and OpenAI alone lists 112 chat models. Each card shows how many models that provider offers and whether it needs an API key, a base URL, or neither.</p><p><code>supports_function_calling</code> is load-bearing rather than decoration. The agent is entirely tool-driven, so a model that cannot call a function cannot drive it at all, and the catalogue surfaces that on every model.</p><p>Local providers are first-class. Ollama and a generic OpenAI-compatible endpoint both need a base URL and no key, which covers a model running on the same machine or on your own network.</p><p>A <strong>ChatGPT Plus or Pro plan is a second billing path, not a second key</strong>. <code>openai/gpt-5.4</code> authenticates with a key you paste and spends OpenAI API credits; <code>chatgpt/gpt-5.4</code> authenticates with an OAuth sign-in and spends your plan. The same bare model name reaches two different bills, which is why the UI badges the billing path everywhere a model is named. See <a href="./chatgpt-subscription">ChatGPT Subscription</a>.</p><h2 id="composing-a-question" tabindex="-1">Composing a Question <a class="header-anchor" href="#composing-a-question" aria-label="Permalink to &quot;Composing a Question&quot;">​</a></h2><p>Two controls sit in the composer beside the model name, because both belong to the question being asked rather than to a settings page.</p><p><strong>Reasoning effort</strong> offers Default, Low, Medium and High, described in the UI as the model&#39;s own, fastest, balanced and deepest. It only appears for models that actually reason.</p><p><strong>Web search</strong> is on by default and switched off per conversation from the composer&#39;s plus menu. Off does not mean &quot;prefer not to search&quot;: the search tools are not put in the request at all.</p><p>Providers, keys, budgets and the effort a model row defaults to are set on <a href="./configuration">Agent Configuration</a>.</p><h2 id="where-the-configuration-lives" tabindex="-1">Where the Configuration Lives <a class="header-anchor" href="#where-the-configuration-lives" aria-label="Permalink to &quot;Where the Configuration Lives&quot;">​</a></h2><p>Every agent setting is a row in this instance&#39;s own database. <strong>Nothing about the agent is read from <code>.env</code></strong>, which is why there is a configuration screen for it at all: a provider, a model, a key or a limit can be changed from the UI and takes effect on the next request with no restart.</p><p>Six tables carry it: the models you have registered, the encrypted secrets, the settings, the conversations, the messages, and an append-only audit of every mutating tool call.</p><p>API keys are encrypted with Fernet using the same cipher the platform already uses for broker tokens, and they are <strong>never shown again after entry</strong>. What the UI shows instead is a fingerprint, which is enough to tell two keys apart and not enough to recover one. See <a href="./configuration">Agent Configuration</a> for the walkthrough.</p><h2 id="working-in-a-conversation" tabindex="-1">Working in a Conversation <a class="header-anchor" href="#working-in-a-conversation" aria-label="Permalink to &quot;Working in a Conversation&quot;">​</a></h2><ul><li>Conversations persist and can be reopened from the list. Starting a new chat creates nothing until the first message is sent, so an abandoned thread leaves no empty row behind.</li><li>A question can be edited in place. The edit replaces the answer below it and everything after.</li><li>An answer can be copied, a failed turn can be retried, and a running turn can be stopped.</li><li>Files and images can be attached by clicking, dragging or pasting, on vision-capable models. On the chart panel the same menu also offers Attach chart screenshot.</li><li>The tool calls a turn made are shown as a collapsed timeline you can open to read the arguments and the result of each one.</li><li>Each turn reports its token usage and its cost. A model with no published price renders a dash rather than <code>$0.00</code>, because unknown is not zero. A turn that ran on a ChatGPT plan reports tokens and reads &quot;included in your ChatGPT plan&quot;, because plan quota was consumed rather than nothing being spent.</li></ul><p><a href="./examples">Agent by Example</a> covers all of this with what each control actually does.</p><h2 id="what-it-is-not" tabindex="-1">What It Is Not <a class="header-anchor" href="#what-it-is-not" aria-label="Permalink to &quot;What It Is Not&quot;">​</a></h2><ul><li>It is not a strategy that runs on its own. It answers a question you asked and stops. Scheduled, unattended execution belongs to the <a href="./../python-strategy-hosting">Python Strategy Host</a>, <a href="./../flow-visual-strategy-builder">Flow</a> and the <a href="./../strategy-rms-engine">Strategy RMS Engine</a>.</li><li>It is not a backtester and it is not the <a href="./../../mcp/README">MCP server</a>, which exposes Tradeboard to an external AI client rather than hosting one.</li><li>It is not a second order pipeline. Approved orders go through the platform&#39;s normal service layer, subject to the same analyzer routing and the same broker.</li></ul><h2 id="operational-boundaries" tabindex="-1">Operational Boundaries <a class="header-anchor" href="#operational-boundaries" aria-label="Permalink to &quot;Operational Boundaries&quot;">​</a></h2><ul><li>Until a model is registered, enabled and has passed a credential test, the chat page and the chart panel both render a setup screen instead of a composer. A status call that cannot be answered is read as not configured.</li><li>The <code>agno</code> and <code>litellm</code> packages are required. They are pinned dependencies, so a standard <code>uv sync</code> install has them.</li><li>Model and provider availability, context windows, pricing and vision or reasoning support are whatever the installed LiteLLM build reports. The catalogue is advisory: a model missing from it can still be added by hand, and the model test on the configuration page is what answers for a particular account.</li><li>The agent sees the same market data your broker gives the rest of the platform. Historical range, live fields and depth levels depend on the active broker and account entitlement.</li><li>A quote, an indicator value or a drawn level is analysis, not a guarantee of execution price.</li><li>The chart panel is offered no order tools regardless of the trading switch. An order request belongs on <code>/agent</code>.</li><li>Prompts, attachments and tool results are sent to whichever model provider you configured. A local provider is the only configuration in which nothing leaves the machine.</li><li>Voice is off until you enable it and add an OpenAI key, and spoken orders are off separately. A spoken approval is only as private as the room you are in.</li><li>Test with the platform in Analyzer mode before letting the agent approve orders against live capital.</li></ul><h2 id="in-this-section" tabindex="-1">In This Section <a class="header-anchor" href="#in-this-section" aria-label="Permalink to &quot;In This Section&quot;">​</a></h2><ul><li><a href="./capabilities">What the Agent Can Do</a> covers every toolkit in detail: what the agent can read, draw, compute and write, and the order path in full.</li><li><a href="./configuration">Agent Configuration</a> covers <code>/agent/config</code>: registering a model, storing a key, the trading switch, reasoning effort and web search.</li><li><a href="./voice">Voice Agent</a> covers Vega: the microphone on <code>/agent</code>, what speaks and what thinks, spoken orders and what an open session costs.</li><li><a href="./chatgpt-subscription">ChatGPT Subscription</a> covers running the agent on a ChatGPT Plus or Pro plan instead of an API key.</li><li><a href="./examples">Agent by Example</a> is a set of worked prompts with what each one actually returns.</li></ul>',51)])])}const g=t(r,[["render",s]]);export{u as __pageData,g as default};
