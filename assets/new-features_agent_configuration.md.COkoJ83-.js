import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Agent Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"new-features/agent/configuration.md","filePath":"new-features/agent/configuration.md"}'),r={name:"new-features/agent/configuration.md"};function s(i,e,d,h,l,g){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="agent-configuration" tabindex="-1">Agent Configuration <a class="header-anchor" href="#agent-configuration" aria-label="Permalink to &quot;Agent Configuration&quot;">​</a></h1><p>The agent ships with nothing configured. Before it can answer a question you have to register at least one model, give it a credential, and test that credential. All of that happens on one page, <code>/agent/config</code>, and every value it holds lives in your own Tradeboard database.</p><h2 id="nothing-belongs-in-env" tabindex="-1">Nothing Belongs in .env <a class="header-anchor" href="#nothing-belongs-in-env" aria-label="Permalink to &quot;Nothing Belongs in .env&quot;">​</a></h2><p>Provider, model, credential and policy settings for the agent are database rows, not environment variables. The settings module never reads the process environment, so there is no agent key, no model name and no trading switch to add to <code>.env</code>, and nothing to restart after a change.</p><p>API keys are stored encrypted at rest in the same database. A key is write only: it is sent once when you paste it and no endpoint ever returns it again, not even masked. What you see afterwards is a fingerprint, the last four characters plus a truncated SHA-256, which is enough to confirm the key that is stored is the one you pasted and not enough to recover any of it.</p><p>This is also why the settings page exists at all. Everything on it could be a row edited by hand; none of it should have to be.</p><h2 id="reaching-the-page" tabindex="-1">Reaching the Page <a class="header-anchor" href="#reaching-the-page" aria-label="Permalink to &quot;Reaching the Page&quot;">​</a></h2><p>Three routes lead to <code>/agent/config</code>:</p><ul><li><strong>Admin.</strong> The Admin page carries an <strong>Agent Config</strong> card, described as &quot;LLM providers and models, API keys, trading switch, and web search&quot;. This is the canonical entry point; the agent&#39;s settings sit with the other configuration surfaces rather than in the profile menu.</li><li><strong>The chat header.</strong> A settings control sits beside the model picker on <code>/agent</code>, labelled &quot;Agent settings&quot;. It is the shortest route from a conversation back to its own configuration.</li><li><strong>The setup screen.</strong> An unconfigured instance renders a <strong>Configure the agent</strong> button that lands here.</li></ul><p>The profile menu carries <strong>Agent</strong>, which opens <code>/agent</code> itself, not the settings.</p><h2 id="before-anything-is-configured" tabindex="-1">Before Anything Is Configured <a class="header-anchor" href="#before-anything-is-configured" aria-label="Permalink to &quot;Before Anything Is Configured&quot;">​</a></h2><p>Open <code>/agent</code> on a fresh install and you get a setup screen rather than a composer, headed <strong>Set up your agent</strong>:</p><blockquote><p>Choose a model provider and add its API key to start using the agent. Keys are stored encrypted in your own database and are never written to a configuration file. A local provider is supported if you would rather nothing left this machine.</p></blockquote><p>The chart agent on <code>/trading</code> shows the same gate in a narrower form. Both read one status query, so they always agree.</p><p>The gate is not decoration on top of a working chat. &quot;Configured&quot; has an exact meaning: one model that is <strong>enabled</strong>, holds the <strong>default</strong>, and whose <strong>last credential test passed</strong>. Until that is true every chat route on the server answers <code>409</code> rather than opening a stream, so the setup screen is describing a refusal that is really there. A status call that cannot be answered at all is read as unconfigured, never as an error.</p><p>Two further conditions are checked on the server rather than on this page, and each has its own refusal so you are not left guessing which one bit. The <code>agno</code> package has to be installed, which a standard <code>uv sync</code> handles. And <strong>your Tradeboard API key has to exist</strong>: generate one at <code>/apikey</code>. That is the platform&#39;s own key, nothing to do with a model provider, and it is what the agent&#39;s tools resolve your user, broker and auth token through. Without it a chat request comes back with &quot;This deployment has no Tradeboard API key. Generate one at /apikey so the agent&#39;s tools can reach the platform.&quot; A model can be registered, tested and default and the agent still answer nothing until that key is there.</p><h2 id="the-page-itself" tabindex="-1">The Page Itself <a class="header-anchor" href="#the-page-itself" aria-label="Permalink to &quot;The Page Itself&quot;">​</a></h2><p>Six sections, in this order:</p><ol><li><strong>Models</strong>, the registry of what you have already added.</li><li><strong>ChatGPT subscription</strong>, the OAuth sign-in for a Plus or Pro plan.</li><li><strong>Trading</strong>, the order switch.</li><li><strong>Voice</strong>, the microphone switch, the OpenAI speech key and Vega&#39;s settings. See <a href="./voice">Voice Agent</a>.</li><li><strong>Providers</strong>, the browsable catalogue.</li><li><strong>Web search</strong>.</li></ol><p>A header button, <strong>Add model</strong>, opens the same dialog the catalogue uses, with nothing prefilled.</p><h2 id="choosing-a-provider" tabindex="-1">Choosing a Provider <a class="header-anchor" href="#choosing-a-provider" aria-label="Permalink to &quot;Choosing a Provider&quot;">​</a></h2><p>The Providers section is read live from LiteLLM&#39;s own in-package data every time the page loads. There is no catalogue table in the database and no generated list in the frontend, so upgrading the LiteLLM package is the whole maintenance story: new providers and new models appear on their own.</p><p>At the version Tradeboard currently pins that is <strong>93 chat providers</strong>. Twenty four cards are drawn at a time, with a <strong>Show more providers</strong> button and a search box that filters the whole list; the count line under the grid always names the total. The providers a trader actually reaches for lead the grid, and everything else follows in the server&#39;s own order.</p><p>Each card tells you four things before you commit to it:</p><ul><li><strong>How many models it lists.</strong> OpenAI alone lists 112 chat models. Fireworks AI lists 295. LM Studio lists none, because a local server publishes no catalogue.</li><li><strong>Whether it needs a key.</strong> A card marked <strong>no key needed</strong> either authenticates nothing (a self-hosted server) or authenticates with ambient cloud credentials rather than with a string you can paste.</li><li><strong>Whether it needs a URL.</strong> Ollama, LM Studio, vLLM, Azure and the OpenAI-compatible kinds all address an endpoint you have to name.</li><li><strong>What you already have.</strong> A <strong>key stored</strong> badge and an <strong>N added</strong> count, both derived from the registry rather than from a second source of truth.</li></ul><p>Open a provider and you get its models, one per row, with the context window, the price per million tokens in and out, and a capability badge. That badge matters more than it looks: this agent is entirely tool driven, so a model marked <strong>cannot call tools, so it cannot drive this agent</strong> is greyed out with the reason rather than quietly listed, and a model nobody has priced reads as &quot;price unknown&quot; rather than claiming to be free. A checkbox, <strong>Only models that can call tools</strong>, filters the list down.</p><p>If the catalogue does not list the model you want, <strong>Model not listed</strong> and <strong>Add a model by hand</strong> open the same dialog with the name blank. The catalogue is advisory; the registry is your intent.</p><p>Local providers are first class. Ollama and an OpenAI-compatible endpoint are both supported, and a private or loopback base URL is accepted deliberately, which is what makes a model that never leaves the machine possible.</p><h2 id="adding-a-model" tabindex="-1">Adding a Model <a class="header-anchor" href="#adding-a-model" aria-label="Permalink to &quot;Adding a Model&quot;">​</a></h2><p>The Add a model dialog asks for at most six things.</p><ul><li><strong>Provider kind.</strong> A closed list of five: OpenAI, Anthropic, &quot;Ollama, on your own machine&quot;, &quot;OpenAI-compatible endpoint&quot;, and &quot;Any other provider, through LiteLLM&quot;. The catalogue prefills this.</li><li><strong>Model name.</strong> Exactly as the provider publishes it. For the LiteLLM kind, include the provider prefix, for example <code>groq/llama-3.3-70b-versatile</code>; for the others the bare name is right.</li><li><strong>Display name.</strong> What the chat picker shows. Suggested from the model name, editable, and optional: left blank, the model name is used.</li><li><strong>Base URL.</strong> Shown only for Ollama and OpenAI-compatible. It must start with <code>http://</code> or <code>https://</code>, must not carry a username or password, and cannot be a cloud metadata address.</li><li><strong>API key.</strong> Shown only for the kinds that authenticate with one. Pasted once, stored encrypted, never shown again.</li><li><strong>Key applies to.</strong> Either <strong>Every model of this provider</strong> or <strong>This model only</strong>. Provider scope is the default and is what lets one pasted OpenAI key serve every GPT model you register: add a second model of the same provider and the key field can be left blank, and the dialog says so. Model scope is the override for a second account with the same provider, and it wins over the shared key.</li></ul><p>A model added this way is registered but not yet usable. It is never made the default at creation.</p><h2 id="testing-a-model" tabindex="-1">Testing a Model <a class="header-anchor" href="#testing-a-model" aria-label="Permalink to &quot;Testing a Model&quot;">​</a></h2><p>The dialog offers <strong>Test now</strong> the moment a model is registered, and the registry table carries a <strong>Test</strong> button on every row. Take the offer: a mistyped key should be found on the screen where it was pasted, not at your first question.</p><p>The test is the cheapest real call the provider allows: one streamed completion capped at a single token, with retries off. It streams because streaming is the only way the agent ever runs a model, so a test that passed on a different code path would be worth nothing.</p><ul><li><strong>A pass</strong> records the time it took and clears any stored error, and the row&#39;s Last test column reads <strong>Passed</strong> with a relative timestamp. If no model currently holds the default and this one is enabled, the first passing test promotes it, because a passing test that leaves the platform unconfigured is a dead end with no obvious next click.</li><li><strong>A failure</strong> stores the provider&#39;s own message verbatim, minus the key itself in case the provider quoted the credential it rejected. That is deliberate: &quot;invalid API key&quot; and &quot;model not found&quot; need different fixes, and a generic failure message helps nobody. A failing test also strips the default from a model that held it, because requests must not resolve to credentials that are known not to work.</li></ul><p>Anything that only checks configuration, such as an OpenAI-compatible model registered with no endpoint, fails without a network call and says which field is wrong.</p><h2 id="enabling-a-row-and-choosing-the-default" tabindex="-1">Enabling a Row and Choosing the Default <a class="header-anchor" href="#enabling-a-row-and-choosing-the-default" aria-label="Permalink to &quot;Enabling a Row and Choosing the Default&quot;">​</a></h2><p>The registry table gives every model an <strong>Enabled</strong> switch and a <strong>Default</strong> radio.</p><p>Enabled decides whether a model is offered anywhere. The chat model picker lists every enabled row; a disabled one appears in neither the picker nor the default column.</p><p>Exactly one model carries the default, and the server enforces it in a single transaction so there is never a moment with two. The default is what a request that names no model resolves to. A row that cannot hold it has its radio disabled with the reason in the tooltip, and the server refuses the promotion as well, so the two agree:</p><ul><li>&quot;Enable this model before making it the default&quot;</li><li>&quot;Test this model before making it the default&quot;</li></ul><p>The default governs more than the chat picker&#39;s initial selection. <strong>The chart agent on <code>/trading</code> has no model picker at all and always runs the default.</strong> Changing the default here changes what that panel talks to, with no other setting involved.</p><p>Removing a model warns you when it is the only one, in which case <code>/agent</code> goes back to the setup screen, and when it is the default, in which case the server hands the default to another enabled model that has passed a test. Removing a model takes its per-model key override with it; a key shared with the rest of the provider stays, because other models still answer to it.</p><h2 id="trading" tabindex="-1">Trading <a class="header-anchor" href="#trading" aria-label="Permalink to &quot;Trading&quot;">​</a></h2><p>The <strong>Trading</strong> panel is one switch, labelled &quot;Allow the agent to place, modify and cancel orders&quot;. <strong>It is off by default.</strong> Until it is on, the order tools are never built into the run&#39;s schema at all, so the agent declines to trade rather than asking you to approve anything. Placing, modifying and cancelling are opt-in.</p><p>What the switch does <em>not</em> do is place anything. Every order tool pauses the run and shows you its exact arguments, and a risk guard runs after you approve. <a href="./capabilities">What the Agent Can Do</a> covers that path in full, including the shipped limits.</p><p>Where an approved order lands is a separate question, decided by the platform&#39;s own analyzer toggle rather than by anything on this page. The panel states the consequence rather than leaving it to be inferred:</p><blockquote><p>This instance is in live mode, so an order you approve is sent to your broker with real money. Switch the platform to analyzer mode first if you want approved orders to reach the sandbox instead.</p></blockquote><p>On an analyzer instance it says the opposite, plainly: an approved order reaches the sandbox rather than your broker.</p><p>The risk limits themselves are settings in the same database, and this page has no panel for them yet. The chart agent on <code>/trading</code> is offered no order tools under any setting.</p><h2 id="reasoning-effort" tabindex="-1">Reasoning Effort <a class="header-anchor" href="#reasoning-effort" aria-label="Permalink to &quot;Reasoning Effort&quot;">​</a></h2><p>Reasoning effort is chosen per question, in the composer, next to the model. Four options:</p><table tabindex="0"><thead><tr><th>Option</th><th>What it means</th></tr></thead><tbody><tr><td>Default</td><td>the model&#39;s own</td></tr><tr><td>Low</td><td>fastest</td></tr><tr><td>Medium</td><td>balanced</td></tr><tr><td>High</td><td>deepest</td></tr></tbody></table><p><strong>Default</strong> sends no override at all and leaves the model row&#39;s own configured effort in place.</p><p>The control only appears for a model that actually reasons. GPT-4 and GPT-4o take no reasoning effort, and the server refuses to send one for them whatever is asked, so offering the menu would be a control that silently did nothing. Select such a model and the menu says so instead: &quot;does not take a reasoning effort. Pick a reasoning model to control how hard it thinks.&quot;</p><p>Whether a model reasons is resolved against LiteLLM&#39;s own capability table, not against a checkbox. The <strong>Supports reasoning</strong> toggle in the edit dialog only fills in for a model LiteLLM has never heard of, such as one served by a local endpoint. That is why the picker and the run never disagree.</p><p>In the registry table, the Capabilities column shows <strong>Reasoning</strong> followed by that model&#39;s configured default effort for a model that reasons, plus <strong>Vision</strong> and <strong>Tools unreliable</strong> where they apply, and reads <strong>Text and tools</strong> for a model that has none of them.</p><h2 id="web-search" tabindex="-1">Web Search <a class="header-anchor" href="#web-search" aria-label="Permalink to &quot;Web Search&quot;">​</a></h2><p>Web search is <strong>on by default and toggled per conversation</strong>, in the composer&#39;s plus menu rather than here. When it is off the composer footer says so, and the turn is given no web search tools at all: the two tool schemas cost input tokens whether or not they are used, so off is also cheaper. What this page owns is which provider answers, the keys, and the budgets.</p><p>Three providers are configured on this page, and the routing is the part worth reading, because it is not guessable from a provider list:</p><ul><li><strong>DuckDuckGo</strong> needs no key, is the default, and answers link searches out of the box. It scrapes rather than calling an API, so it throttles under load.</li><li><strong>Tavily</strong> needs a key and answers link searches in place of DuckDuckGo once that key is stored. Select it without a key and link searches silently fall back to DuckDuckGo; the panel warns you rather than letting you find out at run time.</li><li><strong>Perplexity</strong> needs a key and returns a synthesised answer with citations rather than links, so it answers a different tool. Selecting it leaves link searches on DuckDuckGo.</li></ul><p>A key is saved, tested and removed per provider, and <strong>Test</strong> works on a key typed but not yet saved. Keys here follow the same rule as model keys: never rendered, blank means keep the stored one.</p><p>Two budgets guard the spend. <strong>Searches per turn</strong> accepts 0 to 50 and defaults to 5. <strong>Daily cap</strong> accepts 0 to 10000 and defaults to 200, is counted in the database on the IST date, and resets by the date itself rather than by a scheduled job, because a per-turn budget alone is bypassed by sending another message. A meter shows how many searches are left today. A daily cap of 0 disables web search entirely and says so.</p><p>Results returned by a search are treated as lower-trust than the platform&#39;s own data, and an answer built on them cites its sources as real links.</p><h2 id="editing-a-registered-model" tabindex="-1">Editing a Registered Model <a class="header-anchor" href="#editing-a-registered-model" aria-label="Permalink to &quot;Editing a Registered Model&quot;">​</a></h2><p>The edit dialog changes everything about a row except its identity. The provider kind and the model id are fixed: to run a different model, register it instead.</p><p>Editable: display name, base URL, API key and its scope, Enabled, Supports reasoning and its default effort, Supports vision, and &quot;Unreliable at tool calling&quot; for a model you have found cannot be trusted with a function call.</p><p>Two behaviours are worth knowing before you use it:</p><ul><li><strong>A blank key field means keep the stored key, never clear it.</strong> The field starts empty even when a key is configured, because a secret is never sent back to the browser, so blank is the state of every edit that was not about the key.</li><li><strong>Changing a base URL clears the test result and the default status.</strong> The credentials were tested against the old endpoint and say nothing about the new one.</li></ul><p>There is no &quot;clear key&quot; action, because there is no route for one. Removing the model removes its per-model override.</p><h2 id="operational-boundaries" tabindex="-1">Operational Boundaries <a class="header-anchor" href="#operational-boundaries" aria-label="Permalink to &quot;Operational Boundaries&quot;">​</a></h2><ul><li>Provider and model counts, prices, context windows and capability flags all come from LiteLLM&#39;s shipped data, not from a live provider API. They move when the package is upgraded, and a model absent from that data can still be registered by hand.</li><li>A passing credential test proves the provider accepted one one-token request. It is not a guarantee of quota, rate limit headroom or of the model&#39;s behaviour on a long tool-driven turn.</li><li>A model that cannot call tools cannot drive this agent at all, whatever else it is good at.</li><li>Registered keys cannot be read back from any endpoint or any screen. Losing one means pasting it again from wherever you keep it.</li><li>The voice key is a second, separate credential. It is stored encrypted like every other key here, is never sent back to the browser, and is used only to mint a speech session - the thinking still runs on whichever model you made the default.</li><li>Everything on this page is instance-wide. Tradeboard is single user per deployment, so there is no per-session or per-surface override of the model, the trading switch or the search budget.</li><li>The agent&#39;s own routes are rate limited like the rest of the platform: 30 streamed turns a minute, 12 model tests a minute, and 240 other agent API calls a minute. A throttled request comes back as an error in the page rather than as a redirect.</li><li>With trading on and the platform in live mode, an order you approve reaches your broker with real capital. Test the agent&#39;s order path in analyzer mode first.</li></ul><h2 id="see-also" tabindex="-1">See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See Also&quot;">​</a></h2><ul><li><a href="./README">Agent</a> for the overview and the three surfaces.</li><li><a href="./voice">Voice Agent</a> for Vega, the microphone on <code>/agent</code>.</li><li><a href="./chatgpt-subscription">ChatGPT Subscription</a> for the plan billing path, the OAuth sign-in, and which models it covers.</li><li><a href="./capabilities">What the Agent Can Do</a> for the toolkits a registered model is given, and the order path in full.</li><li><a href="./examples">Agent by Example</a> for what these settings look like from inside a conversation.</li><li><a href="./../trading-terminal">Chart Trading Terminal</a> for the chart the docked agent reads and draws on.</li><li><a href="./../api-analyzer">API Analyzer</a> for the platform-wide toggle that decides where an approved order lands.</li><li><a href="./../action-center">Action Center</a> for the platform&#39;s other approval queue, which is separate from the agent&#39;s own approval pause.</li></ul>',75)])])}const p=t(r,[["render",s]]);export{u as __pageData,p as default};
