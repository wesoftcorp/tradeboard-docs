import{_ as o,o as t,c as a,a2 as n}from"./chunks/framework.BXzK3EA4.js";const p=JSON.parse('{"title":"Version 2.0.2.2 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.2.2-released.md","filePath":"change-log/release/version-2.0.2.2-released.md"}'),r={name:"change-log/release/version-2.0.2.2-released.md"};function s(d,e,i,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="version-2-0-2-2-released" tabindex="-1">Version 2.0.2.2 Released <a class="header-anchor" href="#version-2-0-2-2-released" aria-label="Permalink to &quot;Version 2.0.2.2 Released&quot;">​</a></h1><p><strong>Date: 29th August 2026</strong></p><p><strong>Stability and Security Release: closes the eventlet boundary defects behind the long-standing &quot;the first order works and the next one hangs the app&quot; reports, sweeps broker credentials out of every log surface across 30 plugins, stops Dhan symbol mapping routing an order to the wrong instrument, and opens the charting terminal to user-written indicators loaded at runtime with no build step. Also modernises the Motilal Oswal plugin, extends GTT to Angel One, Fyers and Upstox, gives Flow&#39;s order fields payload-driven values, and cuts a fresh install&#39;s clone from 276 MB to 20 MB</strong></p><p>This release spans 143 commits since 2.0.2.1 and is dominated by two sweeps that touch the whole tree rather than one surface.</p><p>The first is the <strong>eventlet boundary</strong>. Production runs <code>gunicorn --worker-class eventlet -w 1</code>, which monkey-patches the stdlib before the app is imported, so <code>threading.Lock</code>, <code>Event</code> and <code>Queue</code> all become green primitives owned by the hub. A handful of threads in this project are genuinely real OS threads, principally the asyncio loop in the websocket client, and every crossing between the two worlds was a defect: a real thread touching a green lock raises <code>greenlet.error: Cannot switch to a different thread</code> inside <code>fire_timers</code> and blocks that thread forever, a greenlet waiting on a C-served timeout freezes the entire worker, and a waiter that is simply never woken sits out its whole timeout on data that was ready all along. All three were present. None of them reproduce on <code>uv run app.py</code>, which is why they survived three separate issue reports (#1402, #1473, #1569) over months.</p><p>The second is <strong>credential exposure in logs</strong>. A grep for credential variable names finds the easy half. The larger class is a secret riding inside something else that gets logged: a WebSocket URL with the token in the query string, an auth request or response body, a headers dict, an exception message whose text embeds the full URL. Roughly twenty broker adapters were also actively replacing a redacting logger with a bare <code>logging.getLogger()</code>. Every candidate here was rendered with a realistic value and run through the production <code>SENSITIVE_PATTERNS</code> rather than read, which is what separated the 2 real leaks from the 5 sites already safe.</p><p>Outside those, the charting terminal gained runtime-loadable user indicators and moved from <code>tradeboard-charts</code> 1.6.0 to 1.8.2, Flow had its 61 node types audited against the executor, the Dhan and Motilal Oswal plugins were substantially repaired, GTT reached three more brokers, and the repository stopped committing 165 MB of reproducible compression artifacts.</p><hr><p><strong>Highlights</strong></p><ul><li><p><strong>The eventlet boundary, in four commits (<code>27799885</code>, <code>9a488cae</code>, <code>05496d8b</code>, <code>ebf03340</code>, #1402, #1473, #1569)</strong> - what users reported was the first order going through and the next one hanging the app for over a minute, with the order itself having taken 62 milliseconds. Four separate defects, each fatal on its own.</p><p><strong>A lock shared with the loop thread must be real.</strong> The websocket client runs its asyncio loop on a real OS thread, because asyncio cannot run on a green one, and it invoked every registered market-data, auth and error callback from that thread. Four crossings existed: <code>WebSocketClient.lock</code>, the sandbox engine&#39;s <code>_lock</code>, the scalping risk monitor&#39;s <code>_lock</code> and the Flow executor&#39;s tick <code>data_event</code>. Contended, the hub tries to resume a waiter belonging to another thread and leaves the loop thread blocked on that lock forever, so the feed stops answering pings and stops resolving subscribe acks. They now take their primitives from <code>utils/real_threading</code>, which resolves to the unpatched originals under eventlet and the stdlib otherwise. The <code>Event</code> needed more than a swap: a green <code>Event</code> never wakes on a <code>set()</code> from a real thread, and a real <code>Event.wait()</code> wakes but blocks the hub, so <code>real_threading.wait_for</code> polls the flag and yields between checks.</p><p><strong>Logging handler locks and inline callback dispatch.</strong> <code>logging.Handler</code> builds its lock in <code>__init__</code>, which under gunicorn happens after monkey-patching, so it is green, and every real thread in this project logs. Measured, a real thread logging while a greenlet holds the handler lock deadlocks permanently and raises exactly the production traceback; #1569 reports the give-away that precedes it, <code>AttributeError: &#39;StreamHandler&#39; object has no attribute &#39;lock&#39;</code> on unrelated requests. <code>Handler.createLock</code> is now patched on the class, so third-party handlers get a real lock too. Separately, <code>_handle_message</code> ran on the loop&#39;s real thread and called every subscriber inline, and those subscribers reach SocketIO, the event bus and the sandbox engine. The loop thread now only enqueues onto a real bounded queue and a green thread does the calling.</p><p><strong><code>PRAGMA busy_timeout</code> waits inside C.</strong> A greenlet waiting that way freezes the entire worker for the whole timeout, and that is not merely slow: the greenlet holding the write lock cannot be scheduled to commit while the waiter is blocking the hub, so the wait can only ever end in &quot;database is locked&quot;. Measured, a holder that needed the lock for 0.5s produced a 16.46s failure; waiting the same 0.5s in Python succeeds in 0.47s with the hub running. The in-SQLite wait drops to 100ms and the real waiting moves into a Python retry loop, keeping the same 15-second total budget.</p><p><strong><code>run_coroutine_threadsafe</code> never wakes its caller.</strong> It hands back a <code>concurrent.futures.Future</code> whose <code>result()</code> waits on a green <code>threading.Condition</code> while the loop thread resolving it is real, so the future resolves correctly and the caller sleeps on regardless. Measured, an ack that arrived in 0.3s cost the caller the full 10s, which is why <code>subscribe()</code> took its whole 12 seconds whenever it had to wait at all. Nothing crosses the boundary now except one plain boolean.</p><p><code>test/test_eventlet_cross_thread_locks.py</code> and <code>test/test_sqlite_lock_cooperative.py</code> prove each direction under a real hub, in a subprocess because <code>monkey_patch()</code> cannot be undone, asserting on elapsed time and hub liveness rather than return values, which were always right. <code>95d6d1fa</code> records the whole rule set in CLAUDE.md so the next person does not rediscover it.</p></li><li><p><strong>Broker credentials no longer reach the logs (<code>209c305e</code>, <code>438af07f</code>, <code>c8dad476</code>, <code>fe0875f3</code>, <code>4c3abf7f</code>, #1854, #1855)</strong> - four passes, each finding a class the previous one could not see.</p><p><strong>73 call sites across 60 files in 30 plugins</strong> were using a bare <code>logging.getLogger()</code>, which bypasses <code>SensitiveDataFilter</code>. This was not merely a missing filter: <code>websocket_proxy/base_adapter.py</code> already sets a redacting logger, so every adapter subclass reassigning <code>self.logger</code> in its own <code>__init__</code> was actively replacing it with a non-redacting one.</p><p><strong>Credentials interpolated directly into messages</strong>, across 12 plugins. Removed entirely rather than truncated, because a 10-character API key prefix is still key material and fivepaisa logged the first 50 and last 50 characters of its access token on adjacent lines, which together expose far more of a JWT than either alone.</p><p><strong>Secrets riding inside a URL, payload, headers dict or exception message</strong>: 35 sites across 24 files in 15 plugins. The worst were flattrade logging <code>hash_input</code>, which is literally the API key concatenated with the secret; definedge putting the API key in the URL <em>path</em> and then logging the <code>httpx.HTTPStatusError</code> whose message embeds the full URL, so it leaked on every 4xx, which is exactly the wrong-credential case; aliceblue logging the full login response at INFO on every login; and upstox logging the AWS SigV4 presigned feed URL including <code>X-Amz-Signature</code>.</p><p><strong>A defect in the redaction pattern itself.</strong> <code>(Bearer\\s+)[\\w\\-\\.]+</code> stops at the first separator, and two brokers send a two-part credential in that header, so the filter redacted the harmless half and left the real secret. Found independently in tradejini and aliceblue, which is what proved the pattern was wrong rather than the call sites. <code>cookie</code> was added to the alternation, <code>utils/logging.py</code> gained its first tests, and the frontend stopped sending mutating requests with no CSRF token when <code>fetchCSRFToken()</code> throws.</p></li><li><p><strong>Dhan: symbol mapping routed orders to the wrong instrument (<code>da553a56</code>, <code>49717118</code>, #1929, #1930, #1932, #1934, #1924)</strong> - four related defects in the master contract and the feed, all verified against the live 197,254-row scrip master.</p><p>Dhan scopes <code>SEM_SMST_SECURITY_ID</code> per segment, not per exchange, and <code>assign_values</code> matched on instrument name alone. NSE segment D and segment M both mapped to NFO, giving 8,642 security ids two contracts each: token 153964 was both a TCS option and a SILVERM option, and <code>get_symbol()</code> returns the first match. A live equity option position could therefore resolve to a commodity symbol, the position book lookup would miss it, and a strategy would conclude it was flat and skip the exit. Colliding rows go from 17,284 to 0, NSE segment M now maps to NCO with 25,400 rows, and 722 stranded MCX index option rows are recovered.</p><p>On the forward lookup, symbols were built from display strings. The equity branch ignored <code>SEM_SERIES</code>, so ELECTCAST was both the equity and its warrant and an order could reach the wrong one; strikes were parsed from the two-decimal <code>SEM_CUSTOM_SYMBOL</code>, which misformatted 1,628 NFO contracts as <code>.50CE</code> and, in currency, collapsed EURUSD 1.010 and 1.015 onto one symbol. <strong>Breaking: 7,190 NSE equity symbols gain a series suffix</strong> (<code>ELECTCAST-W1</code>, matching Zerodha&#39;s convention). Only 9 were genuinely ambiguous and the rest are mostly bonds and government securities, but the BE, SM and ST series include tradable equities, so a strategy referencing one by its bare symbol needs updating. No migration is required, since <code>master_contract_download()</code> clears the table before reinserting.</p><p><code>unsubscribe()</code> only forgot the instrument locally and never told the broker, so subscriptions accumulated toward the 5,000-per-connection limit invisibly, Dhan not acknowledging subscribes. <code>dhan_sandbox</code> was worse: it sent <code>{&quot;RequestCode&quot;: 0}</code>, which is not a valid code at all. Both now send the code matching the mode the token was subscribed in. <code>dhan_sandbox</code> also carried byte-identical copies of every mapping bug above and is brought to parity.</p></li><li><p><strong>User chart indicators, loaded at runtime (<code>f4359718</code>, <code>a95460fe</code>, #1923)</strong> - drop a <code>.js</code> file in <code>strategies/indicators/</code> and it becomes an indicator in the <code>/trading</code> picker, with a generated settings dialog, legend row and saved-layout persistence. No build step, no Node.js, no restart. They are loaded over HTTP rather than bundled, because <code>frontend/dist/</code> is built by CI from what is committed and user indicators are deliberately gitignored, so a bundled one would be erased by the next <code>git pull</code>. The folder sits under <code>strategies/</code> to mirror <code>strategies/scripts/</code>, which also puts it inside the <code>tradeboard_strategies</code> Docker volume. Custom indicators register after the built-in tier, so a colliding id overrides a built-in. Validation happens in the browser against the library the chart already has loaded, catching both silent killers: a column that is not <code>bars.length</code> long, and a plot key <code>calc</code> never filled. Neither raises anything at runtime, which is what made a broken indicator look like one that simply does not work.</p></li><li><p><strong>Charting terminal: 1.6.0 to 1.8.2, and everything the pin was hiding (<code>8eda625d</code>, <code>cbacb83b</code>, <code>0dc8ac19</code>, <code>a1a2dc92</code>)</strong> - the pin sat at 1.6.0, so none of the descriptor surface added since was reachable. Bumping it exposed a chain of gates that would each have rejected a valid indicator before the user saw it: the loader accepted six input types where the library now defines eleven, the settings dialog could render none of the five new ones, and the column check assumed one column per plot so a candle plot fed by an <code>ohlc</code> group read as four missing columns. Separately, the chart was never told the instrument tick size, so its price scale inferred precision from the visible range and RELIANCE rendered as 1,303.5 rather than 1303.50 while drawings snapped to a grid nobody chose. The terminal already had the number from the symbol master and simply never passed it. The version is now pinned exactly rather than with a caret: <code>^1.6.0</code> resolves to 1.9.0 on the registry, and deprecation only prints a warning, it does not stop an install.</p></li><li><p><strong>Charting terminal features (<code>5ea4d080</code>, <code>d7cdbf54</code>, <code>c0d73cee</code>, <code>55447bd0</code>, <code>7b867c9d</code>, <code>0c8c29a6</code>)</strong> - market replay with a transport bar and scrub; a chart settings dialog rendered from the engine&#39;s own declarative schema across five tabs, with a reset that resets to <em>this terminal&#39;s</em> baseline rather than the engine&#39;s per-control defaults; chart sync groups linking crosshair, time and symbol independently; a warm-load history cache, measured at two history requests for a cold symbol and one for a return visit; toolbar undo and redo for drawings; and an indicator browser with categories, favourites and recents replacing a single 91-entry scrolling column. Plus the corner session clock reading the exchange&#39;s wall time through the chart&#39;s configured timezone.</p></li><li><p><strong>Motilal Oswal: repaired and modernised (<code>f76818be</code>, #1912)</strong> - every endpoint moved to its documented version (placeorder v1 to v2, modifyorder v2 to v5, getorderbook v2 to v5, gettradebook v1 to v4, getposition v1 to v4, getdpholding v1 to v3). <code>get_open_position</code> matched a broker scripname against a position-book symbol and never matched, so every smart order saw a flat position; smart orders also looked up positions with no exchange, so F&amp;O MIS positions never matched. The env convention was corrected: sending the secret as <code>ApiKey</code> caused MO2005, and the client code is now persisted from the TOTP page rather than read from an env var, fixing the empty vendorinfo (MO2012). Then a WebSocket audit found four leaks: the services build a fresh <code>BrokerData</code> per request so an instance-cached socket could never hit, and every depth and multiquote call opened a brand new WebSocket that nothing closed. Pooled per session in a module-level registry, five requests now open one socket instead of five. Three further leaks surfaced on re-audit: a cold-start registration race (4 concurrent requests created 4 sockets and leaked 3), duplicate poll threads on every proxy recovery path republishing every subscription to ZMQ, and tick caches never purged, which an option-chain sweep grows by hundreds of strikes per refresh. Pinned by eight tests mirroring <code>test_aliceblue_ws_reuse.py</code>.</p></li><li><p><strong>GTT for Angel One, Fyers and Upstox (<code>6d7f2552</code>, #1922)</strong> - extends GTT beyond Zerodha and Dhan. Each broker registers itself by shipping <code>api/gtt_api.py</code> plus <code>mapping/gtt_data.py</code>, with the compatibility matrix derived by parsing for it. Angel One has no OCO rule type, so an Tradeboard OCO becomes two rules returned as a composite trigger id, with the first leg rolled back if the second fails. Fyers signals OCO by the presence of <code>orderInfo.leg2</code> and requires leg1 above the LTP, so the target leg maps to leg1. Upstox has no OCO at all: every GTT needs an ENTRY rule, making its multi-leg form a bracket rather than an exit-only pair, so an Tradeboard OCO opens the entry position before arming the legs, logged loudly at place time because the resulting EDIS rejection names demat authorisation rather than the entry leg.</p></li><li><p><strong>Flow: nodes no longer act on data they do not have (<code>cb954658</code>, <code>ce9db84a</code>, <code>44ebce13</code>, <code>732afe4d</code>, <code>98e693db</code>, <code>252bc7c1</code>, #1904, #1909)</strong> - an audit of all 61 node types, each finding reproduced before it was touched. <code>priceCondition</code>, <code>positionCheck</code> and <code>fundCheck</code> read the broker response without checking its status, so a 401 gave LTP 0.0 and <code>status: success</code>: &quot;if LTP &lt; 100 then BUY&quot; fired on an expired session and &quot;if no position then BUY&quot; doubled an open one. An errored condition settled a gate with its placeholder <code>False</code>, driving a real order down the FALSE branch before the run was marked failed. A condition reachable by two paths ran twice and placed two orders from one trigger. <code>timeWindow</code> could not cross midnight, making 22:00-02:00 unsatisfiable and, inverted, always true. Subscriptions were never given back to the process-wide websocket singleton, and a specific-mode <code>unsubscribe</code> with no symbol fell through to <code>unsubscribe_all</code>, clearing the subscriptions the sandbox engine uses to trigger pending SL and LIMIT orders. Two injection paths in <code>httpRequest</code>: the URL was interpolated twice so a payload could substitute its own <code>&amp;#123;&amp;#123;...&amp;#125;&amp;#125;</code>, and header JSON was interpolated before parsing so a value carrying a quote became structure.</p><p>Separately, every order field now accepts a <code>&amp;#123;&amp;#123;reference&amp;#125;&amp;#125;</code>, not just <code>symbol</code>: a dropdown cannot express <code>&amp;#123;&amp;#123;webhook.exchange&amp;#125;&amp;#125;</code> and a number input cannot express <code>&amp;#123;&amp;#123;webhook.quantity&amp;#125;&amp;#125;</code>, so a webhook-driven order could name its instrument and nothing else. The executor always interpolated these; the gap was entirely in the form. Interval schedules are anchored to the clock rather than to activation, so a 5-minute job lands on :00, :05, :10 rather than five minutes after a button press, offset by <code>FLOW_INTERVAL_ALIGN_OFFSET</code> (2s) so the run sits just inside the new bar. Derivative segments default to NRML rather than a stored MIS on every node. MCX commodity options are resolvable, priced off the nearest unexpired future since there is no spot instrument, and multi-leg baskets can be built leg by leg instead of only through hand-written JSON.</p></li><li><p><strong>Shoonya returns a quote for a different instrument, 9% of the time (<code>252bc7c1</code>, part of #1904)</strong> - measured against the live API, two NFO options polled round-robin at 2 req/sec for 13 minutes: 119 of 1318 replies came back as a complete, <code>stat=Ok</code> snapshot of another instrument, every one of them the NSE index, in a run that never requested the index. Nothing about the price reveals it: a leaked index quote is internally consistent and its LTP sits inside its own OHLC, so the stale-quote check cannot catch it. It reaches order fills, and a sandbox MARKET buy on <code>NIFTY18AUG2624600CE</code> filled at 24391.25, the NIFTY spot, against an LTP of 39.85. All five <code>GetQuotes</code> paths now compare the echoed exch/token against the request and retry up to three times; the two multiquotes fetchers bypassed <code>get_api_response</code> entirely and had no check at all. The same session&#39;s WebSocket feed carried 1719 ticks with zero wrong instruments, so this is specific to the REST endpoint.</p></li><li><p><strong>Repository size: 276 MB to 20 MB per install (<code>4fe47bd0</code>, <code>02361622</code>, <code>a557c12e</code>, #1896, #1897, #1898)</strong> - the Vite build emitted <code>.br</code> and <code>.gz</code> next to each hashed asset and CI force-committed them on every rebuild. Compressed output can be neither deflated nor delta-compressed by git, and content-hashed filenames mean every rebuild produces new blobs that never go away, so these had grown into 165 MB of the 248 MB <code>frontend/dist</code> occupies in history. Repository history had tripled in 60 days. They are generated at startup now, idempotently and atomically, in about 30 ms on a warm boot. Tracked <code>dist</code> files drop from 586 to 270 and history growth from roughly 83 MB/month to 28 MB/month. Alongside it, all four install scripts and the nine user-facing clone commands in the docs use <code>--filter=blob:none</code>, measured against GitHub at 20 MB and 8s rather than 280 MB and 36s, keeping all 4,824 commits and every branch and tag. <code>install-multi.sh</code> clones inside its per-instance loop, so a five-instance install went from ~1.4 GB.</p></li><li><p><strong>Sandbox: three clock boundaries resolved (<code>7403631b</code>, <code>5292719f</code>, <code>e4918105</code>, <code>f0b39cf1</code>, <code>c3e568ea</code>, #1789, #1801, #1808)</strong> - <code>sandbox_positions.updated_at</code> is written by <code>func.now()</code>, which on SQLite is UTC, and <code>get_open_positions</code> compared it against a boundary built from naive local wall time. On an IST host the boundary landed 5:30:00 late, dropping open positions last updated between 03:00 and 08:30 IST from the position book, which is what a polling strategy reads to decide whether it is flat. The same shape affected the T+1 settlement cutoff, pushing a CNC position created between 00:00 and 05:30 IST into settlement a day early. <code>as_db_utc()</code> now exists in one place, since this file has had the same bug twice. Separately, a <code>SESSION_EXPIRY_TIME</code> of <code>&#39;25:00&#39;</code> parses cleanly as an int and then raises on <code>replace()</code>, outside the try, and <code>catch_up_mis_squareoff()</code> swallows it, so a config typo silently disabled a risk control. And two same-symbol fills arriving together each read the same quantity and wrote their own delta, so the second overwrote the first with both orders still showing complete; the regression test fires several concurrent pairs because a single pair only interleaves about two thirds of the time.</p></li><li><p><strong>Holdings returned no LTP or average price (<code>5bd4cd6a</code>, <code>d36936a6</code>, #1917, #1919)</strong> - Angel ignored <code>averageprice</code> and <code>ltp</code>, and Zerodha read <code>last_price</code> to derive the P&amp;L percentage and then threw it away. The holdings page rendered a dash in both columns, the CSV export wrote them empty, and portfolio analytics weighted by a zero investment value. Both brokers also failed the whole request on one bad row: Angel reports an empty demat as <code>holdings: null</code> rather than <code>[]</code>, so an account with nothing bought got an HTTP 500 instead of an empty table, and a single Zerodha holding with a null <code>average_price</code>, <code>last_price</code> or <code>pnl</code> turned the entire request into a 500 rather than degrading that one row.</p></li><li><p><strong>Flattrade reported a funded pledge as no pledge (<code>3ca1adfa</code>, #1936)</strong> - the Limits response carries several collateral figures and the wrong one was read. <code>brkcollamt</code> is the pre-valued collateral amount and sits at 0.00 on an ordinary pledged account, while the pledged value lives in <code>collateral</code>, which is what the Flattrade app labels &quot;Holdings Collateral&quot;. The reported account held 9,385.34 cash and 1,890.30 of pledged NSE collateral; Tradeboard showed the cash correctly and collateral 0.00. Shoonya and Zebu share this Noren API and carry the same line, left alone here pending confirmation against a live pledged account.</p></li><li><p><strong>CORS_ENABLED=FALSE opted the deployment <em>into</em> the permissive policy (<code>c88b5c8e</code>, #1860, #1848)</strong> - <code>get_cors_config()</code> returned an empty dict when disabled and flask-cors then applied its own defaults, which set <code>origins=&quot;*&quot;</code>. Measured against the previous behaviour, a GET to <code>/api/v1/funds</code> carrying <code>Origin: https://evil.example</code> returned that origin back, and an OPTIONS preflight for a JSON order POST returned 200 allowing <code>x-api-key</code>. That let a cross-origin page place an order through the user&#39;s own instance with a leaked API key, from the user&#39;s broker-registered IP. The same applied to any <code>.env</code> predating the variable. The enabled-but-unconfigured case now fails closed too.</p></li><li><p><strong>TradeSmart quotes were throttled to a fraction of the allowance (<code>b8e4cbdd</code>, #1928)</strong> - quotes are metered separately by the broker, so pacing them against the shared 10/sec and 120/min per-user budget was wrong. <code>/GetQuotes</code> now reserves from its own 100/sec bucket with no per-minute ceiling, and the REST quote fan-out pool sizes to that rather than to the general constant, which had capped throughput at 8/sec regardless. Orders, funds, margin and history keep the shared gate. Placed orders are also tagged <code>tradeboard</code> in the Noren <code>remarks</code> field, which the OMS echoes back in the order book.</p></li><li><p><strong>Option resolver validation (<code>0c41679d</code>, #1829)</strong> - the strike interval was used as a divisor with no guard, so 0 raised <code>ZeroDivisionError</code> and a negative, NaN or infinite value produced a strike that formats into a symbol no exchange lists, both surfacing as HTTP 500. An unrecognised option type was silently priced as a put: every offset calculation branches on CE and takes the else branch for everything else, so &quot;CALL&quot;, &quot;C&quot; or <code>None</code> resolved to the put strike with no error and a log line saying it was correct. 145 test cases; neutering the two validators fails 91 of them.</p></li><li><p><strong>Auth: no way out of a half-finished login (<code>cab45dc9</code>, <code>51ea6013</code>)</strong> - the broker-auth pages render outside <code>Layout</code> and therefore have no navbar, and <code>POST /auth/login</code> redirects back to <code>/broker</code> whenever a user is already in the session, so someone who completed the password login but not the broker login had no UI path out. The only escapes were typing <code>/auth/logout</code> by hand or clearing cookies. The confirm dialog is shown unconditionally rather than signing out on the first click, because Tradeboard allows concurrent devices sharing one broker feed and <code>logout()</code> takes the full teardown path across every device. Password login also clears the session before writing any authenticated value, so a reset token or a half-finished TOTP park cannot survive underneath.</p></li><li><p><strong>CI now tests what the project advertises (<code>a551845b</code>, #1894)</strong> - <code>pyproject</code> declares <code>requires-python &gt;=3.12</code> and the frontend declares three Node majors, but CI pinned one point in each range. <code>backend-test</code> now runs on Python 3.12, 3.13 and 3.14 and the frontend jobs on Node 20, 22 and 24, with <code>fail-fast</code> off. All 166 pinned dependencies resolve to an identical set on all three Python versions, so this is coverage rather than a porting effort. Note that matrix jobs report as <code>backend-test (3.12)</code>, so a required status check list must use the expanded names.</p></li><li><p><strong>Test isolation was decided by import order (<code>4852d463</code>)</strong> - <code>conftest</code> assigns <code>DATABASE_URL</code> to the test databases, then <code>utils/config.py</code> calls <code>load_dotenv(override=True)</code> at import and puts the real paths back. Whether that happened depended entirely on which module a given test imported first. The visible result was seven Flow workflows from the QA regression suite appearing in the operator&#39;s Flow Editor, named &quot;retention test&quot;, &quot;age test&quot;, &quot;mine&quot;, &quot;theirs&quot;, removable only by hand. Verified before and after: a full run put seven workflows in <code>db/tradeboard.db</code> before and none after.</p></li><li><p><strong>A silent feed logged a warning every two minutes (<code>3f2fa11e</code>)</strong> - a subscribed feed is legitimately silent every night, every weekend and every trading holiday, and the check cannot tell that apart from a dead socket without an exchange-aware calendar. A Saturday afternoon logged an identical line forty times in ninety minutes, counting up to &quot;no ticks for 4716s&quot;. Moved to debug; <code>get_adapter_health()</code> still reports seconds since the last tick per user.</p></li><li><p><strong>Smaller fixes</strong> - the frontend rate limiter retained a timestamp exactly <code>windowMs</code> old, so waiting exactly as long as the module reported was not enough (<code>78e4e490</code>, #1830). Multi-option Greeks batch state is keyed by leg index (<code>0f99ab6c</code>, #1819). Client error reports no longer persist a reset-password token or a broker OAuth code into <code>log/errors.jsonl</code> (<code>e9b29821</code>, <code>07a5f649</code>, #1851). <code>get_history()</code> rejects an unsupported source at the entry point (<code>c654d85e</code>, #1826) and the market calendar helpers return 400 rather than 500 for a non-string date (<code>242f80d4</code>, #1824). Navigation links declare <code>aria-current=&quot;page&quot;</code>, which no link in the app did (<code>1d4cade3</code>, #1833). The dead <code>zmq==0.0.0</code> stub, a placeholder package shipping no code, is dropped from all three dependency lists (<code>4fa997dc</code>, #1895). Ten boot-time lines that state an expected fact moved from INFO to debug (<code>5fd89238</code>). The security audit&#39;s own NullPool check was failing on the documentation warning against the defect (<code>301fe95a</code>).</p></li><li><p><strong>Docs and skills</strong> - broker plugin counts synchronised to 36 across 17 files plus the FAQ, the devsprint guide and the design docs, with historical snapshots deliberately left alone (<code>eb7d25c2</code>, <code>122dbed5</code>, #1844, #1906, #1910). A documentation-only contribution workflow (<code>287e8d02</code>, #1846), a completed CONTRIBUTING table of contents (<code>447786fa</code>, #1883), contributor test commands aligned with what CI actually runs (<code>1d6ce13f</code>, <code>a32cb277</code>, #1841), corrected frontend build-artifact and Node guidance (<code>6d3a0d08</code>, #1842) and obsolete <code>/react</code> routes replaced with <code>/dashboard</code> (<code>7e409e49</code>, #1840). Five claims in the Flow import format were proved wrong by the docs rewrite and corrected (<code>6ed6442d</code>), including <code>marketHoursOnly</code> defaulting to false in the executor while the editor writes true, and <code>days</code> never being read by the <code>daily</code> branch, so a &quot;weekdays only&quot; daily schedule still fired at the weekend. Three new skills: <code>chart-indicator</code> (<code>f4359718</code>), <code>flow-builder</code> (<code>cb954658</code>) and <code>verify</code> (<code>262abe06</code>), the last encoding six rules each earned from a specific failure in this cycle rather than invented.</p></li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>tradeboard-charts</code>: <strong>1.6.0</strong> to <strong>1.8.2</strong> (eleven input types, <code>ohlc</code> plot groups, draws, background and bar colours, instrument tick on the calculation context, and the stop-limit drag fix that had been sending <code>price: 0</code> to the broker)</li><li><code>zmq==0.0.0</code> removed from <code>pyproject.toml</code>, <code>requirements.txt</code> and <code>requirements-nginx.txt</code>. It is a placeholder package on PyPI shipping no code; the <code>zmq</code> module every call site imports comes from <code>pyzmq</code>, which is already pinned.</li></ul><p>No other Python dependencies changed. The pinned <code>tradeboard</code> SDK stays at 2.0.3.</p><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> - release management; the eventlet boundary sweep (#1402, #1473, #1569) and its regression suites; the four-pass broker credential redaction sweep and the <code>SensitiveDataFilter</code> Bearer defect; Dhan master contract, symbol construction, NCO segment and unsubscribe fixes (#1929, #1930, #1932, #1934); runtime-loaded user chart indicators (#1923); the charting terminal from <code>tradeboard-charts</code> 1.6.0 to 1.8.2, replay, settings dialog, chart sync, history cache, undo/redo and the indicator browser; Flow node-contract audit across all 61 types, payload-driven order fields, clock-anchored interval schedules and the market-hours window; the sandbox session-boundary and T+1 clock fixes; Flattrade collateral (#1936); repository size work (#1896, #1897, #1898); Motilal Oswal WebSocket pooling and leak fixes; CI matrix across Python 3.12-3.14 and Node 20/22/24 (#1894); test database isolation; the <code>chart-indicator</code>, <code>flow-builder</code> and <code>verify</code> skills.</li><li><strong>@Kalaiviswa</strong> - Motilal Oswal plugin repair and modernisation (#1912); GTT for Angel One, Fyers and Upstox (#1922); Angel and Zerodha holdings LTP, average price and null-row resilience (#1917, #1919); Dhan unsubscribe and master contract PRs (#1932, #1934); TradeSmart order tagging and the 100/sec quote budget (#1928); Flow MCX commodity options, leg-by-leg multi-leg baskets and NRML defaults on derivative segments (#1904, #1909); the Shoonya wrong-instrument quote guard.</li><li><strong>@santhiprakash (Santhi Prakash)</strong> - sandbox position-book session boundary in the database clock (#1789); MIS square-off by <code>updated_at</code> session boundary (#1801); concurrent position update serialization (#1808); dropped unused <code>market_data</code> secondary indexes in Historify (#1803).</li><li><strong>@siddharthg2309 (Siddharth Gouthaman)</strong> - history source allowlist at the service entry point (#1875); client error report URL sanitization on both boundaries (#1886); <code>aria-current</code> on navigation links (#1880); option chain view mode toggle coverage (#1876).</li><li><strong>@solstxce</strong> - API key redaction in core service logs (#1914); credential removal from broker logs (#1916).</li><li><strong>@nightcityblade</strong> - client errors for invalid calendar dates (#1861); completed CONTRIBUTING table of contents (#1883).</li><li><strong>@WilliamK112 (Ching Wei Kang)</strong> - documentation-only contribution workflow (#1907); rate limiter expiry at the window boundary (#1868).</li><li><strong>@ANONYMOUSZED-beep (Arun)</strong> - <code>CORS_ENABLED=FALSE</code> honoured instead of falling through to the flask-cors wildcard (#1860).</li><li><strong>@Narasimha722 (NarasimhaReddy)</strong> - strike interval and option type validation at the option resolver boundary (#1829).</li><li><strong>@Pragitics (Pragit R V)</strong> - multi-option Greeks batch state keyed by leg index (#1885).</li><li><strong>@Meraj-08 (Md Meraj Alam)</strong> - eliminated contradictory hard-coded broker plugin counts (#1906).</li><li><strong>@K-PRAGALATHAN (PRAGALATHAN K)</strong> - converted the history format reconnaissance script into hermetic pytest coverage (#1887).</li><li><strong>@NavadeepDj (NavadeepDJ)</strong> - type hints and a 37-case suite for the data schema validators (#1864).</li><li><strong>@suhaslord (Suhas)</strong> - contributor test commands aligned with CI (#1889).</li><li><strong>@thaildhe172591 (Luu Thai)</strong> - corrected frontend build-artifact and Node guidance in CONTRIBUTING (#1863).</li><li><strong>@yiheng-kkk</strong> - replaced obsolete <code>/react</code> frontend routes in CONTRIBUTING (#1867).</li><li><strong>@PadmaBalajiL (Padma Balaji Leelavinodhan)</strong> - devsprint participants list (#1814).</li><li><strong>@cracker314</strong> - devsprint participants list (#1817).</li></ul><p>Thank you to everyone who filed an issue, reproduced a defect or reviewed a pull request this cycle. The eventlet work in particular rests on three separate reports (#1402, #1473, #1569) filed months apart by users who had every reason to believe the problem was on their own machine.</p><hr><p><strong>Upgrading</strong></p><p><code>git pull</code> on <code>main</code> is the canonical path - <code>frontend/dist</code> is tracked there and rebuilt by CI, so no Node.js is needed on the server. The compressed <code>.br</code> and <code>.gz</code> asset variants are no longer committed and are regenerated at startup instead, so the first boot after this upgrade spends a few seconds on that and every later boot finds them current.</p><p>Run <code>cd upgrade &amp;&amp; uv run migrate_all.py</code> on an existing installation. This release ships migration 012, which drops three unused secondary indexes on Historify&#39;s <code>market_data</code> table: none of them leads with <code>symbol</code>, so the planner never selected them, while their DuckDB ART index memory stayed fully resident and caused out-of-memory failures on large 1-minute backfills (#1779). The drop roughly halves the database file and speeds up ingest about 1.6x at no query cost. Use <code>--status</code> first if you want to see what would change.</p><p><strong>Dhan users:</strong> 7,190 NSE equity symbols gain a series suffix in this release, so ELECTCAST now names the equity and ELECTCAST-W1 its warrant. The master contract clears and reloads on your next login, so no migration is needed, but a strategy referencing a non-EQ series by its bare symbol has to be updated. Most of the affected rows are bonds and government securities; the BE, SM and ST series include tradable equities, so check those.</p>',23)])])}const u=o(r,[["render",s]]);export{p as __pageData,u as default};
