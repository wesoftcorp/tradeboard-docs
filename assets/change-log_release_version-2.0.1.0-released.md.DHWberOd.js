import{_ as o,o as t,c as s,a2 as a}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Version 2.0.1.0 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.0-released.md","filePath":"change-log/release/version-2.0.1.0-released.md"}'),i={name:"change-log/release/version-2.0.1.0-released.md"};function n(d,e,r,l,c,p){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="version-2-0-1-0-released" tabindex="-1">Version 2.0.1.0 Released <a class="header-anchor" href="#version-2-0-1-0-released" aria-label="Permalink to &quot;Version 2.0.1.0 Released&quot;">​</a></h1><p><strong>Date: 3rd May 2026</strong></p><p><strong>Major Feature Release: Remote MCP — Self-Hosted OAuth 2.1 + MCP HTTP/SSE Server for ChatGPT, Claude.ai, and Claude Mobile, Plus Per-Purpose 2FA Enforcement, Symbol Search Expansion, Admin Diagnostics, and Zerodha NCO/GLOBAL_INDEX Support</strong></p><p>This is the biggest release in the 2.0.x line, covering <strong>20+ commits</strong> since v2.0.0.9. The headline change is <strong>Remote MCP</strong> — a self-hosted OAuth 2.1 + Model Context Protocol HTTP/SSE transport that lets hosted AI clients (ChatGPT.com, Claude.ai, Claude iOS / Android) connect to your Tradeboard install over HTTPS with the same 40 tools the local stdio MCP already exposes. The local stdio MCP (Claude Desktop, Cursor, Windsurf) is <strong>untouched</strong> — Remote MCP is a parallel, opt-in transport, off by default, that ships behind two enabler scripts (one for native Ubuntu, one for Docker) and a full admin operations console at <code>/admin/remote-mcp</code>. Alongside Remote MCP, this release lands per-purpose TOTP enforcement (login / MCP / password reset), a multi-exchange Symbol Search expansion (issue #1326), an admin Diagnostics page with downloadable system reports, and Zerodha NCO + GLOBAL_INDEX exchange support.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Remote MCP — Hosted AI Clients via OAuth</strong> — Brand-new <code>/mcp</code> HTTP/SSE transport with full OAuth 2.1 + PKCE, Dynamic Client Registration (DCR), JWKS-published RS256 JWTs, refresh-token rotation with reuse-detection family revocation, and per-token-per-scope rate limits. ChatGPT-compatible discovery (<code>/.well-known/oauth-authorization-server</code>, <code>/.well-known/oauth-protected-resource</code>), claude.ai-compatible scope flow. Off by default; turned on by <code>install/enable-remote-mcp.sh</code> (native) or <code>install/enable-remote-mcp-docker.sh</code> (Docker).</li><li><strong>Per-purpose 2FA enforcement</strong> — TOTP can now be required independently for <strong>dashboard login</strong>, <strong>MCP authorization (<code>write:orders</code> consent)</strong>, and <strong>password reset</strong> — set the master switch in Profile → TOTP, then pick which purposes apply. Saving requires a fresh TOTP code in the same request to prove authenticator access for both enabling and disabling.</li><li><strong>Admin → Remote MCP operations console</strong> — Approve / revoke DCR clients, browse the full MCP tool-call audit log (<code>log/mcp.jsonl</code>) by tool / scope / outcome, and a one-click <strong>Kill switch</strong> that atomically revokes every refresh token across every approved client.</li><li><strong>Symbol Search rewrite (#1326)</strong> — Multi-exchange + multi-instrumenttype filtering, CSV download, AmiBroker / TradingView / Python / Excel copy formats, lifted the 500-row hard cap, exchange-only browse mode, and per-user search history.</li><li><strong>Admin Diagnostics page</strong> — <code>/admin/diagnostics</code> shows live system info (Python / Flask versions, DB sizes, broker session state), a paginated error browser over <code>log/errors.jsonl</code>, and a one-click downloadable diagnostic bundle (env-redacted) for support tickets.</li><li><strong>Zerodha NCO + GLOBAL_INDEX exchanges</strong> — NCO (NSE Commodities) and GLOBAL_INDEX (US30 / JAPAN225 / HANGSENG, plus GIFTNIFTY from NSE IFSC) now route correctly through the master contract download and quote endpoints. Quote-only on GLOBAL_INDEX (no orders by exchange convention).</li><li><strong>&quot;Virtual / Paper&quot; → &quot;Sandbox&quot; rename</strong> — All in-product copy, docs, and SDK examples now uniformly say <em>Sandbox</em> (the database, blueprint, and API endpoint names were already <code>sandbox</code> — only display strings were inconsistent).</li><li><strong>Platform version bump</strong> — <code>2.0.0.9</code> → <code>2.0.1.0</code>. <code>.sample.env</code> <code>ENV_CONFIG_VERSION</code> <code>1.0.6</code> → <code>1.0.7</code>. SDK pin (<code>tradeboard==1.0.49</code>) unchanged.</li></ul><hr><p><strong>Remote MCP — feature deep dive</strong></p><p>Remote MCP brings the Tradeboard MCP toolset to <strong>hosted AI clients</strong> over the public internet. Same 40 tools as the local stdio integration, exposed at <code>https://yourdomain.com/mcp</code> with full OAuth 2.1.</p><p><strong>Architecture in one sentence:</strong> the hosted client (ChatGPT / Claude.ai) only ever holds an OAuth Bearer JWT signed by your server&#39;s RS256 keypair; tool dispatch on the server side reuses your existing <code>/api/v1/*</code> API key (looked up server-side at boot) over loopback. The hosted client never sees your API key or your broker tokens.</p><p><strong>OAuth foundation (Phase 2a + 2c + 2d)</strong> — <code>e86cf450f</code>, <code>0f4f71f0</code>-derived family, <code>cb9350f27</code>:</p><ul><li>RS256 keypair generated and rotated via <code>utils/oauth_keys.py</code>; public set published at <code>/oauth/jwks.json</code></li><li>Dynamic Client Registration at <code>/oauth/register</code> (RFC 7591) — clients land in a <em>pending</em> bucket, gated behind admin approval (default <code>MCP_OAUTH_REQUIRE_APPROVAL=True</code>)</li><li>Authorization Code + PKCE-S256 only — <code>plain</code> is not advertised; <code>alg=none</code> and <code>alg=HS256</code> JWTs are rejected by the verifier</li><li>Refresh-token rotation with <strong>family revocation</strong> (RFC 6749 §10.4): single-use, atomic <code>UPDATE ... WHERE revoked_at IS NULL</code>, replay of a revoked token revokes the entire family</li><li><code>/oauth/revoke</code> for explicit token retirement, <code>/oauth/token</code> for code-exchange and refresh</li><li>Argon2 hashing with <code>API_KEY_PEPPER</code> for client_secret + refresh-token storage in <code>database/oauth_db.py</code></li></ul><p><strong>Per-purpose 2FA (Phase 2b + Phase 2 UI)</strong> — <code>dbc595e88</code>, <code>ff44adf46</code>:</p><ul><li>Four new boolean columns on <code>users</code>: <code>totp_enabled</code> + <code>totp_required_for_login</code> + <code>totp_required_for_mcp</code> + <code>totp_required_for_password_reset</code></li><li><code>is_totp_required_for(purpose)</code> helper centralizes the gating decision</li><li>Login flow: POST <code>/auth/login</code> → on TOTP requirement, returns <code>totp_required</code> flag + temp ticket → POST <code>/auth/login/totp</code> with code</li><li>Configure flow at <code>/auth/2fa/configure</code> requires a fresh TOTP in the same request (proves authenticator access for <em>both</em> enabling and disabling — closes the &quot;stolen session can disable 2FA&quot; hole)</li><li>React UI: <code>TwoFactorEnforcement.tsx</code> profile-page toggle, <code>Login.tsx</code> TOTP step, <code>ResetPassword.tsx</code> TOTP path</li></ul><p><strong>HTTP transport (Phase 3)</strong> — <code>7d805af15</code>:</p><ul><li>JSON-RPC 2.0 dispatcher at <code>/mcp</code> (POST) + Server-Sent Events transport for streaming</li><li>Per-token-per-scope sliding-window rate limits (<code>MCP_RATE_LIMIT_READ</code> / <code>MCP_RATE_LIMIT_WRITE</code>)</li><li>CORS allowlist (default: <code>https://claude.ai,https://chatgpt.com</code>) with <code>WWW-Authenticate</code> exposed via <code>Access-Control-Expose-Headers</code> so browsers can read the realm hint on 401</li><li>Audit log writes to <code>log/mcp.jsonl</code> — every tool call with timestamp, JTI, scope, outcome, latency, <code>params_hash</code> (raw params are deliberately <em>not</em> logged)</li><li>Tool registry (<code>utils/mcp_tool_registry.py</code>) maps all 40 tools to scopes (<code>read:market</code> / <code>read:account</code> / <code>write:orders</code>) and exposes FastMCP-generated JSON schemas in the <code>tools/list</code> response</li><li>Pre-flight refusal: server refuses to boot with <code>MCP_HTTP_ENABLED=True</code> and <code>FLASK_DEBUG=True</code> together (debug tracebacks would leak bearer tokens)</li></ul><p><strong>Install integration (Phase 4)</strong> — <code>b36637b5e</code>, <code>90897a550</code>:</p><ul><li><code>install/enable-remote-mcp.sh</code> — native Ubuntu enabler. Detects all <code>tradeboard-*</code> systemd services, refuses if <code>FLASK_DEBUG=True</code>, backs up <code>.env</code>, sets the four MCP keys, runs <code>upgrade/migrate_all.py</code>, restarts the service, and probes the discovery / JWKS / <code>/mcp/healthz</code> endpoints to confirm boot.</li><li><code>install/enable-remote-mcp-docker.sh</code> — Docker enabler. Walks <code>/opt/tradeboard/*/docker-compose.yaml</code>, picks a stack, backs up the bind-mounted <code>.env</code>, updates keys, restarts the container (whose <code>start.sh</code> runs migrations automatically), and runs the same smoke probe.</li><li><code>install/Remote-MCP-readme.md</code> — operator-focused install guide with same-domain Mode 1 (automated) and subdomain Mode 2 (manual nginx + certbot recipe), threat model, and disabling instructions.</li><li>New <code>docs/userguide/remote-mcp.md</code> — end-user guide for connecting ChatGPT (Settings → Apps → New App BETA → Advanced OAuth → DCR) and Claude.ai (Settings → Connectors → + → Add custom connector).</li></ul><p><strong>Admin operations (Phase 5)</strong> — <code>8be942a7c</code>:</p><ul><li><code>/admin/remote-mcp</code> React page with three tables (Pending / Approved / Revoked) + audit viewer + kill switch</li><li><code>GET /admin/api/oauth/clients</code> lists clients by status; approve/revoke endpoints require typed-string confirmation for destructive actions</li><li><code>GET /admin/api/mcp/audit</code> — paginated audit log over <code>log/mcp.jsonl</code> with whitelisted query keys</li><li><code>POST /admin/api/mcp/kill-switch</code> — typed-string confirm, atomically revokes every refresh token across every approved client (read-only access tokens still expire on their existing 15-minute TTL)</li></ul><p><strong>Security audit fixes + ChatGPT compatibility</strong> — <code>926a597bf</code>:</p><ul><li>Migrated from deprecated <code>authlib.jose</code> → <code>joserfc</code> with explicit <code>algorithms=[&quot;RS256&quot;]</code> pinning</li><li><code>MCP_PUBLIC_URL</code> is a hard requirement when <code>MCP_HTTP_ENABLED=True</code> — collapsing it to empty would let JWTs minted on instance A be replayed against instance B&#39;s loopback</li><li><code>error_detail</code> in MCP responses replaced with generic <em>&quot;Tool execution failed&quot;</em> — the full detail is in the audit log only, so SQL errors / stack traces don&#39;t leak to the model</li><li>Added path-relative discovery alias <code>/mcp/.well-known/oauth-protected-resource</code> because ChatGPT fetches it that way (RFC 9728 says root-relative is canonical, but ChatGPT does both)</li><li>Default <code>MCP_RATE_LIMIT_WRITE</code> raised from 5/min → 50/min based on real ChatGPT/Claude usage patterns</li><li>Removed the vestigial <code>MCP_OAUTH_LOGIN_AUTH_LEVEL</code> env var (replaced by the per-purpose 2FA flags)</li></ul><p><strong>CSP and consent screen fixes</strong> — <code>52c96a11a</code>, <code>5f60b4817</code>, <code>3175a4104</code>:</p><ul><li>Per-page CSP on the consent screen sets <code>form-action</code> to allow exactly the registered redirect_uri&#39;s origin — fixes the form-submit block when the global CSP middleware would otherwise refuse the cross-origin POST</li><li>Global CSP middleware (<code>csp.py</code>) now respects view-set CSP headers (won&#39;t overwrite if a header is already set)</li><li><code>tools/list</code> response now includes the real Pydantic-generated JSON schemas (reach into FastMCP&#39;s <code>_tool_manager._tools</code>) so ChatGPT stops hallucinating parameter names like <code>product_type</code> instead of <code>product</code></li><li>Removed Jinja-time <code>csrf_token()</code> dependency on the consent template — render the token via <code>_csrf_token_value()</code> helper inside the view so timing-of-globals isn&#39;t an issue</li></ul><p><strong>Three new admin endpoints + four new database models:</strong></p><table tabindex="0"><thead><tr><th>New</th><th>Purpose</th></tr></thead><tbody><tr><td><code>database/oauth_db.py</code></td><td><code>OAuthClient</code>, <code>OAuthRefreshToken</code> (with <code>family_id</code> / <code>parent_id</code>), <code>OAuthSigningKey</code></td></tr><tr><td><code>database/user_db.py</code></td><td>4 boolean columns + <code>find_user_by_exact_username()</code></td></tr><tr><td><code>utils/oauth_keys.py</code></td><td>RS256 keypair generation + rotation + <code>public_jwks()</code></td></tr><tr><td><code>utils/oauth_tokens.py</code></td><td><code>issue_access_token</code>, <code>rotate_refresh_token</code>, <code>verify_access_token</code></td></tr></tbody></table><p><strong>Default security posture:</strong></p><table tabindex="0"><thead><tr><th>Setting</th><th>Default</th><th>Why</th></tr></thead><tbody><tr><td><code>MCP_HTTP_ENABLED</code></td><td><code>False</code></td><td>Off until you opt in</td></tr><tr><td><code>MCP_OAUTH_REQUIRE_APPROVAL</code></td><td><code>True</code></td><td>DCR clients land pending until admin approves</td></tr><tr><td><code>MCP_OAUTH_WRITE_SCOPE_ENABLED</code></td><td><code>False</code></td><td>Order placement unreachable until you flip the switch</td></tr><tr><td><code>MCP_RATE_LIMIT_READ</code></td><td><code>60/min</code></td><td>Per-token sliding window</td></tr><tr><td><code>MCP_RATE_LIMIT_WRITE</code></td><td><code>50/min</code></td><td>Per-token sliding window</td></tr><tr><td><code>MCP_MAX_ORDER_QTY</code></td><td><code>0</code> (no cap)</td><td>Recommend setting a sane cap</td></tr></tbody></table><hr><p><strong>Symbol Search expansion (#1326)</strong></p><ul><li><code>232c637fb</code> — <code>feat(search): lift 500 cap, allow exchange-only browse, add search history</code></li><li><code>f0c03eede</code> — <code>feat(search): multi-exchange/instrumenttype, CSV download, copy formats</code></li></ul><p>The Symbol Search page now supports filtering across multiple exchanges and multiple instrument types in a single query, browsing an entire exchange without entering a search term, downloading the result set as CSV, and copying selections in AmiBroker / TradingView / Python / Excel formats. The previous 500-row hard cap is gone — large result sets stream incrementally. Per-user search history is preserved across sessions.</p><hr><p><strong>Admin / Operations</strong></p><ul><li><code>566113d49</code> — <code>feat(admin): add Diagnostics page with system info, error browser, and downloadable report</code></li></ul><p><code>/admin/diagnostics</code> consolidates the moving parts of &quot;what&#39;s going on with this install&quot; into a single React page: Python / Flask / SQLAlchemy versions, database sizes for all six SQLite/DuckDB databases, broker session state, configured env vars (with secrets redacted), a paginated browser over <code>log/errors.jsonl</code> with stack traces and Flask request context, and a one-click <strong>Download diagnostic bundle</strong> that produces an env-redacted ZIP suitable for attaching to a support ticket.</p><hr><p><strong>Brokers</strong></p><p><strong>Zerodha</strong></p><ul><li><code>6bc37381e</code> — <code>feat(zerodha): support NCO and GLOBAL_INDEX exchanges</code></li></ul><p>Adds two Zerodha-only exchange codes:</p><ul><li><strong>NCO</strong> — NSE Commodities (Zerodha&#39;s symbol format differs from NCDEX; this fix routes correctly through the master contract download)</li><li><strong>GLOBAL_INDEX</strong> — US30, JAPAN225, HANGSENG, plus <code>GIFTNIFTY</code> from NSE IFSC. Quote-only by exchange convention; orders are not supported on GLOBAL_INDEX.</li></ul><hr><p><strong>Documentation</strong></p><ul><li><code>199c544d1</code> — <code>docs: rename &quot;virtual/paper trading&quot; to &quot;sandbox trading&quot; terminology</code></li><li><code>6efaf1655</code> — <code>docs: rename remaining &quot;virtual&quot; trading terms to &quot;sandbox&quot; equivalents</code></li><li><code>500e27cbb</code> — <code>docs: add Remote MCP user guide for ChatGPT and Claude.ai</code></li><li>New <code>install/Remote-MCP-readme.md</code> — operator-focused install + threat model</li><li>New <code>docs/prd/remote-mcp.md</code> — full product requirements doc with architecture, MUST/SHOULD/COULD security controls</li></ul><p>The &quot;virtual / paper trading&quot; rename only touched display strings — the database (<code>db/sandbox.db</code>), blueprint (<code>blueprints/sandbox.py</code>), and API endpoints (<code>/api/v1/sandbox/*</code>) were already named <code>sandbox</code>. The rename closes a long-standing inconsistency between in-product copy and the underlying schema.</p><hr><p><strong>Configuration changes</strong></p><p><code>.sample.env</code>:</p><ul><li><code>ENV_CONFIG_VERSION</code> <code>1.0.6</code> → <code>1.0.7</code></li><li>New section: <code>MCP_HTTP_ENABLED</code>, <code>MCP_PUBLIC_URL</code>, <code>MCP_OAUTH_REQUIRE_APPROVAL</code>, <code>MCP_OAUTH_WRITE_SCOPE_ENABLED</code>, <code>MCP_HTTP_CORS_ORIGINS</code>, <code>MCP_HTTP_IP_ALLOWLIST</code>, <code>MCP_OAUTH_ACCESS_TTL</code>, <code>MCP_OAUTH_REFRESH_TTL</code>, <code>MCP_OAUTH_CODE_TTL</code>, <code>MCP_RATE_LIMIT_READ</code>, <code>MCP_RATE_LIMIT_WRITE</code>, <code>MCP_MAX_ORDER_QTY</code></li><li>Removed: <code>MCP_OAUTH_LOGIN_AUTH_LEVEL</code> (vestigial — replaced by per-purpose 2FA flags)</li></ul><p><code>pyproject.toml</code>:</p><ul><li><code>version = &quot;2.0.1.0&quot;</code></li><li>SDK pin (<code>tradeboard==1.0.49</code>) unchanged</li></ul><p><code>utils/version.py</code>:</p><ul><li><code>VERSION = &quot;2.0.1.0&quot;</code></li></ul><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># update.sh runs migrate_all.py — schema changes for the OAuth + 2FA columns</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># are applied automatically. Remote MCP stays disabled by default.</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The container&#39;s start.sh runs migrate_all.py before gunicorn boots.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Remote MCP stays disabled by default.</span></span></code></pre></div><p><strong>To enable Remote MCP (after upgrading):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Native Ubuntu</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/enable-remote-mcp.sh</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Docker</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/enable-remote-mcp-docker.sh</span></span></code></pre></div><p>Both enabler scripts are idempotent and back up <code>.env</code> before any change. They print a one-liner rollback command if the smoke probe fails.</p><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management, Remote MCP architecture and full OAuth 2.1 implementation (5 phases — scaffold → OAuth foundation → 2FA wiring → discovery + JWKS + DCR → /authorize + /token + /revoke → Phase 2 UI → HTTP/SSE transport → install integration → admin UI), security audit + joserfc migration, CSP fixes for the consent flow, ChatGPT-compatibility hardening (path-relative discovery, real tool schemas, generic error_detail), Docker enabler, install integration, comprehensive PRD + operator docs + end-user docs, Symbol Search rewrite (#1326), Admin Diagnostics page, Zerodha NCO + GLOBAL_INDEX support, and the &quot;virtual → sandbox&quot; terminology cleanup.</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Remote MCP guide</strong>: <a href="https://docs.algo.wesoftcorp.com/mcp/remote-mcp" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com/mcp/remote-mcp</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul><hr>`,73)])])}const g=o(i,[["render",n]]);export{u as __pageData,g as default};
