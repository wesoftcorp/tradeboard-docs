import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Security Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"security/architecture.md","filePath":"security/architecture.md"}'),i={name:"security/architecture.md"};function n(s,e,d,c,l,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="security-architecture" tabindex="-1">Security Architecture <a class="header-anchor" href="#security-architecture" aria-label="Permalink to &quot;Security Architecture&quot;">​</a></h1><p>Tradeboard is a self-hosted trading application. Its controls protect the application boundary, but operators remain responsible for host security, TLS, proxy configuration, secret handling, backups, and broker-account controls.</p><h3 id="authentication-and-sessions" tabindex="-1">Authentication and Sessions <a class="header-anchor" href="#authentication-and-sessions" aria-label="Permalink to &quot;Authentication and Sessions&quot;">​</a></h3><ul><li>Application passwords are Argon2-hashed with the installation pepper.</li><li>TOTP can be required independently for login, Remote MCP write authorization, and password reset.</li><li>Application sessions use HTTP-only, SameSite cookies; the Secure attribute is enabled for HTTPS deployments.</li><li>A user can have at most five active application sessions.</li><li>Session-status polling refreshes <code>last_seen</code> no more than once every 30 seconds.</li><li>Broker-session expiry preserves a valid application session and redirects the user to reconnect the broker.</li><li>Password changes revoke active device sessions.</li></ul><p>The application session and the installation-wide active broker session are distinct.</p><h3 id="api-keys-and-broker-tokens" tabindex="-1">API Keys and Broker Tokens <a class="header-anchor" href="#api-keys-and-broker-tokens" aria-label="Permalink to &quot;API Keys and Broker Tokens&quot;">​</a></h3><p>Tradeboard generates one current 64-character hexadecimal API key per user. The database stores an Argon2 hash for verification and an encrypted copy for authenticated retrieval. Regenerating the key invalidates the previous value immediately.</p><p>Broker authentication and feed tokens are encrypted before database storage. Broker API credentials configured in <code>.env</code> are not an encrypted database secret; protect the file with host permissions and exclude it from source control and support bundles.</p><p>Most <code>/api/v1</code> routes require <code>apikey</code> in the JSON body. The REST namespace is exempt from browser CSRF because it uses API-key authentication, while session-authenticated state-changing routes retain CSRF protection unless explicitly exempted for a documented callback or webhook.</p><h3 id="request-boundary-and-ip-controls" tabindex="-1">Request Boundary and IP Controls <a class="header-anchor" href="#request-boundary-and-ip-controls" aria-label="Permalink to &quot;Request Boundary and IP Controls&quot;">​</a></h3><p><code>SecurityMiddleware</code> checks the resolved client IP against bans stored in <code>logs.db</code> before Flask handles a request. Automatic banning is disabled by default. Current persisted defaults are:</p><table tabindex="0"><thead><tr><th>Setting</th><th style="text-align:right;">Default</th></tr></thead><tbody><tr><td>404 threshold in 24 hours</td><td style="text-align:right;">100</td></tr><tr><td>404 ban duration</td><td style="text-align:right;">0 hours (permanent)</td></tr><tr><td>Invalid-key threshold in 24 hours</td><td style="text-align:right;">100</td></tr><tr><td>Invalid-key ban duration</td><td style="text-align:right;">0 hours (permanent)</td></tr><tr><td>Repeat-offender limit</td><td style="text-align:right;">2 bans</td></tr></tbody></table><p>The dashboard can change these values and enable automatic banning. Localhost addresses are excluded from automatic bans.</p><p><code>TRUST_PROXY_HEADERS</code> is false by default. Enable it only when a controlled reverse proxy is the sole route to Tradeboard; otherwise a direct client can spoof forwarded IP headers. The application does not implement a CIDR allowlist.</p><h3 id="browser-and-transport-controls" tabindex="-1">Browser and Transport Controls <a class="header-anchor" href="#browser-and-transport-controls" aria-label="Permalink to &quot;Browser and Transport Controls&quot;">​</a></h3><p>Tradeboard configures CSRF, CORS, CSP, referrer, permissions, frame, and related response headers in the application. Public deployments should terminate TLS at a maintained reverse proxy and restrict direct access to Flask, WebSocket, and ZeroMQ ports.</p><p>Security cookies become HTTPS-only when the configured public URL uses HTTPS. A misconfigured HTTP deployment does not gain transport security merely because the application supports secure-cookie settings.</p><h3 id="traffic-and-audit-data" tabindex="-1">Traffic and Audit Data <a class="header-anchor" href="#traffic-and-audit-data" aria-label="Permalink to &quot;Traffic and Audit Data&quot;">​</a></h3><p>Traffic logging stores metadata: timestamp, client IP, method, path, status, duration, host, optional middleware error, and user ID when available. It does not store request bodies, response bodies, headers, user agents, or a processing timeline.</p><p>Order, analyzer, login, MCP, latency, health, and notification domains have their own event or persistence paths. No single traffic table is a complete audit trail of every application action.</p><h3 id="websocket-and-mcp-boundaries" tabindex="-1">WebSocket and MCP Boundaries <a class="header-anchor" href="#websocket-and-mcp-boundaries" aria-label="Permalink to &quot;WebSocket and MCP Boundaries&quot;">​</a></h3><p>The public market-data WebSocket requires an Tradeboard API-key authentication message within the configured grace period. Subscription capacity, queue size, ping interval, and timeout are bounded by configuration. The internal ZeroMQ endpoint is unauthenticated and must remain private.</p><p>Remote MCP is opt-in. Its OAuth implementation uses exact redirect-URI validation, S256-only PKCE, signed access tokens, refresh-token rotation, and token-family revocation when refresh-token reuse is detected. <code>.sample.env</code> keeps the transport off but has approval off and write scope enabled if only the master switch is changed; the Docker helper applies the stricter inverse. Write scope can require fresh TOTP.</p><h3 id="data-stores" tabindex="-1">Data Stores <a class="header-anchor" href="#data-stores" aria-label="Permalink to &quot;Data Stores&quot;">​</a></h3><p>Tradeboard uses five SQLite databases and one DuckDB store. Isolation limits unrelated lock contention and persistence scope; it is not database-wide encryption. Back up and protect all six stores together with <code>.env</code>, Fernet salt/key material, and MCP signing keys.</p><h3 id="operational-baseline" tabindex="-1">Operational Baseline <a class="header-anchor" href="#operational-baseline" aria-label="Permalink to &quot;Operational Baseline&quot;">​</a></h3><ol><li>Use the supported installer and keep Tradeboard, the OS, reverse proxy, and dependencies updated.</li><li>Enable TLS and TOTP before exposing a deployment publicly.</li><li>Keep <code>TRUST_PROXY_HEADERS</code> off unless the proxy boundary is enforced.</li><li>Protect <code>.env</code>, databases, keys, strategy files, and backups.</li><li>Review <code>/logs/security</code>, <code>/logs/traffic</code>, login activity, and broker activity after unexpected events.</li><li>Test automatic-ban thresholds before enabling permanent bans.</li><li>Retain direct access to the broker terminal and host console for incident response.</li></ol>',27)])])}const b=t(i,[["render",n]]);export{u as __pageData,b as default};
