import{_ as t,o,c as a,a2 as n}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.2.5 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.2.5-released.md","filePath":"change-log/release/version-2.0.2.5-released.md"}'),r={name:"change-log/release/version-2.0.2.5-released.md"};function s(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="version-2-0-2-5-released" tabindex="-1">Version 2.0.2.5 Released <a class="header-anchor" href="#version-2-0-2-5-released" aria-label="Permalink to &quot;Version 2.0.2.5 Released&quot;">​</a></h1><p><strong>Date: 14th September 2026</strong></p><p><strong>Feature Release: adds a voice surface to the agent, so the platform can be asked a question out loud and answer on screen; ships order placement switched off by default; carries the charting terminal from tradeboard-charts 2.1.7 to 2.2.0 with all 85 drawing tools; and makes the 5paisa XTS, Tradejini, 5paisa and Kotak feeds survive a full symbol book</strong></p><p>29 commits since v2.0.2.4, excluding the automated frontend build commits. <strong>This release requires a database migration</strong> - run <code>cd upgrade &amp;&amp; uv run migrate_all.py</code> after pulling. No configuration changes are needed. The platform version moves from 2.0.2.4 to 2.0.2.5; the pinned <code>tradeboard</code> SDK stays at 2.0.5.</p><p>The release is dominated by one thread. The agent at <code>/agent</code> gained a third surface beside chat and chart: a spoken one. It is not a second agent. The speech model hears and speaks and decides nothing, and every answer it reads out comes from the model configured at <code>/agent/config</code>, through the same toolkits, the same risk guard and the same audit rows a typed question goes through. Most of the commits after it are the consequences of putting a microphone in front of a trading platform, and several of them are corrections to decisions made in the commit before: what happens to a session nobody is using, what an order actually requires before it reaches a broker, what a spoken error is allowed to say, and what must never be written down.</p><p>The second thread is the broker feeds. Three adapters were sending one HTTP request per symbol at subscribe time, which is a thousand sequential round-trips on a full book and the same again on every reconnect; one of them tore down the entire feed when a single symbol was dropped.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Voice surface on <code>/agent</code> and <code>/agent/config</code> (<code>d642b4cef</code>)</strong> - <code>gpt-live-1</code> is minted in client delegation mode, so a spoken turn runs through the same <code>send()</code> the composer uses and renders as an ordinary message with its tool timeline and any chart it drew. No audio passes through the server: the browser holds the WebRTC connection directly with the provider, and the server&#39;s only involvement is one outbound call to mint the session. Design notes in <code>docs/design/56-voice-agent/README.md</code>.</li><li><strong>Order placement is opt-in (<code>7faab9cd6</code>, #2036)</strong> - the agent&#39;s trading switch shipped on, so a fresh install could reach an order tool by typing a sentence with nobody having chosen that. Place, modify and cancel are now not offered to the agent at all until an operator switches them on. An existing installation that never touched the setting picks the new value up on upgrade, in the safe direction. The voice surface keeps a second switch, also off, so enabling trading does not by itself put it on the spoken surface.</li><li><strong>The spoken approval phrase is gone (<code>7faab9cd6</code>, <code>e17d644fa</code>, <code>2ef597bf7</code>)</strong> - a configured secret word was the only way to approve an order out loud, and it cost more than it bought: every approval became a memory test, it was read aloud by the speech model until that was fixed, and it was written to the audit table in plaintext every time it was spoken - the one table this codebase deliberately makes shareable for triage. What protects an order now is the read-back: nothing is approved until the order has been spoken back in full, and the decision stays server-side.</li><li><strong>A spoken order places, instead of being announced (<code>e85747e1b</code>)</strong> - the agent resolved the contract and then said it would place the order without calling the tool. No tool call means no pause, no approval prompt and no order, while the trader has just been told one is on its way. Brevity now governs how a thing is said, never whether it is done.</li><li><strong>The voice surface can draw (<code>5637c4bcb</code>, <code>89fa7f161</code>)</strong> - &quot;show me my order book&quot; was answerable by typing and refused by voice in the same conversation. A spoken turn renders on screen exactly as a typed one does, and the premise of the surface is that the ear takes the conclusion while the screen keeps the detail. <code>render_ui</code> also draws figures the operator supplied in their own message, titled so they cannot be mistaken for an account.</li><li><strong>Every spoken line is recorded, and every session gets a thread (<code>564f63583</code>, <code>707c33bb1</code>, <code>2049320b7</code>)</strong> - a spoken turn previously reached the record only when it needed the agent, so a greeting or an acknowledgement left no trace, and the sidebar only ever asked for chat threads, which made fourteen existing voice conversations unreachable. Lines are now captured under a <code>transcript</code> phase, the first line of a session opens the thread, and the thread renders in the order it happened.</li><li><strong>A quiet microphone hangs up (<code>cd3873f9e</code>)</strong> - an open microphone is billed for as long as it is open, and silence is still audio being streamed. Three minutes of nobody speaking ends the session, the countdown restarts on any sign of life, and the interval is a setting because how long a desk tolerates a quiet microphone is the operator&#39;s call.</li><li><strong>Voice errors are written for a trader (<code>9e424a9d0</code>, <code>85e85893d</code>, <code>66d8a178f</code>)</strong> - an account with no credit produced &quot;The voice provider refused the session (HTTP 500)&quot;, which sent operators through settings that were all correct. Messages now name the cause and the next step in plain words, on the browser half too: no more &quot;secure origin&quot;, &quot;WebRTC&quot; or &quot;SDP&quot;. The rule is recorded in <code>CLAUDE.md</code> under Conventions so it applies to the next feature rather than to this one.</li><li><strong>The agent&#39;s voice is named Vega (<code>fe150ace9</code>)</strong> - chosen to sit far from an order instruction phonetically, and an options Greek reads as deliberate on a trading platform. Only the shipped default moves; an operator&#39;s own name is left alone.</li><li><strong>tradeboard-charts 2.2.0 with all 85 drawing tools (<code>50f94b717</code>)</strong> - advanced channels, pitchforks, Fibonacci and Gann geometry, wavefronts and manual patterns. Menu labels and glyphs follow the installed package through generated metadata, so an upgrade cannot leave the rail describing tools that are no longer there, and the drawing renderer stays lazy. Saved drawing IDs and version 2 documents remain compatible.</li><li><strong>Notes, balloons, comments, signposts, price notes and tables use the content editor (<code>50f94b717</code>)</strong> - font colours reach the rendered letters, content edits preserve table grids and theme defaults, and each editor shows only the controls its tool supports.</li><li><strong>Shared branding with tradeboard-charts 2.1.9 (<code>718d10bfa</code>)</strong> and <strong>2.1.8 (<code>80b24d7f8</code>)</strong> - the terminal&#39;s watermark and chrome come from the engine, removing the local copy.</li><li><strong>TPO and session volume profile restored to the chart-type menu (<code>804eb1e94</code>)</strong> - both are selectable again with their existing settings, saved layouts, live updates and intraday interval handling. TPO now defaults to letters rather than letters and blocks (<code>704b2ca75</code>), and the Objects rail button is a glyph like every other panel on the rail instead of a word spelled down it (<code>40b6bb733</code>).</li><li><strong>5paisa XTS subscriptions batched into one request per mode (<code>c1bc96c90</code>, #2042)</strong> - the XTS API takes an instruments array, but the adapter sent one blocking HTTP POST per symbol, so a 1000-symbol startup was 1000 sequential round-trips and every reconnect replayed the book the same way. Two silent bookkeeping defects had to be fixed for batching to be safe: every LTP and depth unsubscribe was targeting the quote feed, and the tick filter compared an int against a string so it dropped every depth tick.</li><li><strong>5paisa XTS unsubscribe no longer tears down the whole feed (<code>c1bc96c90</code>, #2042)</strong> - it called <code>disconnect()</code> on every invocation, so dropping one symbol killed the feed for every other subscribed symbol and nothing could bring it back. Reachable from the normal path whenever a watchlist entry is removed or a chart switches symbol.</li><li><strong>Kotak always emits <code>ltp</code> in quote and depth payloads (<code>c1bc96c90</code>, #2038)</strong> - it was the only adapter that dropped the key when the price was zero, and mode 2 suppressed the publish entirely. The trading chart subscribes depth alone for tradeable symbols, so a payload without the key reads as &quot;keep polling&quot; and nothing could ever clear the REST fallback.</li><li><strong>Tradejini feed syncs coalesced into one request per feed (<code>4f3a321cd</code>, #2044)</strong> - a subscribe request replaces the server-side symbol list rather than appending to it, so every change has to re-send the complete list, which made subscription O(N^2) in tokens sent: 1000 symbols cost 1000 requests carrying roughly 500k token entries, and teardown cost the same again. Part of #1350.</li><li><strong>5paisa <code>last_snapshot</code> cache bounded (<code>4f3a321cd</code>, #2044)</strong> - nothing ever removed an entry, so the dict grew for the life of the Gunicorn worker, which never restarts. The leak was the slower half: a remembered value merged over a zero field resurfaced on a later re-subscribe as a price that never traded, and across the 3 AM token rollover as the previous day&#39;s.</li><li><strong>Ctrl+C on the development server actually exits (<code>95583fdac</code>)</strong> - it printed a scheduler traceback every five seconds and needed two or three presses. Three causes: six APScheduler instances were never stopped and kept firing into closed thread pools; the websocket proxy thread was non-daemon with its cleanup registered through <code>atexit</code>, which cannot work, since <code>atexit</code> runs only after that same thread is joined; and the health collector slept its whole sampling interval in one call so it could not see its stop flag. Measured on a real server with a live feed: never exited before, 1.5s after.</li><li><strong>The LiteLLM usage warning no longer prints on every turn (<code>932887b87</code>)</strong> - a pydantic serializer warning on every <code>chatgpt/</code> plan turn, several lines each, that nobody can act on. Noise on every turn is worse than it sounds: a warning everybody learns to scroll past includes the next one that matters.</li><li><strong>Documentation</strong> - SDK pin references synced to 2.0.5 (<code>81d71dbbf</code>, #2050); the retired YouTube subscriber badge replaced in the README (<code>3f06c5641</code>); the charting terminal user guide and the <code>chart-indicator</code> skill regenerated against 2.2.0.</li></ul><hr><p><strong>Migration</strong></p><p>This release adds two migration scripts, both idempotent and both supporting <code>--status</code>:</p><ul><li><code>upgrade/migrate_agent_voice.py</code> - seeds the voice settings rows on an installation that has none, leaving an operator&#39;s own values alone.</li><li><code>upgrade/migrate_agent_voice_phrase_removal.py</code> - removes the two retired approval-phrase settings rows.</li></ul><p>Run them the usual way:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate_all.py</span></span></code></pre></div><hr><p><strong>Dependencies</strong></p><ul><li><code>tradeboard-charts</code>: <strong>2.1.7</strong> to <strong>2.2.0</strong> (<code>80b24d7f8</code>, <code>718d10bfa</code>, <code>50f94b717</code>)</li><li>The pinned <code>tradeboard</code> SDK: unchanged at <strong>2.0.5</strong></li><li>No other dependencies changed</li></ul><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajeev Upadhyay)</strong> - release management; the voice surface on <code>/agent</code> and <code>/agent/config</code> and everything that followed it, including order placement made opt-in (#2036), the removal of the spoken approval phrase and its audit-trail leak, the read-back that replaced it, spoken orders that place rather than being announced, drawing from the voice surface, spoken transcripts captured and threaded, the idle hangup, the plain-language rewrite of every voice error, and the two migrations; tradeboard-charts 2.1.8, 2.1.9 and 2.2.0 with all 85 drawing tools and the generated drawing metadata; TPO and session volume profile restored to the chart-type menu, the TPO letters default and the Objects rail icon; the Ctrl+C shutdown fix on the development server; the LiteLLM usage warning filter; the charting terminal user guide and the <code>chart-indicator</code> skill regenerated for 2.2.0.</li><li><strong>@Kalaiviswa</strong> - 5paisa XTS websocket subscriptions batched into one request per mode, with the unsubscribe message-code and tick-filter defects fixed alongside; 5paisa XTS unsubscribe no longer disconnecting the whole feed; Kotak always emitting <code>ltp</code> in quote and depth payloads (#2038); Tradejini feed syncs coalesced into one request per feed (part of #1350); the 5paisa <code>last_snapshot</code> cache bounded on unsubscribe and cleared on disconnect (#2042, #2044).</li><li><strong>@Ayush7614</strong> - SDK pin references synced to 2.0.5 across the API and MCP architecture documentation (#2050).</li></ul><p>Thank you to everyone who filed an issue, reproduced a defect or reviewed a pull request this cycle. #2036 is the clearest example of why that matters: the agent shipped able to place orders on a fresh install, nobody inside the project had read it as a default worth questioning, and it took someone opening an issue to say so.</p><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul>',25)])])}const u=t(r,[["render",s]]);export{g as __pageData,u as default};
