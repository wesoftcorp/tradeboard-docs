import{_ as t,o as a,c as r,a2 as i}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Strategy RMS Engine","description":"","frontmatter":{},"headers":[],"relativePath":"new-features/strategy-rms-engine.md","filePath":"new-features/strategy-rms-engine.md"}'),n={name:"new-features/strategy-rms-engine.md"};function s(o,e,d,l,c,h){return a(),r("div",null,[...e[0]||(e[0]=[i(`<h1 id="strategy-rms-engine" tabindex="-1">Strategy RMS Engine <a class="header-anchor" href="#strategy-rms-engine" aria-label="Permalink to &quot;Strategy RMS Engine&quot;">​</a></h1><p>The Strategy RMS Engine runs configured multi-leg option strategies and signal-driven strategies with durable order intent, per-leg protection, and run-level risk controls. It preserves the operational distinction that matters: an exit request is not a flat position until broker fills confirm it.</p><h2 id="what-it-supports" tabindex="-1">What It Supports <a class="header-anchor" href="#what-it-supports" aria-label="Permalink to &quot;What It Supports&quot;">​</a></h2><h3 id="batch-strategies" tabindex="-1">Batch strategies <a class="header-anchor" href="#batch-strategies" aria-label="Permalink to &quot;Batch strategies&quot;">​</a></h3><p>Batch strategies resolve and enter their configured legs together. They are controlled with <code>start</code> and <code>stop</code>, and can use per-leg stop loss, target, and trailing rules; overall MTM stop loss and target; lock-profit and trail-to-entry controls; scheduled starts and square-off; and daily loss limits.</p><h3 id="signal-strategies" tabindex="-1">Signal strategies <a class="header-anchor" href="#signal-strategies" aria-label="Permalink to &quot;Signal strategies&quot;">​</a></h3><p>Signal strategies act on one configured leg at a time. Alert actions are <code>long_entry</code>, <code>long_exit</code>, <code>short_entry</code>, and <code>short_exit</code>. The first accepted signal for the platform session opens the run; there is no separate <code>start</code> action or <code>mode</code> field for this strategy kind.</p><p>The engine treats a repeated entry or an exit for a position that is not held as a successful no-op. That prevents an alert sender retry from becoming a second position. A direction or leg mismatch is rejected so it can be corrected in the sender or strategy configuration.</p><h2 id="how-a-run-is-managed" tabindex="-1">How A Run Is Managed <a class="header-anchor" href="#how-a-run-is-managed" aria-label="Permalink to &quot;How A Run Is Managed&quot;">​</a></h2><ol><li>The engine records a durable pending order intent before it calls a broker.</li><li>It records the broker acknowledgement against that exact intent, then consumes broker order updates and fills.</li><li>A stop or close request records its intent, submits MARKET exits for the exact held contracts, and remains managed while any fill, retry, or reconciliation is pending.</li><li>Only confirmed-flat ownership and fill evidence finalises the run and writes its realised P&amp;L.</li></ol><p>An <code>overall_target</code> or <code>overall_sl</code> stop reason records the risk rule that initiated the exit, not a promise that the eventual realised P&amp;L equals the threshold. Prices can move while individual MARKET exits are filled.</p><h2 id="modes-and-safety-gates" tabindex="-1">Modes And Safety Gates <a class="header-anchor" href="#modes-and-safety-gates" aria-label="Permalink to &quot;Modes And Safety Gates&quot;">​</a></h2><ul><li>New strategies are sandbox-only. Live trading must be enabled explicitly on the strategy page before any <code>live</code> start is accepted.</li><li>Batch starts require an explicit <code>mode</code> of <code>sandbox</code> or <code>live</code>; there is no default that could accidentally place live orders.</li><li>A leg whose entry has been accepted but is not yet filled is never exited at its configured quantity. Doing so could create a naked position if the entry later cancels. The run remains managed and reports the refusal.</li><li>A refused exit remains retryable. It does not falsely mark the leg flat or finalise the run.</li><li>Risk rules use the basket&#39;s latest known LTP marks. If the WebSocket source becomes stale, the engine uses its polling fallback while it can; stale data is recorded in the event trail.</li></ul><h2 id="monitoring-and-audit" tabindex="-1">Monitoring And Audit <a class="header-anchor" href="#monitoring-and-audit" aria-label="Permalink to &quot;Monitoring And Audit&quot;">​</a></h2><p>The strategy detail page shows the active run and broker-backed orderbook, tradebook, and positions where the broker supports them. A broker response is preferred for broker truth; local strategy rows are retained as a labelled fallback when that source is unavailable.</p><p>Use the strategy&#39;s <strong>Events</strong> and <strong>History</strong> views, or the RESTX API, to investigate a run. In particular, treat these events as operator-action items:</p><table tabindex="0"><thead><tr><th>Event</th><th>Meaning</th></tr></thead><tbody><tr><td><code>run_stop_failed</code></td><td>A stop could not flatten all owned exposure. The run remains open and managed.</td></tr><tr><td><code>order_ack_unrecorded</code></td><td>The broker accepted an order but its acknowledgement was not persisted after retry; it needs reconciliation.</td></tr><tr><td><code>leg_expiry_fallback</code></td><td>The configured expiry rank was unavailable and a nearer expiry was traded.</td></tr><tr><td><code>flip_outgoing_exit_rejected</code></td><td>The old side of a signal flip is still held and remains separately managed.</td></tr></tbody></table><p>The run history records finalised P&amp;L. While a run is still open, live P&amp;L is a mark, not final settlement; a stopped run has zero unrealised P&amp;L.</p><h2 id="alerts-and-api-automation" tabindex="-1">Alerts And API Automation <a class="header-anchor" href="#alerts-and-api-automation" aria-label="Permalink to &quot;Alerts And API Automation&quot;">​</a></h2><p>Use the <strong>Public Strategy Webhook</strong> for alert senders such as TradingView. Its URL includes a high-entropy token, which is the credential. Do not put an API key inside the alert body; protect the token in sender configuration and every proxy/access-log layer, and rotate it if exposure is suspected.</p><p>Use the authenticated <strong>Strategy RMS RESTX API</strong> for lifecycle actions and audits from a script, integration, or dashboard. It can list, inspect, start, stop, close, and query history, but cannot create or edit a strategy, enable live trading, rotate the webhook token, or delete a strategy.</p><ul><li><a href="./../api-documentation/v1/strategy-rms-api/README.html">Strategy RMS RESTX API</a></li><li><a href="./../api-documentation/v1/strategy-rms-api/webhook.html">Public Strategy Webhook</a></li><li><a href="./../developers/design-documentation/39-strategy-module.html">Developer architecture</a></li></ul><h2 id="operational-boundaries" tabindex="-1">Operational Boundaries <a class="header-anchor" href="#operational-boundaries" aria-label="Permalink to &quot;Operational Boundaries&quot;">​</a></h2><p>Strategy RMS validates strategy configuration and manages the orders it owns; it is not a substitute for broker RMS or account-level margin controls. Before using live mode, validate each strategy in sandbox, understand the broker&#39;s product and market-data support, and retain an operational path to inspect or close broker positions during an incident.</p><h2 id="existing-installations-required-migration" tabindex="-1">Existing Installations: Required Migration <a class="header-anchor" href="#existing-installations-required-migration" aria-label="Permalink to &quot;Existing Installations: Required Migration&quot;">​</a></h2><p>An existing Tradeboard installation must run the standard migration runner before starting this version. Take a consistent database backup first (or stop the application before making a filesystem copy), then run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate_all.py</span></span></code></pre></div><p>The Strategy RMS migration is idempotent. It creates the six <code>sm_</code> strategy tables on installations that do not have them and, for earlier Strategy RMS installations, adds nullable durable-order and pending-stop columns plus the ownership index. Existing strategy/order rows are preserved; values that were never recorded on an older row remain <code>null</code> rather than being guessed.</p><p>This migration is required: if it fails, <code>migrate_all.py</code> exits non-zero. Do not start the updated application until it succeeds. See <a href="./../getting-started/upgrade.html">Upgrade Tradeboard</a> for backup and post-upgrade checks.</p>`,29)])])}const u=t(n,[["render",s]]);export{g as __pageData,u as default};
