import{_ as o,o as t,c as s,a2 as r}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.1.3 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.3-released.md","filePath":"change-log/release/version-2.0.1.3-released.md"}'),i={name:"change-log/release/version-2.0.1.3-released.md"};function a(n,e,d,l,c,p){return t(),s("div",null,[...e[0]||(e[0]=[r(`<h1 id="version-2-0-1-3-released" tabindex="-1">Version 2.0.1.3 Released <a class="header-anchor" href="#version-2-0-1-3-released" aria-label="Permalink to &quot;Version 2.0.1.3 Released&quot;">​</a></h1><p><strong>Date: 11th June 2026</strong></p><p><strong>Feature + Performance Release: Arrow Broker Integration (edge.arrow.trade, Full REST + Binary WebSocket Streaming), Order Hot-Path Latency Overhaul (Idle-Gap Orders ~150ms → ~15ms via Connection Keep-Warm, Zero Inline fsync on the Order Path), Broker-Token Daily-Rollover Resilience (WS Self-Heal + Session Kept Intact), an FD/Resource-Leak Audit, the Vite 8 Frontend Toolchain, and a Crypto/Security Hardening Sweep</strong></p><p>This release spans 80+ commits since v2.0.1.2. The headline feature is the <strong>Arrow broker integration</strong> (PR #1494) — a complete REST + WebSocket implementation for edge.arrow.trade covering orders, smart orders, single and basket margin, master contract, quotes/depth/historical data, and a binary market-data stream parser. The headline performance work is an <strong>order hot-path latency overhaul</strong>: a new connection keep-warm service eliminates the ~150ms TCP+TLS handshake that orders paid after idle gaps (verified live: ~150ms → ~15ms broker RTT), and every inline SQLite commit is moved off the order response path. Reliability work makes the <strong>daily ~3:00 AM IST broker-token rollover</strong> a non-event: WebSocket adapters refresh tokens and self-heal on reconnect, and an expired broker token now flags the session for re-authentication instead of destroying it. The codebase also gets a <strong>file-descriptor/resource-leak audit</strong> with fixes and an enforced NullPool engine factory, the frontend moves to <strong>Vite 8</strong> with precompressed assets, and crypto startup validation is hardened.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Arrow broker integration (#1494)</strong> — New broker plugin for <strong>Arrow (edge.arrow.trade)</strong> supporting NSE, BSE, NFO, BFO, CDS, BCD, MCX and index quotes. Full implementation: token authentication, place/modify/cancel/smart orders, Dhan-style single + basket margin routing, master contract rebuilt from the live instrument CSV, REST quotes/multiquotes with batch capping, and full binary quote/depth parsing on the market-data stream. Hardened post-merge with live-trading review fixes and a position cache for smart-order flows.</li><li><strong>Order latency overhaul</strong> — Three changes verified with live orders: <ul><li><strong>Connection keep-warm</strong> (<code>services/broker_keepalive_service.py</code>): the shared httpx pool recycles idle connections after 30s, so any order after a longer gap paid a fresh TCP+TLS handshake — measured ~150ms vs ~15ms warm. A daemon thread now HEAD-pings the active broker&#39;s API origin every 20s during market hours through the same pooled client, so every order finds a warm connection.</li><li><strong>Zero inline fsync on the order path</strong>: the latency record and traffic log commits (previously synchronous SQLite fsyncs before the HTTP response) now run on background single-worker executors; the per-request IP-ban query and per-order order-mode query are cached with explicit invalidation, so enforcement semantics are unchanged.</li><li><strong>Honest measurement</strong>: the API Playground previously included its CSRF-token round trip in the displayed response time, reading 10-40ms high; the timer now covers only the actual request.</li></ul></li><li><strong>Broker-token rollover resilience (#1419, #1226, #1421, #1453, #1468)</strong> — WebSocket adapters read a fresh auth token on every (re)connect and self-heal on Zerodha auth failures; the broker token is revoked at the daily session rollover so the next morning&#39;s WS comes up clean; the data-stall watchdog is fed from ping/pong so it stops false-firing; Socket.IO heartbeats relax to engine.io defaults to stop reconnect loops; depth packets route to LTP consumers.</li><li><strong>Session UX on token expiry</strong> — An expired broker token no longer logs you out of Tradeboard: the session stays intact, the UI flags the stale token and offers a re-authentication path, and login resume requires a valid broker session.</li><li><strong>FD/resource-leak audit</strong> — A full file-descriptor audit (documented in <code>docs/</code>), two leak fixes (FD-5, FD-6), and a shared <code>database/engine_factory.py</code> that enforces NullPool for every SQLite engine project-wide.</li><li><strong>Vite 8 frontend toolchain</strong> — Migration completed (the attempt reverted in v2.0.1.2 now lands cleanly), with precompressed assets + vendor chunk splitting for faster page loads, Plotly compatibility fixes for <code>/tools</code> pages, zero Biome lint warnings, and stabilized e2e waits.</li><li><strong>Security hardening</strong> — <code>APP_KEY</code> is validated at startup instead of silently allowing <code>None</code>; SMTP key derivation strengthened and weak pepper fallbacks removed; first-run secret rotation now persists through Docker bind-mounted <code>.env</code>; <code>starlette</code> bumped 0.52.1 → 1.0.1 and <code>react-router</code>/<code>vitest</code> CVEs patched.</li><li><strong>Platform version bump</strong> — <code>2.0.1.2</code> → <code>2.0.1.3</code>. SDK pin (<code>tradeboard</code>) <code>1.0.51</code> → <code>2.0.1</code>, <code>opengreeks</code> <code>0.1.0</code> → <code>0.2.0</code>.</li></ul><hr><p><strong>Arrow broker — deep dive</strong></p><p><code>feat(broker): add Arrow (edge.arrow.trade) broker integration</code> — <code>6f2be4a6</code>, merged via PR #1494 (<code>9af53e2f</code>), followed by six hardening commits.</p><ul><li><strong>Auth</strong>: token authentication against <code>https://edge.arrow.trade</code> with corrected <code>authenticate-token</code> request field names (<code>1060bf71</code>).</li><li><strong>Orders</strong>: place, modify, cancel, cancel-all, close-position and smart orders following the Zerodha reference algorithm, with a 1-second position cache to prevent N+1 position fetches when multiple smart orders queue.</li><li><strong>Margin</strong>: single + basket margin routing modeled on the Dhan implementation (<code>212d37c9</code>).</li><li><strong>Master contract</strong>: rebuilt from Arrow&#39;s live instrument CSV with correct price/strike scaling (<code>e0d3dba9</code>).</li><li><strong>Market data</strong>: REST quotes/multiquotes with symbol resolution and batch capping (<code>7352ad50</code>); the WebSocket adapter parses Arrow&#39;s binary quote and depth frames natively (<code>6413905a</code>).</li><li><strong>Post-merge hardening</strong>: live-trading review findings addressed (<code>776f00ed</code>); lessons captured in the broker-integration docs (<code>b13c2fb5</code>).</li></ul><hr><p><strong>Order latency — deep dive</strong></p><p>Measured end-to-end with live NHPC MIS MARKET orders on Arrow:</p><table tabindex="0"><thead><tr><th>Stage</th><th>Before</th><th>After</th></tr></thead><tbody><tr><td>Order after &gt;30s idle gap (handshake)</td><td>~150ms broker RTT</td><td>~15ms broker RTT</td></tr><tr><td>Inline SQLite commits before response</td><td>2 (latency.db + logs.db) + 2 hot-path DB queries</td><td>0 commits, both queries cached</td></tr><tr><td>Warm platform overhead</td><td>3-4ms (plus hidden write time)</td><td>2.9-4.1ms, nothing hidden</td></tr></tbody></table><ul><li><code>perf(latency): move order hot-path DB writes off the request thread</code> — <code>55244799</code>. The latency record and traffic log now commit on shared single-worker executors after the response is sent; worker threads remove their scoped sessions so no SQLite read transaction is left open. The IP-ban verdict is cached per IP (60s TTL, invalidated on ban/unban so enforcement stays immediate) and the per-order <code>order_mode</code> lookup is cached (invalidated by <code>update_order_mode</code>).</li><li><code>feat(broker): keep the pooled broker HTTP connection warm in market hours</code> — <code>e6af52f6</code>. New env-configurable service; the deliberate <code>keepalive_expiry=30s</code> pool setting is untouched, so genuinely stale connections still recycle outside trading hours. Brokers expose their API origin via <code>broker/{name}/api/baseurl.py</code>; brokers without one are skipped.</li><li><code>fix(playground): exclude CSRF token fetch from displayed response time</code> — <code>66e220f1</code>.</li><li><code>perf(arrow): demote order hot-path payload/response logs to debug</code> — <code>c8bd25ba</code>. Console writes from the order path cost real milliseconds on Windows terminals; payloads remain visible under <code>LOG_LEVEL=DEBUG</code>.</li></ul><hr><p><strong>WebSocket + session reliability</strong></p><ul><li><code>340010ca</code> / <code>c3564cc8</code> — broker WS adapters refresh the auth token on every (re)connect and self-heal Zerodha auth failures (#1419, #1226, #1421).</li><li><code>3f167e23</code> — broker token revoked at daily session rollover, fixing the dead-WS-next-morning symptom (#1419).</li><li><code>fd026bed</code> — data-stall watchdog fed from ping/pong frames so idle-but-healthy feeds stop false-firing (#1419).</li><li><code>8d913b74</code> — Socket.IO heartbeat relaxed to engine.io defaults, stopping reconnect loops (#1419).</li><li><code>b9154f66</code> — depth packets route to LTP-mode consumers (#1453, #1468).</li><li><code>f69cb587</code>, <code>fa0b90a3</code>, <code>5e3f5f0e</code> — broker-token expiry no longer destroys the app session: the session-status endpoint flags instead of downgrading, and the UI offers a re-authentication path.</li><li><code>efc589b8</code> — login resume requires a valid broker session.</li><li><code>09729eaf</code> — Health Monitor page shows WebSocket connection details again.</li></ul><hr><p><strong>FD / resource-leak audit</strong></p><ul><li><code>c2e46bc6</code> + <code>190c6652</code> — full file-descriptor/resource-leak audit documented, with cross-review corrections.</li><li><code>8e297e62</code> — FD-5 and FD-6 leaks fixed.</li><li><code>7d59a62c</code> — <code>database/engine_factory.py</code> enforces NullPool for every SQLite engine, including all broker <code>master_contract_db.py</code> modules.</li><li><code>1feb07bf</code> — FD-hygiene workflow added to the contributor instructions.</li></ul><hr><p><strong>Frontend</strong></p><ul><li><code>29d93244</code> — toolchain migrated to <strong>Vite 8</strong> (with lockfile syncs <code>af9749a3</code>, <code>f8ca2245</code> and e2e stabilization <code>95dd2fb1</code>).</li><li><code>734ddd85</code> — assets precompressed (gzip + brotli) at build time and the vendor chunk split for faster page loads.</li><li><code>e92fbeff</code> + <code>74861b38</code> — Plotly <code>/tools</code> pages fixed under Vite 8 (<code>global=globalThis</code>, react-plotly CJS factory unwrap).</li><li><code>d2abfb3e</code> + <code>0eb6d1d6</code> — reusable EmptyState component replaces plain-text empty states (#1484).</li><li><code>7d497ac3</code> — orderbook gets a sortable Price column and per-instrument price grouping.</li><li><code>82d4b8b9</code> — navbar fits portrait monitors and small laptops; <code>c7abd7b8</code> — Leverage menu gated by broker capabilities.</li><li><code>b29fa579</code> + <code>ce678e40</code> — Reset Password TOTP input shows a numeric keypad and keeps its 6-digit pattern (#1478).</li><li><code>46338586</code> — zero Biome lint warnings; <code>204f3ac8</code> — Dhan/Zerodha broker notice readable in light theme.</li></ul><hr><p><strong>Broker fixes</strong></p><ul><li><code>cb1b08bc</code> — <strong>Dhan</strong>: null symbol/exchange in positions no longer crashes the UI (#1463, #1477).</li><li><code>bbac66d1</code> — <strong>Dhan</strong>: data-API rate limit split by endpoint class (#1476).</li><li><code>7583b8b3</code> — <strong>Dhan</strong>: baskets use the multi-margin calculator.</li><li><code>f5451bab</code> — <strong>Definedge</strong>: historical data date range corrected (#1475).</li><li><code>4dc48eb0</code> + <code>8963407d</code> — <strong>Indmoney</strong>: master-contract CSV NaN crash resolved (#1427) and <code>update_status</code> calls match the real signature.</li><li><code>ef6e8cbc</code> — Strategy Builder handles expired multi-option Greeks.</li><li><code>b4a80e25</code> — Python Strategy host hardens restored-process lifecycle.</li></ul><hr><p><strong>Security</strong></p><ul><li><code>624dbe9b</code> — <code>APP_KEY</code> validated at startup instead of allowing <code>None</code>.</li><li><code>02087959</code> — SMTP key derivation strengthened; weak pepper fallbacks removed.</li><li><code>0cd5979c</code> — first-run secret rotation persists through Docker bind-mounted <code>.env</code> (#1337 family).</li><li><code>663c3d8c</code> — <code>starlette</code> 0.52.1 → 1.0.1 (Dependabot advisory).</li><li><code>8811fec7</code> — <code>react-router</code> and <code>vitest</code> CVE patches.</li><li><code>b1bd779a</code> — loopback HTTP health-check false alarm fixed on native Ubuntu installs.</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>tradeboard</code> SDK pin: <code>1.0.51</code> → <code>2.0.0</code> → <strong><code>2.0.1</code></strong> (PyPI: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a>).</li><li><code>opengreeks</code> <code>0.1.0</code> → <code>0.2.0</code>.</li><li><code>starlette</code> <code>0.52.1</code> → <code>1.0.1</code>.</li><li>Frontend toolchain: <code>vite</code> 7 → 8; <code>react-router</code> / <code>vitest</code> security bumps.</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>utils/version.py</code>: <code>VERSION = &quot;2.0.1.3&quot;</code></p><p><code>pyproject.toml</code>: <code>version = &quot;2.0.1.3&quot;</code>; SDK and dependency pins as above (<code>uv.lock</code> regenerated).</p><p><code>.sample.env</code> — three new <strong>optional</strong> variables (defaults apply if absent; no action required):</p><ul><li><code>BROKER_CONNECTION_KEEPALIVE=&#39;TRUE&#39;</code> — enable/disable the broker connection keep-warm service</li><li><code>BROKER_KEEPALIVE_INTERVAL=&#39;20&#39;</code> — seconds between keep-warm pings (keep below the 30s pool expiry)</li><li><code>BROKER_KEEPALIVE_WINDOW=&#39;09:00-23:30&#39;</code> — IST window (Mon-Fri) covering NSE/BSE through the MCX close</li></ul><p><code>VALID_BROKERS</code> gains <code>arrow</code>.</p><p>There are <strong>no database schema changes</strong> in this release.</p><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend: a plain pull already ships the CI-built dist. Only rebuild if</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you are editing React code:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management; Arrow broker integration (#1494) with auth, orders, margin, master contract, binary streaming and post-merge hardening; order hot-path latency overhaul (keep-warm service, deferred DB writes, hot-path caches, playground timing fix); broker-token rollover and session-intact fixes; Vite 8 migration with asset precompression; crypto/startup security hardening; FD audit and NullPool engine factory; dependency sweep.</li><li><strong>@Kalaiviswa</strong> — Dhan positions null-crash fix (#1463, #1477), Definedge historical date range (#1475), depth-to-LTP routing (#1453, #1468).</li><li><strong>@Minh_Nguyen</strong> — reusable EmptyState component (#1484).</li><li><strong>@Christo John</strong> — Indmoney CSV NaN crash fix (#1427).</li><li><strong>@Quang Pham</strong> — numeric keypad for the Reset Password TOTP input (#1478).</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard/issues</a></li></ul>`,56)])])}const k=o(i,[["render",a]]);export{g as __pageData,k as default};
