import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.BXzK3EA4.js";const p=JSON.parse('{"title":"What the Agent Can Do","description":"","frontmatter":{},"headers":[],"relativePath":"new-features/agent/capabilities.md","filePath":"new-features/agent/capabilities.md"}'),r={name:"new-features/agent/capabilities.md"};function s(i,e,d,h,c,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="what-the-agent-can-do" tabindex="-1">What the Agent Can Do <a class="header-anchor" href="#what-the-agent-can-do" aria-label="Permalink to &quot;What the Agent Can Do&quot;">​</a></h1><p>The agent is not a chat window bolted onto Tradeboard. It reaches the same service layer the rest of the platform uses, through a fixed set of tools that fetch their own data. The model decides which tool to call and what to ask it for; it does not supply prices, strikes, expiries or candles. A contract it cannot resolve against the instrument master is refused rather than guessed.</p><p>There are 15 toolkits holding 57 tools. Which of them a turn is offered is decided before the model sees anything, by the surface it runs on and by two switches. A withheld tool is absent from the request entirely, so it cannot be called and cannot be talked into being called.</p><table tabindex="0"><thead><tr><th>Surface</th><th>Tools offered</th></tr></thead><tbody><tr><td><code>/agent</code>, the full page</td><td>47, with trading and web search on</td></tr><tr><td><code>/trading</code>, the chart panel</td><td>45</td></tr></tbody></table><p>The chart panel gains ten chart tools that the page does not have, and loses the twelve the page keeps to itself: seven order tools, the general rendering tool, two strategy-writing tools and two Flow tools.</p><h2 id="market-data-and-instruments" tabindex="-1">Market Data and Instruments <a class="header-anchor" href="#market-data-and-instruments" aria-label="Permalink to &quot;Market Data and Instruments&quot;">​</a></h2><p>Symbol resolution comes first, because every other tool treats a symbol and exchange pair as an exact contract identity. <code>search_symbols</code> turns a name or a fragment into listed Tradeboard symbols. <code>get_symbol</code> confirms one contract and returns the lot size, tick size and freeze quantity. <code>get_expiry_dates</code> lists the live expiries for an underlying, and it is the only sanctioned source for the date segment of a derivative symbol. A lookup that finds nothing comes back with near matches rather than &quot;not found&quot;, so the model corrects itself instead of telling you your instrument does not exist.</p><p>For raw numbers there is <code>get_quote</code>, <code>get_quotes</code> (up to 50 instruments in one call), <code>get_depth</code> and <code>get_intervals</code>. An index is quoted on an index exchange, so a request for NIFTY on NSE is checked against the symbol database and moved to <code>NSE_INDEX</code> when that is what actually resolves. The correction is data driven and only ever moves towards an index exchange.</p><p>The answer to &quot;what is RELIANCE at&quot; is <code>show_instrument</code>, which draws a full instrument card: the last traded price and the move against the previous close, the day&#39;s open, high, low and volume, an intraday price and volume chart, the 52 week range with how far below the high it sits, the top of the order book on both sides, and your own quantity, average price and unrealised profit or loss when you hold it. Every figure is a service call. The card carries no fundamentals, because Tradeboard has no source for one: there is no price/earnings ratio, market capitalisation, earnings per share or dividend yield anywhere in the platform, and an absent tile is treated as more honest than a placeholder.</p><p>Buy and Sell buttons sit at the foot of the card on the chat page, on a tradable instrument. They write an order request into the message box and place nothing: the card itself says nothing is ordered until you send it and approve it. They are absent on an index card, which is quote only, and absent on the chart panel.</p><h2 id="charts-and-history" tabindex="-1">Charts and History <a class="header-anchor" href="#charts-and-history" aria-label="Permalink to &quot;Charts and History&quot;">​</a></h2><p><code>get_history</code> computes its summary over every row the service returned and then returns a bounded tail of at most 200 candles, saying how many older ones it dropped. Handing a month of minute bars to a model would spend the whole context answering a question about the last few.</p><p><code>plot_price_chart</code> draws a price chart in the conversation using <code>tradeboard-charts</code> 1.9.2, the same engine <code>/trading</code> renders with. The tool fetches the candles itself, so the chart cannot show a bar the platform did not return, and the series never passes through the model&#39;s context: you get the chart and the answer gets one line of summary. Both tools can read the broker (<code>api</code>) or the local Historify store (<code>db</code>).</p><h2 id="options-analytics" tabindex="-1">Options Analytics <a class="header-anchor" href="#options-analytics" aria-label="Permalink to &quot;Options Analytics&quot;">​</a></h2><p><code>get_option_chain</code> returns a window of five strikes each side of ATM by default, up to 100, and asking for every listed strike has to be spelled out. With Greeks requested it inverts implied volatility and prices delta, gamma, theta and vega in one Black-76 pass off a forward derived from the ATM call and put by put-call parity, not off spot, because Indian index futures carry a premium and pricing a chain off the spot last trade biases every delta. <code>get_option_symbol</code>, <code>get_option_greeks</code> and <code>get_synthetic_future</code> cover single contracts and the synthetic.</p><p>Four analytics charts are drawn with Plotly, the same engine behind <code>/strategybuilder</code> and the option analytics pages:</p><ul><li><code>plot_open_interest</code>, call and put open interest by strike with the put-call ratio. Default 15 strikes each side, maximum 100.</li><li><code>plot_gamma_exposure</code>, net gamma exposure by strike. It prices the Greeks of every listed strike around ATM, so it is slow and is worth asking for only when it is the question.</li><li><code>plot_volatility_surface</code>, a 3D implied volatility surface. At most eight expiries, eight strikes each side by default, 25 at most.</li><li><code>plot_combined_premium</code>, several option legs summed over time.</li></ul><p>The combined premium is two different series behind one tool. Name no legs and you get the rolling ATM straddle, where the strike is recomputed from the underlying close on every candle. Name contracts and you get those exact contracts with the strikes held constant, which becomes a directional position as spot moves away. The card says which one it drew. The series is built from closes and drawn as a line on purpose: a combined high is not the sum of the legs&#39; highs, because the legs move against each other and adding them invents a peak that never traded.</p><p><code>plot_payoff</code> draws the payoff diagram of an option structure at expiry, from at most eight legs you name, or from your own open positions when called with no arguments at all. It uses the same payoff maths and the same chart as <code>/strategybuilder</code>. The card reports maximum profit, maximum loss, the breakevens, and a scenario table at expiry and at T+0. An unbounded loss is rendered as the words &quot;Unlimited loss&quot;, because a number there would be a lie about the risk. Cash equity is not on the curve, and the card says so when you hold shares in the same underlying, since a covered call charted without its shares looks like a naked short call.</p><h2 id="technical-indicators-and-the-two-catalogues" tabindex="-1">Technical Indicators, and the Two Catalogues <a class="header-anchor" href="#technical-indicators-and-the-two-catalogues" aria-label="Permalink to &quot;Technical Indicators, and the Two Catalogues&quot;">​</a></h2><p>This is the part most worth reading twice, because two different indicator libraries are in play and their names only partly overlap.</p><p><strong>The chart draws.</strong> <code>/trading</code> renders indicators from <code>tradeboard-charts</code> 1.9.2, a JavaScript catalogue of <strong>102</strong> indicators: 36 Trend, 29 Momentum, 22 Volatility, 15 Volume.</p><p><strong>The agent computes.</strong> The indicator tools calculate values with <code>tradeboard.ta</code> from the pinned <code>tradeboard</code> 2.0.3 SDK, a Rust-backed Python library of <strong>127</strong> indicators: 20 trend, 19 oscillators, 18 TA-Lib extras, 17 volume, 15 volatility, 13 utility, 9 momentum, 9 statistical, 7 hybrid.</p><p><strong>Only 34 ids exist in both.</strong> AlphaTrend and HalfTrend can be drawn on the chart and never tabulated. <code>adxr</code>, <code>crsi</code> and <code>beta</code> can be tabulated and never drawn. A name missing from one list says nothing about the other, and the agent is told to consult the right catalogue before concluding an indicator is unavailable.</p><p>The two catalogues also name some shared ideas differently, so the overlap of ids understates the overlap of concepts. Bollinger Bands is <code>bbands</code> in the Python library and <code>bollinger</code> on the chart, and both exist. If a name is rejected, ask for the other catalogue&#39;s spelling before assuming the indicator is missing.</p><p>Five tools cover the computing side. <code>list_indicators</code> and <code>describe_indicator</code> search the catalogue by intent as well as by name, so &quot;bollinger&quot;, &quot;trend strength&quot; and &quot;money flow&quot; find <code>bbands</code>, <code>adx</code> and <code>mfi</code>. <code>compute_indicator</code> runs one indicator over real candles it fetches itself. <code>compute_indicators_batch</code> runs up to eight on one shared candle fetch, so the values line up bar for bar and the broker sees one history request instead of eight. <code>scan_symbols</code> screens up to fifteen instruments for a single condition.</p><p>Warm-up is padded automatically, which is why asking for the last ten values of <code>beta</code> returns numbers rather than ten nulls: it needs 253 bars before its first finite value, <code>lrslope</code> needs 101, <code>crsi</code> 100 and <code>adxr</code> 41. At most 120 values come back per output.</p><p>The scan condition grammar is a fixed parser, not an expression evaluator. It accepts a comparison of one output against a number, such as <code>rsi &lt; 30</code> or <code>adx &gt; 25</code>, or a cross between two outputs of the same indicator, such as <code>crossover(macd_line, signal_line)</code>. There is no arithmetic, no <code>and</code>, no <code>or</code>, and no function other than <code>crossover</code>, <code>crossunder</code> and <code>cross</code>.</p><h2 id="the-chart-surface" tabindex="-1">The Chart Surface <a class="header-anchor" href="#the-chart-surface" aria-label="Permalink to &quot;The Chart Surface&quot;">​</a></h2><p>Ten tools exist only on the <code>/trading</code> panel. The panel reports the chart&#39;s symbol, exchange, interval, viewport, indicators and your own drawings with every message, read fresh at send time rather than captured when the panel opened, so no tool here takes a symbol, an exchange, an interval or a date.</p><ul><li><code>read_chart</code> reports what is on screen, including your drawings with their anchors and the exact indicator settings. It fetches nothing.</li><li><code>analyse_chart</code> reports trend, structure and momentum from one candle fetch: the swing sequence, the slope of the fitted rails, the period range, and RSI, MACD, ADX and ATR from the same bars. 60 to 1500 candles, 300 by default.</li><li><code>find_patterns</code> searches for both kinds and marks every hit on the chart. Candlestick: doji, marubozu, spinning top, hammer, inverted hammer, hanging man, shooting star, bullish and bearish engulfing, harami, tweezer top and bottom, piercing line, dark cloud cover, morning and evening star, three white soldiers, three black crows, inside and outside bars. Chart: double top and bottom, head and shoulders and its inverse, ascending, descending and symmetrical triangles, and ranges.</li><li><code>draw_levels</code>, <code>draw_trendline</code> and <code>draw_zone</code> put support and resistance, rails and demand, supply or consolidation zones on the canvas.</li><li><code>list_chart_indicators</code>, <code>add_chart_indicator</code> and <code>remove_chart_indicator</code> manage the chart&#39;s own indicators.</li><li><code>clear_drawings</code> removes the agent&#39;s markup.</li></ul><p>Provenance is stricter here than anywhere else in the product, because a level drawn on a chart reads as a fact about the market. No tool accepts a price. Every number reaching the canvas is computed from candles the server fetched. A caption is the one string the model does put on the canvas, so every digit is stripped out of it, along with every character outside letters, spaces and a little punctuation, and what is left is capped at 48 characters. A label can therefore name a zone and cannot state a level.</p><p>Everything drawn goes into one of four named groups: <code>levels</code>, <code>trendline</code>, <code>zone</code> and <code>patterns</code>. A draw replaces its group rather than stacking a second set on the first, and a clear removes only shapes under the agent&#39;s own id prefix. Your own drawings are never touched.</p><p>The panel runs the configured default model and has no model picker of its own, and on a vision-capable model the composer can attach a screenshot of the chart. <a href="./examples">Agent by Example</a> covers opening the panel and the starter chips above its composer.</p><p>If you have written your own chart indicators, they load in the browser at runtime from <code>strategies/indicators/</code> and cannot appear in <code>list_chart_indicators</code>. <code>add_chart_indicator</code> passes an unrecognised id through anyway and the chart accepts or ignores it, so a name absent from the list is worth trying.</p><h2 id="live-streaming" tabindex="-1">Live Streaming <a class="header-anchor" href="#live-streaming" aria-label="Permalink to &quot;Live Streaming&quot;">​</a></h2><p><code>stream_quotes</code> opens a live card for a list of instruments in <code>LTP</code>, <code>Quote</code> (the default) or <code>Depth</code> mode. At most 12 instruments, or 4 in Depth mode, and anything past the cap is dropped rather than refused. <code>stream_combo</code> opens a card showing one derived value recomputed on every tick of its legs: a straddle by default, or a strangle, call or put spread, call or put ratio, the synthetic future, or the basis. Expiry can be <code>current_week</code>, <code>next_week</code>, <code>current_month</code>, <code>next_month</code> or an exact date, and the result names the date and strike it actually resolved.</p><p>Neither tool streams anything itself. Both resolve the instruments against the instrument master, seed a snapshot so the card renders complete on first paint, and hand the subscription to the market data manager the browser already runs. A symbol that does not resolve is named back and left off the card, because a subscription that never ticks looks exactly like a dead feed. Each row shows where its value came from and how old the last tick is, and the card carries the trading session so it can say the market is closed rather than showing a still price as though it were live.</p><p>The legs of a combo are pinned when they are resolved. If spot later moves to a different strike, the card says so rather than quietly relabelling itself as a different straddle while you are watching it.</p><p>Every instrument on a card is a live subscription held for as long as the message is on screen, and a conversation accumulates messages. That is what the caps are for.</p><h2 id="account-and-books" tabindex="-1">Account and Books <a class="header-anchor" href="#account-and-books" aria-label="Permalink to &quot;Account and Books&quot;">​</a></h2><p>Seven read-only tools answer &quot;what do I have and what did I do&quot;: funds, positions, holdings, the order book, the trade book, one open position, and one order&#39;s status. They are offered on every surface. None of them mutates anything, so none pauses for approval.</p><p>Every result carries a mode of <code>live</code> or <code>analyze</code>, read at call time from the same platform toggle the services themselves consult, so a sandbox balance is never reported as a live one. An empty book is an answer rather than a failure: if you hold nothing, you are told you hold nothing.</p><h2 id="research-on-the-web" tabindex="-1">Research on the Web <a class="header-anchor" href="#research-on-the-web" aria-label="Permalink to &quot;Research on the Web&quot;">​</a></h2><p><code>web_search</code> returns titles, URLs and snippets. <code>web_research</code> returns a synthesised answer with the citations behind it. They are separate tools so that one third-party summary cannot enter the conversation wearing the authority of primary sources. Which provider answers each of them, the keys they need and the per-turn and daily budgets are set on <a href="./configuration">Agent Configuration</a>.</p><p>Web search is <strong>on</strong> by default and is switched off per conversation from the plus menu in the composer. Off is not a preference: the two tools are not built into the request at all, and the line under the composer says web search is off for this conversation.</p><p>The query the model asked for never reaches a provider. The outgoing query is constructed so that every token in it is provably a substring of your own message for that turn; when nothing survives, your message is sent verbatim; when there is no operator message to build from, the search is refused. Results come back labelled as third-party content, lower trust than platform data, and the answer cites real links.</p><h2 id="generated-code" tabindex="-1">Generated Code <a class="header-anchor" href="#generated-code" aria-label="Permalink to &quot;Generated Code&quot;">​</a></h2><p>The agent writes Tradeboard Python in the conversation, in a code block you can copy in one press. Two tools put generated work on disk, and both are chat-page only:</p><ul><li><code>save_python_strategy</code> writes into <code>strategies/scripts/</code>, the directory <code>/python</code> reads. It <strong>writes a file and stops</strong>: it never starts a strategy, never schedules one and never registers one for automatic start, because a hosted strategy runs as a subprocess with your decrypted API key. Starting it stays a separate, explicit action on <code>/python</code>. The source is refused, with the problem named so the model can correct itself, if it does not parse, if it hardcodes a credential, or if it does not read <code>TRADEBOARD_API_KEY</code>, <code>HOST_SERVER</code> and <code>WEBSOCKET_URL</code> from the environment. <code>list_python_strategies</code> reports what is already there so a generated file does not collide with one you have.</li><li><code>save_flow</code> validates workflow JSON against the same validator the <code>/flow</code> import endpoint uses, and imports a valid one as an <strong>inactive</strong> workflow. An invalid workflow is never saved, and the toolkit can neither activate nor execute one. <code>validate_flow</code> checks without saving.</li></ul><h2 id="orders-and-the-approval-pause" tabindex="-1">Orders, and the Approval Pause <a class="header-anchor" href="#orders-and-the-approval-pause" aria-label="Permalink to &quot;Orders, and the Approval Pause&quot;">​</a></h2><p>Trading is <strong>off by default</strong>. Read this section before switching it on.</p><p>Seven order tools exist, all of them on the chat page only: <code>place_order</code>, <code>place_smart_order</code>, <code>modify_order</code>, <code>cancel_order</code>, <code>cancel_all_orders</code>, <code>close_position</code> and <code>close_all_positions</code>.</p><p>Every one of them is a human-in-the-loop tool. The run <strong>pauses</strong> on the call and shows a panel reading &quot;This turn is waiting for your approval.&quot;, naming the tool and listing its exact arguments. Nothing is sent until you press Approve. Reject ends the call. The pause is enforced by construction rather than by prompt wording: the toolkit refuses to build at all if a tool it registers is not on the confirmation list.</p><p>Approval is not the last gate. After you approve, a risk guard runs inside the tool body, before the broker is touched. It reads no prompt, so nothing said in the conversation, in a symbol name or in an earlier tool result can change its verdict. Its checks run in a fixed order: the kill switch, whether trading is enabled, analyzer mode if you have required it, the symbol, the exchange, the product, the quantity, the session order cap, a duplicate-order window, the notional and limit-price deviation, and affordability against available funds. A refusal comes back as the tool&#39;s result, so the model reports the reason instead of retrying a rejection as though it were a transient fault.</p><p>The shipped limits are 20 orders a session, 10,000 units an order, INR 500,000 of notional an order, 5 percent maximum limit-price deviation, a 10 second duplicate window, and 100 percent funds utilisation. Account-wide destructive operations are <strong>off</strong>, which is why <code>cancel_all_orders</code> and <code>close_all_positions</code> refuse outright until you turn them on: the refusal is the intended answer, and the agent is told to offer to cancel the specific orders instead.</p><p>Where an approved order lands is decided by the platform, not by the agent:</p><ul><li>In <strong>live mode</strong> it goes to your real broker with real money. The config page states exactly that, in a red alert, whenever trading is on and the instance is live.</li><li>In <strong>analyzer mode</strong> it goes to the sandbox instead, and the panel says so.</li></ul><p>The kill switch at the head of that check order has two forms, and the second one needs no browser. Besides the stored setting, the guard refuses every order while a <strong>kill-switch file</strong> exists, <code>agent_kill_switch</code> in the repository root by default. Create it from a shell and the agent stops trading on the next call; delete it and trading resumes. Nothing else is affected, so reading, charting and analysis carry on.</p><p>Orders the agent places carry the strategy name <code>Tradeboard Agent</code>, so they are identifiable in the order book alongside anything else you run. Every mutating call writes two rows to an append-only audit table in the instance&#39;s own database, one before the service is called and one after, plus a row for each approve or reject. Two rows rather than one, because an attempt with no matching result is how a call that hung becomes visible. Deleting the conversation does not delete them: they are a trade record and they outlive the conversation the trade was typed into.</p><p><strong>The chart panel offers no order tools at all.</strong> It never asks for trading, so the server never builds one into that run&#39;s schema. That is structural, not a matter of wording, and it is why there is no approval prompt on the panel and why the Buy and Sell buttons on an instrument card are withheld there.</p><h2 id="what-an-answer-is-allowed-to-do" tabindex="-1">What an Answer Is Allowed to Do <a class="header-anchor" href="#what-an-answer-is-allowed-to-do" aria-label="Permalink to &quot;What an Answer Is Allowed to Do&quot;">​</a></h2><p>The model is treated as untrusted input, because everything it reads is. Tool output, a symbol name, a broker&#39;s rejection text and a web result can all carry somebody else&#39;s instructions, and the same conversation that read them writes the answer you are looking at.</p><p>So an answer is rendered and never interpreted. Raw HTML in an answer is dropped rather than parsed into elements: a <code>&lt;script&gt;</code> or an <code>&lt;iframe&gt;</code> the model emits renders as nothing at all, and there is no allowlist to get subtly wrong because nothing is being parsed. Markdown images are blocked outright, not filtered: an image URL is an exfiltration channel, because a model steered by injected content can put a value into a URL and the browser fetches it without anyone clicking anything. Tool arguments and tool results in the turn&#39;s timeline are rendered as text for the same reason.</p><p>The provenance rules elsewhere on this page belong to the same posture. A chart caption is stripped of every digit, a search query is built only from words in your own message, and no drawing tool accepts a price.</p><h2 id="operational-boundaries" tabindex="-1">Operational Boundaries <a class="header-anchor" href="#operational-boundaries" aria-label="Permalink to &quot;Operational Boundaries&quot;">​</a></h2><ul><li>Trading is off by default, so a fresh install reaches no order tool at all. Once it is on, what stands between a sentence and a broker is the approval pause, the risk guard and the limits above.</li><li>The agent never decides live versus sandbox. The platform&#39;s own analyzer toggle does. Switch to analyzer mode first if you want approved orders to reach the sandbox. See <a href="./../api-analyzer">API Analyzer</a>.</li><li>Tradeboard has no fundamentals source, so no tool returns a price/earnings ratio, market capitalisation, earnings per share or dividend yield, and no card shows one.</li><li>Every figure comes from your broker session, so history depth, available intervals, depth levels and entitlements are the broker&#39;s, not the agent&#39;s. A displayed quote is not a guarantee of execution price.</li><li>Generated work never runs itself. A saved Python strategy is started by you in <a href="./../python-strategy-hosting">Python Strategy Hosting</a>; an imported workflow arrives inactive and is activated by you in <a href="./../flow-visual-strategy-builder">Flow</a>.</li><li>The drawing catalogue (102) and the computing catalogue (127) share only 34 ids, and some shared ideas are spelled differently in each. Check the right catalogue, and try the other spelling, before concluding an indicator is missing.</li><li><code>vi</code> and <code>ulcerindex</code> return all-NaN on the pinned SDK build and are refused with that reason rather than answering with a column of nulls.</li><li>Caps that shape an answer: 200 history candles returned, 50 instruments a multi-quote, 8 indicators a batch, 15 symbols a scan, 12 live instruments (4 in Depth), 8 payoff legs, 8 surface expiries, 100 option strikes.</li><li><strong>A single run may make 25 tool calls and then stops.</strong> A question that would need more is answered from what it had rather than looping, so a request that fans out over many instruments is better asked as two questions than as one.</li><li>Web search is the only tool that leaves the machine. Turning it off is the only way to guarantee nothing does. Provider results are third-party content and are labelled as lower trust than platform data.</li><li>The risk limits are agent settings held in the instance&#39;s own database. The config page carries the master trading switch, the model registry, the ChatGPT subscription, the provider catalogue and the web search providers; the numeric limits have no panel of their own yet.</li><li>The audit trail is a database table with no screen in the UI. Read it with SQL when you need it.</li><li>An answer is a model&#39;s reading of real data. It is not advice, and it is not a substitute for checking the order you are about to approve.</li></ul><h2 id="see-also" tabindex="-1">See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See Also&quot;">​</a></h2><ul><li><a href="./README">Agent</a> for the overview and the three surfaces.</li><li><a href="./voice">Voice Agent</a> for Vega, the microphone on <code>/agent</code>.</li><li><a href="./examples">Agent by Example</a> for prompts that reach the tools above, and what the answers look like.</li><li><a href="./configuration">Agent Configuration</a> for the trading switch, the model registry and the web search providers.</li><li><a href="./../trading-terminal">Chart Trading Terminal</a> for the chart the panel docks to.</li><li><a href="./../../analytics-and-options-tools">Analytics and Options Tools</a> for the standalone versions of the option analytics above.</li><li><a href="./../../symbol-format">Symbol Format</a> for the symbol and exchange vocabulary every tool takes.</li></ul>',69)])])}const m=t(r,[["render",s]]);export{p as __pageData,m as default};
