import{_ as t,o,c as s,a2 as a}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Version 2.0.1.1 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.1-released.md","filePath":"change-log/release/version-2.0.1.1-released.md"}'),i={name:"change-log/release/version-2.0.1.1-released.md"};function d(r,e,n,c,l,p){return o(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="version-2-0-1-1-released" tabindex="-1">Version 2.0.1.1 Released <a class="header-anchor" href="#version-2-0-1-1-released" aria-label="Permalink to &quot;Version 2.0.1.1 Released&quot;">​</a></h1><p><strong>Date: 17th May 2026</strong></p><p><strong>Major Feature Release: WhatsApp Bot Integration — Event-Driven Alerts + Slash-Command Queries, Plus a WebSocket Reliability Sweep Across 14 Brokers, New Broker Integrations (IIFLCapital Streaming, Groww Option Chain + WS Depth, Upstox GLOBAL_INDEX), and Per-Install Fernet Salt Rotation with Crash-Safe Auto-Migration</strong></p><p>This release spans <strong>70+ commits</strong> since v2.0.1.0. The headline change is <strong>WhatsApp</strong> — a self-hosted, event-driven WhatsApp bot that fires order alerts on the same event bus that drives Telegram, accepts slash-command queries (<code>/orderbook</code>, <code>/positions</code>, <code>/quote</code>, …) from the operator&#39;s own phone, and exposes a single send endpoint over REST. Pairing happens once from the Tradeboard admin UI with a QR scan; the encrypted session blob is then stored in <code>tradeboard.db</code> and the bot auto-reconnects on every server boot. Alongside WhatsApp, this release lands a WebSocket reliability sweep — subscribe batching across 6+ brokers, reconnect hardening across 4+ brokers, file-descriptor leak fixes in two streaming layers, and proxy-level fixes (ZMQ bind, mode normalization, request_id correlation). Three new broker integrations land: IIFLCapital streaming, Groww option chain + WS depth, and Upstox GLOBAL_INDEX world feeds. Security: a per-install random <code>FERNET_SALT</code> now feeds the Fernet KDF, with a crash-safe online migration of existing ciphertext.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>WhatsApp Bot — event-driven alerts + slash-command queries</strong> — Brand-new <code>/whatsapp</code> admin page with auto-rotating QR pair flow, a single trader-facing <code>POST /api/v1/whatsapp/notify</code> endpoint that accepts text + image + document attachments, a dedicated worker thread that satisfies wars&#39;s PyO3 unsendable contract, and a <code>subscribers/whatsapp_subscriber</code> that wires every order topic on the existing event bus so order/position/batch events fire WhatsApp messages in parallel with Telegram. Single-user gate via WhatsApp&#39;s own <code>is_from_me=True</code> mark — random contacts who message the operator&#39;s number cannot drive the bot.</li><li><strong><code>client.whatsapp()</code> in the tradeboard Python SDK (1.0.50)</strong> — One unified call for every common case: send to self, to a single E.164 number, to up to 5 numbers (ToS-safety cap), with text, image, or document payloads. <code>wait_for_delivery=True</code> by default so the response carries a real per-recipient delivery report.</li><li><strong>WebSocket reliability sweep across 14 brokers</strong> — Subscribe batching for Kotak (HSI multi-scrip frames + 50ms debounce), AliceBlue, Nubra, Shoonya, Angel, Flattrade. Reconnect hardening for Dhan (auto-resubscribe + data-stall watchdog), Dhan-Sandbox (eventlet-safe asyncio + single-loop reconnect), Upstox (stall-vs-network reconnect logging), Fyers TBT (batch queue + pong validation + exponential backoff + health check). Cold-subscribe latency cut ~5× on Shoonya; Zerodha lost a ~4s sleep floor.</li><li><strong>WebSocket-proxy + client fixes</strong> — Bind ZMQ publisher to <code>ZMQ_HOST</code> instead of all interfaces (#1378), normalize subscribe/unsubscribe mode case-insensitively (#1375), correlate ack via request_id (#1376), route cache invalidation through <code>SharedZmqPublisher</code> to eliminate the PUB→PUB topology (#1374).</li><li><strong>Three new broker integrations</strong> — <strong>IIFLCapital streaming</strong> (full WS adapter; file-descriptor leaks closed across reconnect cycles, #1416 + #1430), <strong>Groww</strong> (full option chain + WS depth, expiry filter for expired contracts, broker-symbol mangling fix, #1392), <strong>Upstox GLOBAL_INDEX</strong> (US30 / JAPAN225 / HANGSENG world feeds via the existing Upstox WS adapter).</li><li><strong>Per-install random Fernet salt</strong> — <code>FERNET_SALT</code> env var is now provisioned per install (32-byte hex, generated by <code>utils/env_check.py</code>) and feeds the Fernet KDF in <code>database/auth_db.py</code>. Crash-safe online migration moves existing ciphertext from the legacy static salt to the new one; if the process dies mid-migration the next boot resumes from the persisted state. Tightens broker-auth-token confidentiality and is the same domain-separated salt the new WhatsApp session blob uses.</li><li><strong>Platform version bump</strong> — <code>2.0.1.0</code> → <code>2.0.1.1</code>. SDK pin (<code>tradeboard</code>) <code>1.0.49</code> → <code>1.0.50</code>.</li></ul><hr><p><strong>WhatsApp Bot — feature deep dive</strong></p><p>WhatsApp brings parity with Telegram for both outbound alerts and interactive command queries, with security choices tuned for the single-user-per-deployment model Tradeboard runs under.</p><p><strong>Architecture in one sentence:</strong> the wars (PyO3 over whatsapp-rust) library hosts a fully linked WhatsApp Web device inside the Flask process; outgoing alerts flow event-bus → <code>whatsapp_subscriber</code> → <code>WhatsAppBotThread</code> → <code>wars.send()</code>; incoming slash-commands flow <code>wars.on_message</code> → <code>is_from_me=True</code> gate → command dispatcher → Tradeboard SDK call → reply via the same bot thread.</p><p><strong>Pairing (admin only)</strong> — <code>c44a420b</code>:</p><ul><li><code>POST /whatsapp/pair</code> (session-cookie auth) spawns a temp-DB wars instance, registers an <code>on_qr</code> callback that streams <code>whatsapp_qr</code> SocketIO events with a <code>data:image/png</code> URL, and waits on <code>wait_until_ready(timeout=300)</code> for the phone-side scan</li><li>QR refreshes ~every 30 seconds; React <code>/whatsapp</code> page swaps the <code>&lt;img&gt;</code> source on each <code>whatsapp_qr</code> event without polling</li><li>On pair success: <code>export_session()</code> → Fernet-encrypted → persisted to <code>whatsapp_config.session_blob</code> in <code>tradeboard.db</code> → temp file unlinked</li><li>Pair-code path (<code>POST /whatsapp/pair</code> with <code>phone</code> parameter) for users who prefer not to scan a QR</li></ul><p><strong>Encryption at rest</strong> — <code>database/whatsapp_db.py</code>:</p><ul><li>Fernet key derived via PBKDF2-SHA256(<code>API_KEY_PEPPER</code>, <code>FERNET_SALT + b&quot;:whatsapp-session&quot;</code>, 100k iters), 32-byte output, base64-urlsafe encoded</li><li>Domain separator (<code>:whatsapp-session</code>) means the same <code>(PEPPER, FERNET_SALT)</code> pair derives <strong>different</strong> Fernet keys for broker auth tokens, Telegram bot tokens, and WhatsApp session blob — compromising one channel&#39;s ciphertext gives no leverage against the others</li><li>Idempotent SQLite <code>ALTER TABLE ADD COLUMN</code> migration runs at every init so existing installs pick up the <code>owner_user_id</code> / <code>owner_username</code> columns without manual schema work</li></ul><p><strong>The unsendable PyO3 trap</strong> — <code>c44a420b</code>:</p><ul><li><code>wars.WhatsApp</code> is <code>#[pyclass(unsendable)]</code> — every method call panics if invoked from a thread other than its creator</li><li>Solution: a dedicated <code>WhatsAppBotThread</code> owns the wars instance for its lifetime; request threads enqueue <code>(op, args, result_holder, event)</code> on a <code>queue.Queue</code> and wait on a <code>threading.Event</code> for the worker to dispatch <code>wars.send()</code></li><li>Re-entrant: command handlers (which wars dispatches on the bot thread itself) bypass the queue via a <code>threading.get_ident() == self._bot_thread_id</code> check so they don&#39;t deadlock on themselves</li><li>Same shape Telegram uses for python-telegram-bot, for the same reason</li></ul><p><strong>REST surface — intentionally narrow</strong> — <code>restx_api/whatsapp_bot.py</code>:</p><ul><li><code>POST /api/v1/whatsapp/notify</code> is the <strong>only</strong> public endpoint</li><li>Pairing, start/stop, config, users, broadcast, stats, preferences live behind the session-authed <code>/whatsapp/*</code> blueprint — admin only</li><li>A leaked API key cannot re-pair the device, enumerate linked recipients, change rate limits, or fan out to the operator&#39;s contact list</li><li>Hard precheck — every send path refuses with <code>409 &quot;WhatsApp is not paired or not connected. Pair the device first from the /whatsapp page in Tradeboard before sending.&quot;</code> if <code>is_ready()</code> is false; we explicitly do not queue when unpaired</li></ul><p><strong>Send paths — one unified call</strong>:</p><ul><li><code>client.whatsapp(&quot;Build #482 deployed&quot;)</code> → wars&#39;s single-arg <code>send(&quot;text&quot;)</code> form, routes to the paired device&#39;s own number (no need to know own JID)</li><li><code>client.whatsapp(&quot;hi&quot;, to=&quot;919876543210&quot;)</code> → single recipient</li><li><code>client.whatsapp(&quot;alert&quot;, to=[...])</code> → broadcast (capped at 5 server-side — anything beyond is dropped; ToS-safety guardrail)</li><li><code>client.whatsapp(&quot;EOD chart&quot;, to=&quot;919...&quot;, image=&quot;/srv/charts/nifty.png&quot;, caption=&quot;...&quot;)</code> → image with caption</li><li><code>client.whatsapp(&quot;report&quot;, username=&quot;alice&quot;, document=&quot;/srv/reports/eod.pdf&quot;, filename=&quot;EOD.pdf&quot;)</code> → document</li><li>Attachment paths validated against <code>WHATSAPP_ATTACHMENT_ROOTS</code> allowlist (default: <code>&lt;tradeboard&gt;/db/attachments/</code>); paths with <code>..</code>, paths under <code>/etc /proc /sys /root /var/log /C:\\Windows</code>, or paths that resolve outside the allowlist are rejected with <code>400 image_path is not allowed</code></li></ul><p><strong>Inbound commands</strong> — <code>services/whatsapp_bot_service.py</code>:</p><table tabindex="0"><thead><tr><th>Command</th><th>Maps to</th></tr></thead><tbody><tr><td><code>/help</code>, <code>/menu</code>, <code>/start</code></td><td>Command list</td></tr><tr><td><code>/status</code></td><td>Bot connection + paired status</td></tr><tr><td><code>/orderbook</code></td><td><code>client.orderbook()</code></td></tr><tr><td><code>/tradebook</code></td><td><code>client.tradebook()</code></td></tr><tr><td><code>/positions</code></td><td><code>client.positionbook()</code></td></tr><tr><td><code>/holdings</code></td><td><code>client.holdings()</code></td></tr><tr><td><code>/funds</code></td><td><code>client.funds()</code></td></tr><tr><td><code>/pnl</code></td><td><code>client.pnl()</code> or <code>client.positionbook()</code></td></tr><tr><td><code>/quote SYM [EXCH]</code></td><td><code>client.quotes()</code></td></tr><tr><td><code>/closeall</code></td><td><code>client.closeposition()</code></td></tr><tr><td><code>/mode</code></td><td>live or analyze</td></tr></tbody></table><p>Auth: the bot only responds when <code>is_from_me=True</code> (WhatsApp&#39;s multi-device protocol marks messages mirrored from the operator&#39;s primary phone with this flag). Random contacts who message the operator&#39;s WhatsApp number arrive with <code>is_from_me=False</code> and are silently ignored. The Tradeboard SDK calls run with the operator&#39;s API key, looked up from <code>auth_db</code> by the <code>owner_username</code> captured at pair time — there is no <code>/link</code> flow because there is no second user to authorize.</p><p><strong>Auto-reconnect on app boot</strong> — <code>app.py:_autostart_whatsapp_bot</code>:</p><ul><li>Background thread spawned during <code>_init_databases_and_schedulers</code> (after <code>db_ready.set()</code>)</li><li>Loads the encrypted session blob, calls <code>wars.WhatsApp.from_bytes(blob)</code>, starts the worker thread, registers handlers</li><li>No QR scan on restart — <code>is_ready()</code> flips to true within ~1s of boot</li><li>If wars isn&#39;t installed (fresh checkout that hasn&#39;t <code>uv sync</code>&#39;d), the autostart logs a warning and degrades gracefully — the rest of the Flask app boots normally</li></ul><p><strong>Frontend</strong> — <code>frontend/src/pages/whatsapp/WhatsAppIndex.tsx</code>:</p><ul><li>Single-page React UI: Status card with Pair QR + Disconnect button, &quot;Send a one-off message&quot; card, no Linked-Users table (single-user app)</li><li>SocketIO subscriptions for <code>whatsapp_qr</code> (auto-rotates the QR image), <code>whatsapp_pair_code</code>, <code>whatsapp_paired</code>, <code>whatsapp_pair_status</code>, <code>whatsapp_status</code></li><li>New <code>MessageCircle</code> icon in the profile dropdown so WhatsApp visually differs from Telegram&#39;s <code>MessageSquare</code></li></ul><p><strong>RUST_LOG quieting</strong> — <code>services/whatsapp_bot_service.py</code>:</p><ul><li>Three known-noisy targets silenced at module import (before any <code>import wars</code>): <code>wacore::send</code> (stale-device warnings), <code>whatsapp_rust::message</code> (PN→LID migration chatter), <code>wacore_libsignal::protocol::session_cipher</code> (no-current-session errors that the upper layer already handles)</li><li><code>os.environ.setdefault(&quot;RUST_LOG&quot;, ...)</code> — operator can still override via shell or <code>.env</code> for diagnostics</li></ul><p><strong>tradeboard Python SDK 1.0.50</strong> — released to PyPI alongside this version:</p><ul><li>New <code>WhatsAppAPI</code> mix-in adds <code>client.whatsapp(...)</code> with the four recipient forms and image/document payloads</li><li>Mirrors the <code>client.telegram()</code> ergonomics for traders already familiar with the SDK</li><li>Available at <a href="https://pypi.org/project/tradeboard/1.0.50/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.50/</a></li></ul><hr><p><strong>WebSocket reliability sweep — 14 brokers touched</strong></p><table tabindex="0"><thead><tr><th>Broker</th><th>Change</th><th>Commit</th></tr></thead><tbody><tr><td><strong>IIFLCapital</strong></td><td>Full websocket adapter integrated + file-descriptor leaks closed across reconnect cycles</td><td><code>0ad69cf3</code> (#1416), <code>15179371</code> (#1430)</td></tr><tr><td><strong>Paytm</strong></td><td>NSE_INDEX/BSE_INDEX symbol normalization + fd-leak fixes in streaming layer + batch ws subscribe + graceful empty for option chain / OI tracker / historical</td><td><code>8dba1ea3</code> (#1413)</td></tr><tr><td><strong>Kotak</strong></td><td>Batch subscribe via HSI multi-scrip frames + cut batch debounce 500ms → 50ms + log emitted scrips</td><td><code>8fef2a57</code> (#1399)</td></tr><tr><td><strong>Groww</strong></td><td>Full option chain + websocket depth integration + drop expired expiries + stop mangling broker symbols</td><td><code>440b78ef</code> (#1392)</td></tr><tr><td><strong>AliceBlue</strong></td><td>Batch ws subscriptions like Zerodha + event-driven connect + leading-edge subscribe debounce</td><td><code>09667a6e</code> (#1389)</td></tr><tr><td><strong>Dhan-Sandbox</strong></td><td>Eventlet-safe asyncio + heartbeat align + single-loop reconnect + batch queue</td><td><code>b5675653</code> (#1344)</td></tr><tr><td><strong>Nubra</strong></td><td>Coalesce per-symbol subscribes into batched SDK calls</td><td><code>6473aa2e</code> (#1366)</td></tr><tr><td><strong>Shoonya</strong></td><td>Batch queue + auth-fail short-circuit + env-var tuning + interruptible sleeps; cold-subscribe latency cut ~5× via leading-edge debounce</td><td><code>4b8578d6</code> (#1381)</td></tr><tr><td><strong>Dhan</strong></td><td>Auto-resubscribe on reconnect + data-stall watchdog</td><td><code>d214a598</code> (#1372)</td></tr><tr><td><strong>Upstox</strong></td><td>Distinguish stall-triggered reconnects from network-induced ones in logs</td><td><code>42927546</code> (#1357)</td></tr><tr><td><strong>Angel</strong></td><td>Subscribe batch-queue to coalesce per-symbol bursts + defensive <code>.get()</code> in place_order response handling</td><td><code>790cc64d</code> (#1352), <code>a4bdac18</code> (#846)</td></tr><tr><td><strong>Zerodha</strong></td><td>Remove ~4s sleep floor from subscribe path + add auth-fail short-circuit + interruptible sleeps + wire MCX_INDEX through quote/history/depth/ws</td><td><code>659d53ab</code>, <code>f21f40cc</code> (#1371), <code>cd4095eb</code> (#1385)</td></tr><tr><td><strong>Fyers</strong></td><td>Harden TBT client with batch queue, pong validation, exponential backoff, and health check</td><td><code>463a3004</code> (#1361)</td></tr><tr><td><strong>Flattrade</strong></td><td>Batch-queue subscriptions like Zerodha adapter</td><td><code>ed37dbc2</code> (#1341)</td></tr><tr><td><strong>Kotak</strong></td><td>Align place/modify order payload with official Neo spec</td><td><code>b06ef4a8</code> (#1398)</td></tr></tbody></table><p><strong>WebSocket-proxy + client layer:</strong></p><ul><li><code>dd9cb64c</code> (#1378) — <code>fix(websocket_proxy): bind ZMQ publisher to ZMQ_HOST instead of all interfaces</code></li><li><code>6ddff2bb</code> (#1375) — <code>fix(websocket_proxy): normalize subscribe/unsubscribe mode case-insensitively</code></li><li><code>9a0f5e42</code> (#1376) — <code>fix(websocket_client): correlate subscribe/unsubscribe acks via request_id</code></li><li><code>521ea129</code> (#1374) — <code>fix(cache_invalidation): route through SharedZmqPublisher to eliminate PUB-&gt;PUB topology</code></li><li><code>25eed728</code> — <code>fix(ui/websocket-test): keep LTP card live when Depth is also subscribed</code></li><li><code>f8a1ff4f</code> (#1386) — <code>chore(websocket): remove dead get_supported_brokers_list()</code></li><li><code>06d4cb34</code> — <code>docs(audit): add WebSocket broker priority audit</code></li></ul><hr><p><strong>New broker integrations + exchange expansion</strong></p><ul><li><strong><code>0ad69cf3</code> / <code>15179371</code></strong> — IIFLCapital websockets integrated + broker hardening + option-chain perf</li><li><strong><code>440b78ef</code></strong> — Groww full option chain + websocket depth integration</li><li><strong><code>c0335582</code></strong> — Upstox GLOBAL_INDEX world feeds (US30 / JAPAN225 / HANGSENG, plus IFSC-routed GIFTNIFTY) on indices and indicators</li><li><strong><code>cd4095eb</code></strong> — Zerodha MCX_INDEX wired through quote/history/depth/ws</li><li><strong><code>ca15b333</code></strong> — Groww NSE_INDEX/BSE_INDEX in historical (#1338/#1342)</li><li><strong><code>11778af5</code></strong> — Symbol search surfaces FUT-only MCX underlyings (#1385)</li></ul><hr><p><strong>Security hardening — Fernet per-install salt</strong></p><p>The Fernet KDF in <code>database/auth_db.py</code> (which encrypts broker auth tokens) previously used a hardcoded static salt. This release lands a per-install random salt with a crash-safe online migration.</p><ul><li><code>2f2e9bee</code> — <code>fix(security): rotate Fernet to per-install salt with crash-safe auto-migration</code></li><li><code>e3c5285d</code> — place <code>FERNET_SALT</code> adjacent to <code>API_KEY_PEPPER</code> in <code>.env</code>, handle the 4 file-state cases cleanly (placeholder / valid / missing / mid-migration)</li><li><code>a1455ff1</code> — atomic <code>.env</code> rewrite falls back to in-place on Docker bind mounts</li><li><code>84922078</code> — degrade gracefully when <code>.env</code> is unwritable + pin Docker UID 1000 (#1394)</li><li><code>2981ff52</code> — post-FERNET_SALT cleanup — security perm-check, dedupe, pool invalidate (#1394)</li><li><code>b9301b78</code> — never recommend rotating <code>API_KEY_PEPPER</code> on a populated DB (carried in from late v2.0.0.9 work, re-asserted here)</li></ul><p>What this means operationally:</p><ul><li>New installs: <code>utils/env_check.py</code> generates a 32-byte hex <code>FERNET_SALT</code>, writes it adjacent to <code>API_KEY_PEPPER</code> in <code>.env</code>, and uses it directly. No migration runs.</li><li>Existing installs upgrading to 2.0.1.1: on first boot, <code>env_check</code> detects the placeholder/missing state, generates the new salt, re-encrypts every broker-auth-token ciphertext on the fly with the new key, and atomically swaps. If the process dies mid-migration, the persisted <code>FERNET_SALT</code> lets the next boot resume cleanly.</li><li>Same salt entropy feeds the new WhatsApp session blob via the <code>:whatsapp-session</code> domain separator.</li></ul><hr><p><strong>UI + frontend</strong></p><ul><li><code>cd692653</code> — <code>feat(orderbook): make Quantity editable in Modify Order dialog</code></li><li><code>f968b403</code> — <code>fix(ui): align table content with stats cards on Positions/Orderbook/Holdings/Tradebook</code></li><li><code>d3aa8b6b</code> — <code>fix(frontend): auto-reload on stale-chunk import failure (#1393)</code> — fixes the &quot;ChunkLoadError&quot; trader sees after a deploy when the browser is still holding the old <code>index-*.js</code> reference</li><li><code>d63ec927</code> — drop hero gradient on the home page, use solid <code>text-primary</code></li><li><code>3f4e2b2b</code> — home: &quot;New in V2 — 12-Tool Options Analytics Suite&quot; pill</li><li><code>624f8726</code> — home: Integrates With + Made for AI sections</li><li><code>61370758</code> / <code>54d83c07</code> — restyle the MCP OAuth consent page to match Tradeboard dashboard + fix alignment</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>.sample.env</code>:</p><ul><li><code>FERNET_SALT</code> — new placeholder line auto-rotated on first boot, placed adjacent to <code>API_KEY_PEPPER</code></li><li>No new keys for WhatsApp — <code>WHATSAPP_KEY_SALT</code> reuses the existing <code>FERNET_SALT</code> with a <code>:whatsapp-session</code> domain suffix (zero new env vars to manage)</li><li>Optional: <code>WHATSAPP_ATTACHMENT_ROOTS</code> (comma-separated absolute dirs; defaults to <code>&lt;tradeboard&gt;/db/attachments/</code>)</li><li>Optional: <code>RUST_LOG</code> — defaults to a filter that silences three known-noisy wars/whatsapp-rust modules</li></ul><p><code>pyproject.toml</code>:</p><ul><li><code>version = &quot;2.0.1.1&quot;</code></li><li>New: <code>wars==0.1.3</code> dependency (also added to <code>requirements.txt</code> + <code>requirements-nginx.txt</code>)</li><li>SDK pin (<code>tradeboard</code>) <code>1.0.49</code> → <code>1.0.50</code></li></ul><p><code>utils/version.py</code>:</p><ul><li><code>VERSION = &quot;2.0.1.1&quot;</code></li></ul><p><code>requirements.txt</code> + <code>requirements-nginx.txt</code>:</p><ul><li><code>wars==0.1.3</code> added after <code>python-telegram-bot==22.6</code></li><li><code>tradeboard==1.0.49</code> → <code>tradeboard==1.0.50</code></li></ul><p><code>frontend/src/stores/alertStore.ts</code>:</p><ul><li>New <code>whatsapp</code> toast category alongside <code>telegram</code></li></ul><hr><p><strong>Database schema</strong></p><p>New tables created by <code>database/whatsapp_db.py</code> on first boot:</p><table tabindex="0"><thead><tr><th>Table</th><th>Purpose</th></tr></thead><tbody><tr><td><code>whatsapp_config</code></td><td>Singleton row holding the Fernet-encrypted session blob + bot config + owner identity</td></tr><tr><td><code>whatsapp_users</code></td><td>Optional linked recipients (unused in single-user mode, retained for future multi-recipient deployments)</td></tr><tr><td><code>whatsapp_command_logs</code></td><td>Slash-command audit log (sensitive args like <code>/link</code>&#39;s api_key are scrubbed before write)</td></tr><tr><td><code>whatsapp_notification_queue</code></td><td>Retry queue for failed alerts (currently unused — single-user model drops on not-paired instead of queueing)</td></tr><tr><td><code>whatsapp_user_preferences</code></td><td>Per-user notification toggles + summary time + language + timezone</td></tr></tbody></table><p>Idempotent <code>PRAGMA table_info</code> migration adds <code>owner_user_id</code> + <code>owner_username</code> columns to <code>whatsapp_config</code> on existing installs.</p><hr><p><strong>Dependencies</strong></p><ul><li><code>wars==0.1.3</code> added — PyO3 binding over the whatsapp-rust crate; provides the WhatsApp Web client. Wheels available for Python 3.12+ via abi3.</li><li><code>tradeboard</code> SDK pin: <code>1.0.49</code> → <code>1.0.50</code> (PyPI: <a href="https://pypi.org/project/tradeboard/1.0.50/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.50/</a>)</li><li><code>urllib3</code> <code>2.6.3</code> → <code>2.7.0</code> (<code>4519f55f</code>) — clears 8 Dependabot high-severity alerts</li><li><code>axios</code> <code>1.15</code> → <code>1.16</code>, <code>python-multipart</code> <code>0.0.26</code> → <code>0.0.27</code>, pin <code>pip&gt;=26.1</code> (<code>6d61e5c6</code>)</li></ul><hr><p><strong>Documentation</strong></p><ul><li>New: <code>docs/api/whatsapp-services/README.md</code> — architecture + security model + slash-command reference</li><li>New: <code>docs/api/whatsapp-services/notify.md</code> — full endpoint reference for <code>POST /api/v1/whatsapp/notify</code></li><li>New: <code>collections/tradeboard/IN_stock/whatsapp_notify.bru</code> — Bruno collection entry (auto-discovered by <code>/playground</code>)</li><li>Updated: <code>docs/api/README.md</code> — adds the WhatsApp Services section under the existing service taxonomy</li><li>Updated: <code>docs/prompt/tradeboard python sdk.md</code> — full <code>client.whatsapp(...)</code> reference with all four recipient forms, image/document attachments, fire-and-forget vs synchronous delivery, and inbound slash-command reference</li><li><code>52eb8650</code> — <code>docs(mcp): rewrite Remote MCP userguide for traders, drop stale install paths</code></li><li><code>3b3054be</code> — <code>docs(claude): update CLAUDE.md with new product surfaces, Ruff tooling, and architecture details (#1412)</code></li><li><code>1a7d3a0a</code> — <code>docs(claude): clarify sandbox terminology and split /sandbox vs /analyzer surfaces</code></li><li><code>c7e5f4a9</code> — <code>docs(services): align order field names with canonical code (pricetype, product)</code></li><li><code>83499518</code> / <code>e2de15ec</code> — Ubuntu Server Installation guide refresh</li></ul><hr><p><strong>Install + infrastructure</strong></p><ul><li><code>79557be5</code> — <code>feat(install): inline Remote MCP prompt in install-docker.sh and install-multi.sh</code></li><li><code>5c8b64b9</code> — <code>feat(install): prompt to enable Remote MCP during install.sh</code></li><li><code>04eac147</code> — <code>feat(install): simplify single-deploy paths + drop enable-remote-mcp.sh</code></li><li><code>647183bd</code> — <code>feat(remote-mcp): UI controls for master switch + posture toggles</code></li><li><code>9ec851ab</code> — <code>fix(mcp): use HOST_SERVER for SDK loopback so install.sh deploys work</code></li><li><code>5fa17bd3</code> — <code>fix(diagnostics): correct dead secret keys + git info inside Docker (#1388)</code></li><li><code>f786e21a</code> — <code>chore: add Caddyfile for local https://tradeboard.local dev</code></li><li><code>4e09da8b</code> — <code>fix(python-strategy): Stop button works under gunicorn-eventlet (#1404)</code></li></ul><hr><p><strong>Bug fixes (non-WebSocket)</strong></p><ul><li><code>a4bdac18</code> — <code>fix(angel/api): defensive .get() in place_order response handling (#846)</code></li><li><code>b06ef4a8</code> — <code>fix(kotak): align place/modify order payload with official Neo spec (#1398)</code></li><li><code>ca15b333</code> — <code>fix(groww/api): support NSE_INDEX/BSE_INDEX in historical (#1338) (#1342)</code></li><li><code>11778af5</code> — <code>fix(search): surface FUT-only MCX underlyings in symbol search (#1385)</code></li></ul><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># update.sh runs migrate_all.py — the new whatsapp_config / whatsapp_users</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># tables and the owner_user_id column are created automatically. The</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># FERNET_SALT migration runs on first boot inside env_check; existing</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># broker-auth ciphertext is re-encrypted on the fly.</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The container&#39;s start.sh runs migrate_all.py before gunicorn boots.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># FERNET_SALT migration runs on first start.</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><p><strong>Enabling WhatsApp</strong> (post-upgrade, optional):</p><ol><li>Log in to Tradeboard, open <code>/whatsapp</code>.</li><li>Click <strong>Start pairing</strong>. A QR code appears.</li><li>On your phone: WhatsApp → Settings → Linked devices → Link a device → scan.</li><li>Done. The bot auto-starts and reconnects on every server boot from the encrypted session in <code>tradeboard.db</code>.</li></ol><p>The session blob never leaves your server. There is no second-party service to register with — it&#39;s a direct Signal-Protocol connection to WhatsApp.</p><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management; WhatsApp architecture and full implementation (database schema with Fernet-encrypted session blob + domain-separated salt, dedicated <code>WhatsAppBotThread</code> to satisfy PyO3&#39;s unsendable contract, event-bus subscriber wired into all 13 order topics, send-only REST API + session-authed admin blueprint, React <code>/whatsapp</code> page with auto-rotating QR, RUST_LOG suppression for the three known-noisy wars modules, attachment-path allowlist with traversal-token rejection, lazy own-JID capture from <code>is_from_me=True</code> messages, slash-command dispatcher with <code>is_from_me</code> gate, auto-reconnect on app boot); tradeboard Python SDK 1.0.50 release with new <code>client.whatsapp(...)</code> API; WebSocket reliability sweep across 14 brokers (subscribe batching, reconnect hardening, fd-leak fixes); new IIFLCapital streaming adapter (#1416, #1430); Groww option chain + WS depth (#1392); Upstox GLOBAL_INDEX world feeds; Zerodha MCX_INDEX wiring (#1385); per-install Fernet salt rotation with crash-safe migration; websocket-proxy fixes (ZMQ bind #1378, mode normalization #1375, request_id correlation #1376, SharedZmqPublisher topology #1374); UI alignment fixes and stale-chunk auto-reload; comprehensive WhatsApp documentation (endpoint reference, SDK prompt-doc, Bruno collection entry).</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/1.0.50/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.50/</a></li><li><strong>WhatsApp service docs</strong>: <a href="https://docs.algo.wesoftcorp.com/api-documentation/v1/whatsapp-services" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com/api-documentation/v1/whatsapp-services</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul><hr><hr>`,97)])])}const g=t(i,[["render",d]]);export{u as __pageData,g as default};
