import{_ as s,o as t,c as a,a2 as o}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.0.6 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.0.6-released.md","filePath":"change-log/release/version-2.0.0.6-released.md"}'),n={name:"change-log/release/version-2.0.0.6-released.md"};function i(r,e,d,l,c,p){return t(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="version-2-0-0-6-released" tabindex="-1">Version 2.0.0.6 Released <a class="header-anchor" href="#version-2-0-0-6-released" aria-label="Permalink to &quot;Version 2.0.0.6 Released&quot;">​</a></h1><p><strong>Date: 27th Apr 2026</strong></p><p><strong>Critical Security Update: APP_KEY / API_KEY_PEPPER Hardening, Encrypted Credentials at Rest, Reverse-Proxy Trust Gate, Docker Permission Fix &amp; Broker Updates</strong></p><p>This is a critical security update covering <strong>10 commits</strong> since v2.0.0.5. It closes the highest-impact remote attack surface in Tradeboard&#39;s web tier (forge-able session cookies signed with the publicly-known sample <code>APP_KEY</code>), encrypts every remaining plaintext credential in the database, gates client-supplied forwarded-IP headers behind an explicit <code>TRUST_PROXY_HEADERS</code> flag (so attackers can no longer spoof source IP to dodge IP bans / rate limits), invalidates all sessions on password change, fixes the Docker <code>.env</code> permission bug behind issue #960, and patches a transitive <code>postcss</code> CVE pulled in by the React build.</p><blockquote><p><strong>Every running deployment should pull and apply this update.</strong> Most installs only need the standard four-step upgrade (stop → pull → migrate → restart) — the code changes are backward-compatible and your data, authenticator codes, broker session, and external API keys are all preserved. See the <strong>Required Upgrade Steps</strong> at the bottom.</p></blockquote><hr><p><strong>Highlights</strong></p><ul><li><strong>Per-instance APP_KEY auto-rotation</strong> — <code>.sample.env</code> no longer ships real-looking hex values for <code>APP_KEY</code> / <code>API_KEY_PEPPER</code>. Instead it carries placeholder strings; the app detects them on first run and replaces them in <code>.env</code> with cryptographically random values via <code>secrets.token_hex(32)</code>. This closes the entire class of &quot;forge a session cookie offline using the public sample key&quot; attacks against any deployment that previously copied <code>.sample.env</code> to <code>.env</code> without manually rotating.</li><li><strong>Credentials encrypted at rest</strong> — TOTP secrets, the Telegram bot token, the Samco 2FA secret, and Flow workflow API keys are now Fernet-encrypted in the database, matching the existing pattern used for broker tokens and TradingView API keys. The Flask session cookie also no longer carries the user&#39;s TOTP secret (it was signed but not encrypted, exposing the seed via cookie reads). All read paths fall back to plaintext for pre-update rows so existing data continues to work without a forced migration.</li><li><strong>Forwarded-IP header trust gate (TRUST_PROXY_HEADERS)</strong> — <code>utils/ip_helper.py</code> previously honoured <code>CF-Connecting-IP</code> / <code>X-Forwarded-For</code> / <code>X-Real-IP</code> / <code>True-Client-IP</code> / <code>X-Client-IP</code> from any client unconditionally. Direct-bind deployments could be fooled by a client setting these headers to fake their source IP, silently bypassing the IP ban list, the per-IP login rate-limiter, the 404 auto-ban tracker, and the login-attempt audit log. Headers are now ignored by default and only trusted when <code>TRUST_PROXY_HEADERS=TRUE</code> is set; the install scripts that configure nginx set this automatically.</li><li><strong>Password change kicks every browser session</strong> — <code>change_password</code>, <code>change_password_api</code>, and the <code>/auth/reset-password</code> flow now clear all active sessions and emit <code>force_logout</code> so other devices stop accepting the old cookie immediately. Closes the gap where an attacker holding a stolen cookie kept their session even after the legitimate user changed the password.</li><li><strong>Docker <code>.env</code> permission fix (closes #960)</strong> — fresh Docker installs no longer crash-loop with <code>Error: .env file not found.</code> because the install scripts now use a permission mode that the container&#39;s <code>appuser</code> can read via the bind mount. The Profile → System health check is Docker-aware and recommends the correct mode for each deployment type.</li><li><strong>postcss CVE-2026-41305</strong> — pinned to <code>&gt;=8.5.10</code> (resolves to 8.5.12) via npm <code>overrides</code>. Closes Dependabot alert #153.</li><li><strong>Two earlier credential-leak vectors</strong> — fixes carried in <code>2c53573f</code> (closes session and log leaks identified in the security audit).</li><li><strong>Broker fixes</strong> — Paytm and Groww decimal-strike option symbols (#908), Kotak native MPP via the <code>mp</code> parameter (#1293), sandbox <code>FundManager._lock</code> self-deadlock when the funds row is missing (#1274).</li></ul><hr><p><strong>Security</strong></p><p><strong>APP_KEY / API_KEY_PEPPER</strong></p><ul><li><code>.sample.env</code> now uses placeholder strings instead of real-looking hex values. New users running <code>cp .sample.env .env</code> followed by <code>uv run app.py</code> see a one-time green <code>[Tradeboard first-run setup]</code> message; the app generates fresh secrets and writes them back to <code>.env</code> atomically.</li><li>Existing users who installed via <code>install.sh</code> (custom keys generated at install time) see no change — the check is a single <code>frozenset</code> lookup that returns immediately for any non-placeholder value.</li><li>The same detection covers <code>.env</code> files copied from a pre-fix commit (the historical leaked literals are also recognised and rotated).</li><li><code>update.sh</code> and <code>update.bat</code> no longer fall through to a <code>cp .sample.env .env</code> that leaves placeholder values in place; they now generate fresh keys in that recovery branch.</li><li><code>.env</code> is tightened to mode <code>0o600</code> on bare-metal installs and <code>0o644</code> on Docker installs (UID 1000 must read it via the bind mount). <code>update.sh</code> retroactively tightens existing-install <code>.env</code> perms when run.</li></ul><p><strong>Encrypted-at-rest credentials</strong></p><p>Four columns previously stored in plaintext are now Fernet-encrypted on write and decrypted on read:</p><ul><li><code>users.totp_secret</code></li><li><code>bot_config.token</code> (Telegram bot token)</li><li><code>auth.secret_api_key</code> (Samco 2FA secret)</li><li><code>flow_workflows.api_key</code> (Flow workflow Tradeboard API key)</li></ul><p>A backward-compatible read path means existing rows continue to work between the code update and any operator-driven re-encryption pass — there is no in-between broken state.</p><p>The Flask session cookie no longer carries <code>session[&quot;totp_secret&quot;]</code> (Flask sessions are signed, not encrypted; the cookie was leaking the TOTP seed to anyone who could read the cookie value).</p><p><strong>Reverse-proxy trust (TRUST_PROXY_HEADERS)</strong></p><ul><li>New <code>.env</code> flag, default <code>FALSE</code>. When false, <code>get_real_ip()</code> returns the immediate peer address only; client-supplied forwarded-IP headers are ignored.</li><li><code>install.sh</code>, <code>install-multi.sh</code>, <code>install-docker.sh</code>, and <code>install-docker-multi-custom-ssl.sh</code> set this to <code>TRUE</code> automatically because they configure an nginx reverse proxy as part of the install (gunicorn binds on a Unix socket / container-gateway-only port that cannot be reached directly from the internet, so the proxy headers are trustworthy).</li><li><code>update.sh</code> migrates existing installs by detecting nginx in <code>/etc/nginx/sites-enabled</code> or <code>/etc/nginx/conf.d</code> and adding the appropriate value to <code>.env</code> — <code>TRUE</code> if a proxy is found, <code>FALSE</code> otherwise. No manual edit needed for the common reverse-proxied bare-metal install.</li><li>Local dev (<code>cp .sample.env .env</code> + <code>uv run app.py</code>) and <code>docker-run.sh</code> desktop installs leave it <code>FALSE</code>, which is the correct default for direct-bind setups.</li></ul><p><strong>Password change session invalidation</strong></p><ul><li><code>blueprints/auth.py:change_password</code>, <code>change_password_api</code>, and the <code>/auth/reset-password</code> <code>step=&quot;password&quot;</code> branch all now call <code>clear_user_sessions(username)</code> and emit <code>socketio.emit(&quot;force_logout&quot;, …)</code> after a successful password change. Every device — including the one that just changed the password — gets logged out and prompted to log in again with the new credentials.</li><li>This matches the convention used by banking and high-security web apps: a password change is a hard reset of the auth state, on the assumption that the change was triggered by suspected compromise (or routine rotation that wants the same effect).</li></ul><p><strong>postcss XSS (CVE-2026-41305)</strong></p><ul><li>Bumped via <code>frontend/package.json</code> <code>overrides</code> to <code>postcss &gt;= 8.5.10</code>. Resolves to 8.5.12 in the lockfile.</li><li><code>npm install</code> now reports 0 vulnerabilities.</li></ul><p><strong>Pip CVE-2026-3219</strong></p><ul><li>Dependabot alert #152 dismissed as <code>tolerable_risk</code>. <code>pip</code> is transitive in <code>uv.lock</code>, only invoked at install time, never at runtime. The advisory has no <code>first_patched_version</code> published yet; will revisit when <code>uv</code> picks up the upstream fix.</li></ul><p><strong>Optional operator tool: <code>upgrade/rotate_pepper.py</code></strong></p><p>A standalone, opt-in tool is included for operators who want to additionally rotate <code>API_KEY_PEPPER</code> to a fresh value and re-encrypt every credential in the database. It is intentionally <strong>not</strong> wired into <code>migrate_all.py</code> because rotating the pepper invalidates Argon2 password hashes (Argon2 is one-way; hashes cannot be migrated) — running it requires a one-time password reset via the TOTP flow afterwards. Use this only if your <code>.env</code> ever held the publicly-known sample <code>API_KEY_PEPPER</code> literal and you want to make absolutely sure no attacker has a copy of decryptable ciphertext from your database. Most users will never need it; the regular four-step upgrade below is sufficient.</p><hr><p><strong>Bug Fixes</strong></p><p><strong>Docker / Install</strong></p><ul><li><strong><code>.env</code> permission bug (#960)</strong> — host <code>.env</code> at mode <code>0o600</code> was unreadable to the container&#39;s <code>appuser</code> (UID 1000) when bind-mounted, causing <code>start.sh</code> to exit with <code>Error: .env file not found.</code> and crash-loop the container. Fixed across <code>install/install-docker.sh</code>, <code>install/install-docker-multi-custom-ssl.sh</code>, and <code>install/docker-run.sh</code> (now use mode <code>0o644</code>). The Profile → System health check at <code>/api/system</code> is Docker-aware and expects the correct mode per deployment.</li><li><strong><code>update.sh</code> recovery branch</strong> — when <code>.env</code> was missing, the script previously copied <code>.sample.env</code> verbatim and exited; the running app would have hit the public sample keys. Now generates fresh keys via <code>secrets.token_hex(32)</code> in that path.</li><li><strong><code>update.sh</code> existing-install hardening</strong> — automatically adds <code>TRUST_PROXY_HEADERS</code> (auto-detected) and tightens <code>.env</code> perms to <code>0o600</code> on the next update if not already set.</li></ul><p><strong>Brokers</strong></p><ul><li><strong>Paytm and Groww decimal strikes (#908)</strong> — option symbols like <code>VEDL25APR24292.5CE</code> now retain the <code>.5</code> instead of being silently stripped during symbol normalisation.</li><li><strong>Kotak — native MPP via <code>mp</code> parameter (#1293)</strong> — replace the local <code>MKT → LMT</code> rewrite with the broker&#39;s native market-protection-price flag.</li></ul><p><strong>Sandbox</strong></p><ul><li><strong><code>FundManager._lock</code> self-deadlock (#1274)</strong> — when the sandbox funds row was missing, the <code>_lock</code>-holding code path tried to acquire the same lock again. Fix unwinds the call so the missing-row branch releases the lock first.</li></ul><p><strong>Auth / Logging</strong></p><ul><li><strong>Two credential-leak vectors closed in <code>2c53573f</code></strong> — session and log paths that could surface API keys / tokens in error contexts.</li></ul><hr><p><strong>Frontend / Tooling</strong></p><ul><li><code>frontend/package.json</code> overrides extended with <code>postcss &gt;= 8.5.10</code>.</li><li><code>db/backups/</code> added to <code>.gitignore</code> (created automatically by the optional pepper-rotation tool).</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>postcss</code>: pinned <code>&gt;= 8.5.10</code> (resolved 8.5.12) — closes CVE-2026-41305 / GHSA-qx2v-qp2m-jg93.</li></ul><hr><p><strong>Required Upgrade Steps</strong></p><p>The standard four-step upgrade. Your data, broker session, authenticator codes, and TradingView/Chartink API keys are all preserved automatically. No new password reset is required for the regular upgrade path.</p><p><strong>1. Stop Tradeboard</strong></p><table tabindex="0"><thead><tr><th>Deployment</th><th>Command</th></tr></thead><tbody><tr><td>Bare-metal (systemd)</td><td><code>sudo systemctl stop tradeboard-&lt;your-deployment&gt;</code></td></tr><tr><td>Docker</td><td><code>cd /opt/tradeboard &amp;&amp; sudo docker compose down</code></td></tr><tr><td>Docker desktop</td><td><code>./install/docker-run.sh stop</code></td></tr><tr><td>Local dev</td><td>Ctrl+C in the terminal running <code>uv run app.py</code></td></tr></tbody></table><p><strong>2. Pull the latest code and dependencies</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span></code></pre></div><p>If you&#39;re on Docker and your <code>.env</code> was at mode <code>0o600</code>, run this once to unbreak the container before the next start:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 644</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .env</span></span></code></pre></div><p>If you installed via <code>install.sh</code> and want the new <code>TRUST_PROXY_HEADERS</code> flag + tightened <code>.env</code> perms applied automatically, run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install/update.sh</span></span></code></pre></div><p>It detects nginx, sets the right value in <code>.env</code>, and tightens perms to <code>0o600</code>. (Skipping this is fine — IP-based features still work; they just see the proxy IP instead of the real client IP until you set <code>TRUST_PROXY_HEADERS=TRUE</code> manually.)</p><p><strong>3. Apply standard database migrations</strong></p><p>This is the same step you&#39;ve always run on every release.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate_all.py</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span></span></code></pre></div><p><strong>4. Restart Tradeboard</strong></p><table tabindex="0"><thead><tr><th>Deployment</th><th>Command</th></tr></thead><tbody><tr><td>Bare-metal (systemd)</td><td><code>sudo systemctl start tradeboard-&lt;your-deployment&gt;</code></td></tr><tr><td>Docker</td><td><code>cd /opt/tradeboard &amp;&amp; sudo docker compose up -d</code></td></tr><tr><td>Docker desktop</td><td><code>./install/docker-run.sh start</code></td></tr><tr><td>Local dev</td><td><code>uv run app.py</code></td></tr></tbody></table><p><strong>What&#39;s preserved (no action needed)</strong></p><ul><li><strong>Authenticator app codes</strong> — your TOTP enrolment is unchanged. The same QR you scanned at setup keeps producing valid codes.</li><li><strong>Login password</strong> — the regular four-step upgrade does not change your password. You keep using the same credentials.</li><li><strong>Broker session</strong> — your saved broker auth token still works. No need to re-OAuth with your broker.</li><li><strong>TradingView / Chartink / Excel / Python API keys</strong> — your Tradeboard API key value is unchanged. External integrations continue to work without any configuration change.</li><li><strong>All trades, positions, strategies, watchlists, settings, logs.</strong></li></ul><p><strong>What changes</strong></p><ul><li>If your <code>.env</code> ever held the publicly-known sample <code>APP_KEY</code> value, the app rotates it to a fresh random value on first start and prints a one-time <code>[Tradeboard first-run setup]</code> line in the console. Your existing browser session ends and you log in again with the same password. Most users who installed via <code>install.sh</code> already have custom keys and see no message.</li><li><code>TRUST_PROXY_HEADERS</code> is added to your <code>.env</code> on the next <code>update.sh</code> run (auto-detected, no manual edit required).</li><li>New TOTP secrets, Telegram bot tokens, Samco 2FA secrets, and Flow workflow API keys created from this release onward are stored encrypted at rest. Existing rows continue to work transparently via the backward-compatible read path.</li></ul><p><strong>Optional: full credential refresh (operator-driven)</strong></p><p>If your installation ever ran with the publicly-known sample <code>API_KEY_PEPPER</code> value in <code>.env</code>, you can additionally run the standalone pepper-rotation tool to generate a fresh pepper and re-encrypt every credential in the database. This is a one-time operator decision and requires a one-time password reset via the TOTP flow afterwards. The tool prompts for confirmation, creates a database backup before any change, and is documented inline:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rotate_pepper.py</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Type &#39;yes&#39; when prompted. Then restart and use /auth/reset-password (TOTP)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># to set a new password.</span></span></code></pre></div><p>Most users do not need this — <code>install.sh</code> deployments have always generated fresh per-instance keys, so their database has never been encrypted under a publicly-known value.</p><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management, broker fixes (Paytm/Groww strike preservation, Kotak native MPP), sandbox <code>FundManager</code> fix, websocket polish, and the v2.0.0.6 critical security update including APP_KEY/PEPPER hardening, encrypted-at-rest credential overhaul, the optional pepper-rotation tool, the reverse-proxy trust gate, password-change session invalidation, the Docker <code>.env</code> permission fix, and the postcss CVE bump.</li></ul><hr><p><strong>Upgrade Quick Reference</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Stop Tradeboard (see table above for your deployment type)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. Pull and sync</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3. (Optional, server-mode only) Apply install-script hardening</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install/update.sh</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 4. Run standard migrations</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> upgrade</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate_all.py</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 5. Restart Tradeboard (see table above for your deployment type)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 6. Open the web UI -&gt; Login (same password as before)</span></span></code></pre></div><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul><hr>`,78)])])}const u=s(n,[["render",i]]);export{g as __pageData,u as default};
