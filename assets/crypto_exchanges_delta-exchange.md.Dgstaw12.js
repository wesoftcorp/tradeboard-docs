import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.BXzK3EA4.js";const r="/assets/Delta1%20(1).C0uBWROR.png",s="/assets/Delta2%20(1).BiaqvxtM.png",i="/assets/Delta3%20(1).BriNil_C.png",c="/assets/Delat5%20(1).oOVMKaMP.png",l="/assets/Delta6%20(1).Dj0YTxJQ.png",b=JSON.parse('{"title":"Delta Exchange","description":"","frontmatter":{},"headers":[],"relativePath":"crypto/exchanges/delta-exchange.md","filePath":"crypto/exchanges/delta-exchange.md"}'),d={name:"crypto/exchanges/delta-exchange.md"};function h(p,e,g,u,y,f){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="delta-exchange" tabindex="-1">Delta Exchange <a class="header-anchor" href="#delta-exchange" aria-label="Permalink to &quot;Delta Exchange&quot;">​</a></h1><p>Tradeboard&#39;s <code>deltaexchange</code> plugin is the repository&#39;s crypto-derivatives adapter. Its <code>plugin.json</code> declares broker type <code>crypto</code>, exchange <code>CRYPTO</code>, and leverage configuration support. The adapter maps supported Delta Exchange order, account, data, and streaming operations into Tradeboard&#39;s normalized interfaces.</p><p>Product availability, fees, settlement, API-key requirements, and account eligibility are controlled by Delta Exchange and can change. Tradeboard does not provide legal or tax classification; verify current exchange documentation and the rules that apply to your location before trading.</p><h3 id="how-delta-exchange-differs-from-a-securities-broker" tabindex="-1">How Delta Exchange differs from a securities broker <a class="header-anchor" href="#how-delta-exchange-differs-from-a-securities-broker" aria-label="Permalink to &quot;How Delta Exchange differs from a securities broker&quot;">​</a></h3><p>This is the only <code>crypto</code> plugin in Tradeboard, and it behaves differently from the Indian securities brokers in four ways that affect how you write strategies.</p><ul><li><strong>One exchange code.</strong> Every Delta instrument sits on the single Tradeboard exchange <code>CRYPTO</code>. There is no NSE/BSE/NFO split. Internally the plugin records the broker exchange as <code>DELTAIN</code> (Delta Exchange India).</li><li><strong>Fractional quantities, on spot only.</strong> Securities plugins take whole share or lot counts. Delta spot instruments accept fractional sizes such as <code>0.0001 BTC</code> or <code>0.05 SOL</code>, and the lot size stored for each instrument comes from the exchange&#39;s own <code>min_order_size</code>. Futures, perpetuals and options are still counted in <strong>whole contracts</strong>: passing a fractional quantity for a derivative is rejected before the order is sent.</li><li><strong>No CNC/MIS/NRML.</strong> Delta has no equity-style product types. Whatever product you pass is forwarded untouched, and every position read back reports as <code>NRML</code>.</li><li><strong>24/7 markets.</strong> Crypto does not close, so there is no daily session rollover to align with.</li></ul><h3 id="order-types-and-leverage" tabindex="-1">Order types and leverage <a class="header-anchor" href="#order-types-and-leverage" aria-label="Permalink to &quot;Order types and leverage&quot;">​</a></h3><p>The plugin maps Tradeboard price types onto Delta&#39;s order model as follows:</p><ul><li><code>MARKET</code> and <code>SL-M</code> become <code>market_order</code></li><li><code>LIMIT</code> and <code>SL</code> become <code>limit_order</code>, with <code>SL</code> adding the stop trigger</li></ul><p>Delta requires leverage to be configured by a separate call before the order is submitted, so Tradeboard sets it for you. The value is resolved in priority order: the <code>leverage</code> field on the order, then the common leverage saved in Tradeboard&#39;s leverage settings, then the environment fallback. Leverage must be a whole number.</p><h3 id="instruments-in-the-master-contract" tabindex="-1">Instruments in the master contract <a class="header-anchor" href="#instruments-in-the-master-contract" aria-label="Permalink to &quot;Instruments in the master contract&quot;">​</a></h3><p>Only contracts that are <strong>live</strong> and <strong>operational</strong> are downloaded. Contracts the exchange has put into reduce-only mode (pre-expiry wind-down or liquidation-only) are skipped deliberately, because storing them would let you place orders the API would reject.</p><p>Instrument types you will see include <code>PERPFUT</code> (perpetual futures), <code>FUT</code>, <code>CE</code> and <code>PE</code> (options), <code>SPOT</code>, <code>MOVE</code>, <code>SPREAD</code>, <code>COMBO</code>, <code>IRS</code>, the turbo options <code>TCE</code> and <code>TPE</code>, and the synthetic options <code>SYNCE</code> and <code>SYNPE</code>. Perpetuals have no expiry date; dated contracts carry one.</p><h3 id="api-key-registration" tabindex="-1">API Key Registration <a class="header-anchor" href="#api-key-registration" aria-label="Permalink to &quot;API Key Registration&quot;">​</a></h3><p>To integrate Tradeboard with Delta Exchange India, you need to generate an API key from within your Delta Exchange account. The API key serves as the authentication mechanism for all programmatic trading requests.</p><h4 id="step-1-log-in-to-delta-exchange-india" tabindex="-1">Step 1: Log in to Delta Exchange India <a class="header-anchor" href="#step-1-log-in-to-delta-exchange-india" aria-label="Permalink to &quot;Step 1: Log in to Delta Exchange India&quot;">​</a></h4><p>Visit <a href="https://delta.exchange" target="_blank" rel="noreferrer">delta.exchange</a> and log in to your account using your Gmail and password</p><figure><img src="'+r+'" alt=""><figcaption></figcaption></figure><figure><img src="'+s+'" alt=""><figcaption></figcaption></figure><h4 id="step-2-navigate-to-algohub-apis" tabindex="-1">Step 2: Navigate to AlgoHub &gt; APIs <a class="header-anchor" href="#step-2-navigate-to-algohub-apis" aria-label="Permalink to &quot;Step 2: Navigate to AlgoHub &gt; APIs&quot;">​</a></h4><figure><img src="'+i+'" alt=""><figcaption></figcaption></figure><p>Once logged in, click on the <strong>AlgoHub</strong> menu in the top navigation bar. From the dropdown, select <strong>APIs</strong> (&quot;Create API key and Start Trading&quot;). This takes you to the Delta Exchange API landing page where you can manage your API keys.</p><h4 id="step-3-create-a-new-api-key" tabindex="-1">Step 3: Create a New API Key <a class="header-anchor" href="#step-3-create-a-new-api-key" aria-label="Permalink to &quot;Step 3: Create a New API Key&quot;">​</a></h4><figure><img src="'+c+'" alt=""><figcaption></figcaption></figure><figure><img src="'+l+`" alt=""><figcaption></figcaption></figure><p>Click the <strong>Create API Key</strong> button on the API page. You will be taken to the API key creation form at delta.exchange/app/account/manageapikeys . Fill in the following details:</p><ul><li><strong>Account Name</strong>: select <strong>Main</strong> (or the sub-account you want the key associated with)</li><li><strong>API Key Name</strong>: enter a recognisable name, e.g., tradeboard</li><li><strong>Whitelisted IP</strong>: enter the IP address of your Tradeboard server. Only whitelisted IPs will be permitted to interact with the trading API. Add the IP and select it from the <strong>IPs to whitelist</strong> dropdown.</li><li><strong>Permissions</strong>: enable both: <ul><li><strong>Read Data</strong>: enabled by default for all API keys</li><li><strong>Trading</strong>: must be explicitly selected to allow order placement</li></ul></li></ul><h4 id="step-4-save-the-api-key-and-secret" tabindex="-1">Step 4: Save the API Key and Secret <a class="header-anchor" href="#step-4-save-the-api-key-and-secret" aria-label="Permalink to &quot;Step 4: Save the API Key and Secret&quot;">​</a></h4><p>After submitting the form, Delta Exchange will display your <strong>API Key</strong> and <strong>API Secret</strong>. Copy and store them securely, because the secret will not be shown again. Add them to your Tradeboard <code>.env</code> file as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BROKER_API_KEY = &#39;your_api_key_here&#39;</span></span>
<span class="line"><span>BROKER_API_SECRET = &#39;your_api_secret_here&#39;</span></span>
<span class="line"><span>REDIRECT_URL = &#39;http://127.0.0.1:5000/deltaexchange/callback&#39;</span></span></code></pre></div><p>There is no browser login step. Clicking <strong>Connect</strong> on the broker page signs a request with the key and secret above and validates it against Delta directly. An invalid key or a bad signature is reported as such, so check both values before looking elsewhere.</p><h4 id="settings-for-a-24-7-market" tabindex="-1">Settings for a 24/7 market <a class="header-anchor" href="#settings-for-a-24-7-market" aria-label="Permalink to &quot;Settings for a 24/7 market&quot;">​</a></h4><p>Two further settings in <code>.env</code> exist because crypto never closes. The install script sets them for you when you choose a crypto broker; set them by hand if you are converting an existing install.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Crypto markets run 24/7, so skip the daily session logout</span></span>
<span class="line"><span>DISABLE_SESSION_EXPIRY = &#39;true&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Master contract cache boundary, in UTC rather than IST</span></span>
<span class="line"><span>CRYPTO_MASTER_CONTRACT_CUTOFF_TIME = &#39;00:00&#39;</span></span></code></pre></div><p>Left at its default of <code>false</code>, <code>DISABLE_SESSION_EXPIRY</code> logs every session out at <code>SESSION_EXPIRY_TIME</code> (03:00 IST by default), which would interrupt a strategy mid-session. <code>CRYPTO_MASTER_CONTRACT_CUTOFF_TIME</code> replaces the IST-aligned cutoff used for Indian exchanges: the first login of each UTC day fetches a fresh instrument list and later logins that day reuse the cache.</p><p>The Delta Exchange plugin supports automated crypto-derivatives workflows where the connected account and instrument are available. Protect API credentials, use the exchange&#39;s current network controls, handle API failures explicitly, and test with the exchange&#39;s test environment before live use. Users remain responsible for verifying the legal, tax, and account rules that apply to their location and activity.</p>`,36)])])}const k=t(d,[["render",h]]);export{b as __pageData,k as default};
