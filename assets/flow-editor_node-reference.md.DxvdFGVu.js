import{_ as o,o as t,c as d,a2 as a}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Node Reference","description":"","frontmatter":{},"headers":[],"relativePath":"flow-editor/node-reference.md","filePath":"flow-editor/node-reference.md"}'),r={name:"flow-editor/node-reference.md"};function c(n,e,i,s,l,h){return t(),d("div",null,[...e[0]||(e[0]=[a('<h1 id="node-reference" tabindex="-1">Node Reference <a class="header-anchor" href="#node-reference" aria-label="Permalink to &quot;Node Reference&quot;">​</a></h1><p>Every node type Flow provides, 61 in total, with its configuration fields, what it writes to its output variable, and the traps specific to it.</p><p>Fields marked <strong>required</strong> must be set or the node fails at run time. Every data node takes an <code>outputVariable</code>; if you leave it blank the node still runs but nothing downstream can read it.</p><p>Text fields accept <code>&amp;#123;&amp;#123;variable&amp;#125;&amp;#125;</code> interpolation. <strong>Trigger fields do not</strong>: a trigger is registered with the scheduler or a monitor at activation, before anything has been computed, so a <code>&amp;#123;&amp;#123;reference&amp;#125;&amp;#125;</code> there has nothing to resolve against.</p><p><strong>Flow ignores a <code>data</code> key that nothing reads.</strong> Writing <code>strikeOffset</code> where the field is <code>offset</code> imports cleanly, runs successfully, and silently uses the default; nothing in the run record says so. Check field names against the tables below, and validate a hand-written workflow before importing it rather than after.</p><hr><h2 id="triggers" tabindex="-1">Triggers <a class="header-anchor" href="#triggers" aria-label="Permalink to &quot;Triggers&quot;">​</a></h2><p>A workflow must have exactly <strong>one</strong> trigger, and there are exactly four: <code>start</code>, <code>priceAlert</code>, <code>webhookTrigger</code>, and <code>orderUpdateTrigger</code>. A second trigger is rejected by strict validation with a <code>multiple_triggers</code> error, so <strong>Run Now</strong>, activation, and import all refuse the graph. See <a href="./concepts.html#triggers">Concepts</a>.</p><h3 id="start-schedule" tabindex="-1"><code>start</code>: Schedule <a class="header-anchor" href="#start-schedule" aria-label="Permalink to &quot;`start`: Schedule&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>scheduleType</code></td><td><code>once</code>, <code>daily</code>, <code>weekly</code>, <code>interval</code>, <code>manual</code></td><td>Required. <code>manual</code> registers no job at all. The panel offers the first four.</td></tr><tr><td><code>time</code></td><td><code>&quot;HH:MM&quot;</code></td><td>For <code>daily</code> and <code>weekly</code>.</td></tr><tr><td><code>days</code></td><td><code>[0,1,2,3,4]</code></td><td>For <code>weekly</code>. <strong>0 = Monday</strong>, 6 = Sunday.</td></tr><tr><td><code>executeAt</code></td><td>date</td><td>For <code>once</code>. The editor writes a bare <code>YYYY-MM-DD</code>, so a <code>once</code> schedule fires at midnight and the panel&#39;s Time box does not apply to it.</td></tr><tr><td><code>intervalValue</code> + <code>intervalUnit</code></td><td>number + <code>seconds</code> / <code>minutes</code> / <code>hours</code></td><td>For <code>interval</code>.</td></tr><tr><td><code>marketHoursOnly</code></td><td>boolean, default <code>true</code> on a new node</td><td>Gates runs to the trading session below.</td></tr><tr><td><code>marketHoursStart</code></td><td><code>&quot;HH:MM&quot;</code>, default <code>09:15</code></td><td>Start of the window.</td></tr><tr><td><code>marketHoursEnd</code></td><td><code>&quot;HH:MM&quot;</code>, default <code>15:15</code></td><td>End of the window. <code>15:15</code> leaves room to square off before a 15:30 close.</td></tr><tr><td><code>marketHoursExchange</code></td><td>exchange code, default <code>NSE</code></td><td>Which exchange calendar the window is measured against.</td></tr></tbody></table><p><code>intervalMinutes</code> appears in older exports and is read by nothing. A workflow relying on it schedules at the default of one minute.</p><p><strong>The window narrows the exchange&#39;s own session, it never reopens it.</strong> The day is resolved through the platform&#39;s market calendar, so a weekend, a trading holiday, or a day the exchange is otherwise shut stays shut whatever <code>marketHoursStart</code> and <code>marketHoursEnd</code> say. A workflow cannot configure its way into trading on Diwali. Within a day the exchange is open, the two times narrow or widen the clock only.</p><p>Set <code>marketHoursExchange</code> to what you actually trade. The calendar knows MCX runs to 23:55 and that CRYPTO never closes; a workflow left on <code>NSE</code> inherits equity hours and stops firing at the equity close.</p><p>All four fields are read from the workflow graph on <strong>every run</strong>, so editing the window applies from the next run without deactivating and reactivating.</p><p><strong>A <code>start</code> node saved before these fields existed carries no <code>marketHoursOnly</code> key, and an absent key is read as off.</strong> The panel renders the switch as on in that case, so the editor and the run disagree. Toggle the switch off and on, then save, to store the key explicitly.</p><p>An <code>interval</code> schedule is <strong>anchored to the clock, not to activation</strong>. A 5-minute job lands on :00, :05, :10 whenever the workflow happened to be switched on, plus a small offset (<code>FLOW_INTERVAL_ALIGN_OFFSET</code>, default 2 seconds). The offset is not zero on purpose: firing exactly on the boundary races the bar that is closing, and whether the feed has opened the next one changes the answer by a whole candle. Sub-minute intervals are left unaligned, because there is no meaningful boundary to align to.</p><p>Jobs persist in the <code>flow_apscheduler_jobs</code> table and survive restarts. The scheduler uses the server process local timezone for cron and date triggers; only the market-hours gate is explicitly IST, so run Tradeboard with the host clock set to IST.</p><p>Overlapping runs are <strong>dropped, not deferred</strong>. The job defaults are <code>coalesce: true</code>, <code>max_instances: 1</code>, and a 60-second misfire grace, and <code>execute_workflow</code> takes the per-workflow lock without blocking and returns <code>already_running</code>. A tick that arrives during a slow run is lost.</p><h3 id="pricealert-price-alert" tabindex="-1"><code>priceAlert</code>: Price Alert <a class="header-anchor" href="#pricealert-price-alert" aria-label="Permalink to &quot;`priceAlert`: Price Alert&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td></td><td>Required.</td></tr><tr><td><code>condition</code></td><td>see below</td><td>13 canonical values. Required.</td></tr><tr><td><code>price</code></td><td>number</td><td>The level to watch. Required for the level and crossing conditions.</td></tr><tr><td><code>priceLower</code> + <code>priceUpper</code></td><td>number</td><td>Required for the four channel conditions. <code>priceLower</code> must be below <code>priceUpper</code>.</td></tr><tr><td><code>percentage</code></td><td>number</td><td>Required for <code>moving_up_percent</code> / <code>moving_down_percent</code>.</td></tr><tr><td><code>trigger</code></td><td><code>once</code>, <code>every_time</code></td><td>Default <code>once</code>.</td></tr><tr><td><code>expiration</code></td><td><code>none</code>, <code>1h</code>, <code>4h</code>, <code>1d</code>, <code>1w</code></td><td>Retires the alert after this window.</td></tr></tbody></table><p>The 13 conditions the monitor evaluates:</p><table tabindex="0"><thead><tr><th>Group</th><th>Values</th><th>Needs</th></tr></thead><tbody><tr><td>Level</td><td><code>greater_than</code>, <code>less_than</code></td><td><code>price</code></td></tr><tr><td>Crossing</td><td><code>crossing</code>, <code>crossing_up</code>, <code>crossing_down</code></td><td><code>price</code></td></tr><tr><td>Channel</td><td><code>entering_channel</code>, <code>inside_channel</code>, <code>exiting_channel</code>, <code>outside_channel</code></td><td><code>priceLower</code>, <code>priceUpper</code></td></tr><tr><td>Movement</td><td><code>moving_up</code>, <code>moving_down</code></td><td>nothing extra</td></tr><tr><td>Percentage move</td><td><code>moving_up_percent</code>, <code>moving_down_percent</code></td><td><code>percentage</code></td></tr></tbody></table><p><code>above</code>, <code>below</code>, <code>crosses_above</code>, and <code>crosses_below</code> are accepted as aliases of <code>greater_than</code>, <code>less_than</code>, <code>crossing_up</code>, and <code>crossing_down</code>.</p><p><strong>A crossing condition can fire on its very first evaluation.</strong> With no previous tick recorded, <code>crossing_up</code> falls back to a plain level test (<code>current &gt; target</code>) rather than returning false. An alert armed while price is already above its level therefore fires on the next poll, not on the next genuine crossing.</p><p>A <code>once</code> alert does not merely remove its own watch when it fires: it <strong>deactivates the whole workflow</strong>. Reactivate it from the editor to arm it again.</p><p>The <code>enabled</code> field exists in older exports and is read by nothing.</p><h3 id="webhooktrigger-webhook" tabindex="-1"><code>webhookTrigger</code>: Webhook <a class="header-anchor" href="#webhooktrigger-webhook" aria-label="Permalink to &quot;`webhookTrigger`: Webhook&quot;">​</a></h3><p>Fires when its URL is POSTed. This is how TradingView, ChartInk, Amibroker and Excel drive a Flow workflow.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>label</code></td><td>string</td><td>Display name on the canvas. The only field the node has.</td></tr></tbody></table><p><strong>The trigger carries no instrument.</strong> It used to declare <code>symbol</code> and <code>exchange</code>, and this documentation once described <code>symbol</code> as a request filter that rejected non-matching requests. <strong>It never was one.</strong> The executor read neither field, so they only labelled the node on the canvas. Both are gone. Everything the workflow acts on arrives in the request body and is read downstream as <code>&amp;#123;&amp;#123;webhook.&lt;field&gt;&amp;#125;&amp;#125;</code>, for example <code>&amp;#123;&amp;#123;webhook.symbol&amp;#125;&amp;#125;</code> and <code>&amp;#123;&amp;#123;webhook.action&amp;#125;&amp;#125;</code>.</p><p>The URL is not stored on the node either. It is derived from the workflow&#39;s own <code>webhook_token</code>, which the editor fetches from <code>GET /api/workflows/&lt;id&gt;/webhook</code>, so there is no <code>webhookId</code> or <code>webhookUrl</code> field.</p><p><strong>The body is parsed as JSON whatever the sender declared.</strong> The platforms posting here are the ones least able to set a <code>Content-Type</code>, so the header is treated as a hint rather than the truth:</p><table tabindex="0"><thead><tr><th>Body</th><th>Becomes</th></tr></thead><tbody><tr><td>JSON object, any declared content type</td><td>its own keys</td></tr><tr><td>Form-encoded</td><td>its own fields</td></tr><tr><td>JSON that is not an object (a list, a bare number)</td><td><code>&amp;#123;&amp;#123;webhook.message&amp;#125;&amp;#125;</code> plus <code>&amp;#123;&amp;#123;webhook.payload&amp;#125;&amp;#125;</code></td></tr><tr><td>Anything else</td><td><code>&amp;#123;&amp;#123;webhook.message&amp;#125;&amp;#125;</code> holding the raw text</td></tr></tbody></table><p><strong>A secret in the payload still requires JSON.</strong> With <code>payload</code> auth the secret is a field and plain text has nowhere to put one, so such a request is refused with 401 and never reaches the workflow. Send JSON, or switch the webhook to URL auth and pass <code>?secret=...</code>.</p><p>Nothing in the payload is validated for you, so guard on the fields you rely on with a condition node before acting.</p><h3 id="orderupdatetrigger-order-update" tabindex="-1"><code>orderUpdateTrigger</code>: Order Update <a class="header-anchor" href="#orderupdatetrigger-order-update" aria-label="Permalink to &quot;`orderUpdateTrigger`: Order Update&quot;">​</a></h3><p>Fires when an order&#39;s status changes, pushed from the same event stream as the account WebSocket feed, no polling.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>orderId</code></td><td>string</td><td>Watch one order. Must be a literal: <code>&amp;#123;&amp;#123;...&amp;#125;&amp;#125;</code> is rejected outright.</td></tr><tr><td><code>symbol</code></td><td></td><td>Watch a symbol.</td></tr><tr><td><code>exchange</code></td><td></td><td>Blank matches any exchange; an explicit value must match.</td></tr><tr><td><code>status</code></td><td><code>any</code>, <code>open</code>, <code>trigger pending</code>, <code>complete</code>, <code>rejected</code>, <code>cancelled</code></td><td>Default <code>complete</code>.</td></tr><tr><td><code>trigger</code></td><td><code>once</code>, <code>every_time</code></td><td>Default <code>once</code>.</td></tr></tbody></table><p><strong>At least one of <code>orderId</code> or <code>symbol</code> is mandatory.</strong> Activating with both blank fails with &quot;orderUpdateTrigger needs an Order ID or a Symbol to watch&quot;; there is no &quot;watch every order&quot; mode.</p><p>A <code>once</code> trigger deactivates the whole workflow when it fires, not just the watch.</p><p>The payload is exposed as <code>&amp;#123;&amp;#123;webhook.orderid&amp;#125;&amp;#125;</code>, <code>&amp;#123;&amp;#123;webhook.symbol&amp;#125;&amp;#125;</code>, <code>&amp;#123;&amp;#123;webhook.order_status&amp;#125;&amp;#125;</code>, <code>&amp;#123;&amp;#123;webhook.filled_quantity&amp;#125;&amp;#125;</code>, <code>&amp;#123;&amp;#123;webhook.average_price&amp;#125;&amp;#125;</code>, <code>&amp;#123;&amp;#123;webhook.rejection_reason&amp;#125;&amp;#125;</code>.</p><h3 id="httprequest-is-not-a-trigger" tabindex="-1"><code>httpRequest</code> is not a trigger <a class="header-anchor" href="#httprequest-is-not-a-trigger" aria-label="Permalink to &quot;`httpRequest` is not a trigger&quot;">​</a></h3><p><code>httpRequest</code> sits beside the triggers in the node list but is a utility node that calls an external URL mid-graph and stores the response. A workflow whose only entry point is an <code>httpRequest</code> fails validation with <code>no_trigger</code>. Its fields are under <a href="#httprequest-http-request">Utilities</a>.</p><hr><h2 id="order-placement" tabindex="-1">Order placement <a class="header-anchor" href="#order-placement" aria-label="Permalink to &quot;Order placement&quot;">​</a></h2><p>Every order node calls the same service layer as <code>/api/v1/</code>, so Analyzer mode, Action Center approval, rate limiting, and Telegram/WhatsApp alerts behave exactly as they do for an API order.</p><h3 id="every-order-field-takes-a-123-123-reference-125-125" tabindex="-1">Every order field takes a <code>&amp;#123;&amp;#123;reference&amp;#125;&amp;#125;</code> <a class="header-anchor" href="#every-order-field-takes-a-123-123-reference-125-125" aria-label="Permalink to &quot;Every order field takes a `&amp;#123;&amp;#123;reference&amp;#125;&amp;#125;`&quot;">​</a></h3><p>There is no field on an order node that must be a literal. The editor shows a picker by default and a small <code>{ }</code> toggle swaps it for a text box, but the stored value is the same either way: a dropdown field holding <code>&amp;#123;&amp;#123;webhook.exchange&amp;#125;&amp;#125;</code> is just a string, and the executor interpolates it like any other. That includes the fields a form makes look fixed: <code>exchange</code>, <code>action</code>, <code>quantity</code>, <code>product</code>, <code>priceType</code>, <code>price</code>, <code>triggerPrice</code>, <code>offset</code>, <code>optionType</code>, <code>expiryType</code>, <code>positionSize</code> and <code>splitSize</code>.</p><p>Only the form ever restricted this. The executor has always interpolated these fields, which is why a webhook-driven order used to be able to name its instrument and nothing else.</p><p>Three rules decide what actually arrives:</p><ul><li><strong>A field holding exactly one whole token keeps its type.</strong><code>&quot;quantity&quot;: &quot;&amp;#123;&amp;#123;webhook.quantity&amp;#125;&amp;#125;&quot;</code> against a payload of <code>{&quot;quantity&quot;: 10}</code> arrives as the number <code>10</code>, not the string <code>&quot;10&quot;</code>. A field mixing a token with other text always resolves to a string.</li><li><strong>Enumerated fields are case-insensitive.</strong> A payload sending <code>&quot;buy&quot;</code>, <code>&quot;nse&quot;</code> or <code>&quot;limit&quot;</code> is accepted. <code>symbol</code> is additionally upper-cased on the order nodes, because the symbol lookup is exact and an alert does not control its own casing: TradingView sends whatever the chart&#39;s ticker carries. The data nodes (<code>getQuote</code>, <code>history</code>, and the rest) do <strong>not</strong> normalise symbol, so send those an upper-case symbol.</li><li><strong>An unresolved reference fails the node.</strong> It does not fall back to a default. This is deliberate: a numeric field could not parse <code>&amp;#123;&amp;#123;webhook.qty&amp;#125;&amp;#125;</code> and used to take the field default of <code>1</code>, and an unresolved <code>priceType</code> fell through the broker mapping to <code>MARKET</code>, so a webhook that simply omitted a key placed a successful order for the wrong size at the wrong price type with nothing in the run to say so.</li></ul><p>Label fields are exempt: <code>strategy</code>, <code>strategyTag</code> and <code>outputVariable</code> still pass an unresolved reference through as text. When a webhook may legitimately omit a value, give the node a literal, or branch on a condition node first.</p><h3 id="product-follows-the-exchange-when-you-do-not-set-it" tabindex="-1"><code>product</code> follows the exchange when you do not set it <a class="header-anchor" href="#product-follows-the-exchange-when-you-do-not-set-it" aria-label="Permalink to &quot;`product` follows the exchange when you do not set it&quot;">​</a></h3><p><code>product</code> is optional on every order and position node. Omit it and the node&#39;s <code>exchange</code> decides: a derivative segment (<code>NFO</code>, <code>BFO</code>, <code>CDS</code>, <code>BCD</code>, <code>MCX</code>, <code>NCDEX</code>, <code>NCO</code>) defaults to <code>NRML</code>, everything else to <code>MIS</code>. A new node in the editor stores no <code>product</code> at all, so switching its exchange to a derivative segment shows and sends <code>NRML</code> while cash stays <code>MIS</code>.</p><p>Write <code>product</code> only to override that. It is used exactly as given, so <code>MIS</code> on an <code>NFO</code> order really is an intraday order the broker squares off at the close. A <code>product</code> that is present but blank is an error, not a fallback: that is a <code>&amp;#123;&amp;#123;variable&amp;#125;&amp;#125;</code> that failed to resolve, and the node refuses rather than guessing.</p><p><code>optionsOrder</code> and <code>optionsMultiOrder</code> are the exception. Their <code>exchange</code> field names where the <em>underlying</em> is quoted, not where the contract trades, so they default to <code>NRML</code> outright.</p><h3 id="placeorder-place-order" tabindex="-1"><code>placeOrder</code>: Place Order <a class="header-anchor" href="#placeorder-place-order" aria-label="Permalink to &quot;`placeOrder`: Place Order&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td></td><td>Required.</td></tr><tr><td><code>action</code></td><td><code>BUY</code>, <code>SELL</code></td><td>Required.</td></tr><tr><td><code>quantity</code></td><td>number</td><td>Required. <strong>Contract units, never lots.</strong> For an NFO order this is lots multiplied by lot size, which you compute yourself.</td></tr><tr><td><code>priceType</code></td><td><code>MARKET</code>, <code>LIMIT</code>, <code>SL</code>, <code>SL-M</code></td><td>Default <code>MARKET</code>.</td></tr><tr><td><code>product</code></td><td><code>CNC</code>, <code>NRML</code>, <code>MIS</code></td><td>Defaults by exchange.</td></tr><tr><td><code>price</code>, <code>triggerPrice</code></td><td>number</td><td><code>price</code> must be positive for <code>LIMIT</code> and <code>SL</code>; <code>triggerPrice</code> must be positive for <code>SL</code> and <code>SL-M</code>. A missing or zero required price fails the node rather than reaching the broker.</td></tr><tr><td><code>strategyTag</code></td><td>string</td><td>Overrides the workflow name as the strategy tag on this order.</td></tr></tbody></table><p>Output: <code>{status, orderid}</code></p><p><code>optionsOrder</code> and <code>optionsMultiOrder</code> are the only nodes that take lots and multiply by the contract lot size for you. <code>placeOrder</code>, <code>smartOrder</code>, <code>splitOrder</code>, and <code>basketOrder</code> all pass <code>quantity</code> through untouched.</p><p><code>disclosedQuantity</code> appears in the frontend node type definition but the executor never sends it, so it is always 0.</p><h3 id="smartorder-smart-order" tabindex="-1"><code>smartOrder</code>: Smart Order <a class="header-anchor" href="#smartorder-smart-order" aria-label="Permalink to &quot;`smartOrder`: Smart Order&quot;">​</a></h3><p>Takes everything <code>placeOrder</code> takes, plus <code>positionSize</code>: the <strong>target</strong> net position. The node computes the delta and places only the difference. <code>positionSize: 0</code> flattens. This is the safe way to reach a position without tracking what you already hold.</p><p><code>quantity</code> may be <code>0</code> on this node, which is the only order node where zero is valid: it means &quot;let <code>positionSize</code> decide the size&quot;.</p><p><code>price</code> and <code>triggerPrice</code> are forwarded, so a <code>LIMIT</code> or <code>SL</code> smart order is priced like any other order and is refused if its required price is missing. (An earlier build dropped both and sent <code>price: 0</code>.)</p><p>Output: <code>{status, orderid}</code></p><h3 id="optionsorder-options-order" tabindex="-1"><code>optionsOrder</code>: Options Order <a class="header-anchor" href="#optionsorder-options-order" aria-label="Permalink to &quot;`optionsOrder`: Options Order&quot;">​</a></h3><p>Resolves the contract for you, then places it.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>underlying</code></td><td>e.g. <code>NIFTY</code></td><td>Required. Decides both exchanges on its own, see below.</td></tr><tr><td><code>expiryType</code></td><td>a relative type <strong>or</strong> a <code>DDMMMYY</code> date</td><td>Default <code>current_week</code>. See <strong>Expiry</strong> below.</td></tr><tr><td><code>expiryDate</code></td><td><code>DDMMMYY</code></td><td>The same explicit date under its own key. <strong>Wins over <code>expiryType</code></strong> when both are set.</td></tr><tr><td><code>offset</code></td><td><code>ATM</code>, <code>ITM1</code>-<code>ITM50</code>, <code>OTM1</code>-<code>OTM50</code></td><td>A <strong>string</strong>, not a number. <code>0</code> and <code>2</code> resolve nothing.</td></tr><tr><td><code>optionType</code></td><td><code>CE</code>, <code>PE</code></td><td>Default <code>CE</code>.</td></tr><tr><td><code>action</code></td><td><code>BUY</code>, <code>SELL</code></td><td>Required.</td></tr><tr><td><code>quantity</code></td><td>number</td><td>Required. <strong>Lots.</strong> The node reads the contract lot size from the master contract and multiplies for you.</td></tr><tr><td><code>priceType</code>, <code>price</code>, <code>triggerPrice</code></td><td></td><td>Same price rules as <code>placeOrder</code>.</td></tr><tr><td><code>product</code></td><td><code>MIS</code>, <code>NRML</code></td><td>Default <code>NRML</code>, not derived from <code>exchange</code>.</td></tr><tr><td><code>splitSize</code></td><td>number</td><td>Splits into child orders of this size for freeze limits.</td></tr><tr><td><code>exchange</code></td><td></td><td>A <strong>fallback</strong>, not an override. Read only for an underlying the table below does not name.</td></tr></tbody></table><p><strong>Expiry.</strong> The editor shows one Expiry control, and the <code>{ }</code> toggle swaps its relative picker for a date or a reference, so both forms arrive under <code>expiryType</code>:</p><ul><li>A relative type (<code>current_week</code>, <code>next_week</code>, <code>current_month</code>, <code>next_month</code>) is resolved against the listed expiries. <code>current_week</code> is the nearest listed expiry and <code>next_week</code> the second, so on a monthly-only product such as an MCX commodity <code>current_week</code> is simply the nearest contract. The monthly types take the last expiry falling in that calendar month.</li><li>A <code>DDMMMYY</code> value such as <code>28OCT25</code> is used exactly as given. This is how you reach a far contract the four relative choices cannot name.</li><li><code>expiryDate</code> carries the same explicit date under its own key, for callers that prefer to send the two apart, and wins when both are set.</li><li>A malformed date is refused with a message naming the expected format. It is not silently ignored.</li></ul><p><strong>Underlying and exchange.</strong> The node resolves <em>two</em> exchanges: the one whose price sets the ATM reference, and the one the option contract trades on. The underlying&#39;s name decides both:</p><table tabindex="0"><thead><tr><th>Underlying</th><th>ATM reference quoted from</th><th>Option trades on</th></tr></thead><tbody><tr><td>NIFTY, BANKNIFTY, FINNIFTY, MIDCPNIFTY, NIFTYNXT50</td><td><code>NSE_INDEX</code> (the index level)</td><td><code>NFO</code></td></tr><tr><td>SENSEX, BANKEX, SENSEX50</td><td><code>BSE_INDEX</code> (the index level)</td><td><code>BFO</code></td></tr><tr><td>GOLD, GOLDM, SILVER, SILVERM, CRUDEOIL, CRUDEOILM, NATURALGAS, NATGASMINI, COPPER, ZINC, MCXBULLDEX</td><td><code>MCX</code> (the near-month <strong>future</strong>)</td><td><code>MCX</code></td></tr><tr><td>anything else</td><td>from the node&#39;s <code>exchange</code> field</td><td></td></tr></tbody></table><p>A named underlying always wins over <code>exchange</code>. That is why: the node ships with <code>exchange</code> pre-set to <code>NSE_INDEX</code> and most authors never touch the dropdown, so trusting it would send every imported SENSEX or CRUDEOIL order to NFO. Use <code>exchange</code> to reach a stock option (<code>&quot;underlying&quot;: &quot;SBIN&quot;, &quot;exchange&quot;: &quot;NFO&quot;</code>) or a commodity the table does not list (<code>&quot;underlying&quot;: &quot;MENTHAOIL&quot;, &quot;exchange&quot;: &quot;MCX&quot;</code>).</p><p>MCX differs from the equity segments in two ways worth knowing when writing a workflow by hand. There is no separate derivatives exchange, so the future, the option and the quote all live on <code>MCX</code>. And there is no spot instrument, so the ATM strike is priced off the nearest unexpired future (<code>CRUDEOIL21SEP26FUT</code>), resolved automatically; if no unexpired future exists the node fails rather than guessing a reference price. Most MCX option contracts carry a lot size of 1, so one lot is one contract there.</p><p>Output when <code>splitSize</code> is 0 (the default): <code>{status, orderid, symbol, exchange, underlying, underlying_ltp, offset, option_type, mode}</code></p><p>Output when <code>splitSize</code> is greater than 0: <code>{status, symbol, exchange, underlying, underlying_ltp, offset, option_type, total_quantity, split_size, results: [...]}</code></p><p><strong>The split shape has no <code>orderid</code></strong>, so <code>&amp;#123;&amp;#123;o.orderid&amp;#125;&amp;#125;</code> is undefined exactly when you use the split feature. Read <code>&amp;#123;&amp;#123;o.results&amp;#125;&amp;#125;</code> instead. <code>mode</code> is <code>analyze</code> in Analyzer mode.</p><h3 id="optionsmultiorder-options-multi-order" tabindex="-1"><code>optionsMultiOrder</code>: Options Multi-Order <a class="header-anchor" href="#optionsmultiorder-options-multi-order" aria-label="Permalink to &quot;`optionsMultiOrder`: Options Multi-Order&quot;">​</a></h3><p>Places several option legs in one call: spreads, straddles, iron condors, calendars.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>underlying</code></td><td>Required. Shared by all legs. Resolves both exchanges exactly as in <code>optionsOrder</code>.</td></tr><tr><td><code>quantity</code></td><td>Required. <strong>Lots per leg.</strong></td></tr><tr><td><code>strategy</code></td><td>The <strong>structure</strong> name: <code>straddle</code>, <code>strangle</code>, <code>iron_condor</code>, <code>bull_call_spread</code>, <code>bear_put_spread</code>, <code>custom</code>. This is <em>not</em> the strategy tag; use <code>strategyTag</code> for that. An absent <code>strategy</code> is read as <code>custom</code>, which then requires <code>legs</code>.</td></tr><tr><td><code>expiryType</code></td><td>One common expiry for every leg. Takes a relative type or a <code>DDMMMYY</code> date, exactly as on <code>optionsOrder</code>.</td></tr><tr><td><code>expiryDate</code></td><td>The same explicit date under its own key. Wins over <code>expiryType</code>. A leg may still override both.</td></tr><tr><td><code>action</code></td><td>Default <code>SELL</code>. Applied to generated legs.</td></tr><tr><td><code>product</code>, <code>priceType</code>, <code>price</code>, <code>triggerPrice</code></td><td>Node-level defaults inherited by every leg. <code>product</code> defaults to <code>NRML</code>.</td></tr><tr><td><code>strangleWidth</code></td><td>Default <code>OTM2</code>, and must match the <code>offset</code> vocabulary. Used by the <code>strangle</code> structure.</td></tr><tr><td><code>legs</code></td><td>Required for <code>custom</code>. See below.</td></tr></tbody></table><p>A generated (non-<code>custom</code>) structure <strong>rejects <code>SL</code> and <code>SL-M</code></strong> outright: its legs carry no individual trigger price, so there is nothing to send the broker. Build the legs as custom legs if you need a trigger.</p><p><strong>Custom legs.</strong> A readymade structure positions every leg at an offset from the money and gives them all one expiry. <code>strategy: &quot;custom&quot;</code> lifts both limits, which is what makes a calendar, a diagonal, a ratio, or a basket pinned to chosen strikes expressible.</p><table tabindex="0"><thead><tr><th>Leg field</th><th>Values</th><th>Notes</th></tr></thead><tbody><tr><td><code>strikeMode</code></td><td><code>OFFSET</code>, <code>STRIKE</code></td><td>Absent is <code>OFFSET</code>. A leg carrying <code>strike</code> and no mode is read as <code>STRIKE</code>.</td></tr><tr><td><code>offset</code></td><td><code>ATM</code>, <code>ITM1</code>-<code>ITM50</code>, <code>OTM1</code>-<code>OTM50</code></td><td>Required unless <code>strike</code> is given. Re-resolved against the live underlying on every run.</td></tr><tr><td><code>strike</code></td><td>number</td><td>Required when <code>strikeMode</code> is <code>STRIKE</code>. An absolute strike, used as given; must be positive and listed for that expiry.</td></tr><tr><td><code>expiry</code></td><td><code>DDMMMYY</code></td><td>Overrides the node expiry with an exact date.</td></tr><tr><td><code>expiryType</code></td><td>relative type</td><td>Overrides the node expiry with a relative one. Ignored when <code>expiry</code> is set.</td></tr><tr><td><code>optionType</code></td><td><code>CE</code>, <code>PE</code></td><td>Required.</td></tr><tr><td><code>action</code></td><td><code>BUY</code>, <code>SELL</code></td><td>Required. The leg&#39;s own side, independent of the node <code>action</code>.</td></tr><tr><td><code>quantity</code></td><td>number</td><td>Required. <strong>Lots</strong>, multiplied by the lot size like the node-level quantity.</td></tr><tr><td><code>product</code>, <code>priceType</code> (or <code>pricetype</code>), <code>price</code>, <code>triggerPrice</code>, <code>splitSize</code></td><td></td><td>Inherit the node&#39;s value when omitted. Unlike a generated structure, a custom leg may use <code>SL</code> and <code>SL-M</code>, because it can carry its own trigger.</td></tr></tbody></table><p>An <strong>omitted</strong> optional field is what tells the executor to inherit, so write no key at all rather than an empty string. A leg naming neither <code>offset</code> nor <code>strike</code> is refused. A leg with no <code>expiry</code> and no <code>expiryType</code> follows the node, so a scheduled workflow rolls to the next contract on its own; pinning a leg&#39;s <code>expiry</code> is correct for a calendar or diagonal and wrong for anything that should keep rolling.</p><p><strong>BUY legs are placed before SELL legs</strong> for margin efficiency, and legs are placed one at a time, so a basket <strong>fails leg by leg</strong>: if leg three is rejected, legs one and two are already filled. That is why a malformed strike or expiry is refused at save time rather than at run time.</p><p>Output: <code>{status, underlying, underlying_ltp, results: [{...}]}</code></p><h3 id="basketorder-basket-order" tabindex="-1"><code>basketOrder</code>: Basket Order <a class="header-anchor" href="#basketorder-basket-order" aria-label="Permalink to &quot;`basketOrder`: Basket Order&quot;">​</a></h3><p>Several unrelated symbols in one submission.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>orders</code></td><td>Required. The editor writes a newline-delimited <strong>string</strong>, one order per line, <code>SYMBOL,EXCHANGE,ACTION,QTY</code>. An array of objects is also accepted, and is interpolated and validated the same way.</td></tr><tr><td><code>product</code>, <code>priceType</code>, <code>price</code>, <code>triggerPrice</code></td><td>Node-level values applied to every row.</td></tr><tr><td><code>basketName</code></td><td>Default <code>flow_basket</code>. This becomes the strategy tag.</td></tr></tbody></table><p><strong>A CSV row carries four fields and nothing else</strong>, so per-row product, price type or price written into the string is not parsed. Only the <strong>array</strong> form can set them per row, under <code>product</code>, <code>pricetype</code> (or <code>priceType</code>), <code>price</code> and <code>triggerprice</code> (or <code>triggerPrice</code>); the node-level values fill in whatever a row omits.</p><p>With no <code>product</code> on the node, each row follows its <strong>own</strong> exchange&#39;s default, so one basket can mix an <code>MIS</code> cash row and an <code>NRML</code> commodity row. A <code>product</code> on the node covers every row that does not set its own.</p><p>Output: <code>{status, results: [{symbol, exchange, product, status, orderid}]}</code></p><h3 id="splitorder-split-order" tabindex="-1"><code>splitOrder</code>: Split Order <a class="header-anchor" href="#splitorder-split-order" aria-label="Permalink to &quot;`splitOrder`: Split Order&quot;">​</a></h3><p>One large order broken into child orders of <code>splitSize</code>, to stay under the exchange freeze quantity. Fields: <code>symbol</code>, <code>exchange</code>, <code>action</code>, <code>quantity</code>, <code>splitSize</code> (all required), plus <code>priceType</code>, <code>product</code>, <code>price</code>, <code>triggerPrice</code>. <code>splitSize</code> must be at least 1.</p><p>Spacing between child orders comes from the global <code>ORDER_RATE_LIMIT</code> setting (<code>10 per second</code>, so 100 ms, by default), not from the node. <code>delayMs</code> appears in older exports and is read by nothing here.</p><p>Output: <code>{status, results: [{order_num, quantity, status, orderid}]}</code></p><h3 id="modifyorder-modify-order" tabindex="-1"><code>modifyOrder</code>: Modify Order <a class="header-anchor" href="#modifyorder-modify-order" aria-label="Permalink to &quot;`modifyOrder`: Modify Order&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>orderId</code></td><td>Required. Usually <code>&amp;#123;&amp;#123;prevOrder.orderid&amp;#125;&amp;#125;</code>.</td></tr><tr><td><code>newQuantity</code>, <code>newPrice</code>, <code>newTriggerPrice</code></td><td>Omitted means unchanged.</td></tr><tr><td><code>symbol</code>, <code>exchange</code>, <code>action</code>, <code>product</code>, <code>priceType</code></td><td><strong>Omit these.</strong> Any value present is treated as a deliberate override.</td></tr></tbody></table><p>The node reads the order back from the order book and changes only the fields you supply, so &quot;omitted means unchanged&quot; is literal: an omitted quantity keeps the order&#39;s quantity, not <code>1</code>. If the order cannot be read, the node fails rather than sending a guessed value.</p><p>Do not set <code>action</code> or <code>product</code> unless you mean to change them. Several brokers carry these on a modify, so an <code>action</code> of <code>BUY</code> on a live SELL order converts the order, and a <code>product</code> of <code>MIS</code> on an NRML position makes it intraday and subject to auto square-off. A <code>modifyOrder</code> node should carry <strong>only</strong> <code>orderId</code> plus whichever of the three <code>new*</code> fields you are changing.</p><p>Pair it with <code>orderUpdateTrigger</code> to react to a partial fill.</p><h3 id="cancelorder-cancelallorders-closepositions" tabindex="-1"><code>cancelOrder</code> / <code>cancelAllOrders</code> / <code>closePositions</code> <a class="header-anchor" href="#cancelorder-cancelallorders-closepositions" aria-label="Permalink to &quot;`cancelOrder` / `cancelAllOrders` / `closePositions`&quot;">​</a></h3><p><code>cancelOrder</code> takes an <code>orderId</code> (required). It is the one order node that does <strong>not</strong> store its result, so an <code>outputVariable</code> on it stays undefined.</p><p><code>cancelAllOrders</code> takes no input and cancels every open order. It does store its result under <code>outputVariable</code>.</p><p><code>closePositions</code> takes <code>symbol</code>, <code>exchange</code>, <code>product</code> and <code>strategyTag</code>, and behaves in two distinct ways:</p><ul><li>With <code>symbol</code> blank, it closes <strong>everything</strong>, in every exchange and product, including overnight NRML and CNC holdings. <code>exchange</code> and <code>product</code> filter nothing on their own.</li><li>With <code>symbol</code> set, the node sends the scoped close: that symbol, that exchange, that product, carrying the strategy tag.</li></ul><p><strong>The scoped close only takes effect in Analyzer mode.</strong> In live trading the underlying service calls the broker&#39;s own square-off-everything endpoint and the symbol, exchange and product are discarded, so the whole book is closed. The node reads as correct in sandbox testing and is not, which is the trap: see <a href="./limitations.html">Limitations</a>.</p><p><code>cancelAllOrders</code> and a symbol-less <code>closePositions</code> are account-wide, <strong>not</strong> strategy-scoped: run in one workflow they affect orders and positions another workflow placed.</p><hr><h2 id="conditions-and-logic" tabindex="-1">Conditions and logic <a class="header-anchor" href="#conditions-and-logic" aria-label="Permalink to &quot;Conditions and logic&quot;">​</a></h2><p>A condition node emits a boolean consumed by edge routing, it has <strong>no</strong> output variable. Reference its <em>inputs</em> downstream, not its result.</p><p>Each has a <code>true</code> and a <code>false</code> source handle (<code>timeCondition</code> and <code>notGate</code> use <code>yes</code> and <code>no</code>; the executor treats the two vocabularies as synonyms).</p><p>Three rules govern all of them:</p><ul><li><strong>A condition that errors takes NEITHER branch.</strong> An unrecognised <code>field</code>, <code>operator</code> or <code>condition</code>, an operand that does not resolve to a number, or a <strong>failed broker read</strong> all produce an error, not a <code>false</code>. That matters because <code>false</code> is a real answer that routes the graph: <code>priceCondition</code>, <code>positionCheck</code> and <code>fundCheck</code> used to read a failed lookup as <code>0</code> and report success, so &quot;if LTP &lt; 100 then BUY&quot; fired on an expired broker session. The run is recorded as <code>failed</code> and the trigger response carries the reason.</li><li><strong>A condition evaluates once per run</strong>, however many paths reach it. A condition reachable by two paths used to run twice and follow its branch each time, placing two orders from one trigger.</li><li>Pass-through edges (no <code>sourceHandle</code>, typically a Log or Telegram node) are followed on every result including an error, so a failure is still visible.</li></ul><h3 id="pricecondition-price-condition" tabindex="-1"><code>priceCondition</code>: Price Condition <a class="header-anchor" href="#pricecondition-price-condition" aria-label="Permalink to &quot;`priceCondition`: Price Condition&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td>Both required.</td></tr><tr><td><code>field</code></td><td><code>ltp</code>, <code>open</code>, <code>high</code>, <code>low</code>, <code>prev_close</code>, <code>change_percent</code>. Default <code>ltp</code>.</td></tr><tr><td><code>operator</code></td><td><code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>, <code>==</code>, <code>!=</code>. Required.</td></tr><tr><td><code>value</code></td><td>number, or <code>&amp;#123;&amp;#123;variable&amp;#125;&amp;#125;</code>. The legacy key <code>threshold</code> is still read.</td></tr></tbody></table><p>Those six fields are the whole vocabulary. <code>close</code> and <code>volume</code> are <strong>not</strong> valid here and make the node error on every run, taking neither branch. For a close-based comparison, read it from a <code>history</code> or <code>barOffset</code> node into a <code>varCondition</code>.</p><p><code>change_percent</code> is computed as <code>(ltp - prev_close) / prev_close * 100</code>.</p><h3 id="varcondition-variable-condition" tabindex="-1"><code>varCondition</code>: Variable Condition <a class="header-anchor" href="#varcondition-variable-condition" aria-label="Permalink to &quot;`varCondition`: Variable Condition&quot;">​</a></h3><p>Compares any two values, each a literal or <code>&amp;#123;&amp;#123;path&amp;#125;&amp;#125;</code>. This is the general comparison node, use it whenever <code>priceCondition</code> is too narrow.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>leftValue</code></td><td>Required. Literal or <code>&amp;#123;&amp;#123;path&amp;#125;&amp;#125;</code>.</td></tr><tr><td><code>operator</code></td><td>Required. <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>, <code>==</code>, <code>!=</code>.</td></tr><tr><td><code>rightValue</code></td><td>Literal or <code>&amp;#123;&amp;#123;path&amp;#125;&amp;#125;</code>.</td></tr></tbody></table><p>An operand that is blank, unresolved, or not a number errors and takes neither branch, which is what you want, but it means a silently missing value looks like &quot;nothing happened&quot;. Check the run log.</p><h3 id="timewindow-time-window" tabindex="-1"><code>timeWindow</code>: Time Window <a class="header-anchor" href="#timewindow-time-window" aria-label="Permalink to &quot;`timeWindow`: Time Window&quot;">​</a></h3><p><code>startTime</code>, <code>endTime</code> (<code>&quot;HH:MM&quot;</code>), and <code>invertCondition</code> to mean &quot;outside this window&quot;. Defaults are <code>09:15</code> and <code>15:30</code>.</p><p><strong>A window whose end is before its start spans midnight.</strong> <code>22:00</code> to <code>02:00</code> is read as the two halves either side of midnight, which is what an overnight MCX or crypto guard needs. It used to be an empty window, so such a guard was always false, and always true once inverted.</p><p><code>days</code> appears in older exports and is read by nothing here, so a &quot;weekdays only&quot; window built from it runs every day. Gate the weekday with <code>&amp;#123;&amp;#123;weekday_num&amp;#125;&amp;#125;</code> in a <code>varCondition</code>, or with <code>marketHoursOnly</code> on the <code>start</code> trigger.</p><h3 id="timecondition-time-condition" tabindex="-1"><code>timeCondition</code>: Time Condition <a class="header-anchor" href="#timecondition-time-condition" aria-label="Permalink to &quot;`timeCondition`: Time Condition&quot;">​</a></h3><p>For &quot;is it past 14:00&quot; style checks against a single time rather than a range.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>conditionType</code></td><td><code>entry</code>, <code>exit</code>, <code>custom</code>. Purely a label: it never changes the comparison, only the log line and the result payload, which is what makes a graph with several time gates readable.</td></tr><tr><td><code>targetTime</code></td><td><code>&quot;HH:MM&quot;</code> or <code>&quot;HH:MM:SS&quot;</code>. Seconds are honoured.</td></tr><tr><td><code>operator</code></td><td><code>&gt;=</code>, <code>&lt;=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>==</code>. <code>==</code> compares at minute precision on purpose: a second-exact equality would almost never fire.</td></tr></tbody></table><p>An unrecognised operator errors and takes neither branch. This node uses <code>yes</code>/<code>no</code> source handles rather than <code>true</code>/<code>false</code>.</p><h3 id="positioncheck-position-check" tabindex="-1"><code>positionCheck</code>: Position Check <a class="header-anchor" href="#positioncheck-position-check" aria-label="Permalink to &quot;`positionCheck`: Position Check&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td>Both required.</td></tr><tr><td><code>condition</code></td><td>Required. <code>exists</code>, <code>not_exists</code>, <code>quantity_above</code>, <code>quantity_below</code>, <code>pnl_above</code>, <code>pnl_below</code></td></tr><tr><td><code>product</code></td><td>Defaults by exchange.</td></tr><tr><td><code>threshold</code></td><td>number, default 0. Used only by the <code>quantity_*</code> and <code>pnl_*</code> modes.</td></tr></tbody></table><p><code>exists</code> and <code>not_exists</code> are the stored values; the editor labels them &quot;Has Position&quot; and &quot;No Position&quot;. Writing <code>has_position</code> or <code>no_position</code> into the JSON makes the node error every run.</p><p>The node also <strong>errors when <code>symbol</code> is blank</strong> rather than evaluating. A blank symbol reads back a zero-quantity position, which makes <code>not_exists</code> unconditionally true, so an unconfigured node would open the gate it exists to guard.</p><p>This asks the <strong>broker</strong>, so it is the correct way to answer &quot;am I already in this trade&quot;: Flow keeps no memory between runs.</p><h3 id="fundcheck-fund-check" tabindex="-1"><code>fundCheck</code>: Fund Check <a class="header-anchor" href="#fundcheck-fund-check" aria-label="Permalink to &quot;`fundCheck`: Fund Check&quot;">​</a></h3><p><code>minAvailable</code> is <strong>required</strong>. The node passes when available cash is at least that much. A node without it cannot guard anything, because the comparison would be <code>availablecash &gt;= 0</code> and true on any balance, so it fails instead of letting the order behind it through.</p><p>A node saved before <code>minAvailable</code> existed carries a legacy <code>threshold</code> plus <code>operator</code>. A greater-than-style legacy operator is read as a minimum and logged as such; anything else is refused rather than inverted, because this node can only express &quot;at least&quot;.</p><h3 id="andgate-orgate-notgate" tabindex="-1"><code>andGate</code> / <code>orGate</code> / <code>notGate</code> <a class="header-anchor" href="#andgate-orgate-notgate" aria-label="Permalink to &quot;`andGate` / `orGate` / `notGate`&quot;">​</a></h3><p>Combine conditions. Connect condition outputs into the gate, and the gate&#39;s output onward.</p><p>A gate waits until <strong>every</strong> connected input has been evaluated before it fires, and fires exactly once per run. <code>andGate</code> and <code>orGate</code> render <code>true</code> and <code>false</code> handles and route their result exactly like a condition, so set <code>sourceHandle</code> on their outgoing edges. <code>notGate</code> emits <code>yes</code>/<code>no</code>.</p><p><code>andGate</code> and <code>orGate</code> take an <code>inputCount</code> (2 to 5, default 2). Two rules apply to it:</p><ul><li>An edge whose <code>targetHandle</code> is <code>input-N</code> for N at or beyond the count is a hard validation error, so raise <code>inputCount</code> before wiring a third input.</li><li><strong>A gate honours its <code>inputCount</code> at run time too.</strong> Configured for three inputs and wired for two, it errors rather than evaluating on part of the condition. Deleting a third edge used to silently downgrade &quot;A AND B AND in-window&quot; to &quot;A AND B&quot; with the canvas still showing three slots.</li></ul><p><strong>An errored condition leaves a gate pending</strong>, it does not settle it as <code>False</code>. That is deliberate: an AND gate that saw a placeholder <code>False</code> from a failed condition computed <code>[False, True] -&gt; False</code> and drove its FALSE branch into a real order, and only afterwards was the run marked failed.</p><p>Only <code>notGate</code> is configuration-free.</p><hr><h2 id="market-data" tabindex="-1">Market data <a class="header-anchor" href="#market-data" aria-label="Permalink to &quot;Market data&quot;">​</a></h2><p>See <a href="./market-data.html">Market Data and Timeframes</a> for the bar limits and caching behaviour that apply across these nodes.</p><h3 id="source-is-the-data-source-not-the-price-field" tabindex="-1"><code>source</code> is the data source, not the price field <a class="header-anchor" href="#source-is-the-data-source-not-the-price-field" aria-label="Permalink to &quot;`source` is the data source, not the price field&quot;">​</a></h3><p>On <code>indicator</code>, <code>barOffset</code> and <code>priorPeriodOhlc</code>, <strong><code>source</code> selects where the history comes from</strong>: <code>api</code> (the broker, the default) or <code>db</code> (Historify, which also resamples locally). It is not a price-field selector. Passing <code>close</code> to <code>source</code> fails with a history error that names neither field, which is a confusing way to learn this. The field that picks a column is <code>sourceField</code>, and it exists only on <code>indicator</code>.</p><p>The indicator and history nodes reuse a fetch for <code>FLOW_HISTORY_CACHE_TTL</code> seconds, 30 by default. That is well under a 5-minute candle and half of a 1-minute one, so lower it to 2 or 3 for a 1-minute strategy.</p><h3 id="getquote-get-quote" tabindex="-1"><code>getQuote</code>: Get Quote <a class="header-anchor" href="#getquote-get-quote" aria-label="Permalink to &quot;`getQuote`: Get Quote&quot;">​</a></h3><p><code>symbol</code>, <code>exchange</code> (both required) → <code>{status, data: {ltp, open, high, low, prev_close, volume, oi, bid, ask&amp;#125;&amp;#125;</code></p><p><code>data.high</code> and <code>data.low</code> are <strong>today&#39;s session extremes</strong>, which is what makes stateless &quot;has price already touched this level today&quot; tests possible.</p><h3 id="multiquotes-multi-quotes" tabindex="-1"><code>multiQuotes</code>: Multi Quotes <a class="header-anchor" href="#multiquotes-multi-quotes" aria-label="Permalink to &quot;`multiQuotes`: Multi Quotes&quot;">​</a></h3><p>Takes a single comma-separated <code>symbols</code> string (required) and one node-level <code>exchange</code> applied to all of them, so every symbol in one node must share an exchange.</p><p>→ <code>{status, results: [{symbol, exchange, data: {...&amp;#125;&amp;#125;]}</code></p><p>Prefer this over several <code>getQuote</code> nodes: quotes are <strong>not</strong> cached and some brokers allow only about one quote request per second.</p><h3 id="getdepth-get-depth" tabindex="-1"><code>getDepth</code>: Get Depth <a class="header-anchor" href="#getdepth-get-depth" aria-label="Permalink to &quot;`getDepth`: Get Depth&quot;">​</a></h3><p><code>symbol</code>, <code>exchange</code> (both required) → <code>{status, data: {bids: [{price, quantity}], asks: [...], ltp, totalbuyqty, totalsellqty&amp;#125;&amp;#125;</code></p><h3 id="history-historical-data" tabindex="-1"><code>history</code>: Historical Data <a class="header-anchor" href="#history-historical-data" aria-label="Permalink to &quot;`history`: Historical Data&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code>, <code>interval</code></td><td>All three required.</td></tr><tr><td><code>interval</code></td><td>A fixed dropdown on this node: <code>1m</code>, <code>5m</code>, <code>15m</code>, <code>1h</code>, <code>1d</code>. Editor default <code>1d</code>.</td></tr><tr><td><code>days</code></td><td>Calendar days back. Editor default 30, maximum 365.</td></tr><tr><td><code>startDate</code>, <code>endDate</code></td><td>An explicit range. When both are set they <strong>take precedence over <code>days</code></strong>.</td></tr></tbody></table><p>→ <code>{status, data: [{timestamp, open, high, low, close, volume, oi}]}</code></p><p>Timestamps are <strong>epoch seconds</strong>, not ISO strings.</p><p>The result is capped at 200 bars by <code>FLOW_MAX_HISTORY_BARS</code>, which is an environment default rather than a fixed ceiling. This node has <strong>no <code>source</code> field</strong>, so it always calls the broker; use <code>indicator</code>, <code>barOffset</code>, or <code>priorPeriodOhlc</code> when you need <code>source: &quot;db&quot;</code>.</p><h3 id="priorperiodohlc-prior-period-ohlc" tabindex="-1"><code>priorPeriodOhlc</code>: Prior Period OHLC <a class="header-anchor" href="#priorperiodohlc-prior-period-ohlc" aria-label="Permalink to &quot;`priorPeriodOhlc`: Prior Period OHLC&quot;">​</a></h3><p>The completed previous hour/day/week/month, the reliable way to get PDH/PDL without off-by-one errors from today&#39;s partial candle. It never returns a still-forming candle, and errors when history is too short.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td>Both required.</td></tr><tr><td><code>period</code></td><td><code>previous_hour</code>, <code>previous_day</code>, <code>previous_week</code>, <code>previous_month</code>. Default <code>previous_day</code>.</td></tr><tr><td><code>source</code></td><td><code>api</code> (default) or <code>db</code> (Historify).</td></tr></tbody></table><p>→ <code>{status, symbol, exchange, period, date, open, high, low, close, volume, pdh, pdl, pdc}</code></p><h3 id="baroffset-bar-offset" tabindex="-1"><code>barOffset</code>: Bar Offset <a class="header-anchor" href="#baroffset-bar-offset" aria-label="Permalink to &quot;`barOffset`: Bar Offset&quot;">​</a></h3><p>One specific bar counted back from the latest: &quot;the close 20 hours ago&quot;, &quot;the high 3 bars ago&quot;.</p><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>symbol</code>, <code>exchange</code></td><td>Both required.</td></tr><tr><td><code>interval</code></td><td>Free text. Default <code>D</code>.</td></tr><tr><td><code>offsetBars</code></td><td>0 is the most recent <strong>closed</strong> bar; 1 is the one before it. Today&#39;s forming candle is excluded.</td></tr><tr><td><code>source</code></td><td><code>api</code> (default) or <code>db</code> (Historify).</td></tr></tbody></table><p>Because it counts bars rather than calendar time, the same node answers &quot;20 hours ago&quot; with <code>interval: &quot;1h&quot;, offsetBars: 20</code>.</p><p>→ <code>{status, symbol, exchange, offsetBars, timestamp, open, high, low, close, volume}</code></p><h3 id="indicator-technical-indicator" tabindex="-1"><code>indicator</code>: Technical Indicator <a class="header-anchor" href="#indicator-technical-indicator" aria-label="Permalink to &quot;`indicator`: Technical Indicator&quot;">​</a></h3><p>All the single-symbol <code>tradeboard.ta</code> indicators. Fully covered in <a href="./indicators.html">Indicators</a>.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>indicatorName</code></td><td>Required. Lowercase, e.g. <code>ema</code>, <code>rsi</code>, <code>macd</code>, <code>supertrend</code>. Not validated until the run.</td></tr><tr><td><code>symbol</code>, <code>exchange</code></td><td>Required except in nested mode.</td></tr><tr><td><code>params</code></td><td>The indicator&#39;s own parameters <strong>as a JSON string</strong>, e.g. <code>&quot;{\\&quot;period\\&quot;: 20}&quot;</code>. A real JSON object is rejected at import; a stored one from an older graph is normalised with a warning.</td></tr><tr><td><code>interval</code></td><td>Free text. Default <code>D</code>.</td></tr><tr><td><code>source</code></td><td><code>api</code> (default) or <code>db</code> (Historify). The data source, not a price field.</td></tr><tr><td><code>lookbackBars</code> / <code>tailBars</code></td><td>How much history to compute over (capped at 200), and how long the returned <code>series</code> is.</td></tr><tr><td><code>offsetBars</code></td><td>Which past value <code>at_offset</code> refers to. 0 is the latest.</td></tr><tr><td><code>sourceSeries</code></td><td>Feed another indicator&#39;s output in, e.g. <code>&amp;#123;&amp;#123;rsi.series&amp;#125;&amp;#125;</code> or a raw <code>&amp;#123;&amp;#123;h.data&amp;#125;&amp;#125;</code>. This is how nesting works.</td></tr><tr><td><code>sourceField</code></td><td><strong>Only used when <code>sourceSeries</code> is set</strong>, to pick a key out of each upstream row. Blank means the first of <code>value</code>, <code>out0</code>, <code>close</code>. It has no effect on an ordinary indicator node.</td></tr></tbody></table><p>→ <code>{status, indicator, nested, inputs, params, outputs, latest, previous, at_offset, series, offset_bars, bars_used}</code></p><p><strong>A single-output indicator exposes <code>value</code>. A tuple indicator exposes <code>out0</code>, <code>out1</code>, and so on, and has no <code>value</code> at all.</strong> <code>supertrend</code> gives <code>out0</code> (the line) and <code>out1</code> (direction); <code>macd</code> gives line, signal and histogram; <code>bbands</code> gives upper, middle and lower. Reading <code>&amp;#123;&amp;#123;ind.latest.value&amp;#125;&amp;#125;</code> on one of those resolves to its own literal text, which looks like a condition that never fires.</p><p>An indicator whose every output is null over the bars available returns an <strong>error</strong>, not a success with nulls, so a too-short warm-up is visible rather than silent.</p><p>Only single-series indicators (sma, ema, rsi, wma, stdev, highest, lowest, and the like) can be nested through <code>sourceSeries</code>. <code>crossover</code>, <code>crossunder</code>, <code>cross</code>, <code>correlation</code> and <code>beta</code> need two independent series and are refused; build a crossover from two <code>indicator</code> nodes plus an <code>andGate</code>.</p><h3 id="strategypnl-strategy-p-l" tabindex="-1"><code>strategyPnl</code>: Strategy P&amp;L <a class="header-anchor" href="#strategypnl-strategy-p-l" aria-label="Permalink to &quot;`strategyPnl`: Strategy P&amp;L&quot;">​</a></h3><p>Realized, unrealized and total P&amp;L for <strong>one strategy</strong>, not the whole account, so a workflow can exit on its own performance even when other strategies hold the same contract.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>strategy</code></td><td>Defaults to this workflow&#39;s name, which is also the tag its order nodes apply. Usually leave blank.</td></tr></tbody></table><p>→ <code>{status, strategy, realized, today_realized, unrealized, total, today_total, open_quantity, unpriced_legs, legs: [{symbol, exchange, product, quantity, average_price, ltp, realized, today_realized, unrealized}]}</code></p><p><code>total</code> is realized + unrealized across all sessions; <code>today_total</code> is the intraday equivalent, pairing <code>today_realized</code> with the same unrealized figure.</p><p><code>legs</code> lists <strong>open legs first</strong>, so whenever <code>open_quantity</code> is non-zero, <code>&amp;#123;&amp;#123;pnl.legs[0]&amp;#125;&amp;#125;</code> is an open leg. The book keeps a strategy&#39;s flat legs and resets their <code>average_price</code> to 0 when they close, so without that ordering a positional read would eventually land on a stale closed leg and a percentage calculation would divide by zero. Guard on <code>open_quantity</code> before reading a leg, and before acting, or an exit re-fires every run once the position is flat and realized P&amp;L still exceeds the target.</p><p><code>unpriced_legs</code> counts open legs with no live price; those are <strong>excluded</strong> from <code>unrealized</code>. A non-zero value means the total is understated.</p><p>The book is fed from orders placed <strong>through Tradeboard carrying a strategy tag</strong>; a position opened by hand in the broker terminal is invisible to it.</p><p>The node returns an <strong>error</strong>, never a zero, when the figures are unknown, whether the position book is unavailable or the strategy book itself could not be read. A P&amp;L of zero always means &quot;flat&quot;, never &quot;could not tell&quot;.</p><h3 id="openposition-open-position" tabindex="-1"><code>openPosition</code>: Open Position <a class="header-anchor" href="#openposition-open-position" aria-label="Permalink to &quot;`openPosition`: Open Position&quot;">​</a></h3><p><code>symbol</code>, <code>exchange</code> (both required) plus <code>product</code> (defaults by exchange) → <code>{status, quantity, pnl}</code>. <code>quantity</code> is signed: negative is short. <code>pnl</code> is what <code>positionCheck</code>&#39;s <code>pnl_above</code> and <code>pnl_below</code> read.</p><h3 id="calendar-calendar" tabindex="-1"><code>calendar</code>: Calendar <a class="header-anchor" href="#calendar-calendar" aria-label="Permalink to &quot;`calendar`: Calendar&quot;">​</a></h3><p>Trading-day facts for a date, and the stateless answer to <strong>&quot;has a new day, week, month, quarter or year started&quot;</strong>.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>date</code></td><td><code>YYYY-MM-DD</code>. Blank uses the current trading session date, which differs from the calendar date between midnight and the 03:00 IST rollover.</td></tr></tbody></table><p>→ <code>{status, date, is_trading_day, is_trading_holiday, is_weekend, is_special_session, weekday, weekday_num, day, month, quarter, year, week_of_year, day_of_year, is_new_day, is_new_week, is_new_month, is_new_quarter, is_new_year, is_last_day_of_week, is_last_day_of_month, is_last_day_of_quarter, is_last_day_of_year, prev_trading_day, next_trading_day, first_trading_day_of_week, first_trading_day_of_month, first_trading_day_of_quarter, last_trading_day_of_week, last_trading_day_of_month, last_trading_day_of_quarter}</code></p><p><code>is_special_session</code> distinguishes a Muhurat or other special session from an ordinary trading day.</p><p>Flow keeps no state between runs, so a workflow cannot remember the last run&#39;s date. It does not need to: &quot;a new month started&quot; is the same statement as &quot;today is the first trading day of this month&quot;, which the exchange calendar answers on its own.</p><p>That is also <strong>more correct</strong> than the tests you would otherwise write:</p><table tabindex="0"><thead><tr><th>Naive test</th><th>Case it gets wrong</th></tr></thead><tbody><tr><td><code>&amp;#123;&amp;#123;day&amp;#125;&amp;#125; == 1</code></td><td>1 Aug 2026 is a Saturday, so the month opens on the 3rd</td></tr><tr><td><code>&amp;#123;&amp;#123;weekday&amp;#125;&amp;#125; == Monday</code></td><td>26 Jan 2026 is Republic Day, so that week opens on Tuesday the 27th</td></tr></tbody></table><p><code>is_trading_holiday</code> is distinct from <code>is_weekend</code>, so you can tell a closed weekday from a weekend. Use <code>is_last_day_of_month</code> for month-end square-off.</p><p><strong>Not exchange-aware.</strong> A date is a trading holiday if the exchange calendar lists one; MCX differs from NSE on a few days a year.</p><h3 id="intervals-supported-intervals" tabindex="-1"><code>intervals</code>: Supported Intervals <a class="header-anchor" href="#intervals-supported-intervals" aria-label="Permalink to &quot;`intervals`: Supported Intervals&quot;">​</a></h3><p>→ <code>{status, data: {seconds, minutes, hours, days, weeks, months&amp;#125;&amp;#125;</code></p><p>Use it to confirm your broker supports an interval before relying on it. Not every broker supports every timeframe, and an unsupported one comes back as a broker error from <code>history</code> or <code>indicator</code>.</p><hr><h2 id="symbols-expiries-and-option-chains" tabindex="-1">Symbols, expiries and option chains <a class="header-anchor" href="#symbols-expiries-and-option-chains" aria-label="Permalink to &quot;Symbols, expiries and option chains&quot;">​</a></h2><h3 id="symbol-symbol-info" tabindex="-1"><code>symbol</code>: Symbol Info <a class="header-anchor" href="#symbol-symbol-info" aria-label="Permalink to &quot;`symbol`: Symbol Info&quot;">​</a></h3><p><code>symbol</code>, <code>exchange</code> (both required) → <code>{status, data: {symbol, brsymbol, lotsize, tick_size, expiry, strike, token, ...&amp;#125;&amp;#125;</code></p><p><code>data.lotsize</code> is how you size an F&amp;O order correctly on the nodes that take contract units.</p><h3 id="expiry-expiry-dates" tabindex="-1"><code>expiry</code>: Expiry Dates <a class="header-anchor" href="#expiry-expiry-dates" aria-label="Permalink to &quot;`expiry`: Expiry Dates&quot;">​</a></h3><p><code>symbol</code>, <code>exchange</code> (both required) plus <code>instrumenttype</code> (<code>options</code> / <code>futures</code>, <strong>lowercase</strong>, default <code>options</code>; anything else falls back to <code>options</code>) → <code>{status, message, data: [&quot;04-AUG-26&quot;, ...]}</code>, nearest first.</p><h3 id="optionsymbol-option-symbol" tabindex="-1"><code>optionSymbol</code>: Option Symbol <a class="header-anchor" href="#optionsymbol-option-symbol" aria-label="Permalink to &quot;`optionSymbol`: Option Symbol&quot;">​</a></h3><p>Resolves an ATM-relative strike to a tradable symbol without placing anything.</p><p><code>underlying</code> and <code>optionType</code> are required; <code>exchange</code>, <code>expiryDate</code> (<code>DDMMMYY</code>) and <code>offset</code> (<code>ATM</code>, <code>ITMn</code>, <code>OTMn</code>) are the rest.</p><p>→ <code>{status, symbol, exchange, lotsize, tick_size, freeze_qty, underlying_ltp}</code></p><p>Note this shape is <strong>flat</strong>, <code>&amp;#123;&amp;#123;os.symbol&amp;#125;&amp;#125;</code>, not <code>&amp;#123;&amp;#123;os.data.symbol&amp;#125;&amp;#125;</code>.</p><p>An unresolved <code>underlying</code> or an <code>expiryDate</code> that is not <code>DDMMMYY</code> fails the node before the lookup, rather than resolving something else.</p><h3 id="optionchain-option-chain" tabindex="-1"><code>optionChain</code>: Option Chain <a class="header-anchor" href="#optionchain-option-chain" aria-label="Permalink to &quot;`optionChain`: Option Chain&quot;">​</a></h3><p><code>underlying</code> (required), <code>exchange</code>, <code>expiryDate</code>, <code>strikeCount</code> → <code>{status, underlying, underlying_ltp, expiry_date, atm_strike, chain: [{strike, ce: {...}, pe: {...&amp;#125;&amp;#125;]}</code></p><h3 id="syntheticfuture-synthetic-future" tabindex="-1"><code>syntheticFuture</code>: Synthetic Future <a class="header-anchor" href="#syntheticfuture-synthetic-future" aria-label="Permalink to &quot;`syntheticFuture`: Synthetic Future&quot;">​</a></h3><p><code>underlying</code> (required), <code>exchange</code>, <code>expiryDate</code>. This node always needs a resolved <code>expiryDate</code>.</p><p>→ <code>{status, underlying, expiry, atm_strike, synthetic_future_price, underlying_ltp}</code></p><hr><h2 id="account-and-orders" tabindex="-1">Account and orders <a class="header-anchor" href="#account-and-orders" aria-label="Permalink to &quot;Account and orders&quot;">​</a></h2><h3 id="funds-funds" tabindex="-1"><code>funds</code>: Funds <a class="header-anchor" href="#funds-funds" aria-label="Permalink to &quot;`funds`: Funds&quot;">​</a></h3><p>→ <code>{status, data: {availablecash, collateral, m2mrealized, m2munrealized, utiliseddebits&amp;#125;&amp;#125;</code></p><h3 id="orderbook-order-book" tabindex="-1"><code>orderBook</code>: Order Book <a class="header-anchor" href="#orderbook-order-book" aria-label="Permalink to &quot;`orderBook`: Order Book&quot;">​</a></h3><p>→ <code>{status, data: {orders: [...], statistics: {...&amp;#125;&amp;#125;}</code></p><p><code>data.statistics.total_buy_orders</code> is the usual stateless &quot;have I already entered today&quot; guard: the order book resets daily and keeps the record after a position closes.</p><h3 id="tradebook-trade-book" tabindex="-1"><code>tradeBook</code>: Trade Book <a class="header-anchor" href="#tradebook-trade-book" aria-label="Permalink to &quot;`tradeBook`: Trade Book&quot;">​</a></h3><p>→ <code>{status, data: [{tradeid, orderid, symbol, average_price, ...}]}</code></p><h3 id="positionbook-position-book" tabindex="-1"><code>positionBook</code>: Position Book <a class="header-anchor" href="#positionbook-position-book" aria-label="Permalink to &quot;`positionBook`: Position Book&quot;">​</a></h3><p>→ <code>{status, data: [{symbol, exchange, product, quantity, average_price, ltp, pnl}]}</code></p><p>There is <strong>no <code>total_pnl</code> key</strong>: <code>&amp;#123;&amp;#123;pb.total_pnl&amp;#125;&amp;#125;</code> resolves to its own literal text. Sum <code>data[].pnl</code> yourself, or use <code>strategyPnl</code>.</p><p>Account-wide and netted per <code>(symbol, exchange, product)</code>, it cannot tell you which strategy opened a position. Use <code>strategyPnl</code> for that.</p><p>Note the shape asymmetry against <code>orderBook</code> and <code>holdings</code>, which is a common source of unresolvable paths: <code>positionBook</code> and <code>tradeBook</code> put their rows directly in <code>data</code>, the other two nest them a level deeper.</p><h3 id="holdings-holdings" tabindex="-1"><code>holdings</code>: Holdings <a class="header-anchor" href="#holdings-holdings" aria-label="Permalink to &quot;`holdings`: Holdings&quot;">​</a></h3><p>→ <code>{status, data: {holdings: [...], statistics: {...&amp;#125;&amp;#125;}</code></p><p>Note the nesting: <code>&amp;#123;&amp;#123;hd.data.holdings[0].symbol&amp;#125;&amp;#125;</code>, not <code>&amp;#123;&amp;#123;hd.data[0].symbol&amp;#125;&amp;#125;</code>.</p><h3 id="margin-margin-calculator" tabindex="-1"><code>margin</code>: Margin Calculator <a class="header-anchor" href="#margin-margin-calculator" aria-label="Permalink to &quot;`margin`: Margin Calculator&quot;">​</a></h3><p>Pre-trade margin for a proposed order or basket.</p><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>positionsJson</code></td><td>A JSON <strong>string</strong> describing the legs, one object per position. This is the only field the editor&#39;s panel builds. The legacy key <code>positions</code> is still accepted.</td></tr><tr><td><code>symbol</code>, <code>exchange</code>, <code>action</code>, <code>quantity</code>, <code>product</code>, <code>priceType</code>, <code>price</code></td><td>The single-position fallback, read only when no basket key is present. The panel offers no controls for them.</td></tr></tbody></table><p>Each position in the basket needs <code>symbol</code>, <code>exchange</code>, <code>action</code>, <code>quantity</code>, <code>product</code> and <code>pricetype</code>; <code>price</code> and <code>trigger_price</code> are optional and follow the same price rules as an order.</p><p><strong>A present but empty or unparseable <code>positionsJson</code> is a hard error, not a fallback.</strong> A basket was configured, so an empty result means interpolation dropped it (an unset <code>&amp;#123;&amp;#123;variable&amp;#125;&amp;#125;</code>), and quietly pricing the single-position fields instead would price a different estimate. With neither a basket nor a <code>symbol</code>, the node fails naming what it is missing rather than calling the broker with an empty symbol. Dropping a malformed entry from a basket is refused for the same reason: half a basket is not the estimate you asked for.</p><h3 id="getorderstatus-order-status" tabindex="-1"><code>getOrderStatus</code>: Order Status <a class="header-anchor" href="#getorderstatus-order-status" aria-label="Permalink to &quot;`getOrderStatus`: Order Status&quot;">​</a></h3><p><code>orderId</code> is required.</p><p>→ <code>{status, data: {order_status, average_price, quantity, ...&amp;#125;&amp;#125;</code></p><p>The node calls the broker once and returns immediately. <code>waitForCompletion</code> appears in older exports and in the frontend node definition, and is read by nothing: there is no polling and no wait. To react to a fill, use <code>orderUpdateTrigger</code> in a separate workflow.</p><h3 id="holidays-timings" tabindex="-1"><code>holidays</code> / <code>timings</code> <a class="header-anchor" href="#holidays-timings" aria-label="Permalink to &quot;`holidays` / `timings`&quot;">​</a></h3><p><code>holidays</code> takes a <code>year</code> (blank means the current year) → the exchange holiday list. <code>timings</code> takes a <code>date</code> (blank means today) → market open/close for that day. Use them to avoid firing on a holiday.</p><hr><h2 id="streaming" tabindex="-1">Streaming <a class="header-anchor" href="#streaming" aria-label="Permalink to &quot;Streaming&quot;">​</a></h2><p>These maintain a WebSocket subscription and pass the latest tick to their output variable. If WebSocket is unavailable they fall back to a single REST call, identical from the workflow&#39;s point of view.</p><p>All three subscribe nodes require <code>symbol</code> and <code>exchange</code>, and all three write a wrapped envelope to their output variable, not the bare value. Read through the envelope.</p><table tabindex="0"><thead><tr><th>Node</th><th>Output variable holds</th><th>Read the price as</th></tr></thead><tbody><tr><td><code>subscribeLtp</code></td><td><code>{status, type: &quot;ltp&quot;, symbol, exchange, ltp, source}</code></td><td><code>&amp;#123;&amp;#123;lt.ltp&amp;#125;&amp;#125;</code></td></tr><tr><td><code>subscribeQuote</code></td><td><code>{status, type, symbol, exchange, data: {ltp, open, high, low, close, volume, ...}, source}</code></td><td><code>&amp;#123;&amp;#123;q.data.ltp&amp;#125;&amp;#125;</code></td></tr><tr><td><code>subscribeDepth</code></td><td><code>{status, type, symbol, exchange, data: {bids, asks, totalbuyqty, totalsellqty, ltp}, source}</code></td><td><code>&amp;#123;&amp;#123;d.data.bids[0].price&amp;#125;&amp;#125;</code></td></tr></tbody></table><p><code>source</code> is <code>websocket</code> or <code>rest_api</code>, so a workflow can tell which path answered.</p><p><code>&amp;#123;&amp;#123;lt&amp;#125;&amp;#125;</code> used as a number and <code>&amp;#123;&amp;#123;q.ltp&amp;#125;&amp;#125;</code> are the two mistakes this shape causes. Neither raises: the unresolved path is passed through as literal text.</p><h3 id="unsubscribe-unsubscribe" tabindex="-1"><code>unsubscribe</code>: Unsubscribe <a class="header-anchor" href="#unsubscribe-unsubscribe" aria-label="Permalink to &quot;`unsubscribe`: Unsubscribe&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>streamType</code></td><td><code>ltp</code>, <code>quote</code>, <code>depth</code>, <code>all</code>. Default <code>all</code>.</td></tr><tr><td><code>symbol</code></td><td>Required unless <code>streamType</code> is <code>all</code>.</td></tr><tr><td><code>exchange</code></td><td>Default <code>NSE</code>.</td></tr></tbody></table><p><strong>A specific stream type with no symbol is refused.</strong> The underlying call for &quot;no symbol&quot; clears every subscription on the instance, including the ones the Sandbox engine uses to trigger pending SL and LIMIT orders, so reaching it because an LTP unsubscribe happened to have no symbol tore down far more than the author asked for. Name a symbol, or set <code>streamType</code> to <code>all</code> if you really do mean everything.</p><p>A rejected unsubscribe now returns an error rather than reporting success.</p><p><strong>You do not need an <code>unsubscribe</code> node for cleanup.</strong> Deactivating or deleting a workflow releases everything it subscribed to. That matters because the WebSocket client is process-wide: a subscription left behind is held for the life of the worker and counts against the per-broker symbol ceiling that <code>/trading</code> and the Sandbox engine share. Use <code>unsubscribe</code> only to drop a stream mid-run.</p><hr><h2 id="utilities" tabindex="-1">Utilities <a class="header-anchor" href="#utilities" aria-label="Permalink to &quot;Utilities&quot;">​</a></h2><h3 id="variable-variable" tabindex="-1"><code>variable</code>: Variable <a class="header-anchor" href="#variable-variable" aria-label="Permalink to &quot;`variable`: Variable&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Values</th></tr></thead><tbody><tr><td><code>variableName</code></td><td>Required.</td></tr><tr><td><code>operation</code></td><td>Default <code>set</code>. One of the eleven below.</td></tr><tr><td><code>value</code></td><td>Literal or <code>&amp;#123;&amp;#123;path&amp;#125;&amp;#125;</code>.</td></tr><tr><td><code>sourceVariable</code></td><td>Required for <code>get</code> and <code>stringify</code>.</td></tr><tr><td><code>jsonPath</code></td><td>Optional for <code>get</code>. Dotted keys and bracketed indexes, e.g. <code>data.items[0].price</code>.</td></tr></tbody></table><p>All eleven operations are implemented:</p><table tabindex="0"><thead><tr><th>Operation</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>set</code></td><td>Stores <code>value</code>. A string starting <code>{</code> or <code>[</code> is parsed as JSON, so structured data can be carried.</td></tr><tr><td><code>get</code></td><td>Copies the raw value from <code>sourceVariable</code>, optionally walking <code>jsonPath</code>.</td></tr><tr><td><code>add</code> / <code>subtract</code> / <code>multiply</code> / <code>divide</code></td><td>Arithmetic against the current value, which initialises to 0 when unset. Division by zero is an error.</td></tr><tr><td><code>increment</code> / <code>decrement</code></td><td>Plus or minus 1, initialising to 0 when unset.</td></tr><tr><td><code>parse_json</code></td><td>Parses the interpolated <code>value</code> as JSON and stores the result. Invalid JSON is an error.</td></tr><tr><td><code>stringify</code></td><td>JSON-serializes <code>sourceVariable</code> into a string.</td></tr><tr><td><code>append</code></td><td>Appends <code>value</code> as text; an unset target starts empty.</td></tr></tbody></table><p>A missing source, an invalid conversion, invalid JSON or a division by zero returns an error and <strong>leaves the target unchanged</strong> rather than writing a partial result. There is no <code>extract</code> operation.</p><p><strong>Variables do not survive the run.</strong> An <code>increment</code> counts within one execution only; it is reset on the next tick. Anything that must persist has to come from the broker.</p><h3 id="mathexpression-math-expression" tabindex="-1"><code>mathExpression</code>: Math Expression <a class="header-anchor" href="#mathexpression-math-expression" aria-label="Permalink to &quot;`mathExpression`: Math Expression&quot;">​</a></h3><p><code>expression</code> (required), arithmetic over interpolated values, e.g. <code>&quot;&amp;#123;&amp;#123;q.data.ltp&amp;#125;&amp;#125; * 1.02&quot;</code>. The result goes to <code>outputVariable</code>, which defaults to <code>result</code>.</p><p>Supported: <code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>%</code>, <code>**</code>, unary minus and plus, parentheses, and one function, <code>floor(...)</code>. Exponents are capped at 100.</p><p><strong>No comparisons.</strong> <code>&amp;#123;&amp;#123;a&amp;#125;&amp;#125; &gt; &amp;#123;&amp;#123;b&amp;#125;&amp;#125;</code> raises &quot;Unsupported expression type: Compare&quot;; use <code>varCondition</code> for that. There is no string manipulation, no date arithmetic, and no other function call: an attribute access, a keyword argument or a wrong argument count is rejected.</p><h3 id="log-log" tabindex="-1"><code>log</code>: Log <a class="header-anchor" href="#log-log" aria-label="Permalink to &quot;`log`: Log&quot;">​</a></h3><p><code>message</code> (interpolated) and <code>level</code> (<code>info</code>, <code>warn</code>, <code>error</code>). The value is <code>warn</code>, not <code>warning</code>: anything else is stored verbatim and rendered at info severity. Written to the workflow&#39;s execution log, the primary way to debug a run.</p><h3 id="telegramalert-telegram-alert" tabindex="-1"><code>telegramAlert</code>: Telegram Alert <a class="header-anchor" href="#telegramalert-telegram-alert" aria-label="Permalink to &quot;`telegramAlert`: Telegram Alert&quot;">​</a></h3><p><code>message</code> (required, interpolated). Requires the Telegram bot to be configured and the workflow owner&#39;s account linked to it.</p><p>The panel still shows an &quot;Tradeboard Username&quot; box, but the executor never reads it. The recipient is resolved from the workflow&#39;s own API key, so the alert always reaches the workflow owner regardless of what is typed there.</p><h3 id="whatsappalert-whatsapp-alert" tabindex="-1"><code>whatsappAlert</code>: WhatsApp Alert <a class="header-anchor" href="#whatsappalert-whatsapp-alert" aria-label="Permalink to &quot;`whatsappAlert`: WhatsApp Alert&quot;">​</a></h3><p><code>message</code> (required, interpolated) and <code>to</code> (phone digits, e.g. <code>919876543210</code>). Blank <code>to</code> sends to the paired device itself. Requires the WhatsApp bot to be paired from the <code>/whatsapp</code> page.</p><h3 id="httprequest-http-request" tabindex="-1"><code>httpRequest</code>: HTTP Request <a class="header-anchor" href="#httprequest-http-request" aria-label="Permalink to &quot;`httpRequest`: HTTP Request&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Notes</th></tr></thead><tbody><tr><td><code>url</code></td><td>Required. Interpolated.</td></tr><tr><td><code>method</code></td><td><code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>, <code>PATCH</code>. Default <code>GET</code>.</td></tr><tr><td><code>headers</code></td><td>A JSON <strong>string</strong> (a JSON object is also accepted). Parsed first, then each value is interpolated.</td></tr><tr><td><code>body</code></td><td>Interpolated. Sent on POST, PUT and PATCH.</td></tr><tr><td><code>timeout</code></td><td><strong>Milliseconds.</strong> Default 30000, capped at 60000.</td></tr><tr><td><code>outputVariable</code></td><td>Default <code>response</code>.</td></tr></tbody></table><p>→ <code>{status, statusCode, data}</code>, where <code>data</code> is the parsed JSON body or, if it is not JSON, the raw text. A non-2xx response sets <code>status</code> to <code>error</code>, so the branch below the node stops and the run is marked failed.</p><p>Only <code>http</code> and <code>https</code> are allowed, and the destination must resolve to a public address: loopback, private, link-local and reserved ranges are rejected, so a workflow cannot be pointed at this server, at a host on the LAN, or at cloud metadata on <code>169.254.169.254</code>. This matters because <code>url</code> interpolates from workflow variables and a webhook trigger puts its caller&#39;s JSON body into that context. Redirects are not followed, and a logged URL has its query string redacted so a token in a query parameter is not written to the execution log.</p><p>The URL is interpolated exactly once. Headers are parsed before their values are interpolated, so a substituted value carrying a quote cannot close the string and add a header the author never wrote.</p><h3 id="delay-delay" tabindex="-1"><code>delay</code>: Delay <a class="header-anchor" href="#delay-delay" aria-label="Permalink to &quot;`delay`: Delay&quot;">​</a></h3><p><code>delayValue</code> + <code>delayUnit</code> (<code>seconds</code> / <code>minutes</code> / <code>hours</code>), or <code>delayMs</code>.</p><p><strong>Blocking, and silently capped at 300 seconds.</strong> A longer value logs a warning and waits 300 seconds instead, then continues: the run does not fail, it just proceeds early. A node configured as &quot;2 hours&quot; waits five minutes.</p><p>The run sleeps and holds its execution slot for whatever it does wait. Do not use it to pace a trading session; use a second workflow on its own schedule.</p><h3 id="waituntil-wait-until" tabindex="-1"><code>waitUntil</code>: Wait Until <a class="header-anchor" href="#waituntil-wait-until" aria-label="Permalink to &quot;`waitUntil`: Wait Until&quot;">​</a></h3><p><code>targetTime</code> (required, <code>&quot;HH:MM&quot;</code> or <code>&quot;HH:MM:SS&quot;</code>; seconds are honoured). Returns immediately with <code>{status, waited: false}</code> if the time has already passed today.</p><p><strong>Capped at 30 minutes.</strong> A target further away errors and points at a schedule trigger instead of waiting. The wait sleeps inside the workflow lock and inside the HTTP request that triggered the run, so an uncapped six-hour wait pinned a worker and answered &quot;already running&quot; to every trigger in between. Use a <code>start</code> schedule for a square-off hours later, or split it into its own workflow.</p><h3 id="group-group" tabindex="-1"><code>group</code>: Group <a class="header-anchor" href="#group-group" aria-label="Permalink to &quot;`group`: Group&quot;">​</a></h3><p>Visual grouping only. <code>label</code> and <code>color</code> are stored; there is no execution behaviour, and the node is a no-op when traversed.</p><hr><h2 id="node-count-by-category" tabindex="-1">Node count by category <a class="header-anchor" href="#node-count-by-category" aria-label="Permalink to &quot;Node count by category&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Category</th><th>Count</th><th>Nodes</th></tr></thead><tbody><tr><td>Triggers</td><td>4</td><td><code>start</code>, <code>priceAlert</code>, <code>webhookTrigger</code>, <code>orderUpdateTrigger</code></td></tr><tr><td>Order placement</td><td>10</td><td><code>placeOrder</code>, <code>smartOrder</code>, <code>optionsOrder</code>, <code>optionsMultiOrder</code>, <code>basketOrder</code>, <code>splitOrder</code>, <code>modifyOrder</code>, <code>cancelOrder</code>, <code>cancelAllOrders</code>, <code>closePositions</code></td></tr><tr><td>Conditions and logic</td><td>9</td><td><code>priceCondition</code>, <code>varCondition</code>, <code>timeWindow</code>, <code>timeCondition</code>, <code>positionCheck</code>, <code>fundCheck</code>, <code>andGate</code>, <code>orGate</code>, <code>notGate</code></td></tr><tr><td>Market data</td><td>11</td><td><code>getQuote</code>, <code>multiQuotes</code>, <code>getDepth</code>, <code>history</code>, <code>priorPeriodOhlc</code>, <code>barOffset</code>, <code>indicator</code>, <code>strategyPnl</code>, <code>openPosition</code>, <code>intervals</code>, <code>calendar</code></td></tr><tr><td>Symbols and options</td><td>5</td><td><code>symbol</code>, <code>expiry</code>, <code>optionSymbol</code>, <code>optionChain</code>, <code>syntheticFuture</code></td></tr><tr><td>Account and orders</td><td>9</td><td><code>funds</code>, <code>orderBook</code>, <code>tradeBook</code>, <code>positionBook</code>, <code>holdings</code>, <code>margin</code>, <code>getOrderStatus</code>, <code>holidays</code>, <code>timings</code></td></tr><tr><td>Streaming</td><td>4</td><td><code>subscribeLtp</code>, <code>subscribeQuote</code>, <code>subscribeDepth</code>, <code>unsubscribe</code></td></tr><tr><td>Utilities</td><td>9</td><td><code>variable</code>, <code>mathExpression</code>, <code>log</code>, <code>telegramAlert</code>, <code>whatsappAlert</code>, <code>delay</code>, <code>waitUntil</code>, <code>httpRequest</code>, <code>group</code></td></tr><tr><td><strong>Total</strong></td><td><strong>61</strong></td><td></td></tr></tbody></table><p>The sections above group nodes by task. The editor&#39;s palette groups them slightly differently: <code>calendar</code>, <code>holidays</code>, and <code>timings</code> sit under Utilities there, and <code>holdings</code>, <code>funds</code>, and <code>margin</code> under Risk Management.</p><p>For the exact JSON schema of every field, the format an AI needs to generate an importable workflow, see <a href="https://github.com/wesoftcorp/tradeboard-docs/blob/main/docs/prompt/flow-import-format.md" target="_blank" rel="noreferrer"><code>docs/prompt/flow-import-format.md</code></a> in the main repository.</p>',302)])])}const g=o(r,[["render",c]]);export{u as __pageData,g as default};
