import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.BXzK3EA4.js";const h=JSON.parse('{"title":"20 - Design Principles","description":"","frontmatter":{},"headers":[],"relativePath":"developers/design-documentation/20-design-principles.md","filePath":"developers/design-documentation/20-design-principles.md"}'),r={name:"developers/design-documentation/20-design-principles.md"};function o(n,e,l,c,d,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="_20-design-principles" tabindex="-1">20 - Design Principles <a class="header-anchor" href="#_20-design-principles" aria-label="Permalink to &quot;20 - Design Principles&quot;">​</a></h1><h2 id="code-is-authoritative" tabindex="-1">Code Is Authoritative <a class="header-anchor" href="#code-is-authoritative" aria-label="Permalink to &quot;Code Is Authoritative&quot;">​</a></h2><p>Registered routes, schemas, service behavior, and broker capability metadata are the source of truth. The <a href="./../../api-documentation/v1/README.html">API documentation</a> records the public contract because Flask-RESTX Swagger UI is intentionally disabled.</p><p>Examples in design pages explain boundaries; they are not substitute implementations.</p><h2 id="broker-agnostic-contract" tabindex="-1">Broker-Agnostic Contract <a class="header-anchor" href="#broker-agnostic-contract" aria-label="Permalink to &quot;Broker-Agnostic Contract&quot;">​</a></h2><p>The public API remains stable across the current 36 broker plugins: 34 securities brokers, Delta Exchange for crypto derivatives, and a Dhan sandbox plugin for paper trading. Each plugin maps Tradeboard symbols, products, actions, and price types into its broker&#39;s contract, then normalizes broker responses back into Tradeboard shapes.</p><p>Plugin presence does not imply that every optional operation, exchange, or WebSocket depth level is supported. <code>plugin.json</code> capability metadata and the broker implementation determine the available subset.</p><h2 id="layer-ownership" tabindex="-1">Layer Ownership <a class="header-anchor" href="#layer-ownership" aria-label="Permalink to &quot;Layer Ownership&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REST resource or session blueprint</span></span>
<span class="line"><span>        -&gt; schema/session validation</span></span>
<span class="line"><span>        -&gt; service orchestration</span></span>
<span class="line"><span>        -&gt; broker, sandbox, database, or calculation module</span></span>
<span class="line"><span>        -&gt; normalized response</span></span></code></pre></div><ul><li>Routes own transport concerns: request parsing, authentication, schema errors, and HTTP status.</li><li>Services own mode selection, policy, business sequencing, and normalized results.</li><li>Broker modules own broker-specific request mapping and response transformation.</li><li>Database modules own persistence and query behavior.</li><li>Subscribers own best-effort side effects such as event logging and notifications.</li></ul><p>Do not move broker-specific conditionals into shared resources when a plugin mapping or capability can express the difference.</p><h2 id="mode-and-risk-boundaries" tabindex="-1">Mode And Risk Boundaries <a class="header-anchor" href="#mode-and-risk-boundaries" aria-label="Permalink to &quot;Mode And Risk Boundaries&quot;">​</a></h2><p>Analyzer mode routes supported execution and account state into the sandbox subsystem. Live mode resolves the active broker session. The two persistence domains remain isolated.</p><p>Semi-auto mode can queue eligible operations in Action Center instead of executing them immediately. Destructive and unsupported operations must follow each service&#39;s explicit policy rather than assuming every order route can be deferred.</p><p>Risk-reducing workflows must not create exposure. Close-position and scalping exits derive the opposite action from current position state, and server validation remains authoritative even when the UI has already validated the request.</p><h2 id="process-aware-state" tabindex="-1">Process-Aware State <a class="header-anchor" href="#process-aware-state" aria-label="Permalink to &quot;Process-Aware State&quot;">​</a></h2><p>Tradeboard&#39;s production deployment uses Gunicorn with one eventlet worker per instance, while some components run in a child process or OS thread. Process-local caches, singletons, and the EventBus are not cross-process coordination mechanisms.</p><p>Cross-process market data and selected cache invalidation use a fixed ZeroMQ fan-in topology: the proxy SUB socket binds, and publisher sockets connect. Persisted state is used when behavior must survive navigation, restart, or process boundaries.</p><h2 id="persistence-discipline" tabindex="-1">Persistence Discipline <a class="header-anchor" href="#persistence-discipline" aria-label="Permalink to &quot;Persistence Discipline&quot;">​</a></h2><p>Tradeboard uses six primary configured stores: five SQLite workloads and Historify DuckDB. <code>database/engine_factory.py</code> gives every SQLite engine <code>NullPool</code> (non-SQLite backends get a normal pool), and scoped sessions are removed during request teardown. New persistence modules must either join the <code>SCOPED_SESSION_MODULES</code> inventory in <code>utils/db_sessions.py</code> or use a context-managed lifecycle that closes every connection.</p><p>Schema changes use idempotent initialization and targeted migrations rather than a general Alembic layer. Changes must work for both fresh and existing databases.</p><h2 id="security-defaults" tabindex="-1">Security Defaults <a class="header-anchor" href="#security-defaults" aria-label="Permalink to &quot;Security Defaults&quot;">​</a></h2><ul><li>API keys are verified with Argon2 plus <code>API_KEY_PEPPER</code>; retrievable key material and broker tokens are encrypted with Fernet-derived helpers.</li><li>Session routes retain CSRF protection except for reviewed broker callbacks, webhooks, broker postbacks, and health exemptions. <code>auth.logout</code> is deliberately kept under CSRF protection because it revokes broker tokens and tears down the shared feed.</li><li>Public <code>/api/v1</code> routes are CSRF-exempt because they authenticate with the Tradeboard API key.</li><li>CORS, CSP, cookie security, proxy trust, IP bans, and rate limits are explicit configuration boundaries.</li><li>Secrets, tokens, and full sensitive arguments must not enter normal logs.</li><li>Remote MCP is opt-in, refuses debug mode, and enforces OAuth scopes for each tool.</li></ul><h2 id="failure-behavior" tabindex="-1">Failure Behavior <a class="header-anchor" href="#failure-behavior" aria-label="Permalink to &quot;Failure Behavior&quot;">​</a></h2><p>Validation and policy errors should be returned before broker execution. Broker failures remain visible as normalized errors; they must not be converted into false success. Best-effort notifications or event subscribers may fail without changing an already-determined trading result, but persistence required by the operation is not treated as optional background work.</p><p>Background services need explicit start, stop, retry, and cleanup behavior. A task should not be launched from request code when application startup owns its lifecycle.</p><h2 id="change-rules" tabindex="-1">Change Rules <a class="header-anchor" href="#change-rules" aria-label="Permalink to &quot;Change Rules&quot;">​</a></h2><ol><li>Add or change schemas before relying on new request fields.</li><li>Keep the REST inventory and endpoint page synchronized with route registration.</li><li>Update broker capability metadata when behavior varies by plugin.</li><li>Cover shared service or schema changes across live and analyzer paths where both apply.</li><li>Verify teardown and process ownership when introducing caches, sockets, schedulers, or database sessions.</li><li>Preserve sensitive-field redaction in logs, events, audits, and exceptions.</li></ol><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./02-backend-architecture.html">Backend Architecture</a></li><li><a href="./27-service-layer.html">Service Layer</a></li><li><a href="./database-layer.html">Security Architecture</a></li><li><a href="./18-database-structure.html">Database Structure</a></li><li><a href="./authentication-platforms.html">WebSockets Architecture</a></li><li><a href="./53-event-bus.html">Event Bus</a></li></ul>`,30)])])}const b=a(r,[["render",o]]);export{h as __pageData,b as default};
