import{_ as t,o,c as a,a2 as r}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Version 2.0.2.1 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.2.1-released.md","filePath":"change-log/release/version-2.0.2.1-released.md"}'),n={name:"change-log/release/version-2.0.2.1-released.md"};function s(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="version-2-0-2-1-released" tabindex="-1">Version 2.0.2.1 Released <a class="header-anchor" href="#version-2-0-2-1-released" aria-label="Permalink to &quot;Version 2.0.2.1 Released&quot;">​</a></h1><p><strong>Date: 21st August 2026</strong></p><p><strong>Flow Release: takes the no-code builder through four independent QA audits and closes every Critical and High finding, replacing hand-written JSON boxes with typed editors on the Indicator and Margin nodes, repairing logic gates that behaved like the wrong operator, and stopping the editor from silently discarding a user&#39;s unsaved graph. Also brings the charting terminal from 20 to 91 built-in indicators, ends the endless &quot;Loading new version&quot; reload loop on nginx installs, and gives migrations the same 15-second SQLite lock timeout the running app has always had</strong></p><p>This release spans 25 commits since 2.0.2.0 and is overwhelmingly about <strong>Flow</strong>. A production QA audit on 21st August 2026 was validated finding by finding against source rather than taken at face value, then remediated across seventeen commits and re-audited three more times; the Critical and High residuals from each round were closed before the next began. The result covers the whole surface: triggers, the scheduler lifecycle, execution reporting, node contracts, the editor, the import format, the generated documentation and a migration that had never actually shipped its column.</p><p>Two of the defects fixed here outrank the audit&#39;s own Critical list because they lose a user&#39;s work: a failed workflow fetch rendered a blank canvas indistinguishable from a new workflow, so the next save overwrote the real graph; and clicking Activate replaced the canvas with the last saved version, discarding unsaved edits with no warning and no undo. A third let Run Now execute a revision the user was no longer looking at.</p><p>Outside Flow, the charting terminal picked up 71 more built-in indicators across two <code>tradeboard-charts</code> upgrades, the home page and dashboard were reframed around the full platform rather than execution alone, and a run of infrastructure fixes landed on nginx asset serving, migration lock timeouts, the Telegram REST surface, the MCP loopback probe, TradeSmart, Delta Exchange and the Python strategy scheduler.</p><hr><p><strong>Highlights</strong></p><ul><li><p><strong>Flow: the QA audit remediation (<code>175124751</code>, #1815)</strong> - seventeen commits closing the 2026-08-21 production QA audit and the three re-audits that followed it. Each finding was verified against current source before being fixed: several were overstated, one was materially worse than reported, and six further defects surfaced during the check.</p><p><strong>Triggers.</strong> A one-shot price alert was de-registered on the monitor thread immediately after submitting its worker, so the worker found it gone and dropped the run - measured 40 of 40 dropped once the pool is warm, with only the first alert in a process surviving because <code>Thread.start()</code> happens to yield the GIL. The run now owns de-registration and claims the alert by identity. A consumed one-shot trigger clears <code>is_active</code>, so restoring alerts at startup cannot re-arm a spent alert and re-fire its order. <code>restore_price_alerts()</code> was added to mirror <code>restore_order_update_watches</code>, since an active price-alert workflow previously went silently dead on restart while the activate endpoint refused it as <code>already_active</code>. Retirement no longer runs in <code>finally</code>: a one-shot that arrived while a previous run was still in flight used to be consumed and its workflow deactivated without the graph ever seeing the event; it is spent only when <code>execute_workflow</code> actually ran. An order update arrives from the bus once and is never replayed, so a refused submit now runs inline on the dispatch thread rather than dropping the fill.</p><p><strong>Logic gates delivered the wrong answer.</strong> A wire into an AND/OR/NOT gate carries the source condition&#39;s value, but the graph walk treated it as control flow and filtered it by the branch taken, so a <code>False</code> input never reached the gate. OR fired only when every input was true (behaving like AND), NOT could never fire at all, no gate could drive its false branch, and the outcome depended on which input the depth-first walk evaluated last - the same graph and the same market data gave different results. Verified over the full truth table in both traversal orders: 16 of 16 OR cases and 16 of 16 AND cases now correct, and NOT(False) fires.</p><p><strong>The Price Alert node evaluated a vocabulary it does not use.</strong> The executor compared the stored condition against the monitor&#39;s canonical names only, while the editor writes <code>above</code> / <code>below</code> / <code>crosses_above</code> / <code>crosses_below</code>. An unmapped name matched no branch, so <code>condition_met</code> stayed false, the order wired to Yes was skipped, and the run still reported completed with no errors - indistinguishable from the level never being reached. A monitor-fired run also no longer re-fetches a quote, since re-checking races the tick that fired it.</p><p><strong>Order nodes fail on unresolved variables.</strong> Interpolation returns the literal <code>&amp;#123;&amp;#123;name&amp;#125;&amp;#125;</code> text for an unknown path, which <code>get_int</code> could not parse and so returned its default of 1, while an unresolved <code>priceType</code> fell through the broker mapping to MARKET. A webhook that simply omitted a key therefore placed a successful order for the wrong size at the wrong price type with nothing in the run to say so. Order-defining fields on the nine order-mutating node types are now checked before dispatch, so the broker call is never made with substituted values.</p><p><strong>Node contracts aligned with the executor.</strong> Modify Order reads the live order and changes only what was supplied - it had been sending hardcoded action BUY, product MIS, price type LIMIT and quantity 1, flipping a live SELL to BUY, NRML to MIS, and silently resizing a 500-lot order to 1. The editor&#39;s own <code>DEFAULT_NODE_DATA.modifyOrder</code> shipped exchange NSE and action BUY, which the executor treats as deliberate overrides, so the node now carries only <code>orderId</code>. Close Positions honours its advertised symbol/exchange/product filter instead of always calling the unconditional square-off. HTTP Request parses headers from the JSON string the editor writes (they were silently dropped), supports PATCH, reads timeout as milliseconds capped at 60s, uses its own client rather than the shared broker pool, and rejects non-http(s) schemes plus loopback, private, link-local and reserved destinations - its URL interpolates from webhook-supplied context, so its caller could steer the request. Fund Check and Position Check fail closed instead of guarding against zero or an empty symbol, which had made them always true. Delay is capped at 300s.</p><p><strong>Condition nodes fail rather than answer from a substituted value.</strong> <code>false</code> is a real answer that routes the graph down the false branch, so a check that could not run must not produce one. <code>priceCondition</code> validated its operator against nothing, read 0.0 for an unknown field, and turned a non-numeric threshold into 0.0; all three now return an error and take neither branch. <code>timeCondition</code> keeps the seconds it parses - dropping them made &quot;after 15:29:59&quot; fire a minute early while <code>waitUntil</code> honoured the very same string.</p><p><strong>Execution reporting.</strong> The check-then-acquire duplicate-run guard was replaced with an atomic try-acquire, since two simultaneous triggers ran the whole workflow twice. A node returning status error now stops its branch and marks the run failed: a rejected entry leg used to let the hedge leg place and the alert fire, then record the run completed and answer HTTP 200 success. Condition errors are recorded in <code>executor.errors</code> too. Execute and webhook responses map <code>already_running</code> to 409 and failure to 5xx.</p><p><strong>Scheduler lifecycle.</strong> Activation persists <code>is_active</code> before registering and rolls back on failure - it previously registered first and discarded what the database returned, so a failed write reported success while a live persistent job traded against a row marked inactive, unstoppable because deactivate short-circuits and delete only removes the job when active. <code>reconcile_scheduler_jobs()</code> now runs at startup: the jobstore is persistent, so a stale job was restored at every boot and kept trading a workflow the user believed was off. The API key is no longer pickled into <code>flow_apscheduler_jobs.job_state</code>, where it sat in cleartext in the same database that Fernet-encrypts it; it is resolved per run, so regenerating the key no longer breaks schedules. Editing the trigger of an active workflow re-arms it during the save rather than only reporting <code>needs_reactivate</code>.</p><p><strong>Typed fields replace hand-written JSON.</strong> The Indicator node asked for its parameters as a JSON string; selecting an indicator now renders its real parameters - number inputs, dropdowns where the SDK documents a fixed set, a switch for booleans, and an explicit &quot;takes no parameters&quot; note for OBV, ADL and the other bare indicators. <code>INDICATOR_PARAMS</code> is generated from the <code>tradeboard.ta</code> signatures rather than hand-maintained, verified against the live SDK across all 116 indicators and all 36 dropdown values. Switching the indicator now prunes keys the new one does not accept. The Margin Calculator gained a repeatable leg editor whose placeholder JSON could never have worked (it omitted <code>product</code> and <code>pricetype</code>), with NFO and BFO quantities in lots, and its lot-size lookups batched into one <code>POST /flow/api/symbol-lotsizes</code> request capped at 50 pairs. Storage is unchanged in both cases and raw JSON stays reachable behind a toggle for <code>&amp;#123;&amp;#123;variable&amp;#125;&amp;#125;</code> references.</p><p><strong>The editor stopped losing work.</strong> A failed workflow GET left <code>isLoading</code> false and the data undefined, so the editor rendered a blank canvas; adding two nodes and saving PUT those two over the real graph and reset the name to the store default. Activate and Deactivate invalidated the workflow query, and the refetch re-ran the hydration effect, replacing the canvas with the last saved graph and clearing <code>isModified</code> - unsaved edits vanished a moment after clicking Activate. A save PUTs the graph as it stood when the request was issued, but <code>markSaved()</code> cleared the dirty flag regardless, so the canvas showed B while the server held A and Run Now then executed A; <code>markSaved</code> now takes the revision actually sent and <code>saveIfDirty</code> loops until a save completes with the canvas unchanged. Run Now, Activate and Deactivate save first when the canvas is dirty, since they send only the workflow id.</p><p><strong>Output variables and node subtitles.</strong> Nine node types rendered an output-variable name as their input&#39;s fallback while saving an empty string, so the box looked filled, the executor stored nothing, and every downstream reference resolved to its own literal text. Basket Order counted <code>orders</code> as an array while the editor writes newline-delimited text, and Margin counted <code>positions</code> while the editor writes <code>positionsJson</code>, so both read zero on every node. <code>NodeType</code> is now derived from the ReactFlow registry: the hand-maintained <code>NODE_TYPES</code> was missing 16 live types and named 10 components that no longer exist, which is what let those count bugs past the compiler.</p><p><strong>The <code>api_key</code> migration had never shipped.</strong> <code>create_flow_workflows_table</code> returns early when the table exists, so an installation predating the column could never gain it; the only thing adding it was a startup ALTER whose failures were swallowed at debug level, while <code>--status</code> reported &quot;All changes applied&quot; against a schema with no <code>api_key</code> and every activation silently failed to persist. Alongside it, <code>create_execution</code> now stamps <code>started_at</code> - it was only ever set by a status transition nothing performs, so every execution ever recorded had a NULL start time (53 of 53 rows on the live database), and the history query ordering on that column collapsed to insertion order ascending, listing the oldest runs first and showing a workflow&#39;s first ever run as its last. Both were drilled against a copy of a real populated database forced back to the old schema.</p><p><strong>Execution history is bounded.</strong> <code>prune_workflow_executions</code> trims per workflow to <code>FLOW_EXECUTION_RETENTION_COUNT</code> (default 500) and drops anything older than <code>FLOW_EXECUTION_RETENTION_DAYS</code> (default 30), running as history is written so the same activity that grows the table trims it. Both Flow monitors now register their shutdown with <code>atexit</code>; neither had a caller anywhere, so the poll thread, the bus subscription and both worker pools survived until the process was killed.</p><p><strong>Tests and docs.</strong> 118 regressions in the audit file and 212 in the backend flow suite, plus a store test driving the real Zustand store for the save race, each verified against pre-fix code rather than assumed. <code>flow-import-format.md</code> is authoritative and its 67 JSON blocks all parse; the generated indicator reference emitted Python <code>False</code>/<code>True</code>/<code>None</code> rather than JSON, so none of its 116 examples were executable. <code>conftest</code> sets <code>LOG_DIR=log/test</code>, since errors provoked deliberately by tests were being appended to the operator&#39;s production <code>errors.jsonl</code>, which is truncated to its last 1000 lines at startup.</p></li><li><p><strong>Charting terminal: 20 to 91 built-in indicators (<code>305c1e737</code>, <code>106e9c151</code>)</strong> - <code>tradeboard-charts</code> 1.1.0 takes the catalogue from 20 to 86 and adds shaded fill regions to 22 of them; 1.2.0 brings it to 91 with CPR and floor pivots across daily, weekly and monthly frames, Seasonality rendered as a table over the chart, AlphaTrend, Range Analysis and WaveTrend Pro. The terminal needed no code changes for either - the indicator layer is registry-driven, so descriptors appear from the version bump alone and five of the new ones render as grouped sections without any work. What did need changing is the menu: 86 entries in a grouped dropdown is a long scroll, so it now has a search box filtering on both display name and id. 1.2.0 also carries a correctness fix for charts already in use: VWAP&#39;s session anchor and the daily CPR frame were pinned to an IST calendar day, which is 18:30 UTC and therefore the middle of a US session; sessions are read from the bar gaps now, with NSE behaviour unchanged.</p></li><li><p><strong>The endless &quot;Loading new version&quot; reload loop (<code>2e4584506</code>, <code>bf54acfc3</code>, #1807)</strong> - three independent defects that together explain a route flashing &quot;Loading new version&quot; forever and intermittently returning 503, on one browser but not another. The reload guard could never hold: <code>clearChunkReloadFlag()</code> ran at module scope on every page load including the one the reload produced, and since the app shell always mounts and only the lazy route chunk fails, the loop was reload, mount, clear flag, chunk fails, reload. It is now a 30s cooldown that is never cleared, so a second failure inside the window shows the real error while auto-recovery re-arms afterwards. <code>/assets/&lt;file&gt;</code> served three representations of one URL unsafely - brotli 33108 bytes, gzip 41166, identity 154440, with the identity fallback carrying no <code>Vary</code> header while every response is cached immutable for a year, so a shared cache could hand the raw copy to any later request for that URI. The forced upgrade header was then finished off across the remaining nginx config: <code>change-domain.sh</code> would have reinstated the broken block when a user moves domains, and the Ubuntu server design doc&#39;s paste-in sample now uses the proper <code>map $http_upgrade $connection_upgrade</code> form.</p></li><li><p><strong>Migrations got the app&#39;s 15-second SQLite lock timeout (<code>6bf0db4b0</code>, #1726)</strong> - <code>database/__init__.py</code> registers a global connect listener setting <code>PRAGMA busy_timeout=15000</code>, which is why the running application waits 15 seconds for a write lock. Migrations never got it: <code>migrate_all.py</code> runs each script through <code>subprocess.run</code>, so every migration is a separate process that imports SQLAlchemy directly and never imports the database package, falling back to the sqlite3 default of 5 seconds. Measured at 5000 with no database import and 15000 after one. A migration therefore gave up three times sooner than the rest of Tradeboard under identical contention, and an install failed with &quot;database is locked&quot; where waiting a moment longer would have succeeded. <code>upgrade/_pragmas.py</code> registers the same listener and is imported by the 21 migration scripts that build their own engine.</p></li><li><p><strong>Telegram REST surface repaired (<code>38f0cacb9</code>, #1577)</strong> - every write endpoint on <code>/api/v1/telegram</code> was broken, and nothing caught it because the <code>/telegram</code> web UI talks to a different blueprint, so only API users (Excel, Python, MCP) ever reached the code and they got an HTTP 500. <code>POST /start</code> passed a keyword <code>initialize_bot</code> does not accept and then called a method on a name that was never defined or imported anywhere in the module; <code>POST /stop</code> handed a plain tuple to <code>run_until_complete()</code> for a function that is synchronous. Both now use the same calls the working UI route uses, and <code>/notify</code> is gated.</p></li><li><p><strong>MCP loopback health probe honours <code>MCP_LOOPBACK_URL</code> (<code>45fa60cfb</code>, #1441)</strong> - the probe&#39;s docstring claimed it resolved its target the same way <code>blueprints/mcp_http.py</code> does, but it never read the variable. That variable is first in <code>mcp_http.py</code>&#39;s order precisely because an operator only sets it when neither default answers, which is exactly the topology where the probe then reported a false alarm on <code>/admin</code>. It is tried first now, each target carries its own label, and an unreachable override still falls through.</p></li><li><p><strong>TradeSmart: WebSocket lifecycle and rate limits (<code>16bf8e7f8</code>, <code>9980c48c1</code>, #1805, #1802)</strong> - the plugin had drifted from the patterns Shoonya, Flattrade, Zebu and Definedge use, which made an unrelated broker-side problem far noisier and left two real leaks behind. websocket-client 1.9 hands a received CLOSE frame to <code>on_error</code> as though it were an exception, so every polite server hangup logged an ERROR with &quot;None - None&quot;; close frames are now told from faults and logged at INFO. The heartbeat worker slept with <code>time.sleep(30)</code>, which cannot be interrupted, so every disconnect burned the full join timeout - it waits on a shutdown event now. Rate limits were corrected to the enforced 10/sec and 120/min, and bulk quotes are served from the WebSocket feed instead of REST.</p></li><li><p><strong>Delta Exchange: pooled feed kept alive after the last unsubscribe (<code>28600cc90</code>, #1799)</strong> - subscribing to an option chain stopped delivering ticks entirely: the subscription was accepted and logged, no data arrived, and the proxy reported &quot;Stale feed, connected but no ticks&quot;. The adapter tore down its own transport once its symbol count reached zero while the connection pool kept holding and reusing the same instance; <code>disconnect()</code> sets <code>_stop_flag</code>, so the retry loop exited permanently and <code>_get_adapter_with_capacity()</code> reused the dead adapter with no health check. Option chains hit this because switching expiry or strikes unsubscribes the whole set before subscribing the new one. The pool now owns the adapter lifecycle.</p></li><li><p><strong>Python strategies read session windows from the market calendar (<code>faeea5de4</code>)</strong> - the <code>/python</code> exchange selector hardcoded every session window in the frontend, so all four equity and F&amp;O exchanges showed 09:15-15:30. NFO and BFO have run to 15:40 since SEBI&#39;s Closing Auction Session moved the cash close to an auction while derivatives kept trading, and the market calendar DB already carried 15:40. The visible label was the smaller half: <code>NewPythonStrategy</code> prefilled the schedule from the same constants, so picking NFO set a stop time of 15:30 and silently cut a strategy off ten minutes before the F&amp;O session ends. <code>GET /python/api/exchanges</code> now serves the options with their real windows.</p></li><li><p><strong>Home page and dashboard reframed (<code>8697467f7</code>, <code>994db3f1a</code>, <code>83c8d5022</code>)</strong> - the hero pill points at Open Varsity, and the &quot;New in V2&quot; tools pill was replaced by a &quot;One platform, many desks&quot; section presenting the unified broker API, the charting terminal, Flow, the options and portfolio suite, the scalping terminal and sandbox testing as one self-hosted stack rather than an execution-only platform. Counts are read from the code rather than copied from the site, so they stay accurate. The dashboard&#39;s Telegram Alerts quick-access card now points at Open Varsity.</p></li><li><p><strong>Devsprint contributor prep guide (<code>fa90f09bf</code>, #1804)</strong> - a setup guide for developers joining the FOSS United / BangPypers devsprint, covering what the existing docs get wrong or leave out for a first-time contributor: a broker account is only needed to reach the logged-in UI, Node.js is not required for backend or docs work, <code>.env</code> needs no manual key generation, and whole-repo Ruff is not clean so only touched files should be linted.</p></li><li><p><strong>CI flake pinned (<code>2d8edfd0e</code>)</strong> - the third of three calendar integration tests in Strategy Builder was the only one without <code>SLOW_INTEGRATION_TEST_TIMEOUT</code> despite being the same shape as its neighbours; it measured 5078ms against the 5000ms default on the runner and failed the 2.0.2.0 release commit while touching no frontend code.</p></li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>tradeboard-charts</code>: upgraded to <strong>1.2.0</strong> (20 to 91 built-in indicators, VWAP and CPR session anchoring, frontend only)</li></ul><p>No Python dependencies changed in this release.</p><hr><p><strong>Upgrading</strong></p><p><code>git pull</code> on <code>main</code> is the canonical path - <code>frontend/dist</code> is tracked there and rebuilt by CI, so no Node.js is needed on the server. Run <code>cd upgrade &amp;&amp; uv run migrate_all.py</code> on an existing installation: this release ships the <code>flow_workflows.api_key</code> column that only ever existed on fresh installs, and backfills <code>flow_executions.started_at</code>, without which the Executions panel lists the oldest run first and the dashboard shows a workflow&#39;s first ever run as its last.</p>',16)])])}const g=t(n,[["render",s]]);export{u as __pageData,g as default};
