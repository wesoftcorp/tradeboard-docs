import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.BXzK3EA4.js";const p=JSON.parse('{"title":"39 - Strategy RMS Engine","description":"","frontmatter":{},"headers":[],"relativePath":"developers/design-documentation/39-strategy-module.md","filePath":"developers/design-documentation/39-strategy-module.md"}'),n={name:"developers/design-documentation/39-strategy-module.md"};function i(s,e,d,l,c,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="_39-strategy-rms-engine" tabindex="-1">39 - Strategy RMS Engine <a class="header-anchor" href="#_39-strategy-rms-engine" aria-label="Permalink to &quot;39 - Strategy RMS Engine&quot;">​</a></h1><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>The Strategy RMS Engine is Tradeboard&#39;s durable strategy execution subsystem. It owns multi-leg batch strategies and signal-driven strategies, their broker orders, risk state, recovery data, and operator audit trail. It is separate from Chartink, Flow, Python-hosted strategies, and ordinary REST order placement, even though all eventually use the broker integration layer.</p><h2 id="entry-surfaces" tabindex="-1">Entry Surfaces <a class="header-anchor" href="#entry-surfaces" aria-label="Permalink to &quot;Entry Surfaces&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Authentication</th><th>Responsibility</th></tr></thead><tbody><tr><td>Browser <code>/strategy</code></td><td>User session and CSRF controls</td><td>Create/edit configuration, enable live, rotate a webhook token, operator controls, detail views</td></tr><tr><td>RESTX <code>/api/v1/strategy/*</code></td><td>Tradeboard API key</td><td>Lifecycle actions and read-only strategy/run/order/event access</td></tr><tr><td>Public <code>POST /strategy/webhook/&lt;token&gt;</code></td><td>High-entropy URL token</td><td>External batch and signal alerts</td></tr></tbody></table><p>RESTX cannot create a strategy, enable live mode, rotate a token, or delete a strategy. The webhook takes no API key. Its token is stored only as a SHA-256 digest and shown to the operator only at creation or rotation.</p><h2 id="model-and-lifecycle" tabindex="-1">Model And Lifecycle <a class="header-anchor" href="#model-and-lifecycle" aria-label="Permalink to &quot;Model And Lifecycle&quot;">​</a></h2><p>Strategies have a <code>batch</code> or <code>signal</code> kind. A batch run resolves all configured legs and enters them together, then responds to <code>start</code>/<code>stop</code>. A signal run accepts exactly one leg transition at a time: <code>long_entry</code>, <code>long_exit</code>, <code>short_entry</code>, or <code>short_exit</code>.</p><p>The engine keeps durable strategy, leg, run, order, checkpoint, and event records. A position owner is identified by an exact <code>position_ref</code>; a signal flip may additionally retain one outgoing <code>superseded</code> owner until it is confirmed flat. The engine never treats a leg id or broker symbol alone as enough identity to close exposure.</p><h3 id="order-invariant" tabindex="-1">Order invariant <a class="header-anchor" href="#order-invariant" aria-label="Permalink to &quot;Order invariant&quot;">​</a></h3><ol><li>Write a pending strategy-order intent before broker dispatch.</li><li>Dispatch the order.</li><li>Persist the acknowledgement against that exact pending row.</li><li>Apply broker updates and terminal fills idempotently to that owner.</li></ol><p>If a broker fills synchronously before the acknowledgement is recorded, the engine holds an unmatched update briefly and replays it as soon as the durable row is linked. If acknowledgement persistence remains unavailable, the intent row and a critical <code>order_ack_unrecorded</code> event remain for reconciliation; the engine does not place a duplicate order to repair it.</p><h3 id="exit-invariant" tabindex="-1">Exit invariant <a class="header-anchor" href="#exit-invariant" aria-label="Permalink to &quot;Exit invariant&quot;">​</a></h3><p>An accepted broker exit is not terminal. The engine records a pending stop, submits MARKET exits for exact filled owners, and retains the run&#39;s subscription and risk management until fills prove every owner flat. An unfilled entry is not exited at configured quantity because the opposite order could create naked exposure if the entry later cancels. A rejected or cancelled exit becomes retryable; it must not strand a claim or finalise a run.</p><p>Confirmed-flat finalisation atomically writes the run stop fields, releases the strategy&#39;s current run, and calculates final realised P&amp;L from durable fill and ownership evidence. An overall target/stop is the trigger reason; the eventual realised P&amp;L can differ because exits fill at market.</p><h2 id="risk-and-price-inputs" tabindex="-1">Risk And Price Inputs <a class="header-anchor" href="#risk-and-price-inputs" aria-label="Permalink to &quot;Risk And Price Inputs&quot;">​</a></h2><p>The run state evaluates per-leg SL, target, trailing, overall MTM SL/target, lock profit, trail-to-entry, scheduler square-off, expiry, and daily-loss limits. It consumes one latest-known LTP mark per symbol. WebSocket marks are preferred while current; polling is used as a fallback when the WebSocket input is stale. A stale mark never silently wins over a live recovered source.</p><p>RMS work uses run-scoped state locks for in-memory transitions only. Database and broker I/O happen outside those locks so an eventlet worker cannot block other strategy state transitions on a slow broker or SQLite operation.</p><h2 id="recovery-and-reconciliation" tabindex="-1">Recovery And Reconciliation <a class="header-anchor" href="#recovery-and-reconciliation" aria-label="Permalink to &quot;Recovery And Reconciliation&quot;">​</a></h2><p>On startup the recovery path rebuilds open-run owners from durable strategy orders, then validates checkpoints only when their owner shape and quantities match the rebuild. Order/fill evidence has authority over a stale checkpoint. Ambiguous or unpriced exposure stays open and reserved for manual reconciliation rather than being guessed flat or valued as zero.</p><p>The browser detail page uses broker orderbook, tradebook, and positions when available, narrowed to the contracts this strategy traded. It labels local records as a fallback when broker data cannot be obtained. A broker position row can be shared with a manual order or another source, so its quantity and unrealised P&amp;L must not be attributed exclusively to this strategy.</p><h2 id="webhook-security" tabindex="-1">Webhook Security <a class="header-anchor" href="#webhook-security" aria-label="Permalink to &quot;Webhook Security&quot;">​</a></h2><p>The public webhook performs route rate limiting and declared-size rejection before the validation pipeline. Admitted requests are checked in this order: token, kill switch, client IP allowlist, JSON body, strategy-kind action, batch mode/live gates, dedupe/cooling-off where applicable, then engine dispatch. Each admitted terminal result is audited; rate-limit and pre-body 413 refusals are not.</p><p>The real client IP is extracted through the deployment proxy rules before it is compared to a configured CIDR allowlist. Rate-limit token keys use the token digest, not the raw credential. Tokens and token-shaped URL fragments are redacted from application logs, traffic records, and shipped proxy access-log configuration. External senders and custom proxies still need their own credential protection.</p><h2 id="key-modules" tabindex="-1">Key Modules <a class="header-anchor" href="#key-modules" aria-label="Permalink to &quot;Key Modules&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>blueprints/strategy_module.py</code></td><td>Session-authenticated strategy configuration and browser data endpoints</td></tr><tr><td><code>restx_api/strategy.py</code></td><td>API-key lifecycle and audit namespace</td></tr><tr><td><code>restx_api/strategy_schema.py</code></td><td>Strict RESTX request schemas and page limits</td></tr><tr><td><code>services/strategy_module/engine.py</code></td><td>Run lifecycle, entry/exit orchestration, RMS decisions</td></tr><tr><td><code>services/strategy_module/state.py</code></td><td>In-memory run state, ownership claims, and checkpoints</td></tr><tr><td><code>services/strategy_module/webhook.py</code></td><td>Public webhook validation, dedupe, audit, and dispatch</td></tr><tr><td><code>services/strategy_module/recovery.py</code></td><td>Restart recovery and durable evidence reconciliation</td></tr><tr><td><code>database/strategy_module_db.py</code></td><td>Strategy RMS persistence and scoped queries</td></tr></tbody></table><h2 id="api-contract" tabindex="-1">API Contract <a class="header-anchor" href="#api-contract" aria-label="Permalink to &quot;API Contract&quot;">​</a></h2><p>The external contract and field examples live in the <a href="./../../api-documentation/v1/strategy-rms-api/README.html">Strategy RMS RESTX API</a>. The public alert format lives in <a href="./../../api-documentation/v1/strategy-rms-api/webhook.html">Public Strategy Webhook</a>. Those pages are the user-facing source; this document records the architecture and invariants that implementation changes must preserve.</p>',28)])])}const y=t(n,[["render",i]]);export{p as __pageData,y as default};
