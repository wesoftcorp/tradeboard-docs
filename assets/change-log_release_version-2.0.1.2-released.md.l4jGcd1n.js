import{_ as o,o as t,c as s,a2 as i}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Version 2.0.1.2 Released","description":"","frontmatter":{},"headers":[],"relativePath":"change-log/release/version-2.0.1.2-released.md","filePath":"change-log/release/version-2.0.1.2-released.md"}'),n={name:"change-log/release/version-2.0.1.2-released.md"};function a(r,e,d,c,l,p){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="version-2-0-1-2-released" tabindex="-1">Version 2.0.1.2 Released <a class="header-anchor" href="#version-2-0-1-2-released" aria-label="Permalink to &quot;Version 2.0.1.2 Released&quot;">​</a></h1><p><strong>Date: 28th May 2026</strong></p><p><strong>Maintenance + Performance Release: Option Greeks Rust Core (<code>opengreeks</code>, ~13× Faster Chain Refresh with Bit-for-Bit Parity), WebSocket Self-Healing + Subprocess Isolation Under Gunicorn-Eventlet, an Accessibility Sweep, Broker Data-Quality Fixes (Dhan Holdings, Definedge, Kotak Indices), and a Security/Dependency Sweep (ws CVE-2026-45736, SDK 1.0.51 Connection Pooling, idna)</strong></p><p>This release spans 20+ commits since v2.0.1.1. It is a stabilisation and performance pass on top of the WhatsApp release. The headline change is the <strong>option Greeks engine swap</strong> — <code>py_vollib</code> is replaced by <code>opengreeks</code>, a Rust + PyO3 Black-76 core with byte-identical function signatures, bit-for-bit numerical parity, and a ~13× speedup on a full option-chain refresh. Alongside it, the <strong>WebSocket layer self-heals</strong> on stale broker auth tokens (no more container restart after a new-trading-day re-login) and now runs as an <strong>isolated subprocess under gunicorn-eventlet</strong> to escape the greenlet/real-thread lock-switching hazard. The frontend gets an <strong>accessibility sweep</strong> (aria-labels on 63 icon-only buttons, color-contrast fixes). Three broker data-quality bugs are fixed (Dhan <code>/holdings</code>, Definedge master contract, Kotak index quotes). On security, the <code>ws</code> npm transitive dependency is patched (CVE-2026-45736), the bundled <strong>tradeboard SDK pin moves to 1.0.51</strong> (a connection-pooling fix that prevents socket exhaustion in long-running strategies), and <code>idna</code> is bumped.</p><hr><p><strong>Highlights</strong></p><ul><li><strong>Option Greeks Rust core — <code>opengreeks</code> replaces <code>py_vollib</code></strong> — The Black-76 backend for option Greeks and implied volatility is now a Rust + PyO3 core (<code>opengreeks==0.1.0</code>, NumPy-only runtime dep). Function signatures are byte-identical, so call sites change only their import path. Numerical parity is bit-for-bit on delta/gamma/theta/vega, float-64-last-bit on rho (7.9e-16), ~13 significant digits on IV. Pure-math speedups: implied volatility 46×, theta 28×, rho 19×, delta/vega/gamma 7–8×; a 40-option chain refresh (IV + 5 Greeks) drops <strong>1.485 ms → 0.116 ms (~12.8×)</strong>. Every downstream consumer (IV Smile, Vol Surface, GEX, IV Chart, Straddle Chart, Flow, <code>/api/v1/optiongreeks</code>, <code>/api/v1/multioptiongreeks</code>, MCP) routes through <code>option_greeks_service.calculate_greeks()</code> and inherits the speedup with no further changes.</li><li><strong>WebSocket self-heals on stale auth tokens (#1419)</strong> — When a broker adapter returns or raises an auth error (401/403/token expired) on connect or subscribe, the <code>ConnectionPool</code> now clears cached tokens, calls <code>initialize(force=True)</code> to re-read from <code>auth_db</code>, and retries once. Removes the container-restart requirement after a new-trading-day re-login.</li><li><strong>WebSocket proxy runs as a subprocess under gunicorn-eventlet (#1421, #1438)</strong> — In-process WS + Flask under eventlet shares one process with the eventlet hub, so monkey-patched stdlib locks touched from both the hub and the WS asyncio thread trigger <code>greenlet.error: Cannot switch to a different thread</code> and silently corrupt WS state. The app now detects eventlet at runtime (<code>is_monkey_patched(&quot;socket&quot;)</code>) and spawns <code>python -m websocket_proxy.server</code> as a child process with no monkey-patching, so all locks are real OS primitives. Atexit handler SIGTERMs with a SIGKILL fallback; the child stays in the gunicorn cgroup so systemd reaps it on hard crash. The dev-server path (no eventlet) is unchanged.</li><li><strong>Accessibility sweep</strong> — <code>aria-label</code> added to 63 icon-only buttons across the app, and 9 color-contrast violations resolved on the home page.</li><li><strong>Broker data-quality fixes</strong> — <strong>Dhan <code>/holdings</code></strong> now enriches each row with the real exchange (NSE/BSE, resolved via <code>securityId</code> probe) and LTP (batch <code>get_multiquotes</code>) instead of passing through the demat-wide <code>&quot;ALL&quot;</code> placeholder with blank price/P&amp;L (#1446). <strong>Definedge</strong> master contract had swapped <code>LotSize</code>/<code>TickSize</code> columns in <code>allmaster.csv</code> — now corrected (#1450, #1457). <strong>Kotak</strong> index quotes resolve for <code>MIDCPNIFTY</code> and other indices (#1436).</li><li><strong>Security + dependency sweep</strong> — <code>ws</code> npm transitive dependency patched for CVE-2026-45736 (uninitialized memory disclosure) via an <code>overrides</code> pin to <code>&gt;=8.20.1</code> (resolves to 8.21.0); bundled <code>tradeboard</code> SDK pin <code>1.0.50</code> → <code>1.0.51</code> (connection-pooling fix); <code>idna</code> <code>3.11</code> → <code>3.15</code>; unused <code>scipy</code> pin dropped; <code>py_vollib==1.0.1</code> + <code>py_lets_be_rational==1.0.1</code> removed (superseded by <code>opengreeks</code>).</li><li><strong>Platform version bump</strong> — <code>2.0.1.1</code> → <code>2.0.1.2</code>. SDK pin (<code>tradeboard</code>) <code>1.0.50</code> → <code>1.0.51</code>.</li></ul><hr><p><strong>Option Greeks — Rust core deep dive</strong></p><p><code>perf(greeks): replace py_vollib with opengreeks Rust core</code> — <code>8d973504</code>.</p><p>The Greeks/IV math previously ran on <code>py_vollib==1.0.1</code> (pure Python, backed by <code>py_lets_be_rational</code>). This release swaps it for <code>opengreeks==0.1.0</code>, a Rust + PyO3 implementation with a NumPy-only runtime footprint.</p><p><strong>Why it&#39;s a drop-in:</strong> function signatures are byte-identical, so the migration touches only import paths in <code>services/option_greeks_service.py</code> (and <code>services/iv_chart_service.py</code>, <code>broker/dhan_sandbox/api/data.py</code>). Nothing about the public API or response shape changes.</p><p><strong>Numerical parity</strong> (40-sample replay, NIFTY 26-MAY-2026 chain):</p><table tabindex="0"><thead><tr><th>Quantity</th><th>Abs error vs py_vollib</th></tr></thead><tbody><tr><td>delta / gamma / theta / vega</td><td>0.0e+00 (bit-for-bit identical)</td></tr><tr><td>rho</td><td>7.9e-16 (float-64 last bit)</td></tr><tr><td>implied_volatility</td><td>4.1e-13 (~13 significant digits)</td></tr></tbody></table><p><strong>Pure-math speedup</strong> (5000-rep median, ATM call):</p><table tabindex="0"><thead><tr><th>Function</th><th>Before</th><th>After</th><th>Speedup</th></tr></thead><tbody><tr><td>implied_volatility</td><td>17.29 µs</td><td>0.38 µs</td><td>46.1×</td></tr><tr><td>theta</td><td>5.79 µs</td><td>0.21 µs</td><td>27.7×</td></tr><tr><td>rho</td><td>3.96 µs</td><td>0.21 µs</td><td>18.9×</td></tr><tr><td>delta / vega / gamma</td><td>~1.5 µs</td><td>0.21 µs</td><td>7–8×</td></tr><tr><td><strong>40-option chain refresh (IV + 5 Greeks)</strong></td><td><strong>1.485 ms</strong></td><td><strong>0.116 ms</strong></td><td><strong>12.8×</strong></td></tr></tbody></table><p><strong>Migration evidence</strong> is captured in <code>docs/benchmarks/</code> (baseline JSON + MD, post-migration JSON, parity + speedup report) and is reproducible via the new <code>scripts/bench_greeks_*.py</code> / <code>scripts/bench_parity_opengreeks.py</code> suite.</p><hr><p><strong>WebSocket reliability</strong></p><p><code>d077559f</code> bundles two fixes from @Kalaiviswa:</p><ul><li><strong><code>fix(websocket): self-heal pool on stale auth-token failure (#1419)</code></strong> — <code>ConnectionPool</code> detects auth errors on connect/subscribe, clears cached tokens, re-initializes from <code>auth_db</code> with <code>force=True</code>, and retries once. The common symptom this kills: WS data silently stops after the ~3:00 AM IST broker token rollover until the operator restarts the container.</li><li><strong><code>fix(websocket): spawn WS proxy as subprocess under gunicorn+eventlet (#1421) (#1438)</code></strong> — Under eventlet, the WS asyncio thread and the eventlet hub share monkey-patched stdlib locks (logging <code>RLock</code>, socket.io lock, broker adapter <code>threading.Lock</code>), which can throw <code>greenlet.error: Cannot switch to a different thread</code> and corrupt WS state. The proxy now runs as a separate, un-monkey-patched child process when eventlet is active; the dev server (standard threading) is unchanged.</li></ul><hr><p><strong>Frontend + accessibility</strong></p><ul><li><code>ce597e52</code> — <code>fix(a11y): add aria-label to 63 icon-only buttons</code></li><li><code>7b2ab7c5</code> — <code>fix(a11y): resolve 9 color-contrast violations on home page</code></li><li><code>bb147762</code> — <code>fix(frontend): silence vite build warnings</code></li><li><code>da6a6826</code> — <code>fix(frontend): use automatic JSX runtime in vitest</code></li><li><code>f0246d00</code> — <code>style(frontend): apply biome safe-fix cleanup</code></li><li>A <code>vite 7 → 8</code> / <code>@vitejs/plugin-react 5 → 6</code> bump (<code>0804b6ee</code>) was attempted and then <strong>reverted</strong> (<code>d4e881f0</code>); the toolchain remains on <code>vite ^7.3.2</code>.</li></ul><hr><p><strong>Broker fixes</strong></p><ul><li><code>c3bb4436</code> — <code>fix(dhan): enrich /holdings with real exchange + LTP (#1446)</code> — Dhan&#39;s <code>/v2/holdings</code> returns <code>exchange=&quot;ALL&quot;</code> for every row and omits LTP (only <code>avgCostPrice</code>). <code>map_portfolio_data</code> now resolves the real NSE/BSE exchange via a <code>securityId</code> probe and batch-fetches LTP via <code>get_multiquotes</code>, restoring the exchange badge, Avg Price/LTP, P&amp;L, and the real-time WebSocket subscription on the Investor Summary UI.</li><li><code>abbcd2ec</code> — <code>fix(definedge): correct swapped LotSize/TickSize columns in allmaster.csv (#1450) (#1457)</code></li><li><code>3099a4e4</code> — <code>fix(kotak): resolve index quotes for MIDCPNIFTY and other indices (#1436)</code></li><li><code>b4134b41</code> — <code>fix(whatsapp): make normalize_phone tolerate int phones, reject float/bool</code></li></ul><hr><p><strong>Admin + infrastructure</strong></p><ul><li><code>d5ee335f</code> — <code>fix(admin): handle Docker bind-mounted .env on MCP settings save (#1337)</code></li><li><code>7e48b2e8</code> — <code>feat(scripts): add broker token extraction utility for self-hosted owners</code> (<code>scripts/extract_broker_token.py</code>)</li><li><code>a226839c</code> — <code>feat(examples): add PyPI download-stats comparator for Indian broker SDKs</code> (<code>examples/python/broker_sdk_downloads.py</code>)</li></ul><hr><p><strong>Tradeboard Python SDK 1.0.51 — connection pooling fix</strong></p><p>Released to PyPI alongside this version and pinned by the platform (<code>tradeboard==1.0.50</code> → <code>1.0.51</code>).</p><ul><li><strong>The problem:</strong> every REST call (orders, quotes, funds, history, …) opened a brand-new TCP connection and discarded it. Over a full trading day that left thousands of sockets in <code>TIME_WAIT</code>, eventually exhausting the OS&#39;s ephemeral ports — often misread as a memory/RAM crash when the real cause was socket exhaustion.</li><li><strong>The fix:</strong> the SDK now reuses a single shared, connection-pooled HTTP client (keep-alive) across all REST calls instead of opening a fresh connection each time. The Strategy webhook sender got the same treatment.</li><li><strong>What you get:</strong> flat socket count all day (no port/socket exhaustion in long sessions), lower per-call latency (no repeated TCP/TLS handshake), lower CPU and kernel overhead, and clean shutdown via <code>client.close()</code> / context-manager support.</li><li><strong>Note:</strong> the reuse benefit is fully realized in production behind gunicorn (keep-alive). The local dev server closes connections per request, so the effect is less visible there.</li><li>Available at <a href="https://pypi.org/project/tradeboard/1.0.51/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.51/</a>.</li></ul><hr><p><strong>Security — <code>ws</code> CVE-2026-45736</strong></p><p>A moderate-severity Dependabot alert (GHSA-58qx-3vcg-4xpx, CVE-2026-45736 — uninitialized memory disclosure in <code>ws</code>) flagged a transitive npm dependency reachable through <code>socket.io-client → engine.io-client</code>, which pinned <code>ws: ~8.18.3</code> and so locked the resolution to the vulnerable 8.18.x line.</p><ul><li>Fix: added <code>&quot;ws&quot;: &quot;&gt;=8.20.1&quot;</code> to the existing <code>overrides</code> block in <code>frontend/package.json</code>; the lockfile now resolves <code>ws</code> to <strong>8.21.0</strong>. <code>npm install</code> reports <strong>0 vulnerabilities</strong>.</li><li>No browser-bundle impact: <code>socket.io-client</code> uses the browser&#39;s native <code>WebSocket</code> in the React build, not the Node <code>ws</code> package — the override only affects the Node-side dependency graph and the lockfile that Dependabot scans.</li></ul><hr><p><strong>Dependencies</strong></p><ul><li><code>opengreeks&gt;=0.1.0</code> added — Rust + PyO3 Black-76 Greeks/IV core; NumPy-only runtime footprint. Replaces <code>py_vollib</code>.</li><li><code>py_vollib==1.0.1</code> and <code>py_lets_be_rational==1.0.1</code> <strong>removed</strong> (superseded by <code>opengreeks</code>).</li><li><code>tradeboard</code> SDK pin: <code>1.0.50</code> → <code>1.0.51</code> (PyPI: <a href="https://pypi.org/project/tradeboard/1.0.51/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.51/</a>) — connection-pooling fix.</li><li><code>idna</code> <code>3.11</code> → <code>3.15</code>.</li><li><code>scipy</code> — unused pin dropped (<code>a1eca63b</code>).</li><li><code>ws</code> (npm, transitive) → <code>&gt;=8.20.1</code> override, resolves to 8.21.0 (clears Dependabot alert 180 / CVE-2026-45736).</li></ul><hr><p><strong>Configuration changes</strong></p><p><code>pyproject.toml</code>:</p><ul><li><code>version = &quot;2.0.1.2&quot;</code></li><li><code>opengreeks&gt;=0.1.0</code> added; <code>py_vollib==1.0.1</code> + <code>py_lets_be_rational==1.0.1</code> removed</li><li><code>tradeboard==1.0.50</code> → <code>tradeboard==1.0.51</code></li><li><code>idna==3.11</code> → <code>idna==3.15</code></li><li>unused <code>scipy</code> pin removed</li></ul><p><code>utils/version.py</code>:</p><ul><li><code>VERSION = &quot;2.0.1.2&quot;</code></li></ul><p><code>requirements.txt</code> + <code>requirements-nginx.txt</code>:</p><ul><li><code>opengreeks==0.1.0</code> added; <code>py_vollib</code> / <code>py_lets_be_rational</code> removed</li><li><code>tradeboard==1.0.50</code> → <code>tradeboard==1.0.51</code></li><li><code>idna==3.11</code> → <code>idna==3.15</code></li></ul><p><code>frontend/package.json</code>:</p><ul><li><code>overrides</code> gains <code>&quot;ws&quot;: &quot;&gt;=8.20.1&quot;</code></li></ul><hr><p><strong>Upgrade procedure</strong></p><p><strong>For existing installs (Native Ubuntu):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/python/tradeboard-flask/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deploy-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tradeboard</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install/update.sh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># update.sh runs migrate_all.py. No schema migration is required for this</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># release; uv sync pulls opengreeks and drops py_vollib automatically.</span></span></code></pre></div><p><strong>For existing installs (Docker):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/tradeboard/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">domai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>For local developers (uv):</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend: a plain pull already ships the CI-built dist. Only rebuild if</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you are editing React code:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.py</span></span></code></pre></div><p>There are no new environment variables and no database schema changes in this release.</p><hr><p><strong>Contributors</strong></p><ul><li><strong>@marketcalls (Rajandran)</strong> — release management; option Greeks Rust-core migration (<code>py_vollib</code> → <code>opengreeks</code>) with parity + speedup benchmark suite; Dhan <code>/holdings</code> exchange + LTP enrichment (#1446); Kotak index-quote resolution (#1436); accessibility sweep (63 aria-labels + color-contrast fixes); frontend build-warning cleanup and biome safe-fixes; Docker bind-mounted <code>.env</code> handling on MCP settings save (#1337); broker token extraction utility and PyPI download-stats example; dependency + security sweep (ws CVE-2026-45736 override, SDK 1.0.51 connection-pooling pin, idna bump, scipy drop); tradeboard Python SDK 1.0.51 release.</li><li><strong>@Kalaiviswa</strong> — WebSocket self-heal on stale auth-token failure (#1419) and WS-proxy subprocess isolation under gunicorn-eventlet (#1421, #1438).</li><li><strong>Community</strong> — Definedge <code>allmaster.csv</code> LotSize/TickSize fix (#1450, #1457).</li></ul><hr><p><strong>Links</strong></p><ul><li><strong>Repository</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs</a></li><li><strong>Documentation</strong>: <a href="https://docs.algo.wesoftcorp.com" target="_blank" rel="noreferrer">https://docs.algo.wesoftcorp.com</a></li><li><strong>Python SDK on PyPI</strong>: <a href="https://pypi.org/project/tradeboard/1.0.51/" target="_blank" rel="noreferrer">https://pypi.org/project/tradeboard/1.0.51/</a></li><li><strong>Discord</strong>: <a href="https://www.tradeboard.in/discord" target="_blank" rel="noreferrer">https://www.tradeboard.in/discord</a></li><li><strong>Issue tracker</strong>: <a href="https://github.com/wesoftcorp/tradeboard-docs/issues" target="_blank" rel="noreferrer">https://github.com/wesoftcorp/tradeboard-docs/issues</a></li></ul>`,66)])])}const u=o(n,[["render",a]]);export{g as __pageData,u as default};
