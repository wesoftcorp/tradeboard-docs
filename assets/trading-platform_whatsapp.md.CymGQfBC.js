import{_ as t,o as s,c as a,a2 as o}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Whatsapp","description":"","frontmatter":{},"headers":[],"relativePath":"trading-platform/whatsapp.md","filePath":"trading-platform/whatsapp.md"}'),i={name:"trading-platform/whatsapp.md"};function n(r,e,d,l,p,c){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="whatsapp" tabindex="-1">Whatsapp <a class="header-anchor" href="#whatsapp" aria-label="Permalink to &quot;Whatsapp&quot;">​</a></h1><h4 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>The Tradeboard WhatsApp Bot connects your Tradeboard install to a WhatsApp account that you control. It does two things:</p><ol><li><strong>Outbound</strong>: fires real-time order alerts to you (and optionally to a small list of recipients) via the same event bus that already drives Telegram, so a <code>/api/v1/placeorder</code> call lands as a WhatsApp message on your phone moments later.</li><li><strong>Inbound</strong>: accepts slash-command queries (<code>/orderbook</code>, <code>/positions</code>, <code>/quote</code>, and so on) that you type from your <strong>own phone</strong>. The bot replies in the same chat. Commands are gated by WhatsApp&#39;s own multi-device protocol, so random contacts who message your number cannot drive the bot.</li></ol><p>Unlike Telegram, WhatsApp has no separate &quot;bot account&quot; concept. The Tradeboard server runs as a <strong>linked device</strong> on your personal WhatsApp account, the same way WhatsApp Web does. You pair once with a QR scan and the encrypted session lives in <code>tradeboard.db</code>.</p><h4 id="features" tabindex="-1">Features <a class="header-anchor" href="#features" aria-label="Permalink to &quot;Features&quot;">​</a></h4><ul><li><strong>One-time pairing</strong>: Scan a QR code from the admin web UI. The session blob is Fernet-encrypted at rest and auto-reconnects on every server boot. No bot token, no third-party service registration.</li><li><strong>Event-driven alerts</strong>: Every order topic the event bus publishes (<code>order.placed</code>, <code>order.modified</code>, <code>order.cancelled</code>, <code>orders.all_cancelled</code>, <code>position.closed</code>, <code>basket.completed</code>, <code>split.completed</code>, <code>options.completed</code>, <code>multiorder.completed</code>, plus the GTT topics) fires a WhatsApp message in parallel with Telegram.</li><li><strong>Unified send API</strong>: One <code>client.whatsapp(...)</code> call in the Python SDK and one <code>POST /api/v1/whatsapp/notify</code> endpoint over REST handle text, image, document, self-send, single recipient, and small broadcast (up to 5) cases.</li><li><strong>Real-time trading queries</strong>: Slash-commands from the operator&#39;s own phone trigger SDK calls and reply with the result in the same chat.</li><li><strong>Single-user security model</strong>: The paired device IS the operator. The bot only responds to messages where WhatsApp marks <code>is_from_me=True</code> (mirrored from the operator&#39;s primary phone). Random contacts who message the operator&#39;s number arrive with <code>is_from_me=False</code> and are silently ignored.</li><li><strong>Admin-only pairing</strong>: Pair, unpair, start, stop, config, broadcast, stats, and preferences live behind the session-authed <code>/whatsapp</code> admin page. The REST API surface is deliberately narrowed to send-only so a leaked API key cannot re-pair the device or enumerate recipients.</li></ul><h4 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h4><p><strong>1. Pair Your WhatsApp Device in Tradeboard</strong></p><ol><li>Log in to Tradeboard.</li><li>From the profile dropdown (top-right) click <strong>WhatsApp Bot</strong>, or navigate to <code>/whatsapp</code>.</li><li>Click <strong>Start pairing</strong>. A QR code renders inline on the page.</li><li>On your phone: open WhatsApp, then <strong>Settings</strong>, <strong>Linked devices</strong>, <strong>Link a device</strong>, and scan the QR.</li><li>The QR refreshes automatically every ~30 seconds. Each refresh streams a fresh <code>whatsapp_qr</code> SocketIO event to your browser, so the UI swaps the image without polling. When wars supplies one, a pair code is shown under the QR as an alternative to scanning.</li><li>On successful scan, the status badge flips to <strong>Connected</strong> and the bot is ready.</li></ol><p>That&#39;s the entire setup. No bot token, no developer account, no external service.</p><blockquote><p><strong>Note:</strong> WhatsApp permits a maximum of four (currently) linked devices per account. If you&#39;re already at the cap, remove an unused linked device on your phone before pairing Tradeboard.</p></blockquote><p><strong>2. (Optional) Generate an Tradeboard API Key</strong></p><p>Slash-command queries (<code>/orderbook</code>, <code>/positions</code>, and the rest) execute against the Tradeboard SDK using <strong>your own</strong> Tradeboard API key, looked up server-side by the owner username captured at pair time. If you haven&#39;t generated one yet:</p><ol><li>Navigate to <strong>API Key</strong> in the profile dropdown.</li><li>Generate a key.</li></ol><p>The bot pulls this key automatically from <code>auth_db</code>. You don&#39;t paste it anywhere on WhatsApp, and the key never leaves the server. If no owner username was recorded (for example, the device was paired by an older build), the bot tells you to re-pair from <code>/whatsapp</code> while logged in.</p><p><strong>3. (Optional) Configure Attachment Allowlist</strong></p><p>If you plan to send images or documents via the API, set <code>WHATSAPP_ATTACHMENT_ROOTS</code> in <code>.env</code> to a comma-separated list of absolute directories from which the server may read media:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WHATSAPP_ATTACHMENT_ROOTS=/srv/charts,/srv/reports</span></span></code></pre></div><p>When unset, the default allowlist is <code>&lt;tradeboard&gt;/db/attachments/</code> only. Paths containing <code>..</code>, paths under sensitive system trees (<code>/etc</code>, <code>/proc</code>, <code>/sys</code>, <code>/root</code>, <code>/var/log</code>, <code>C:\\Windows</code>, <code>C:\\Users\\Default</code>), and paths that resolve outside the allowlist are always rejected with <code>400 image_path is not allowed</code>.</p><h4 id="how-to-send-commands" tabindex="-1">How to Send Commands <a class="header-anchor" href="#how-to-send-commands" aria-label="Permalink to &quot;How to Send Commands&quot;">​</a></h4><p>Commands work differently from Telegram. WhatsApp has no separate bot identity: the bot <strong>is</strong> your own WhatsApp account, running as a linked device on the Tradeboard server.</p><ol><li>Open WhatsApp on your phone.</li><li>Scroll to the top of your chat list. There&#39;s a chat titled <strong>&quot;You&quot;</strong> or your own name (the &quot;Message yourself&quot; chat that WhatsApp creates automatically).</li><li>Type a command starting with <code>/</code>, for example <code>/orderbook</code>.</li><li>The linked device on the Tradeboard server sees the message as <code>is_from_me=True</code>, dispatches it, runs the matching SDK call, and replies in the same chat.</li><li>The reply arrives back on your phone within a second or two.</li></ol><p>The technical gate is <code>is_from_me</code>, not the identity of the chat. A slash-command you type in any chat, including a conversation with somebody else, is accepted and answered <strong>in that chat</strong>. Use the &quot;Message yourself&quot; chat so a reply carrying your orderbook never lands in front of a third party.</p><h4 id="available-commands" tabindex="-1">Available Commands <a class="header-anchor" href="#available-commands" aria-label="Permalink to &quot;Available Commands&quot;">​</a></h4><p><strong>Connection Status</strong></p><ul><li><code>/start</code>, <code>/help</code>, <code>/menu</code>: Show the full command list</li><li><code>/status</code>: Bot connection state, paired status, paired number, owner username</li></ul><p><strong>Trading Data</strong></p><ul><li><code>/orderbook</code>: Today&#39;s orders</li><li><code>/tradebook</code>: Today&#39;s executed trades</li><li><code>/positions</code>: Open positions</li><li><code>/holdings</code>: Portfolio holdings</li><li><code>/funds</code>: Account funds</li><li><code>/pnl</code>: Position-level P&amp;L. The bot asks the SDK for a dedicated P&amp;L call and falls back to the positionbook when the installed SDK does not expose one, which is the case for the currently pinned SDK</li></ul><p><strong>Market Data</strong></p><ul><li><code>/quote &lt;symbol&gt; [exchange]</code>: Quote for a symbol <ul><li>Example: <code>/quote RELIANCE</code></li><li>Example: <code>/quote NIFTY NSE_INDEX</code></li><li>Defaults to <code>NSE</code> if exchange omitted</li></ul></li></ul><p><strong>Trade Actions</strong></p><ul><li><code>/closeall</code>: Square off all open positions. Unlike the Telegram bot, this runs immediately with no confirmation step</li></ul><p><strong>Mode</strong></p><ul><li><code>/mode</code>: Show whether the Tradeboard instance is in <code>live</code> or <code>analyze</code> (sandbox) mode. Read-only; the WhatsApp bot cannot switch modes</li></ul><p>Anything else beginning with <code>/</code> gets <code>Unknown command. Send /help for the list.</code></p><p>Replies are plain-text WhatsApp messages. The bot does not curate the payload: it flattens the SDK response into <code>key: value</code> lines under a <code>*Title*</code> header, prints at most the first 10 entries of any list, and truncates the whole body at 3,500 characters with a <code>...(truncated)</code> marker. WhatsApp&#39;s own <code>*bold*</code>, <code>_italic_</code> and monospace markers survive because nothing rewrites them.</p><h4 id="order-alerts-automatic-notifications" tabindex="-1">Order Alerts (Automatic Notifications) <a class="header-anchor" href="#order-alerts-automatic-notifications" aria-label="Permalink to &quot;Order Alerts (Automatic Notifications)&quot;">​</a></h4><p><strong>Overview</strong></p><p>The bot automatically sends a WhatsApp message to the paired device&#39;s own number for every order-related API activity. No additional commands are needed: alerts are sent automatically when orders flow through the Tradeboard API.</p><p><strong>Supported Order Events</strong></p><table tabindex="0"><thead><tr><th>Topic</th><th>Trigger</th></tr></thead><tbody><tr><td><code>order.placed</code></td><td><code>/api/v1/placeorder</code> succeeded</td></tr><tr><td><code>order.no_action</code></td><td>Smart order found nothing to do</td></tr><tr><td><code>order.modified</code></td><td><code>/api/v1/modifyorder</code> succeeded</td></tr><tr><td><code>order.cancelled</code></td><td><code>/api/v1/cancelorder</code> succeeded</td></tr><tr><td><code>orders.all_cancelled</code></td><td><code>/api/v1/cancelallorder</code> succeeded</td></tr><tr><td><code>position.closed</code></td><td><code>/api/v1/closeposition</code> succeeded</td></tr><tr><td><code>basket.completed</code></td><td>All legs of a <code>/basketorder</code> completed</td></tr><tr><td><code>split.completed</code></td><td>All sub-orders of a <code>/splitorder</code> completed</td></tr><tr><td><code>options.completed</code></td><td>All legs of an <code>/optionsorder</code> (split path) completed</td></tr><tr><td><code>multiorder.completed</code></td><td>All legs of an <code>/optionsmultiorder</code> completed</td></tr><tr><td><code>gtt.placed</code></td><td><code>/api/v1/placegttorder</code> succeeded</td></tr><tr><td><code>gtt.modified</code></td><td><code>/api/v1/modifygttorder</code> succeeded</td></tr><tr><td><code>gtt.cancelled</code></td><td><code>/api/v1/cancelgttorder</code> succeeded</td></tr><tr><td><code>gtt.triggered</code></td><td>A resting GTT fired and placed its order</td></tr><tr><td><code>gtt.expired</code></td><td>A GTT lapsed past <code>expires_at</code> without firing</td></tr></tbody></table><p>Failure events (<code>order.failed</code>, <code>order.modify_failed</code>, <code>order.cancel_failed</code>, <code>gtt.failed</code>, <code>gtt.modify_failed</code>, <code>gtt.cancel_failed</code>, <code>analyzer.error</code>) deliberately do <strong>not</strong> fire WhatsApp messages, matching the existing Telegram convention so a flood of validation rejections doesn&#39;t spam the operator&#39;s phone.</p><p><code>gtt.placed</code>, <code>gtt.modified</code> and <code>gtt.cancelled</code> fire in both Live and Analyze mode. <code>gtt.triggered</code> and <code>gtt.expired</code> are published by the sandbox GTT manager, so today they only reach you in Analyze mode: a live GTT fires at the broker, where Tradeboard does not observe the moment it triggers.</p><p>The alert formatter has a dedicated layout for each order API type. GTT events do not have one yet, so they arrive under the generic <code>*Order Update*</code> heading with the mode line, strategy name and timestamp but no per-trigger detail.</p><p><strong>Alert Format</strong></p><p>Each alert includes:</p><ul><li><strong>Strategy Name</strong>: First line, if <code>strategy</code> was provided in the API call</li><li><strong>Mode Indicator</strong>: <ul><li><code>*LIVE MODE - Real Order*</code>: order executed with the broker</li><li><code>*ANALYZE MODE - No Real Order*</code>: sandbox / simulated order</li></ul></li><li><strong>Order Details</strong>: Symbol, action, quantity, price type, exchange, product</li><li><strong>Status</strong>: Order ID on success, or an error line with the failure message</li><li><strong>Timestamp</strong>: <code>Time: HH:MM:SS</code> on the last line</li></ul><p><strong>Example Notifications</strong></p><p><strong>Live Order Placed:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>*Order Placed*</span></span>
<span class="line"><span>Strategy: MyStrategy</span></span>
<span class="line"><span>*LIVE MODE - Real Order*</span></span>
<span class="line"><span>---------------------</span></span>
<span class="line"><span>Symbol: RELIANCE</span></span>
<span class="line"><span>Action: BUY</span></span>
<span class="line"><span>Quantity: 10</span></span>
<span class="line"><span>Price Type: MARKET</span></span>
<span class="line"><span>Exchange: NSE</span></span>
<span class="line"><span>Product: MIS</span></span>
<span class="line"><span>Order ID: 250408000989443</span></span>
<span class="line"><span>Time: 14:23:45</span></span></code></pre></div><p><strong>Analyze (Sandbox) Mode Order:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>*Order Placed*</span></span>
<span class="line"><span>Strategy: TestStrategy</span></span>
<span class="line"><span>*ANALYZE MODE - No Real Order*</span></span>
<span class="line"><span>---------------------</span></span>
<span class="line"><span>Symbol: RELIANCE</span></span>
<span class="line"><span>Action: BUY</span></span>
<span class="line"><span>Quantity: 10</span></span>
<span class="line"><span>Price Type: MARKET</span></span>
<span class="line"><span>Exchange: NSE</span></span>
<span class="line"><span>Product: MIS</span></span>
<span class="line"><span>Order ID: ANALYZE123456</span></span>
<span class="line"><span>Time: 14:23:45</span></span></code></pre></div><p><strong>Configuration</strong></p><ul><li>Alerts are <strong>enabled by default</strong> for the paired owner. No toggle is needed for the single-user case.</li><li>On disconnect or not-paired state, alerts are <strong>silently dropped</strong> (not queued). Pair from <code>/whatsapp</code> first; once the bot is connected, new order events flow normally.</li><li>Zero impact on order execution speed. Every alert goes through the event bus&#39;s thread pool, never on the order-placement critical path.</li></ul><p><strong>Requirements for Receiving Alerts</strong></p><ol><li>WhatsApp device must be paired in Tradeboard (<code>/whatsapp</code> page in the web UI).</li><li>The bot must be connected. It auto-reconnects on every boot from the encrypted session blob, so in practice this means the server is up and the session is still valid.</li><li>The API key on the order must resolve to the Tradeboard username recorded as <code>owner_username</code> at pair time. Orders placed by a different Tradeboard user do not alert the paired device.</li><li>Orders must be placed through the Tradeboard API (REST <code>/api/v1/*</code>, the Python SDK, or any tool that ultimately hits the API).</li></ol><h4 id="sending-messages-via-api" tabindex="-1">Sending Messages via API <a class="header-anchor" href="#sending-messages-via-api" aria-label="Permalink to &quot;Sending Messages via API&quot;">​</a></h4><p>In addition to the automatic order alerts, you can send arbitrary WhatsApp messages from your own code through the Tradeboard REST API or the Python SDK.</p><p><strong>Python SDK</strong></p><p>The <code>whatsapp()</code> method ships in the <code>tradeboard</code> package. Tradeboard currently pins <code>tradeboard==2.0.3</code>.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tradeboard </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">api_key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;your_api_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">host</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://127.0.0.1:5000&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Send to yourself</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.whatsapp(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Build #482 deployed. P&amp;L: +1.2%&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Send to a single number</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.whatsapp(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order placed: BUY RELIANCE x 10&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">to</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;919876543210&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Small broadcast (max 5 recipients)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.whatsapp(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;Server maintenance in 10 minutes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    to</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;919876543210&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;919812345678&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;919900112233&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Image with caption</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.whatsapp(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;NIFTY end-of-day chart&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    to</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;919876543210&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    image</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/srv/charts/nifty_eod.png&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Document attachment</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.whatsapp(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;Daily P&amp;L report attached&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    document</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/srv/reports/eod.pdf&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    filename</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;DailyPnL.pdf&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Only <code>message</code> is positional; <code>to</code>, <code>username</code>, <code>image</code>, <code>document</code>, <code>caption</code>, <code>filename</code> and <code>wait_for_delivery</code> are keyword-only. The SDK picks the recipient field for you: <code>username</code> wins, then a list <code>to</code> becomes <code>phones</code>, then a string <code>to</code> becomes <code>phone</code>, and when you pass none of them it sends <code>self: true</code>.</p><p><strong>REST API</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://127.0.0.1:5000/api/v1/whatsapp/notify</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;apikey&quot;: &quot;your_api_key&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;self&quot;: true,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;message&quot;: &quot;Order placed: BUY RELIANCE x 10&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  }&#39;</span></span></code></pre></div><p><strong>Sample success response:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;success&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Delivered to 1, failed 0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;sent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:    [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;self&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  [],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;skipped&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Sample not-paired response (HTTP 409):</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;WhatsApp is not paired or not connected. Pair the device first from the /whatsapp page in Tradeboard before sending.&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The API refuses with HTTP 409 rather than silently queueing. A trader expects an alert to either deliver or fail loudly, not appear later out of nowhere.</p><p><strong>Recipient Forms</strong></p><p>Exactly one of the following must be specified. Over raw REST there is no default: a body with none of these fields is rejected with <code>400 Specify one of: &#39;self&#39;, &#39;username&#39;, &#39;phone&#39;, or &#39;phones&#39;</code>. The Python SDK never hits that case because it fills in <code>self: true</code> when you name no recipient.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>self</code></td><td>bool</td><td><code>true</code> sends to the paired device&#39;s own number (the operator)</td></tr><tr><td><code>username</code></td><td>string</td><td>Tradeboard username, resolved via the linked-users table</td></tr><tr><td><code>phone</code></td><td>string</td><td>Single E.164 digit string, e.g. <code>&quot;919876543210&quot;</code></td></tr><tr><td><code>phones</code></td><td>array</td><td>Up to 5 E.164 digit strings (small broadcast). Anything beyond 5 is dropped server-side</td></tr></tbody></table><p><strong>Payload Fields</strong></p><p>At least one of <code>message</code>, <code>image_path</code> or <code>document_path</code> is required.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>message</code></td><td>string</td><td>Text body, max 4096 characters</td></tr><tr><td><code>image_path</code></td><td>string</td><td>Server-local path to an image (must be inside <code>WHATSAPP_ATTACHMENT_ROOTS</code>)</td></tr><tr><td><code>document_path</code></td><td>string</td><td>Server-local path to a document</td></tr><tr><td><code>caption</code></td><td>string</td><td>Caption for image, or follow-up text for document</td></tr><tr><td><code>filename</code></td><td>string</td><td>Override the document&#39;s display name on the recipient device</td></tr><tr><td><code>wait_for_delivery</code></td><td>bool</td><td>Default <code>true</code>. When <code>true</code>, block until WhatsApp confirms and return the per-recipient delivery report</td></tr></tbody></table><p>With <code>wait_for_delivery</code> set to <code>false</code> the response is a bare acknowledgement instead, <code>{&quot;status&quot;: &quot;success&quot;, &quot;message&quot;: &quot;Queued for N recipient(s)&quot;, &quot;queued&quot;: N}</code>, and no delivery report is produced.</p><h4 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h4><p><strong>Pairing Stays Inside the Web UI</strong></p><p>The QR-scan / pair-code flow lives behind <code>POST /whatsapp/pair</code>, which is protected by the Flask <strong>session cookie</strong> (<code>@check_session_validity</code>). It is deliberately <strong>not</strong> exposed in the public REST API. An Tradeboard API key alone cannot:</p><ul><li>Create a new paired device session</li><li>Wipe the existing session</li><li>Read or rotate <code>whatsapp_config</code></li><li>List linked recipients</li><li>Fan out a <code>/broadcast</code> to all linked users</li><li>Read command stats</li></ul><p>A leaked API key can only send messages via <code>POST /api/v1/whatsapp/notify</code>, the narrowest possible surface for the trader&#39;s automation use case.</p><p><strong>Encryption at Rest</strong></p><p>The paired-device session blob (~300 KB of Signal Protocol private keys, identity material, and registration info from wars/whatsapp-rust) is <strong>Fernet-encrypted</strong> before writing to <code>tradeboard.db</code>:</p><ul><li>Fernet key derived via PBKDF2-SHA256 from <code>API_KEY_PEPPER</code> and <code>FERNET_SALT + b&quot;:whatsapp-session&quot;</code> (100,000 iterations, 32-byte output)</li><li>The <code>:whatsapp-session</code> suffix is a domain separator. Broker auth tokens in <code>database/auth_db.py</code> derive their key from the same <code>API_KEY_PEPPER</code> with the bare <code>FERNET_SALT</code>, so the same pepper and salt pair produces two <strong>different</strong> Fernet keys and compromising one channel&#39;s ciphertext gives no leverage against the other. Telegram bot tokens and stored Telegram API keys (<code>database/telegram_db.py</code>) are separated further still: they derive from <code>API_KEY_PEPPER</code> with an entirely different salt, <code>TELEGRAM_KEY_SALT</code>.</li></ul><p>Compromise model:</p><table tabindex="0"><thead><tr><th>Attacker has</th><th>Outcome</th></tr></thead><tbody><tr><td><code>tradeboard.db</code> only</td><td>Useless, ciphertext without key</td></tr><tr><td><code>.env</code> only</td><td>Useless, no ciphertext to decrypt</td></tr><tr><td><code>tradeboard.db</code> + <code>.env</code></td><td>Full impersonation of the linked WhatsApp device</td></tr></tbody></table><p>Keep both off public hosts, off public git, and off any backup destination that mixes the two.</p><p><strong>Owner-Only Bot Commands</strong></p><p>Slash-commands are gated by WhatsApp&#39;s own multi-device cryptography. When the operator types <code>/orderbook</code> from their primary phone, WhatsApp marks the message as <code>is_from_me=True</code> when mirroring it to the linked Tradeboard device. Random contacts who message the operator&#39;s number arrive with <code>is_from_me=False</code>. The bot&#39;s handler unconditionally drops the latter. There is no allowlist to maintain and no <code>/link</code> flow to manage.</p><p><strong>Attachment Path Allowlist</strong></p><p>Image and document paths are validated server-side against:</p><ol><li><strong>Path-traversal rejection</strong>: paths containing <code>..</code> are refused before any filesystem call</li><li><strong>Absolute-path requirement</strong>: relative paths are refused</li><li><strong>Deny-list</strong>: <code>/etc</code>, <code>/proc</code>, <code>/sys</code>, <code>/root</code>, <code>/var/log</code>, <code>C:\\Windows</code>, <code>C:\\Users\\Default</code> are rejected outright</li><li><strong><code>WHATSAPP_ATTACHMENT_ROOTS</code> allowlist</strong>: the resolved real path (one symlink hop followed) must live under one of the configured roots</li></ol><p>Rejected paths return <code>400 image_path is not allowed</code> without echoing the path back, so misuse doesn&#39;t leak the operator&#39;s filesystem layout.</p><p><strong>Sensitive Args Scrubbed from Audit Logs</strong></p><p>The <code>whatsapp_command_logs</code> table records every slash-command for auditability, but command args carrying credentials are replaced with <code>&lt;redacted&gt;</code> before write.</p><h4 id="database-schema" tabindex="-1">Database Schema <a class="header-anchor" href="#database-schema" aria-label="Permalink to &quot;Database Schema&quot;">​</a></h4><p>The bot uses SQLAlchemy ORM with the following tables in <code>tradeboard.db</code>:</p><p><strong>whatsapp_config</strong></p><p>Singleton row (id=1) holding:</p><ul><li><code>session_blob</code>: Fernet-encrypted wars session bytes</li><li><code>own_jid</code>, <code>own_phone</code>, <code>bot_username</code>: captured lazily after the first <code>is_from_me=True</code> message</li><li><code>owner_user_id</code>, <code>owner_username</code>: captured at pair time from the Flask session</li><li><code>is_paired</code>, <code>is_active</code>, <code>paired_at</code>: lifecycle state</li><li><code>max_message_length</code>, <code>rate_limit_per_minute</code>, <code>broadcast_enabled</code>: operational tunables</li></ul><p><strong>whatsapp_users (optional, multi-recipient)</strong></p><p>Linked recipient phone numbers and their Tradeboard username and api_key mapping. This table stays empty in the standard single-user deployment: there is no <code>/link</code> command on the WhatsApp bot, so rows only appear if a deployment populates them itself.</p><p><strong>whatsapp_command_logs</strong></p><p>Audit trail of every slash-command: JID, command name, scrubbed parameters, timestamp.</p><p><strong>whatsapp_notification_queue</strong></p><p>Reserved for failed-delivery retry. Single-user mode does not queue (it refuses with HTTP 409 if not paired), and the table is kept for future multi-recipient deployments.</p><p><strong>whatsapp_user_preferences</strong></p><p>Per-user notification toggles (<code>order_notifications</code>, <code>trade_notifications</code>, <code>pnl_notifications</code>, <code>daily_summary</code>, <code>summary_time</code>, <code>language</code>, <code>timezone</code>).</p><h4 id="technical-architecture" tabindex="-1">Technical Architecture <a class="header-anchor" href="#technical-architecture" aria-label="Permalink to &quot;Technical Architecture&quot;">​</a></h4><p><strong>Components</strong></p><ol><li><strong><code>services/whatsapp_bot_service.py</code></strong>: <code>WhatsAppBotService</code> singleton <ul><li>Owns the wars (PyO3 over whatsapp-rust) instance via a dedicated <code>WhatsAppBotThread</code>. wars&#39;s <code>WhatsApp</code> class is marked <code>#[pyclass(unsendable)]</code> and panics if touched from any thread other than its creator, so all <code>wars.send()</code> calls funnel through a <code>queue.Queue</code> and are dispatched by the worker thread.</li><li>Re-entrant: command handlers (which wars dispatches on the bot thread itself via <code>on_message</code>) bypass the queue via a <code>threading.get_ident() == self._bot_thread_id</code> check so they don&#39;t deadlock on themselves.</li><li>Handles pair flow (temp wars instance plus <code>wait_until_ready</code> as the authoritative &quot;paired&quot; signal), connection lifecycle, slash-command dispatch, and SDK-backed query handlers (<code>/orderbook</code>, <code>/positions</code>, and the rest).</li></ul></li><li><strong><code>services/whatsapp_alert_service.py</code></strong>: <code>WhatsAppAlertService</code><ul><li>Outbound notifier. Formats order/position/batch events into plain-text WhatsApp messages with a LIVE or ANALYZE mode prefix.</li><li>Single-user owner resolution: matches the event&#39;s <code>api_key</code> to a username via <code>auth_db.get_username_by_apikey</code>, then checks against <code>whatsapp_config.owner_username</code> captured at pair time. If matched, fires a self-send through wars&#39;s single-arg <code>send(&quot;text&quot;)</code> form (no need to know own JID, because wars knows its own identity internally).</li></ul></li><li><strong><code>subscribers/whatsapp_subscriber.py</code></strong>: Event-bus subscriber <ul><li>Registered alongside <code>telegram_subscriber</code> in <code>subscribers/__init__.register_all()</code> on 22 topics: the 14 order, position, batch and analyzer topics plus the 8 GTT topics.</li><li>Mirrors the Telegram convention: failure events (<code>order.failed</code>, <code>order.modify_failed</code>, <code>order.cancel_failed</code>, the three GTT failure topics, <code>analyzer.error</code>) are silently dropped.</li></ul></li><li><strong><code>database/whatsapp_db.py</code></strong>: SQLAlchemy models <ul><li>5 tables plus Fernet encryption helpers and an idempotent <code>PRAGMA table_info</code> migration for the <code>owner_user_id</code> and <code>owner_username</code> columns</li></ul></li><li><strong><code>restx_api/whatsapp_bot.py</code></strong>: <code>POST /api/v1/whatsapp/notify</code><ul><li>The only public REST endpoint. Validates recipient, payload and attachment paths, dispatches synchronously by default (<code>wait_for_delivery=true</code>) so the response carries the real delivery report.</li></ul></li><li><strong><code>blueprints/whatsapp.py</code></strong>: Session-authed admin routes <ul><li><code>/whatsapp/pair</code>, <code>/whatsapp/pair/status</code>, <code>/whatsapp/unlink</code>, <code>/whatsapp/bot/start</code>, <code>/whatsapp/bot/stop</code>, <code>/whatsapp/bot/status</code>, <code>/whatsapp/config</code>, <code>/whatsapp/users</code>, <code>/whatsapp/broadcast</code>, <code>/whatsapp/send</code>, <code>/whatsapp/test-message</code>, <code>/whatsapp/stats</code></li><li>All gated by <code>@check_session_validity</code> and consumed by the React <code>/whatsapp</code> page.</li></ul></li><li><strong><code>frontend/src/pages/whatsapp/WhatsAppIndex.tsx</code></strong>: React admin page <ul><li>Pair flow with auto-rotating QR (SocketIO <code>whatsapp_qr</code> event), a Disconnect button, and a send-to-phone composer.</li></ul></li><li><strong>Auto-reconnect on app boot</strong>: <code>app.py:_autostart_whatsapp_bot</code><ul><li>Background thread spawned in <code>_init_databases_and_schedulers</code> after DB init completes</li><li>If <code>whatsapp_config.is_paired</code> is true, loads the encrypted blob and starts the worker thread without operator intervention</li></ul></li></ol><p><strong>Event Flow</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST /api/v1/placeorder</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>services/place_order_service.place_order(...)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>bus.publish(OrderPlacedEvent(api_key, ...))</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├──&gt; log_subscriber          (writes to log/orders.jsonl)</span></span>
<span class="line"><span>        ├──&gt; socketio_subscriber     (emits order_event for the dashboard)</span></span>
<span class="line"><span>        ├──&gt; telegram_subscriber     (queues telegram_alert)</span></span>
<span class="line"><span>        └──&gt; whatsapp_subscriber     (queues whatsapp_alert)</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼</span></span>
<span class="line"><span>             whatsapp_alert_service.send_order_alert</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼ alert_executor (5-worker thread pool)</span></span>
<span class="line"><span>             whatsapp_bot_service.send_sync(to=None, text=msg)</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼ enqueue on _cmd_queue</span></span>
<span class="line"><span>             WhatsAppBotThread picks up the command</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼</span></span>
<span class="line"><span>             self._wa.send(msg)   (wars&#39;s single-arg form goes to the owner)</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼</span></span>
<span class="line"><span>             WhatsApp servers deliver to the operator&#39;s phone</span></span></code></pre></div><p><strong>Threading Model</strong></p><ul><li>The Flask app runs under Gunicorn plus eventlet (production) or the threaded dev server (development).</li><li>The WhatsApp bot runs on a <strong>dedicated OS thread</strong> (<code>WhatsAppBotThread</code>), spawned via <code>threading.Thread</code>. wars&#39;s internal Rust runtime spawns its own worker threads but routes Python callbacks back to the creator thread, satisfying PyO3&#39;s unsendable contract.</li><li>Outbound sends from request threads cross to the bot thread via <code>queue.Queue</code> plus <code>threading.Event</code>.</li></ul><h4 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h4><p><strong>Bot Not Sending Alerts</strong></p><ol><li>Open <code>/whatsapp</code> and verify the status badge shows <strong>Connected</strong>. If it shows <strong>Not paired</strong>, scan the QR.</li><li>Check that you have an Tradeboard API key generated at <code>/apikey</code> (slash-commands need it for SDK calls).</li><li>Confirm the order actually flowed through <code>/api/v1/placeorder</code> (or the SDK, a strategy, or any other API path). Orders placed directly via a broker website do NOT trigger event-bus events.</li><li>Check the server logs for lines like <code>WhatsApp alert queued for owner user=&lt;username&gt; type=placeorder</code>. If present, the alert was dispatched.</li></ol><p><strong>&quot;WhatsApp is not paired or not connected&quot; (HTTP 409)</strong></p><p>The bot lost its connection, typically after a long offline period, a WhatsApp protocol upgrade, or your phone being offline for many days.</p><ol><li>Open <code>/whatsapp</code> and re-pair if the badge says <strong>Not paired</strong>.</li><li>If the badge says <strong>Connected</strong> but sends still fail, restart the Tradeboard server. Auto-reconnect rebuilds the session from the encrypted blob.</li></ol><p><strong>Slash Commands Don&#39;t Reply</strong></p><ol><li>Make sure you typed the command in the <strong>&quot;Message yourself&quot;</strong> chat (your own contact at the top of the chat list).</li><li>Commands must start with <code>/</code> and use one of the supported names. Check <code>/help</code> for the list.</li><li>Verify the Tradeboard owner has an API key on file (<code>/apikey</code> page).</li><li>Check the <code>whatsapp_command_logs</code> table for the command. If it&#39;s logged, the bot received and processed it.</li></ol><p><strong>Attachment Path Rejected</strong></p><p><code>400 image_path is not allowed</code> means the path is outside the <code>WHATSAPP_ATTACHMENT_ROOTS</code> allowlist or contains a traversal token.</p><ol><li>Move the file to <code>&lt;tradeboard&gt;/db/attachments/</code> (the default allowlist), or</li><li>Add the file&#39;s directory to <code>WHATSAPP_ATTACHMENT_ROOTS</code> in <code>.env</code> and restart Tradeboard.</li></ol><p>Symlinks resolving outside the allowlist are also rejected.</p><p><strong>&quot;WhatsApp Web is full&quot; / Pairing Fails</strong></p><p>WhatsApp allows up to 4 simultaneously linked devices per account. On your phone, open <strong>Settings</strong>, then <strong>Linked devices</strong>, and remove an unused one (often &quot;WhatsApp Web on Chrome&quot; left over from months ago).</p><h4 id="environment-variables" tabindex="-1">Environment Variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment Variables&quot;">​</a></h4><p>The bot respects the following environment variables:</p><ul><li><code>DATABASE_URL</code>: Main Tradeboard database (WhatsApp tables live here)</li><li><code>API_KEY_PEPPER</code>: Encryption pepper, feeds the Fernet KDF</li><li><code>FERNET_SALT</code>: Per-install random salt (auto-rotated on first boot by <code>utils/env_check.py</code>); the <code>:whatsapp-session</code> domain suffix is applied internally</li><li><code>HOST_SERVER</code>: Tradeboard server URL the bot uses for SDK loopback calls (defaults to <code>http://127.0.0.1:5000</code>)</li><li><code>WHATSAPP_ATTACHMENT_ROOTS</code>: Optional comma-separated allowlist for media paths. Defaults to <code>&lt;tradeboard&gt;/db/attachments/</code> only.</li><li><code>WHATSAPP_RATE_LIMIT</code>: Optional rate limit for <code>POST /api/v1/whatsapp/notify</code>. Defaults to <code>30 per minute</code>.</li><li><code>WHATSAPP_MESSAGE_RATE_LIMIT</code>: Optional rate limit for the <code>/whatsapp/broadcast</code>, <code>/whatsapp/send</code> and <code>/whatsapp/test-message</code> admin routes. Defaults to <code>10 per minute</code>.</li><li><code>RUST_LOG</code>: Optional log-level filter for wars / whatsapp-rust. The default silences three known-noisy modules while keeping genuine errors visible.</li></ul><h4 id="api-endpoints" tabindex="-1">API Endpoints <a class="header-anchor" href="#api-endpoints" aria-label="Permalink to &quot;API Endpoints&quot;">​</a></h4><p><strong>Public REST API (API-key auth)</strong></p><ul><li><code>POST /api/v1/whatsapp/notify</code>: Send a message. The only public endpoint.</li></ul><p><strong>Session-Authed Admin (web UI only)</strong></p><ul><li><code>GET /whatsapp/config</code>: Read bot config and pair state</li><li><code>POST /whatsapp/config</code>: Update operational settings (broadcast toggle, rate limit, max message length)</li><li><code>POST /whatsapp/pair</code>: Start pairing flow</li><li><code>GET /whatsapp/pair/status</code>: Poll pair state (alternative to SocketIO)</li><li><code>POST /whatsapp/unlink</code>: Wipe the encrypted session blob</li><li><code>POST /whatsapp/bot/start</code>: Connect bot using stored session</li><li><code>POST /whatsapp/bot/stop</code>: Disconnect (session retained)</li><li><code>GET /whatsapp/bot/status</code>: Bot lifecycle state</li><li><code>GET /whatsapp/users</code>: List linked recipients (multi-recipient mode)</li><li><code>POST /whatsapp/user/&lt;jid&gt;/unlink</code>: Unlink a recipient</li><li><code>POST /whatsapp/broadcast</code>: Send to all linked users (filtered)</li><li><code>POST /whatsapp/send</code>: One-off send to any number</li><li><code>POST /whatsapp/test-message</code>: Send a test message to the operator</li><li><code>GET /whatsapp/stats</code>: Command usage statistics</li></ul><p><strong>SocketIO Events (server to frontend)</strong></p><ul><li><code>whatsapp_qr</code>: Fresh QR data URL each time wars rotates the code</li><li><code>whatsapp_pair_code</code>: Pair-code alternative to QR</li><li><code>whatsapp_paired</code>: Pair completed successfully</li><li><code>whatsapp_pair_status</code>: Full pair-state snapshot</li><li><code>whatsapp_status</code>: Bot connection state changes</li></ul><h4 id="error-handling" tabindex="-1">Error Handling <a class="header-anchor" href="#error-handling" aria-label="Permalink to &quot;Error Handling&quot;">​</a></h4><ul><li>The bot never blocks order placement: alerts fail-soft. If wars isn&#39;t ready or the worker queue times out, the send returns a failure report but the order itself is unaffected.</li><li>Failed sends are logged with the exception type and a redacted recipient identifier; raw paths and message bodies are never logged.</li><li>Slash-command handlers that raise an exception return a generic &quot;An error occurred handling that command.&quot; reply to the operator and log the full traceback server-side.</li><li>HTTP 409 responses to <code>/api/v1/whatsapp/notify</code> indicate the bot isn&#39;t paired or connected. The API refuses rather than queueing so the caller sees a clear failure.</li></ul><h4 id="performance-considerations" tabindex="-1">Performance Considerations <a class="header-anchor" href="#performance-considerations" aria-label="Permalink to &quot;Performance Considerations&quot;">​</a></h4><ul><li><strong>Worker thread isolation</strong>: wars runs on a dedicated OS thread. Slow <code>wars.send</code> calls (WhatsApp servers throttling, slow network) do not block Flask request threads.</li><li><strong>Connection pooling</strong>: wars maintains a single persistent WebSocket to WhatsApp servers per process.</li><li><strong>Alert pool</strong>: Outbound notifications dispatch through a 5-worker <code>ThreadPoolExecutor</code> so a burst of order placements can fire alerts in parallel.</li><li><strong>Event bus</strong>: In-process pub/sub with a 10-worker thread pool. The WhatsApp subscriber returns to the bus worker within microseconds (real work happens in the alert pool, then the bot thread).</li><li><strong>No polling</strong>: wars uses WhatsApp&#39;s binary protocol over WebSocket. No HTTP polling, no rate-limit consumption on idle.</li><li><strong>Idempotent migrations</strong>: Schema changes apply additively on every boot via <code>PRAGMA table_info</code>, so the upgrade procedure is just <code>git pull &amp;&amp; uv sync &amp;&amp; uv run app.py</code>.</li></ul><h4 id="whatsapp-terms-of-service-practical-risk-note" tabindex="-1">WhatsApp Terms of Service: Practical Risk Note <a class="header-anchor" href="#whatsapp-terms-of-service-practical-risk-note" aria-label="Permalink to &quot;WhatsApp Terms of Service: Practical Risk Note&quot;">​</a></h4><p>Tradeboard&#39;s WhatsApp integration uses <code>wars</code>, an unofficial WhatsApp client. Unofficial clients can get the linked device unlinked, or in rare cases the entire account banned, by Meta&#39;s automation. The dominant trigger is send volume and pattern, not the client itself:</p><ul><li><strong>Low risk (typical Tradeboard usage)</strong>: A handful of self-send order alerts per day, occasional <code>/status</code> replies, sending charts or reports to a small circle of subscribers. Indistinguishable from a person using WhatsApp normally, and well under Meta&#39;s automated thresholds.</li><li><strong>Medium risk</strong>: Sending to dozens of distinct contacts who haven&#39;t messaged you first, frequent broadcasts, sending the same body to many recipients in a short window.</li><li><strong>High risk (don&#39;t)</strong>: Bulk marketing, cold outreach to scraped numbers, evading rate limits. This is what triggers bans. Use the official WhatsApp Business / Cloud API for those use cases.</li></ul><p>The 5-recipient cap on <code>phones[]</code> broadcasts is a deliberate ToS-safety guardrail. Treat your paired session as sensitive: it contains the private keys for your linked device.</p><h4 id="future-enhancements" tabindex="-1">Future Enhancements <a class="header-anchor" href="#future-enhancements" aria-label="Permalink to &quot;Future Enhancements&quot;">​</a></h4><ul><li>[ ] Chart generation (intraday / daily / both), matching the Telegram bot&#39;s <code>/chart</code> command</li><li>[ ] Confirmation prompts before <code>/closeall</code>, matching the Telegram bot&#39;s inline-button flow</li><li>[ ] A dedicated alert layout for GTT events instead of the generic <code>*Order Update*</code> heading</li><li>[ ] Per-recipient notification preferences (currently single-user)</li><li>[ ] Inline reply buttons (a WhatsApp Business-only feature; would require a separate Business API path)</li><li>[ ] Voice-note replies via Whisper transcription</li><li>[ ] Daily P&amp;L auto-summary scheduler</li></ul><h4 id="support" tabindex="-1">Support <a class="header-anchor" href="#support" aria-label="Permalink to &quot;Support&quot;">​</a></h4><p>For issues or questions:</p><ol><li>Check the server logs (<code>log/tradeboard_YYYY-MM-DD.log</code> plus <code>log/errors.jsonl</code>)</li><li>Open <code>/whatsapp</code> and inspect the status badge and pair-state JSON via <code>GET /whatsapp/pair/status</code></li><li>Verify wars is installed: <code>uv run python -c &quot;import wars; print(wars.__version__)&quot;</code>. Tradeboard pins <code>wars==0.1.4</code></li><li>Review this documentation</li><li>Contact Tradeboard support</li></ol><hr><h2 id="agent-instructions-querying-this-documentation" tabindex="-1">Agent Instructions: Querying This Documentation <a class="header-anchor" href="#agent-instructions-querying-this-documentation" aria-label="Permalink to &quot;Agent Instructions: Querying This Documentation&quot;">​</a></h2><p>If you need additional information that is not directly available in this page, you can query the documentation dynamically by asking a question.</p><p>Perform an HTTP GET request on the current page URL with the <code>ask</code> query parameter:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET https://docs.algo.wesoftcorp.com/trading-platform/whatsapp.md?ask=&lt;question&gt;</span></span></code></pre></div><p>The question should be specific, self-contained, and written in natural language. The response will contain a direct answer to the question and relevant excerpts and sources from the documentation.</p><p>Use this mechanism when the answer is not explicitly present in the current page, you need clarification or additional context, or you want to retrieve related documentation sections.</p>`,160)])])}const g=t(i,[["render",n]]);export{u as __pageData,g as default};
